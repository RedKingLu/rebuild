# === 临时/协作/证据区域（不纳入版本控制） ===
交接/
产物/
证据/
工作区/

# === 密钥与配置 ===
.env
.env*
!.env.example
*.key
*.secret
*.pem
credentials.*
*_credentials.*
*.token

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
*.egg

# === Node ===
node_modules/
dist/
build/
*.tsbuildinfo

# === IDE ===
.vscode/
.idea/
*.swp
*.swo

# === OS ===
.DS_Store
Thumbs.db

# === Docker ===
docker-compose.override.yml

# === 日志 ===
*.log
logs/

# 用户导入供应商（仅非敏感配置，Key 不落盘）
backend/app/config/user_providers.yaml
backend/app/config/user_strategies.yaml

# Runtime data (mounted as a volume in containers — not source)
backend/.data/project-sources/
backend/.data/execution-sandbox/
backend/.data/integration-repos/
# 防误 cwd 运行把 .data 运行时库/检查点落到非后端目录（如 frontend/.data）产生游离物
frontend/.data/
/.data/

# R17.2 Q-R17.2-1（用户裁决方案 A）：运行时 DB/检查点/社区包不入版本控制。
# 由 seed_all() + alembic 在启动时重建（空库实证 5 agent/27 skill/30 resource），
# 根除 R11-3/R14-5/R16 反复出现的真实库 drift 与二进制运行时数据污染 git。
**/*.db
**/*.sqlite
community/backend/.data/packages/
