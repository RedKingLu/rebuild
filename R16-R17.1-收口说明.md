# R16 + R17.1 收口说明

> 日期：2026-07-11 ｜ 分支：`r8-closure` ｜ 平台：rebuild V26.1.1
> 定位：R16（社区功能增强，含 R15 并入）联合验收 accepted 收口 + R17 已进行联调工作（R17-1 / R17-X）阶段性固化。
> 下一步：R17.2 彻底联调（交接提示词见 `交接/当前/`）。

---

## 1. 本次收口范围

- **R16「社区文档、知识检索、资源版本与评测展示增强」** — 联合验收后返工，**accepted**（用户复核放行）。
- **R17 已进行的联调工作** — R17-1（全量联调盘点 + P0-P6 真实项目跑通）、R17-X（P0-P6 联调线过度设计复杂度摸排与修复）阶段性固化入库。
- 交接材料归档：`交接/已完成/R16/`（5 份）、`交接/已完成/R17/`（11 份）。

## 2. R16 联合验收：从 rework 到 accepted

用户发起独立联合验收（不预设 agent 设计合理），识别出**社区信息架构方向性缺陷**为主的 6 类问题，返工修复并经真实 E2E 复验后放行：

| 编号 | 问题 | 修复 |
|---|---|---|
| P0-1 | "社区"入口指向平台内镜像页，非独立社区站点（违 D-096） | 导航改跳独立社区站点（`VITE_COMMUNITY_URL`，默认 :8081），外链新标签打开 + 不可达诚实提示 |
| P0-2 | 平台内 CommunityPage 与资源中心职责重复 | 删除 CommunityPage / `/community` 路由（404）/ communityService 收敛为 status |
| P0-3 | 导入资源落 `draft` 不可调度（Agent 用不到） | 4 处导入分支改 `active`+`enabled`（D-061修订：主动导入即可用） |
| P1-1 | E3 版本管理后端真实但前端未接 | 社区站点资源详情加版本历史（只读，历史版本诚实标"仅供追溯"） |
| P1-2 | 功能表/docstring 把已移除的社区审核门标 verified | RES-02/03 漂移修正 + docstring 修正 |
| P1-3 | URL 粘贴导入无 sha256、复活审核状态机、无版本 | A 方案 sha256 尽力校验（不匹配 409）+ 去审核状态机 + 记 imported_version |

**收口启栈真实 E2E 又发现并修复 2 项**（代码级测试未覆盖）：
- **导入身份丢失**：社区包 `manifest.json` 不自描述 → 导入落默认 `tool/"Imported Resource"`。修：社区包 manifest 自描述（resource_type/name/version）+ 导入优先 display_name。
- **本地 http 约束**：`_validate_url` 仅认 https → 本地社区（http://localhost:8081）粘贴导入 422。修：放行**仅回环地址** http，外部仍强制 https。

用户裁定的社区最终形态：**社区导航 = 纯链接跳独立站点；社区资源只提供「复制导入链接」+「导出离线 zip」两种方式；主平台经资源页粘贴链接/上传 zip 导入，导入即可用。**

## 3. 真实库 DB drift 修复（重要）

收口启栈时主后端崩溃：真实库 `backend/.data/rebuild.db` 停在 R14 迁移（`a8b9c0d1e2f3`），缺 R15/R16 列（`package_url` 等）。已备份并 `alembic upgrade head` 补齐 4 迁移至 `e4f5a6b7c8d9`。

**系统性风险留 R17.2**：真实库迁移未纳入启动自检/CI，`create_all` 掩盖缺列（同 R11-3/R14-5 教训）。建议 R17.2 增"启动即校验 alembic head 与真实库一致"。

## 4. 验收证据

- **pytest**：`test_r15r16_import_rework` **8 passed**（含身份还原回归）；主后端全量 **836 passed / 3 skipped / 0 failed**。
- **build**：`frontend` + `community/frontend` 均 exit 0。
- **四服务启栈**：主后端 8000 / 主前端 5173 / 社区后端 8001（docs=4）/ 社区前端 8081 全 200。
- **真实 E2E**：粘贴社区 `/download` 链接导入 → `status=active enabled=True source=community type=case name=Oracle → 达梦 迁移案例 version=1.2.0`。

## 5. 本提交包含 / 不包含

- **包含**（代码）：backend/（R16 社区返工 + R17-X 联调 + 我的验收修复）、community/（自描述 manifest + 文档栏目 + 复制链接）、frontend/（社区入口改造 + R17-X 前端）、新增测试与 R16-B 迁移。
- **不含**（`.gitignore` 边界）：`交接/`、`证据/`、`产物/`（进度追踪与验收证据为本地事实源）；`backend/.env`；本地 `.data/` 库与导入测试 junk 资源目录。

## 6. 遗留 / 移交 R17.2

- D-074 浏览器级截图证据统一在 R17.2 补全留证。
- P2-1（online_source_provider 静态卡核查）/ P2-2（评测 compare 按 metric 分组）/ REC（检索性能标注、重名测试清理）。
- 真实库迁移自检、B-DEBT-SILENT-EXCEPT 债务、以及 R17.2 将系统排查的假链路/硬编码/mock/最小实现（见 R17.2 交接提示词）。

> ENH-03 状态：**accepted**（用户复核放行）。平台自身建设主体完成，进入彻底联调阶段（R17.2）。
