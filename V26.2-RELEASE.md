# rebuild V26.2 发布说明

> 文件身份：发布说明
> 发布日期：2026-09-10
> 发布类型：能力基线发布（上一版 V26.1.1 为首次基线发布）
> 平台：rebuild
> 版本：V26.2
> 许可：MIT
>
> ---
>
> ## ⚠ 后续事实更正（2026-09-17 补记，转公开前）
>
> **本文写于 2026-09-10（R22 发布冻结时点），其后发生的事实使"发布"一词需要限定，故在此补记，不改原文其余部分。**
>
> | 时点 | 事实 |
> |---|---|
> | 2026-09-10 | 本文成稿。R22 六批次完成、通用门 G-1/G-2/G-4~G-8 全过、G-3 部分达成 |
> | **2026-09-14** | **V26.2 总验收（完整 MicroOA 真跑）结论 = `rework_required`** —— 三条理由：① 端到端真跑在 P2 因两处硬编码 `max_tokens=16384` 截断而停止；② `V-02`（两场景真跑到 P5）与 `V-04`（标记臆造依赖）客观未达成；③ P4 产出代码经实测不可运行 |
> | 2026-09-15~17 | 返工与 V26.3 优化（R23 文档核对 / R24 代码与架构核对）；发布收口按用户裁决**移至 V26.3 之后** |
>
> ⇒ **本文所述能力为"R22 冻结时点已实现的功能面"，不代表 V26.2 通过了总验收。** V26.2 的总验收结论是 `rework_required`，其发布收口（含用户亲自前端验收）**未执行**，已移至 V26.3 之后。
>
> **不删除本文、不改原文结论的理由**：它如实记录了 R22 时点的判断与当时取得的证据；后来的总验收推翻的是"可以发布"这一步，不是 R22 各批次的交付事实。删改它等于抹掉"我们当时认为可以冻结、后来被真跑推翻"这一过程——而这个过程本身是有价值的记录（对应 `PROJECT_STATUS.md` 所述"宁可诚实失败"的取向）。
>
> 当前版本状态请以 `PROJECT_STATUS.md` 与 `AGENTS.md §1` 为准。

---

## 关于 rebuild

rebuild 是一个**软件重构平台**：在用户拥有源代码的前提下，用目标驱动的 AI Agent 编排，把"把一套已有软件改造成另一套形态"做成可追踪、可验证、有证据的工程流程。项目按 P0 接入 → P1 建档 → P2 评估 → P3 规划 → P4 执行 → P5 验证 → P6 交付 七个可裁剪阶段推进。

V26.2 起，平台定位从"软件重构与迁移平台，首期信创迁移"扩展为**泛软件重构**。场景体系分两层：

- **典型场景**（平台重点支持、配套资源相对更丰富）：信创切换 `xinchuang_switch`、软件现代化 `modernization`、软件移植 `porting`；
- **开放扩展**："**包括但不限于**"上述典型场景。任何重构 / 迁移类型都可通过新增场景包目录加入，用户也可修改或复制典型场景包后改写。三类典型场景**不是全集**。

场景清单开放，但**准入不开放**。任何场景须同时满足三条判据方可进入 P0-P6：① 有源代码；② 有可判定的目标态；③ 产出物可机器验证。任一不满足，平台明确告知不适用，而不是硬跑一遍交一份看起来像样的报告。

"泛软件重构"不等于"泛软件工程"：本版只解除重构 / 迁移**类型**的限定，不把新建开发、需求管理、运维、测试服务纳入范围。平台仍不扩展为泛 AI 办公工具、通用 Agent 市场、泛项目管理平台、无源码纯咨询平台。

> 定位与场景体系的详述源：`文档/00-项目治理/01-决策记录.md`（D-117 / D-118）。

---

## 1. 变更摘要

V26.2 围绕三条主线推进，顺序不可交换：**安全与开源就绪 → 能力地基 → 场景化与外部范式吸收**。

### 1.1 安全与开源就绪

先修安全，再谈开源——开源等于把漏洞连同利用路径一并公开。

- 修复源码 / 案例导入解压落盘的路径穿越缺陷。原列 4 处，施工中实测发现第 5 处同类漏洞并一并修复；落盘路径经统一收敛函数处理，展示名保留原始输入。
- 子进程环境清理时剥离 URL 内嵌凭据（数据库、缓存、消息队列连接串），口令自带 `@` 的形式亦无残留。
- 危险命令拒绝判定从子串匹配升级为编译正则 + 子串兜底，补齐 fork bomb、`chmod 777 /`、`chown -R root` 三类漏报；原有拦截项一字未改，合法命令零误拦。
- L5 高风险命令强制用户 Gate 接线：执行层拦截后创建可裁决的高风险命令 Gate（携命令原文与风险说明，落库并进审计），前端安全 Gate 分支独立于阶段晋级分支，避免"批准"误推进阶段。L4 明确不建 Gate。
- 写入期明文密钥拦截钩子在**持久库**上由"完全不执行"转为真实生效：钩子行按稳定名称反查内置实现，元数据笔误不再解除安全钩子武装；无法绑定的阻断型钩子改为 fail-closed 并发声；启动时对真实库做只读自检。
- 资源禁用 / 更新 / 切换三个此前可绕过审批的入口补齐审批 Gate；写入前策略检查提升为工具执行入口的代码层不可关闭检查；脱敏实现收敛为单一来源。
- 修复无 `.env` 时后端在导入期即崩溃、完全无法启动的缺陷。此缺陷对首次克隆仓库的外部使用者影响最大，并连带使"主密钥缺失"的诚实告警路径从事实上不可达变为真正可达。
- 开源基础件齐备：`LICENSE`（MIT）、`CONTRIBUTING.md`、`SECURITY.md`、`CODE_OF_CONDUCT.md`、`.github/` 模板，README 面向外部读者重写，两份 `.env.example` 补全为"变量名 + 说明 + 明显占位"。
- 全 git 历史密钥扫描作为硬前置执行：遍历全部 1787 个 blob / 54 个提交、匹配 9 类模式，命中项经逐条核实**全为假阳**（环境变量名、测试用合成假值、占位 webhook），无真实密钥泄漏。
- 新增运行时数据守护脚本 `scripts/runtime_guard.py`（快照 / 校验 / 可选自动回滚），解决"曾被跟踪、现被忽略"的运行库在分支切换时被静默清除的问题。守护默认不自动启用，需显式安装。

### 1.2 能力地基

让"可用版本"名副其实——补齐使验证结论真正成立的底层能力。

- **容器化真实构建**：新增工具链解析、构建诊断抽取、工具链镜像映射配置与镜像预热脚本。P5 构建槽从"宿主有 Docker 却从不使用"转为真实在容器内执行构建并产出真实诊断明细；环境不具备时（Docker 不可达 / 镜像缺失）诚实报证据缺口并给出预热提示，不伪造可用。
- **依赖真实性校验**：对 NuGet / npm / Maven 做仓库存在性与版本可解析性查询，臆造的依赖包被真实标记为不可解析；查询超时时报证据缺口，不猜。
- **模型调用超时兜底**：流式调用补首字节超时、chunk 间读超时与总超时三层保护（此前会在无数据时长时间挂起）；非流式调用此前**完全无界**，本版补外层兜底。零新增环境变量、零新增依赖。
- **编排检查点韧性**：检查点存储异常时自检并恢复，不以删库为代价。
- **产物卫生与前端缺陷批次**：产出目录不再混入阶段中间态 JSON 与空文件；修复前端重复解析响应体、错误静默、状态码处理等缺陷；功能图标统一为线性图标组件。
- **上下文预算首次真正接线**：从"有声明、零消费方"变为真实裁剪 + 产出装配清单，并可通过接口读取真实装配数据。实测确认技能正文确实进入提示词、未被预算挤出。
- **续行授权可观测**：阶段推进依据写入执行轨迹，轮次上限显式化。
- **子进程超时孤儿进程修复**：超时后残留子进程的同类缺陷在两处修复，并抽出共用的终止实现；超时结果为纯增量扩展，既有字段契约不变。
- 新增守卫断言（能力接缝一致性、风险分级与执行模式无关性、规范守卫），把已确立的边界固定为可测断言。

### 1.3 场景化与外部范式吸收

- **场景成为一等公民**：项目新增 `scenario` 字段，取值为**自由文本标识符而非枚举**；场景列表由场景包目录在运行期扫描生成，前端选择项随之动态生成。
- **场景包机制**：`source/skills/scenarios/<id>/` 下以目标态词表、验收锚点、风险清单与元数据组织场景知识。内置 `_generic` 兜底包与三个典型场景包；无专用场景包时按通用重构方法论处理并诚实标注"无专用场景知识"，**不静默套用信创口径**。新增场景的目标形态是"新增一个目录即生效"。
- **非信创场景端到端真跑**：以软件现代化场景（.NET Framework 4.x → .NET 8）走完 P0 到 P6，逐阶段读取真实产出内容。选型阶段**主动拒绝**产出国产化替代建议并说明"若真实目标是国产化应切换场景包"；产出代码中国产化依赖零命中；验证阶段容器内真实构建成功。同源码、同模型、仅切换场景与目标态的信创对照跑同步执行，用于确认既有信创路径行为不回退。
- **抗伪造机制在通用化后依然有效**：平台在手握一个真实构建成功结论的情况下，仍因证据基准类槽位失败而拒绝放行阶段，未用"构建成功"掩盖证据缺陷。该轮真跑同时暴露并修复了一处证据校验误报（多节点覆写同一文件时历史校验值被误判）。
- **外部范式吸收（只吸收范式，不吸收代码）**：参照 DeepSeek Harness 的能力接缝、目标 / 计划分离、防护分级、生命周期钩子与上下文压缩等文档范式，校准自身接缝粒度与命名，并把防护分级**合并进**既有 L0-L5 风险分级而非另立一套。**未引入 TypeScript 运行时、未引入第三方编排框架、未引入任何新依赖**；LangGraph 仍是唯一主编排底座。

### 1.4 发布冻结与对外可读性

- **对外文档体系成形**：新增愿景、路线图、架构三份面向外部读者的文档，与项目入口一起构成四件套。写法上有一条硬约定——**每写一条能力，同时写它的边界**；三份文档各有独立的"明确做不到什么 / 明确不承诺什么"章节。
- **双语轻量方案落地**：四份对外文档提供中英两个语言变体（`X.md` / `X.zh.md`），根目录一份对照清单声明变体关系与**中文为权威版本**这条规则。**未引入任何 i18n 框架或运行时依赖**，也未触碰前端——平台内前端用户可见文案仍中文优先。
- **后端版本号收敛为单一来源**：应用元数据与健康检查响应此前各自持有版本号字面量，现统一读取配置项；同时删除一个已过期且零消费方的阶段描述字段（详见已知限制第 10 条）。
- **产出体量观察指标（新增，非阻断）**：验证阶段新增一项源码与产出代码量对比的**观察**指标（文件数、行数、按扩展名分布、比值），落在验证阶段的观察产物里。它的定位须明确——**不设阈值拦截、不接入 Gate、不参与阶段晋级判定**；只在遇到与阈值无关的结构性异常（产出为空、产出全是空文件、度量本身失败）时标注"需人工复核"，把判断交回给人。设计动机是补一个此前完全缺失的维度：既有验证只检查产出**是否存在**，不看它相对源码的体量是否说得通。**"多低算异常"这个阈值本版有意不定**——它需要真实规模样本才有意义，而真实规模项目的端到端联调尚未进行（见已知限制第 2 条）。
- **对外文件的内部残留清理**：贡献指南与 PR 模板中的内部台账编号已移除；其中一处关于"流式调用缺读超时"的说明是**已修复缺陷的过期归因**，已改写为可确证的表述。测试基线数字同步更新，并写明其对运行环境敏感（见 §1.5）。

### 1.5 基线数字

| 项 | V26.1.1 | V26.2 |
|---|---|---|
| 后端测试 | 1100 passed / 3 skipped / 0 failed | **1673 passed / 4 skipped / 0 failed** |
| 前端构建 | 绿 | 绿 |
| 数据库迁移头 | 单头 | 单头（本版新增项目场景字段一次迁移） |
| 第三方依赖清单 | — | 本版最后一个施工阶段零改动；全程未引入 i18n 框架、编排框架与 TS 运行时 |

> 测试数字在启用内置模型桩（`R176_MOCK_LLM=1`）、后端在运行、可达模型提供方 4 个的前提下取得。其中一条 live 端到端用例依赖真实可达的多提供方环境，环境不满足时会诚实跳过——这会让通过 / 跳过的数字相差 1，属预期行为。

---

## 2. 新增能力

每条能力同时写明其边界，避免被读成"开箱即用"。

| 能力 | 现在能做到 | 边界（必须同时知道） |
|---|---|---|
| **泛软件重构定位** | 信创切换、软件现代化、软件移植三类典型场景 + "包括但不限于"的开放扩展 | 须过准入三判据；无源码、无可判定目标态、产出物无法机器验证的任务被明确拒绝 |
| **场景包机制** | 场景知识以目录形式组织，运行期扫描发现；用户可修改、可复制改写、可新增 | 典型场景包资源明显更丰富；自定义场景走通用兜底方法论，资源较薄，需用户自行补充 |
| **容器内真实构建验证** | 验证阶段在容器内真实执行构建并抽取真实诊断明细 | **需 Docker 可用**且已预热对应工具链镜像；不具备时诚实报证据缺口，不给构建结论 |
| **依赖真实性校验** | 标记出仓库中不存在或版本不可解析的依赖（含模型臆造的包名） | 依赖仓库限流或不可达时报证据缺口而非"通过" |
| **高风险命令强制人工裁决** | L5 命令被拦截并生成可裁决 Gate，携命令原文、风险说明与审计链 | 这是**设计选择**而非未完成的自动化；无人裁决则流程停在该处 |
| **写入期密钥拦截** | 明文密钥写入在持久库上真实被拦截，无法绑定的阻断型钩子 fail-closed | 拦截基于模式匹配，不构成对全部凭据形态的完备保证 |
| **上下文预算与装配清单** | 预算真实生效，装配层次与字符数可通过接口读取 | 真正触发丢层时目前无日志发声（见已知限制） |
| **模型调用超时保护** | 流式三层超时 + 非流式外层兜底，不再无界挂起 | 阈值可由环境变量调整，默认值面向本机开发 |
| **执行超时不留孤儿进程** | 超时后子进程树被确定性终止，结果新增超时标识字段 | 子进程 `stdin` 仍继承父进程（见已知限制） |
| **运行时数据守护** | 快照、完整性与体积异常检测、可选自动回滚，快照存放于仓库外 | **默认不自动启用**，每个克隆需显式安装；文件仍被 git 跟踪时只告警不覆盖 |
| **开源基础件** | MIT 许可、贡献指南、安全上报渠道、行为准则、issue / PR 模板齐备 | 仓库当前仍为私有状态（见已知限制） |

平台的四条设计取向在本版未改变：能力不可用时诚实 `blocked` / `evidence_gap` 而不伪造成功；关键节点必须人工裁决；No Evidence No Completed；推理归模型、采集与验证归确定性代码。

---

## 3. 已知限制

本节按"诚实优于好看"的口径撰写。以下均为**现状**，不是免责声明，也不改写为"优化方向"。

### 3.1 端到端验证覆盖面

1. **本版最后一个施工阶段（外部范式深度吸收）期间未做完整 P0-P6 端到端真跑。** 该阶段"无行为回退"的结论部分依赖全量回归测试，以及上一阶段（场景化）已完成的两次真跑证据；风险最高的一项改动（上下文预算首次真正裁剪）已由真实项目上的真实运行数据排除（实测未触发裁剪、未丢层）。但"该阶段无一次端到端真跑"这一缺口客观存在，不因分析充分而消失。

2. **真实规模项目（千文件级）的端到端联调尚未进行。** 已完成的端到端真跑使用的是样例项目；其中一次用于交叉验证的项目仅 5 个文件，**不代表平台处理真实规模迁移的水平**。真实规模项目上的行为、耗时、成本与失败模式尚未实测。

3. **远程 SSH 执行链未做端到端验证。** 相关判定的实测止于函数级返回值，未真起一台远程主机跑一次远程执行。

4. **本版最后一个施工阶段的前端改动缺浏览器级联调证据。** 该阶段唯一的前端改动（资源页的审批中提示）未在浏览器中验证；同阶段的上下文装配能力有对真实后端的接口级实测（真实响应、真实字段），但接口级实测**不等于**浏览器级证据。需说明的是，本版早期阶段的安全 Gate 前端联调**有**浏览器级真跑证据（含真实请求路径与交互全路径复跑）；此项缺口限于最后一个阶段的改动范围。

5. **全量测试在内置模型桩模式下取得。** 真实模型模式下的全量测试仍是既有缺口，与历史基线口径一致。

### 3.2 已知技术债务（有理由的刻意搁置，非遗漏）

6. **容器执行的临时文件权限为 0755 / 0644，而非私有 0600。** 这是**有明确理由的刻意放宽**：容器以非 root uid 10002 运行、不拥有该宿主临时目录，需要可遍历与可读，直接改 0600 会破坏容器执行。**真实风险**：本机其他用户可在执行窗口内读到待执行代码（若代码含凭据）；在单用户开发机上该风险很低，但它是真实存在的暴露面，不是零风险。要改须换方案（经标准输入传代码或使用容器拷贝），属独立设计变更，未在本版实施。

7. **子进程 `stdin` 仍继承父进程。** 改为丢弃标准输入更稳，但属独立行为变更，未在本版实施。

8. **静态检查槽在部分环境下产不出结构化诊断码。** 根因已实测定位：`dotnet format` 内部自带一次独立的依赖还原，与显式还原是两条不同代码路径；其内部还原失败时抛出的是未处理的 .NET 异常，而非结构化诊断码，因此诊断汇总解析不出任何码，呈现为"总数 0"这种**看起来像"零诊断 = 正常"的误导状态**。该槽位为条件槽，**不阻断构建验证结论**。短期修法已提出（补跳过还原参数，或把未处理异常归为一个诚实的诊断类别），本版未实施。

9. **其余已登记但未修的次要项**：上下文预算在真正丢弃被声明为必需的层时无日志发声（当前实测余量约 7.3 倍，无现实影响）；风险阶梯常量存在两份同值副本；异步子进程管道释放使用了标准库私有属性（已用防御式访问包裹，最坏退回垃圾回收兜底）；一项 fail-closed 性质已核实但缺专属锁定测试。

### 3.3 版本号与内部台账

10. **运行期报告的版本号已与文档层一致，但需重启后端才生效。** 本版把后端版本号收敛为**单一来源**：应用元数据与健康检查响应不再各自持有字面量常量，统一读取配置项，健康检查与版本接口现在返回 `V26.2`，当前建设阶段标识同步更新。**注意**：运行中的后端进程需重启才会加载新值——升级后若健康检查仍报旧版本号，说明进程未重启，不是配置未生效。版本号**未**写入长期路径、API 路由或数据库表名——这一约定本身未被破坏。

    附带说明：`/api/version` 原有的 `stage_name` 字段（一段写死的中文阶段描述，已过期）本版**删除**。删除前已核实该字段无任何消费方：文档与交接材料中没有 `/api/version` 的契约定义，前端零调用；文档里同名的 `stage_name` 均属项目阶段实体（`stages[].stage_name`），与本接口无关。阶段事实由读配置的单一来源字段承载。

11. **内部问题追踪台账中，部分早期世代的遗留条目状态待专项核对。** 已确认存在若干"状态记录与代码现状不一致"的漂移（含正反两个方向）。这些条目的解除条件各含数个子项，完整核验需独立工作量，未在本版完成。**后果**：该台账的"未解决项计数"目前不可作为质量指标直接引用。仅坐实"现象描述过期"就改为已解决属谎报，因此本版选择保留差异登记而不翻状态。

### 3.4 开源与社区

12. **GitHub Discussions 未开启，社区首帖未发。** 仓库当前仍为**私有状态**，转公开是一个独立的、需用户明确批准的动作，本版按"私有状态先做准备"的口径执行：对外文档、许可与基础件已就位，社区通道待仓库转公开后再开启。独立社区门户保持暂缓，其定位与启用条件见 `ROADMAP.md`。

13. **仓库转公开后的历史可见性已知情。** 本项目沿用原有 git 仓库并保留全部提交历史（历史本身即持续迭代的证明）。历史中已存在的内部协作材料会随历史一并可见；全历史密钥扫描已完成且无真实泄漏。

### 3.5 面向使用者的成熟度口径

14. rebuild **不是开箱即用的全自动迁移平台**。必须人工介入的环节：阶段晋级需用户 Gate 授权；改造产物需人工复核后再采纳；必须自备模型凭据（无有效凭据时相关能力诚实 `blocked`，平台不提供内置模型、也不降级为规则引擎假装完成）；验证阶段需 Docker 可用才有真实构建结论。**机器验证不等于业务语义等价保证**——编译通过、测试通过、diff 可读，不等于业务语义完全等价。平台当前面向**单机单用户**本地使用，无登录、账号与租户隔离，**不要直接把服务暴露到公网**；亦无预构建发行物（无 PyPI 包 / npm 包 / 公共镜像），需从源码本地构建。

---

## 4. 升级说明

### 4.1 从 V26.1.1 升级

```bash
# 后端
cd backend
uv sync --dev

# 已有持久数据库：必须执行迁移（本版新增项目场景字段）
uv run alembic upgrade head

# 前端
cd ../frontend
npm install
npm run build     # 或 npm run dev
```

要点：

- **已有数据库必须执行迁移。** 建表逻辑只创建缺失的表、**不会给已存在的表补列**。若持久库落后于迁移版本，后端启动时会以 ERROR 明确报出（不自动迁移、不静默掩盖），据此执行 `alembic upgrade head` 即可。全新安装无需手动迁移。
- **无新增必填环境变量。** 两份 `.env.example` 在本版补充了超时相关变量的语义说明与执行模式注释，既有配置可直接沿用。
- **要用容器内真实构建，需预热工具链镜像。** 见 `deploy/toolchain-images/` 下的脚本与说明。未预热或 Docker 不可达时，验证阶段会诚实报证据缺口而不给构建结论。
- **运行时数据守护为可选、默认不启用**：`python3 scripts/runtime_guard.py install`（安装进 `.git/`，不受工作树切换影响）。建议在会频繁切换分支的克隆上启用。
- **端口约定不变**：后端 8000、前端开发 5173、前端容器 8080。请勿另设端口。

### 4.2 兼容性

- **既有项目无需迁移场景。** `scenario` 为自由文本、可空；未指定时走通用兜底包并诚实标注"无专用场景知识"，不会静默套用信创口径。
- **信创路径行为不回退。** 场景化改造以提示词与技能层参数化实现，同源码、同模型、仅切换场景与目标态的对照跑用于确认这一点。
- **接口契约为增量扩展。** 超时结果新增超时标识字段，既有字段语义与取值不变。
- **未引入 i18n 框架、第三方编排框架与 TypeScript 运行时依赖**；本版最后一个施工阶段的依赖清单（`pyproject.toml` / `package.json` 及两个 lock 文件）零改动。LangGraph 仍是唯一主编排底座。

### 4.3 版本号使用约定

`rebuild` 是平台名称，`V26.2` 只是基线发布号。**不要**把版本号写死到长期目录、API 路由、数据库表名、包名、模块名、前端路由或运行时路径中。本版发布时，版本号声明只出现在文档头部与本发布说明；运行期字面量的收敛见已知限制第 10 条。

---

## 5. 相关文档

仓库根的四份对外文档**提供中英两个语言变体**：`X.md` 为英文、`X.zh.md` 为中文，**中文为权威版本**（英文版是中文版的翻译，两版冲突以中文为准）。语言变体对照清单见 `.i18n.yaml`。

| 文档 | 内容 | 语言变体 |
|---|---|---|
| `README.md` / `README.zh.md` | 项目入口、快速开始、当前成熟度 | 中 / 英 |
| `VISION.md` / `VISION.zh.md` | 为什么存在、解决谁的什么问题 | 中 / 英 |
| `ROADMAP.md` / `ROADMAP.zh.md` | 后续方向、社区门户的定位与启用条件 | 中 / 英 |
| `ARCHITECTURE.md` / `ARCHITECTURE.zh.md` | 架构与关键设计取向 | 中 / 英 |
| `.i18n.yaml` | 语言变体清单与权威版本规则 | — |
| `CONTRIBUTING.md` | 开发环境、测试要求、提交规范 | 仅中文 |
| `CODE_OF_CONDUCT.md` | 行为准则 | 仅中文 |
| `SECURITY.md` | 安全漏洞私密上报渠道 | 仅中文 |
| `文档/00-项目治理/01-决策记录.md` | 全部架构与产品决策的详述源 | 仅中文 |
| `V26.1.1-RELEASE.md` | 上一版发布说明 | 仅中文 |

> 贡献指南、行为准则与安全上报说明目前**只有中文版**，这一现状已在 `.i18n.yaml` 中如实登记（"尚无变体"与"清单漏登记"可区分）。
