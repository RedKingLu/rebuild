# AGENTS.md

> 文件路径：`/home/king/rebuild/AGENTS.md`
> 文档状态：**accepted**
> 平台名称：rebuild
> 当前版本：V26.1.1
> 来源草稿：`产物/草稿/AGENTS-v0.2.md`
> 本次处理人 / Agent：Claude (deepseek-v4-pro) / R2 收口流程
> 最后更新时间：2026-06-24（R2 完成，V26.1.1 基线收口）
> 审核状态：经用户审核通过（2026-06-24）
> 文档定位：rebuild 根目录 Agent 执行准则。后续所有接手 rebuild 建设、文档、施工、审核、验收、交接任务的 Agent，必须先阅读并遵守本文档。
> 关联决策：D-007、D-065（薄编排/NIH 约束）、D-067（模块建设三步法）、D-073（平台助手）
> 设计原则：本文档不固化到某个单一阶段；Agent 必须通过"必读材料"和"当前任务交接"判断当前处于哪个 R 阶段、任务类型、事实源、执行流程与输出要求。
> 修订说明：R2 修订（2026-06-24）：(1) §4.3 路由表回写为磁盘真实 10 目录（00-09），消除 09 双义；(2) §2.4/§6.2 阶段口径更新为 R0/R1/R2 已完成；(3) 关联决策新增 D-073（平台助手）；(4) §20 已确认项回写。V26.1.1 基线收口。

---

## 0. Agent 启动自检

任何 Agent 接手 rebuild 任务时，必须先写下以下自我提醒：

```text
我不能依赖旧窗口记忆。凡涉及 rebuild 项目事实、版本、文档、代码、目录、交接材料、产物、证据、运行记录，都必须先查本地事实源。用户举例只是线索，不是完整范围。复杂任务必须分步读取、分步写入、分段校验。遇到不确定项、范围冲突、路径缺失、实现困难或风险升级，必须停下并列入待用户确认项，不能擅自后延或自行猜测。
```

---

## 1. 本文件的职责

`AGENTS.md` 是 rebuild 当前仓库的 Agent 执行准则入口。

它负责规定：

```text
1. Agent 接手任务时必须如何读取事实源；
2. 如何识别当前阶段和任务类型；
3. 如何区分当前事实源、草稿、正式文档、交接材料、产物、证据和历史参考；
4. 如何分步执行、分步写入和分段校验；
5. 如何编写文档、交接材料、报告和验收记录；
6. 如何处理不确定项、变更、安全、密钥和历史版本参考；
7. 如何区分 R 系列建设任务与 P 系列平台运行任务。
```

它不负责把所有阶段的细节写死在本文档中。

具体阶段计划、任务计划、文档清单、验收标准、工作包、API 字段、前端细节等，必须通过必读材料和当前任务交接文件确定。

### 1.1 AGENTS.md 的唯一入口地位

`AGENTS.md` 是 rebuild 当前仓库中 Agent 接手任务的**唯一根准则入口**。

后续不再单独维护与 `AGENTS.md` 职责重复的 Agent 工作准则独立文档（原 `05-Agent工作准则.md` 已取消，内容合并至本文档）。

如果其他文档、交接材料或历史材料中的 Agent 准则与 `AGENTS.md` 冲突，应以 `AGENTS.md` 为准，并将冲突登记为待确认项或变更项。

---

## 2. 平台与版本硬规则

### 2.1 平台名称

平台名称是：

```text
rebuild
```

### 2.2 当前版本号

当前版本号是：

```text
V26.1
```

V26.1 只是当前版本号，不是平台名称。

禁止把版本号写死到长期目录、API、数据库表、包名、模块名、前端路由或运行时路径中。

### 2.3 产品定位

rebuild 是面向软件重构与迁移的软件重构平台。

首期主要应用于信创迁移场景，在用户拥有源代码的前提下，帮助用户完成项目迁移、重构、验证与交付。

不得把 rebuild 扩展表述为泛 AI 办公平台、泛项目管理平台、通用 Agent 展示平台或无源代码情况下的纯咨询平台。

### 2.4 当前版本状态

```text
R0 基线冻结与启动：✅ 已完成（2026-06-23）
R1 文档草稿全量建立与正式化：✅ 已完成
R2 文档审核与校准：✅ 已完成（2026-06-24）
R3 前端主干与体验壳：📋 准备开工
```

详细 R 阶段计划见：`文档/00-项目治理/01-决策记录.md` D-010。

---

## 3. 阶段体系硬规则

### 3.1 P0-P6：用户项目产品流程

P0-P6 是 rebuild 面向用户项目的可裁剪产品流程：

```text
P0 接入
P1 建档
P2 评估
P3 规划
P4 执行
P5 验证
P6 交付
```

P 阶段用于用户项目，不用于描述 rebuild 当前版本自身建设。

### 3.2 R0-RN：平台自身建设阶段

R0-RN 用于 rebuild 当前版本自身建设。

当前 R 阶段计划必须从项目事实源读取，不得凭记忆判断。

优先读取：

```text
/home/king/rebuild/文档/00-项目治理/01-决策记录.md     ← D-010 列出完整 R0-R17 计划
/home/king/rebuild/交接/当前/                            ← 当前任务交接与提示词
/home/king/rebuild/文档/00-项目治理/00-项目总览.md       ← 当前版本状态
```

> 注：`文档/00-项目治理/06-R阶段总计划.md` 已生成正式候选（待审核），与 01-决策记录 D-010 保持一致。

### 3.3 禁止混用

禁止把 P 阶段当作平台建设阶段。

禁止把 R 阶段当作用户项目流程。

禁止继续使用以下历史阶段作为当前版本主流程：

```text
Phase 15
旧 F0-F6
旧 S0-S7
旧 RunEngine 阶段体系
旧 VNext 主流程
```

---

## 4. 必读材料机制

Agent 不应把本文档当作唯一事实源。

接手任何任务时，必须先按需读取以下材料，并据此判断当前处于什么阶段、应如何执行。

### 4.1 第一层：任务入口材料

优先读取当前任务入口：

```text
/home/king/rebuild/交接/当前/
```

可能包括：

```text
当前任务提示词
阶段交接说明
待确认项
执行计划
验收反馈
返工要求
```

如果任务入口材料与长期文档冲突，必须停下并列出冲突点，不能自行选择其一。

### 4.2 第二层：项目治理材料

优先读取：

```text
/home/king/rebuild/文档/00-项目治理/00-项目总览.md           ← ✅ 已正式化（待审核）
/home/king/rebuild/文档/00-项目治理/01-决策记录.md           ← ✅ 已正式化（待审核）
/home/king/rebuild/文档/00-项目治理/02-术语表.md             ← ✅ 已正式化（待审核）
/home/king/rebuild/文档/00-项目治理/03-版本与目录策略.md     ← ✅ 已正式化（待审核）
/home/king/rebuild/文档/00-项目治理/04-变更控制规范.md       ← ✅ 已正式化（待审核）
/home/king/rebuild/文档/00-项目治理/05-交接与报告规范.md     ← ✅ 已正式化（待审核）
/home/king/rebuild/文档/00-项目治理/06-R阶段总计划.md        ← ✅ 已正式化（待审核）
/home/king/rebuild/文档/00-项目治理/07-版本演进与历史资产吸收.md ← ✅ 已正式化（待审核）
/home/king/rebuild/文档/00-项目治理/文档地图.md               ← ✅ 已落位（待审核）
/home/king/rebuild/README.md                                    ← ✅ 简短入口
```

如果部分文档尚未生成，应读取对应草稿、交接说明或当前决策记录，并标注"正式文档缺失 / 待补"。

**文档事实源层级**（D-068）：项目治理 > 产品定义 > 架构设计 > 专题规范。上级已明确的内容，下级只引用不复述。冲突以上级为准。

### 4.3 第三层：任务相关专题材料

根据任务类型读取相关专题材料。

示例：

```text
文档 / 治理任务：读取 文档/00-项目治理/、产物/草稿/
产品任务：读取 文档/01-产品定义/
架构任务：读取 文档/02-架构设计/
流程任务：读取 文档/03-流程与运行时/
Agent / Skill / 模型 / 资源任务：读取 文档/04-模型与资源/ 与 skills/
API 任务：读取 文档/05-API与集成契约/
前端任务：读取 文档/06-UX与前端/
安全任务：读取 文档/07-安全与权限/
工程 / 验收任务：读取 文档/08-测试与验收/
历史参考任务：读取 文档/09-参考索引/ 与 /home/king/rebuild-archive/
```

> 真值约定：以上为磁盘真实 10 目录（00-09）映射，与 `文档/00-项目治理/文档地图.md` §2、`03-版本与目录策略.md` §4、`00-项目总览.md` §8.1 一致。09 仅"参考索引"（不再有"测试与验收"双义，工程/验收统一 08）。

### 4.4 第四层：当前草稿、产物与证据

按任务需要读取：

```text
/home/king/rebuild/产物/草稿/
/home/king/rebuild/产物/
/home/king/rebuild/证据/
```

草稿、产物和证据身份不同，不得混用。身份定义见：`文档/00-项目治理/02-术语表.md` 第九部分。

### 4.5 第五层：历史归档参考

历史归档只在需要参考、对照、补漏或风险验证时读取：

```text
/home/king/rebuild-archive/V8/
/home/king/rebuild-archive/V10/
/home/king/rebuild-archive/V26.0/
```

历史材料不得直接复制为当前版本正式内容。

---

## 5. 文件读取规则

### 5.1 按需读取，不凭记忆

涉及事实、路径、代码、文档、决策、草稿、交接材料、证据、产物时，必须读取对应文件。

不得凭旧窗口记忆、对话印象或文件名猜测内容。

### 5.2 默认读取顺序

默认读取顺序：

```text
1. 当前任务入口材料；
2. 当前项目治理材料；
3. 当前任务专题材料；
4. 当前草稿、产物与证据；
5. 当前代码或目录事实；
6. 历史归档参考。
```

### 5.3 分步读取

复杂任务不得一次性粗读大量材料后直接下结论。

必须执行：

```text
1. 先列路径；
2. 再读取关键文件；
3. 再摘取事实；
4. 再形成判断；
5. 再输出计划；
6. 最后写入产物、文档或代码。
```

### 5.4 未找到不等于不存在

如果未找到文件或路径，只能写：

```text
未找到 / 待确认
```

不得直接写：

```text
不存在
```

除非已完成充分检索并记录检索范围。

---

## 6. 通用执行流程

任何任务必须先判断任务类型和当前阶段。

### 6.1 任务类型

常见任务类型包括：

```text
文档编写
文档正式化
文档审核
旧版本参考对照
目录整理
交接材料编写
代码施工
前端实现
后端实现
API 契约编写
测试与验收
风险评审
返工
发布或归档
```

### 6.2 当前阶段识别

Agent 必须从任务入口材料和 R 阶段计划中识别：

```text
当前处于哪个 R 阶段；
本任务属于该阶段的哪类工作；
允许做什么；
禁止做什么；
输入是什么；
输出是什么；
完成条件是什么；
是否需要用户 Gate。
```

当前（2026-06-24）R0/R1/R2 已完成，R3 准备开工。具体以 `交接/当前/` 下的最新任务提示词为准。

### 6.3 标准执行步骤

每个任务必须执行：

```text
Step 1：识别任务目标、阶段、边界；
Step 2：确认应读取的事实源；
Step 3：按需读取文件；
Step 4：列出事实、推断、建议；
Step 5：识别风险和待确认项；
Step 6：给出执行计划；
Step 7：按计划分步写入；
Step 8：分段校验；
Step 9：输出完成记录；
Step 10：更新交接材料、总报告、总表或对应状态文件。
```

### 6.4 非迎合式评审

Agent 不能因为用户提出某个方案就直接执行。

必须主动判断：

```text
目标是否正确；
边界是否清楚；
是否存在旧版本污染；
是否存在范围膨胀；
是否遗漏事实源；
是否需要用户确认；
是否存在安全风险；
是否可能导致返工。
```

如果用户方案有风险，必须明确指出并给出建议。

### 6.5 事实、推断、建议分离

Agent 输出时必须区分：

```text
事实：来自已读取文件、代码、目录、证据或用户明确裁决。
推断：基于事实作出的判断，必须标注不确定性。
建议：Agent 提出的处理方案，必须说明理由、风险和替代方案。
```

禁止：

```text
把推断写成事实；
把建议伪装成已确认决策；
把历史版本内容自动视为当前版本事实；
把用户举例当作完整范围。
```

---

## 7. 文档任务通用规则

本节适用于所有文档编写、正式化、审核和返工任务，不限于 R1。

### 7.1 草稿与正式候选流转

如任务涉及草稿正式化，默认流转为：

```text
草稿 → /home/king/rebuild/产物/草稿/
Agent 评估与完善 → /home/king/rebuild/文档/
正式候选状态 → 待审核
用户 + Copilot 审核通过 → 后续才可执行或冻结
```

根目录文件（如 AGENTS.md）按决策记录指定路径写入，同样遵守上述流转规则。

### 7.2 单份文档正式化步骤

每份文档必须走以下步骤：

```text
1. 判断该文档是否应该具备；
2. 定义该文档应具备哪些内容；
3. 检查草稿是否满足需求；
4. 给出完善计划；
5. 生成正式候选文档；
6. 更新对应总报告或总表；
7. 等待用户与 Copilot 审核。
```

### 7.3 判断文档是否应具备

输出格式：

```text
文档是否应具备：是 / 否 / 需要合并 / 需要拆分 / 需要改名
理由：……
```

如果结论为"否 / 合并 / 拆分 / 改名"，必须停止并等待用户确认。

### 7.4 定义应具备内容

必须定义：

```text
文档目标
读者对象
必备章节
必备决策引用
必备术语
必备约束
必须避免的内容
验收标准
需要本地事实源补充的内容
```

### 7.5 检查草稿

至少检查：

```text
是否符合 rebuild 当前定位；
是否错误使用版本号作为平台名称；
是否混用 P 阶段和 R 阶段；
是否过多展开旧版本；
是否遗漏关键决策；
是否遗漏与任务相关的前端、工作区、Gate、Agent、Skill、模型、资源、安全、API、验收等必要内容；
是否有不确定项未登记；
是否存在凭空结论；
是否需要本地路径或代码事实源补充；
是否符合中文命名和用户可见文案要求；
是否符合"基本可用"而不是占位。
```

### 7.6 总报告与总表

不要为每份文档生成零散独立报告。

按任务或阶段维护总报告和总表。

当前 R1 文档正式化使用：

```text
/home/king/rebuild/交接/当前/R1-文档正式化总报告.md
/home/king/rebuild/交接/当前/R1-文档正式化总表.md
```

后续其他阶段可按阶段命名，例如：

```text
R2-文档审核总报告.md
R3-前端主干施工总报告.md
R4-API契约落地总报告.md
```

### 7.7 文档状态规则

常见文档状态包括：

```text
draft        草稿，尚未正式化
待审核       正式候选，等待用户与 Copilot 审核
accepted     已审核通过，可作为当前事实源
superseded   已被后续版本替代
rework       需返工
blocked      阻塞，等待事实源、权限或用户确认
```

写入 `文档/` 的正式候选文档，未经用户与 Copilot 审核，不得标记为 `accepted`、`final`、`已验收` 或 `已冻结`。

---

## 8. 文档编写规则

### 8.1 文档头部必须包含

正式候选文档头部必须包含：

```text
文档路径
文档状态：待审核 / accepted / superseded / draft 等
平台名称：rebuild
当前版本：V26.1.1
来源材料或来源草稿
本次处理人 / Agent
最后更新时间
审核状态
```

### 8.2 文档内容必须满足

```text
结构清晰；
中文为主，专业术语可保留英文；
概念与术语统一（以 文档/00-项目治理/02-术语表.md 为准）；
引用当前决策；
标明待确认项；
区分事实、推断和建议；
避免旧版本污染；
具备验收标准。
```

### 8.2.1 单一事实源 + 引用原则（防漂移硬规则）

每个事实、规则、定义、枚举只在**唯一的"详述源"文档**中完整展开，其余文档一律**引用而不复制**。

```text
详述源约定（示例，以 02-术语表 / 01-决策记录 为权威）：
- Manual / Plan / Auto 三模式完整定义 → 详述源 01 D-025 + 02；其余文档只写"详见 01 D-025 / 02"。
- 风险级别 L0-L5 完整定义       → 详述源 02 第七部分；其余只写"详见 02 第七部分"。
- 材料身份（项目文档/交接/产物/证据/运行记录/参考资料/平台资源）→ 详述源 01 D-014 + 02；其余引用。
- 禁止事项明细                  → 各文档只列与本文职责相关的子集，并引用 AGENTS.md §18 总表。
```

原因：同一内容在多处全文复制会随时间漂移（R1 第一部分验收已实证多处状态/命名/目录漂移）。新增或修改此类内容时，**只改详述源**，其余位置确认仍为引用。

如发现某内容存在两处以上全文复制，应在变更中收敛为"单一详述源 + 引用"，或登记为待确认项。

### 8.3 文档不得包含

```text
密钥、Token、Secret、Password；
未确认推断伪装成事实；
旧版本阶段命名直接迁移；
无依据的实现承诺；
未经确认的范围扩大；
已废弃术语作为当前主流程。
```

---

## 9. 交接材料编写规则

交接材料用于协作，不自动成为项目文档。

交接材料必须自包含，包含：

```text
项目背景
当前阶段
当前裁决
事实源路径
任务目标
执行边界
输入资料
输出要求
禁止事项
验收标准
待确认项
下一步建议
```

交接材料写入：

```text
/home/king/rebuild/交接/当前/
```

本轮结束后再归档到：

```text
/home/king/rebuild/交接/已完成/<轮次>/
```

### 9.1 交接材料检查清单

交接材料必须自包含，并在输出前检查：

```text
是否写明项目背景；
是否写明当前阶段；
是否写明当前裁决；
是否写明事实源路径；
是否写明任务目标；
是否写明执行边界；
是否写明输入资料；
是否写明输出要求；
是否写明禁止事项；
是否写明验收标准；
是否列出待确认项；
是否避免密钥泄露；
是否说明下一步建议。
```

交接材料不自动成为项目文档。需要晋升为正式文档时，必须走文档正式化流程。

---

## 10. 产物、证据与运行记录规则

### 10.1 产物

Agent 输出默认是 Artifact，不自动成为正式文档。

产物写入：

```text
/home/king/rebuild/产物/
```

草稿写入：

```text
/home/king/rebuild/产物/草稿/
```

### 10.2 证据

证据写入：

```text
/home/king/rebuild/证据/
```

证据应记录来源、路径、摘要和引用位置。

### 10.3 运行记录

运行记录、Trace、Audit、事件日志等必须放入运行记录区或后续设计指定目录。

不得混入正式文档正文。

---

## 11. 历史版本使用规则

历史版本只读参考。

允许使用：

```text
设计思想
字段经验
交互心智
失败反例
风险清单
可复用约束
```

禁止使用：

```text
直接复制旧代码
直接复制旧文档作为正式文档
复活旧 RunEngine
复用旧 S0-S7 为当前主流程
把旧 F0-F6 当当前 P0-P6
让历史目录成为当前运行依赖
```

使用历史材料必须在报告中记录：

```text
参考路径
参考内容
吸收理由
适配方式
未吸收内容
风险
```

### 11.1 模块建设三步法（历史资产并入各 R 阶段，硬规则）

历史资产代码级深读不单独成阶段，而是**并入各对应 R 阶段**（前端→R3、工程/API→R4、模型→R5、Agent/Skill/资源→R6 等）。建设任一模块时必须按"三步法"执行：

```text
第一步：先独立提出当前版本自己的方案（不先看旧代码，保证主体性）；
第二步：再深度吸收对应历史方案（archive 只读，按 §11 记录 吸收/不吸收/适配/风险）；
第三步：用吸收到的经验完善自己的方案，再执行。
```

必须先产出"自有方案 → 历史吸收记录 → 完善后方案"三段产物，方可进入施工。不直接复制旧代码、不复活旧主流程。

> 关联决策：D-067、D-062；详见 `文档/00-项目治理/07-版本演进与历史资产吸收.md`。

---

## 12. 安全与密钥规则

允许识别是否存在 `.env`、配置文件、密钥文件等。

禁止输出或写入：

```text
Key
Token
Secret
Password
.env 明文
API Key 明文
认证凭据
```

不得把密钥写入：

```text
报告
日志
Trace
Audit 明文
前端页面
截图说明
交接材料
正式文档
Git 提交
```

如需登记，只能写：

```text
发现某类配置文件，内容已脱敏，需用户或本地环境自行确认。
```

---

## 13. 技术栈与变更规则

当前技术栈暂定，具体以决策记录（D-038、D-039）、技术选型文档和当前任务交接为准。

已确认硬规则：

```text
LangGraph 是当前版本确定的主编排底座。
```

技术栈允许变更，但必须提出申请和理由，说明：

```text
变更内容
变更原因
影响范围
替代方案
迁移成本
风险
验收方式
是否需要用户裁决
```

未经确认不得擅自变更。

### 13.0 薄编排层与 NIH 约束（硬规则）

编排层必须保持"薄"：主编排能力优先复用成熟框架，不自研重复造轮子（No-NIH）。

```text
1. 主编排一律落在 LangGraph，不得自研状态机替代主编排（复活旧自研 RunEngine 是历史教训，见 07-版本演进与历史资产吸收.md）。
2. 模型多服务商适配优先复用 LiteLLM；ModelGateway 只做必要的薄抽象（Provider/Profile/Trace/Audit/成本入口），不重复实现 LiteLLM 已覆盖的能力。
3. 自研代码应集中在领域价值层（迁移正确性、Evidence/Artifact/Trace、Workspace、Policy/Audit、ExecutionProvider 适配），而非通用编排/调用基础设施。
4. 任何"自研替代成熟框架"的提案均按 C4 架构变更处理，必须说明为何现成方案不可用。
```

> 关联决策：D-037（LangGraph 主编排）、D-039（ModelGateway + LiteLLM）、D-065（薄编排/NIH 约束）。

### 13.1 上下文与记忆分层

Context 按 C0-C6 分层组装，Memory 按 Policy/Project/Run/Agent Working Memory 四层管理。详见：

```text
文档/00-项目治理/02-术语表.md 第十五部分
文档/00-项目治理/01-决策记录.md D-043、D-044
```

---

## 14. Agent / Skill 规则

默认少 Agent、多 Skill。

### 14.1 Agent 切换条件

只有满足以下条件时才切换 Agent：

```text
需要干净上下文
需要更换模型
权限边界不同
责任边界不同
并行任务需要隔离
需要专家能力
需要独立验收
```

### 14.2 Skill 分类

Skill 必须区分：

```text
R 系列建设用 Skill
P 系列平台运行用 Skill
```

R 系列建设用 Skill 建议位于：

```text
/home/king/rebuild/skills/R-建设执行/
```

P 系列平台运行用 Skill 存放于（**[规划]** `平台资源/` 目录将于 R6/按需创建，当前尚未建立）：

```text
/home/king/rebuild/平台资源/skills/P系列/
```

不得与 R 系列建设 Skill 混放。P 系列 Skill 属于平台运行期资源，与 Agent、Tool、MCP、Case、Knowledge、Policy、Template 统一在 `平台资源/` 下管理。

> 关联决策：D-008-REV1（2026-06-23 用户确认）

详细定义见：`文档/00-项目治理/02-术语表.md` 第四部分。

### 14.4 平台助手（AI 精灵 / 客服对话 Agent）

平台另设**平台级**内置助手"平台助手"（浮动 AI 精灵图标 + 对话面板），用于帮助用户使用平台、并在对话内切换模型作连通性自测。它**独立于** P0-P6 迁移编排与上述 Agent/Skill 资源体系，不是项目内 Agent。代用户操作平台为 [规划/R-future]，须走 Policy/Gate 授权红线、复用 ModelGateway、不泄露密钥、不泛 AI 平台化。

> 详述源：`文档/00-项目治理/01-决策记录.md` D-073。

### 14.3 AGENTS.md 与 Skill 的边界

`AGENTS.md` 负责定义硬规则、禁止事项、事实源读取顺序、材料身份、阶段识别、安全边界和任务完成自检。

Skill 负责把这些硬规则转化为可复用的执行流程、模板和检查清单。

```text
AGENTS.md = 规则入口 / 硬约束 / 禁止事项 / 判断边界
Skill     = 可复用执行方法 / 模板 / 检查清单 / 操作流程
```

当 Skill 与 `AGENTS.md` 冲突时，以 `AGENTS.md` 为准，并登记冲突项。

当前活跃 Skill：`skills/R-建设执行/rebuild工作准则skill.md`

---

## 15. Gate 与执行模式规则

### 15.1 P 阶段晋级 Gate

每个启用的 P 阶段进入下一阶段前，都必须由用户授权。

### 15.2 执行模式

```text
Manual Mode：用户审核 Stage Plan、Task Plan、授权动作和阶段晋级。
Plan Mode：用户审核 Stage Plan、Task Plan 或 Task Plan Batch；计划内动作由 Hook + Policy 检查并放行；越界、风险升级、不确定项回用户；用户审核阶段晋级。
Auto Mode：Agent 审核 Stage Plan 和 Task Plan；阶段内授权由 Hook + Policy + Auto Review Agent 处理；高风险、低置信度、冲突、超范围回用户；用户审核阶段晋级。
```

### 15.3 Task Plan Batch

当前不设置固定数量限制。

但必须说明：

```text
批次目标
任务范围
风险级别
权限边界
验收方式
异常升级策略
```

如后续出现瓶颈，再提出调整申请与理由。

---

## 16. 命令、Tool、MCP 与风险级别规则

Auto Mode 下低风险命令是否允许 Agent 审核后自动放行，应按照动作所属风险级别判断，而不是硬编码具体命令。

命令、Tool、MCP、写盘、Git 操作等必须先映射到风险级别，再按执行模式处理。

风险级别完整定义见：`文档/00-项目治理/02-术语表.md` 第七部分（L0-L5）。

速查：

```text
L0 只读参考     — 无破坏性，不需要额外授权
L1 方法调用     — 低风险，Plan/Auto Mode 下 Hook+Policy 放行
L2 受控执行     — 中风险，需要 Hook 检查 + 范围校验
L3 外部系统写   — 高风险，Plan 需用户确认，Auto 需 Agent 审核
L4 系统级操作   — 很高风险，Plan 需用户确认，Auto 强制升级用户
L5 不可逆/生产  — 极高风险，所有模式强制用户 Gate
```

L5 典型动作包括：

```text
生产发布
Git push
删除大量文件
破坏性数据库操作
密钥读取、轮换或导出
远程系统写操作
不可逆变更
```

---

## 17. 输出格式要求

每轮回复或执行报告必须包含：

```text
本轮任务
当前阶段
已读取事实源
事实 / 推断 / 建议区分
执行计划
已完成内容
写入路径
待确认项
风险
下一步建议
```

如果生成文件，必须说明：

```text
文件路径
文件身份：草稿 / 正式候选 / 交接材料 / 证据 / 产物
审核状态
下一步处理方式
```

---

## 18. 禁止事项总表

禁止：

```text
1. 凭记忆回答项目事实；
2. 直接复制历史版本文档为当前正式文档；
3. 把草稿直接搬入正式区；
4. 把正式候选标记为已审核或已完成；
5. 把 V26.1 当平台名称；
6. 混用 P 阶段和 R 阶段；
7. 复活旧 RunEngine；
8. 使用旧 S0-S7 或旧 F0-F6 作为当前主流程；
9. 将密钥、Token、Secret、Password 写入任何文档、报告、日志、Trace、前端或提交；
10. 将 Artifact 自动晋升为项目文档；
11. 跳过任务要求的评估、计划、检查和校验；
12. 为每份文档生成零散独立正式化报告，阶段内应统一追加总报告；
13. 遇到不确定项仍继续执行；
14. 以"后续补齐"绕过当前必须确认的问题；
15. 用用户举例替代完整范围判断；
16. 因内容长而省略关键检查。
```

---

## 19. 任务完成自检

任何任务结束前必须自检：

```text
1. 是否识别了当前阶段和任务类型；
2. 是否读取了必要事实源；
3. 是否区分事实、推断和建议；
4. 是否执行了分步读取、分步写入、分段校验；
5. 是否更新了必要的文档、交接材料、总报告或总表；
6. 是否存在待确认项；
7. 是否存在安全或密钥风险；
8. 是否误用历史术语或旧版本事实；
9. 是否把草稿、产物、证据、正式文档混淆；
10. 是否可以交给用户与 Copilot 审核。
```

---

## 20. 待确认项

以下事项需用户或后续 Agent 确认：

```text
1. 是否需要在本文档中增加 Context C0-C6 的 Agent 侧使用示例（可转入 R5/R6 阶段）。
```

> 注：§6.2 当前阶段识别方式已确认（以 交接/当前/ 最新提示词为准）；§7.6 总报告命名约定已确认（维持建议模式）；原"§4.2 所列 03-07 号治理文档创建时机"已不再是待确认项（R1 完成）；原 §14.2 skills 路径待确认随 D-008-REV1 已定，`平台资源/` 标 [规划]。

---

## 21. 本文档验收标准

本 AGENTS.md 达到基本可用标准，当且仅当：

```text
1. 能让新 Agent 明确 rebuild 平台名与 V26.1 版本号；
2. 能区分 P0-P6 与 R0-RN；
3. 明确必须通过必读材料判断当前阶段与任务要求；
4. 明确当前事实源、草稿区、正式文档区、交接区和历史参考区；
5. 明确按需读取文件、分步执行和分段校验；
6. 明确通用文档任务流程，而不是只固化到 R1；
7. 明确阶段总报告追加机制和总表机制；
8. 明确文档和交接材料写法；
9. 明确历史版本只读参考规则；
10. 明确安全与密钥规则；
11. 明确技术栈变更申请规则；
12. 明确 Agent / Skill、Gate、执行模式、风险级别（L0-L5）规则；
13. 明确禁止事项和任务完成自检；
14. 引用已正式化的项目治理文档并标注未创建文档的状态；
15. 引用 R0 完成事实和当前 R1 状态。
```

---

## 22. 本文档自维护规则

`AGENTS.md` 是 Agent 准则入口，但它本身也是一份**待审核项目文档**，其修改同样受文档与变更控制规范约束：

```text
1. 修改 AGENTS.md 必须遵循文档更新流程，保持"待审核"状态，未经用户与 Copilot 审核不得标为 accepted；
2. 每次修改必须更新头部"最后更新时间"与"修订说明"，说明改了什么、为什么；
3. C2 及以上影响（硬规则、禁止事项、阶段边界、安全规则变更）必须登记到当前阶段变更日志与总报告；
4. AGENTS.md 内的事实/规则若与"详述源"重复，遵循 §8.2.1 单一事实源原则——优先引用，避免与决策记录/术语表漂移；
5. 当 AGENTS.md 与 01-决策记录.md 冲突时，以决策记录为事实源，并将 AGENTS.md 的冲突登记为待确认或变更项。
```

> 本次（2026-06-23 R1 第一部分验收）对 AGENTS.md 的修改已按本规则登记于 `交接/当前/R1-变更日志.md` 与 `R1-文档正式化总报告.md`。
