# AGENTS.md

> 文件路径：`/home/king/rebuild/AGENTS.md`
> 文档状态：**accepted**
> 平台名称：rebuild
> 当前版本：V26.1.1
> 来源草稿：`产物/草稿/AGENTS-v0.2.md`
> 本次处理人 / Agent：Claude (deepseek-v4-pro) / R4-2 收口
> 最后更新时间：2026-06-25（R4-2 收口：瘦身为硬规则入口，工作流移至 Skill；R4 修复轮：§10 新增第 17 条引用 D-074 最小前后端联调要求；R5-1：新增 §12 进度追踪强制要求 + §10 第 18 条禁止事项，原 §12→§13）
> 审核状态：经用户审核通过（2026-06-25）
> 文档定位：rebuild 根目录 Agent 执行准则入口。定义硬规则、禁止事项、必读路径。**不包含工作流细节**——工作流、模板、检查清单全部由 Skill 承载。
> 关联决策：D-007、D-065（薄编排/NIH 约束）、D-067（模块建设三步法）、D-073（平台助手）
> 设计原则：本文档只定义"什么必须做/什么禁止做"，不定义"怎么做"。"怎么做"由 `rebuild-work-guidelines` Skill 提供。

---

## 0. Agent 启动硬规则

任何 Agent 接手 rebuild 任务时，必须按以下顺序启动：

```text
1. 写下自检：我不能依赖旧窗口记忆，必须先查本地事实源。
2. 读取本 AGENTS.md（本文档）。
3. 加载并使用 rebuild-work-guidelines Skill（skills/R-建设执行/rebuild-work-guidelines/SKILL.md）。
4. 按 Skill 指引读取交接/当前/ 和项目治理材料，判断阶段和任务类型。
```

**Skill 是强制入口**：本文档定义硬规则，Skill 将其转化为可执行流程。Agent 不得跳过 Skill 直接凭本文档执行任务。

---

## 1. 平台与版本硬规则

- **平台名称**：`rebuild`（全小写，不是版本号）
- **当前版本**：`V26.1.1`（基线发布号，禁止写死到长期路径）
- **产品定位**：软件重构与迁移平台，首期信创迁移。禁止扩展为泛 AI 办公/泛项目管理/纯咨询平台。
- **主编排底座**：LangGraph（D-037，硬规则，变更需 C4 申请）
- **当前阶段**：以 `交接/当前/` 和 `文档/00-项目治理/01-决策记录.md` D-010 为准

---

## 2. 阶段体系硬规则

### 2.1 两套体系严格分离

| 体系 | 用途 | 编号 |
|------|------|------|
| P 阶段 | 用户项目产品流程 | P0-P6 |
| R 阶段 | 平台自身建设 | R0-RN |

**禁止混用**。禁止使用旧 Phase/F0-F6/S0-S7/RunEngine 阶段名。

### 2.2 R 阶段定义

完整 R 阶段列表见 `01-决策记录.md` D-010。当前阶段状态以 `交接/当前/` 最新交接材料为准。

---

## 3. 必读材料（五层事实源）

Agent 接手任务时必须按需逐层读取，不得凭记忆：

| 层级 | 路径 | 内容 |
|------|------|------|
| L1 任务入口 | `交接/当前/` | 当前任务提示词、阶段交接说明 |
| L2 项目治理 | `文档/00-项目治理/00~07` | 决策记录、术语表、阶段计划等 |
| L3 专题材料 | `文档/01~09/` | 按任务类型读取对应专题 |
| L4 产物证据 | `产物/` `证据/` | 当前草稿、产物、运行证据 |
| L5 历史参考 | `rebuild-archive/` | 只读参考，禁止直接复制 |

详细读取策略和按任务类型的专题路由表见 Skill。

---

## 4. 材料身份边界（硬规则）

| 身份 | 路径 | 性质 |
|------|------|------|
| 项目文档 | `文档/` | 长期事实源，状态为 accepted 后方可引用 |
| 交接材料 | `交接/当前/` → `交接/已完成/` | 协作传递，不自动成为项目文档 |
| 产物 | `产物/` | Agent 输出，不自动成为事实源 |
| 草稿 | `产物/草稿/` | 待正式化的中间产物 |
| 证据 | `证据/` | 支撑判断和验收的实证 |
| 运行记录 | 后续指定目录 | 不得混入正式文档 |

**晋升规则**：草稿→正式候选（待审核）→用户审核→accepted。交接材料需晋升为正式文档时必须走文档正式化流程。

---

## 5. 文档与交接流转规则

### 5.1 文档生命周期

```
产物/草稿/  →  Agent 评估完善  →  文档/ (状态: 待审核)  →  用户审核  →  accepted
                                                    ↘  需返工  →  rework
```

### 5.2 交接材料生命周期

```
交接/当前/  →  本轮完成  →  交接/已完成/<轮次>/
```

### 5.3 产物生命周期

```
产物/草稿/  →  处理完成  →  产物/已完成/ 或晋升至 文档/
```

### 5.4 硬规则

- 草稿不得直接晋升为正式文档（必须走正式化流程）
- 产物不得自动成为事实源
- 交接材料不得自动成为项目文档
- 正式候选未经审核不得标记为 accepted
- 禁止为每份文档生成零散独立报告（维护阶段总报告+总表）

---

## 6. 核心执行原则

### 6.1 事实/推断/建议分离（硬规则）

```
事实：来自已读取文件、代码、目录或用户明确裁决
推断：基于事实的判断，必须标注不确定性
建议：Agent 的处理方案，必须说明理由和风险
```

**禁止**：把推断写成事实、把建议伪装成已确认决策、把用户举例当作完整范围。

### 6.2 单一事实源 + 引用（防漂移）

每个事实/规则/定义/枚举只在唯一详述源文档中完整展开，其余文档引用而不复制。修改时只改详述源。

### 6.3 分步执行

```
列路径 → 读关键文件 → 摘取事实 → 形成判断 → 输出计划 → 分步写入 → 分段校验
```

---

## 7. 历史版本使用规则

历史版本（`rebuild-archive/`）**只读参考**。

**允许**：吸收设计思想、字段经验、交互心智、失败反例

**禁止**：直接复制旧代码、直接复制旧文档、复活旧 RunEngine、复用旧 S0-S7/F0-F6 为主流程

### 7.1 模块建设三步法（硬规则）

建设任一模块必须：①先独立提自有方案 → ②再深度吸收历史方案 → ③用吸收到的经验完善方案后施工。必须先产出三段产物方可进入施工。

> 关联决策：D-067、D-062

---

## 8. 安全与密钥（硬规则）

**绝对禁止**输出或写入：Key、Token、Secret、Password、.env 明文、API Key 明文、认证凭据。

不得将密钥写入：报告、日志、Trace、Audit、前端页面、截图说明、交接材料、正式文档、Git 提交。

发现敏感配置只能写：`发现某类配置文件，内容已脱敏，需用户或本地环境自行确认。`

---

## 9. Skill 体系规则

### 9.1 强制使用 Skill

Agent 执行 rebuild 任务时**必须**加载 `rebuild-work-guidelines` Skill（路径：`skills/R-建设执行/rebuild-work-guidelines/SKILL.md`）。Skill 提供完整的工作流、模板、检查清单。

### 9.2 AGENTS.md 与 Skill 边界

```
AGENTS.md = 硬规则 / 禁止事项 / 判断边界（本文档）
Skill     = 可复用执行方法 / 模板 / 检查清单 / 操作流程
```

冲突时以 AGENTS.md 为准，并将冲突登记为待确认项。

### 9.3 Skill 分类

- **R 系列**：建设用 Skill，位于 `skills/R-建设执行/`
- **P 系列**：平台运行用 Skill，位于 `平台资源/skills/P系列/`（R6/按需创建）

两者不得混放。

---

## 10. 禁止事项总表

```text
1. 凭记忆回答项目事实
2. 跳过 Skill 直接凭本文档执行复杂任务
3. 直接复制历史版本文档为当前正式文档
4. 把草稿直接搬入正式区
5. 把正式候选标记为已审核或已完成
6. 把 V26.1.1 当平台名称
7. 混用 P 阶段和 R 阶段
8. 复活旧 RunEngine 或使用旧阶段名
9. 将密钥写入任何文档、报告、日志、Trace、前端或提交
10. 将产物自动晋升为项目文档或事实源
11. 跳过任务要求的评估、计划、检查和校验
12. 为每个小文档生成零散独立报告（应维护总报告+总表）
13. 遇到不确定项仍继续执行
14. 以"后续补齐"绕过当前必须确认的问题
15. 用用户举例替代完整范围判断
16. 因内容长而省略关键检查
17. （R5 起）只写 service/API 而无任何页面或入口实际调用，却把"已写接口"当作"已联调"标记阶段完成（详述源：决策记录 D-074；阶段规则见 06-R阶段总计划 §3.2.1）
18. （R5-1 起）在未更新 证据/进度追踪/ 功能表、阻塞项、待确认项、施工记录和验收证据索引的情况下标记阶段 accepted（详述源：AGENTS.md §12；追踪规则见 证据/进度追踪/06-状态口径与更新规则.md）
```

---

## 11. 任务完成自检

```text
1. 是否加载并遵循了 rebuild-work-guidelines Skill？
2. 是否识别了当前阶段和任务类型？
3. 是否读取了必要事实源？
4. 是否区分事实、推断和建议？
5. 是否执行了分步读取、分步写入、分段校验？
6. 是否更新了必要的文档、交接材料、总报告或总表？
7. 是否存在待确认项？
8. 是否存在安全或密钥风险？
9. 是否误用历史术语或旧版本事实？
10. 是否可以交给用户审核？
```

---

## 12. 模型配置与 ModelGateway 使用规范（R5-3 起）

### 12.1 密钥安全（硬规则，不可绕过）

模型 API Key **仅从环境变量读取**，不得以任何形式硬编码在代码、配置文件、文档中。

- Key 存放位置：`backend/.env`（已被 `.gitignore` 保护，不纳入版本控制）
- 配置文件中只存 Provider 非敏感信息（名称/endpoint/模型列表/能力标签），Key 必须走 env
- API 响应只返 `credential_status`（`configured` / `missing` / `invalid` / `redacted`），不返 Key 值
- 所有报告、日志、Trace、Audit、前端、截图中的 Key 统一标记为 `[REDACTED]`

### 12.2 服务商接入方式

平台支持用户接入自有模型服务商，通过两个文件配合：

1. **`backend/app/config/model_profiles.yaml`**：非敏感配置
   - Provider 定义（provider_id / provider_name / endpoint_openai / endpoint_anthropic / env_key_var）
   - ModelProfile 定义（model_name / display_name / capability_tags / cost_tier / supports_streaming 等）
   - ModelStrategy 定义（优先级 / fallback / retry / trace_policy）
   - 绝对不写 Key

2. **`backend/.env`**：敏感凭据
   - 环境变量名对应 Provider 的 `env_key_var`
   - 标准格式：`{PROVIDER}_API_KEY`（如 `DEEPSEEK_API_KEY`、`AGNES_API_KEY`）
   - 通用兜底：`LLM_API_KEY`（仅单 Provider 本地开发兜底，须显式标记 `generic_fallback`）

### 12.3 API 格式支持

ModelGateway 同时支持 OpenAI 和 Anthropic API 格式：
- 每个 Provider 可分别配置 `endpoint_openai` 和 `endpoint_anthropic`
- `api_format` 字段默认为 `openai`，可选 `anthropic`
- LiteLLM Adapter 自动处理格式转换和模型名前缀规范化（`openai/` / `anthropic/`）

### 12.4 Agent 接手模型相关任务时的标准流程

```text
1. 读取 backend/app/config/model_profiles.yaml 了解已配置 Provider/Profile/Strategy
2. 检查 backend/.env 中对应环境变量是否已填写（仅判断存在性，不读取值）
3. 通过 GET /api/model/providers 查看 Provider 配置状态（credential_status）
4. 通过 POST /api/model/self-test 验证连通性
5. 所有模型调用必须经 ModelGateway → LiteLLM Adapter，不得直连 Provider
6. 不得在聊天/报告/日志/截图/Trace/Audit 中输出 Key 值
```

---

## 13. 进度追踪强制要求（R5-1 起）

### 12.1 追踪目录

`证据/进度追踪/` 是进度追踪与防最小化施工辅助体系。详细规则见 `证据/进度追踪/README.md` 和 `06-状态口径与更新规则.md`。

### 12.2 强制更新

每个 R 阶段方案、施工、验收、收口时，**必须**检查并更新以下追踪文件：

| 文件 | 更新内容 |
|------|---------|
| `01-功能表.md` | 能力状态、证据路径、缺口 |
| `02-阻塞项.md` | 发现/解决的阻塞 |
| `03-待确认项.md` | 发现/确认的待确认项 |
| `04-施工记录.md` | 本阶段每轮施工/验收动作 |
| `05-验收与证据索引.md` | 验收证据索引 |

### 12.3 阶段收口硬 Gate

**若未更新功能表、阻塞项、待确认项、施工记录和验收证据索引，阶段不得标记 accepted。**

收口前必须执行追踪一致性检查（清单见 `06-状态口径与更新规则.md` §3）。

### 12.4 防最小化施工

10 条红线见 `证据/进度追踪/06-状态口径与更新规则.md` §4。核心原则：不得用 placeholder/mock/service-only/build-only 冒充 implemented/accepted；不得将本阶段可完成能力无理由标记 future；不得在未更新追踪材料的情况下结束阶段。

---

## 13. 本文档自维护规则

1. AGENTS.md 只含硬规则——工作流/模板/检查清单变更只改 Skill，不改本文档
2. 硬规则修改（C2 及以上）须经用户审核，更新头部修订说明
3. 版本号 V26.1.1 保持不变（除非正式发布新基线）
4. 阶段状态不写死在本文档——以 `交接/当前/` 最新材料为准
5. 与决策记录冲突时以决策记录为准，登记冲突为待确认项
6. 进度追踪规则（§12）的详述源为 `证据/进度追踪/`，本文档只定义硬 Gate，不复制追踪操作细节
