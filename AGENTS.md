# AGENTS.md

> 文件路径：`/home/king/rebuild/AGENTS.md`
> 文档状态：**accepted**
> 平台名称：rebuild
> 当前版本：V26.3
> 最后更新：**2026-09-17（版本号切换至 V26.3 —— C3 硬规则变更，用户 2026-09-17 明确要求「将所有的版本号核对已经更新为 263」。此前 R22 完成报告 §9 判断"应待总验收通过后由用户授权切换"，用户本次直接授权。§1 版本硬规则改为记录完整演进序列而非单点值，使"V26.2 总验收结论为 rework_required、发布收口已移至 V26.3 后"这一事实不被版本号切换掩盖。代码侧单一来源 `config.py:app_version` 同步切换、`R_STAGES` 补 R23/R24、`r_stage` 默认值改 R24）**；上一版 2026-09-15（用户裁决废止 §7.1 模块建设三步法 —— D-131。硬规则变更，C3 级，用户 2026-09-15 明确裁决「不需要三步法了。历史素材的价值我觉得基本不大了，后面不用继续按照这个模式」。废止范围严格限定于"强制产出三段产物 + 强制历史吸收前置"；§7 主体的历史版本只读规则、§6.3 分步执行、Skill §4.1 施工前必须输出施工计划、§10-24 架构决策须说明权衡，**全部不受影响**。同步改动 Skill §1 路由表 / §3.2 必须停止情形 / §4.3，及 references/workflows.md §3.3 / §4.1。D-067、D-062 中"三步法为强制前置"部分标 superseded；已产出的三段产物材料不作废）**；上一版 2026-09-02（V26.2 启动，用户裁决：§1 定位扩展为泛软件重构 + 「典型场景 + 开放扩展」两层场景体系（"包括但不限于"，典型场景资源相对更丰富）+ 准入三判据；新增 §10-27（禁把场景做成固定枚举）与 §10-28（禁未过历史密钥扫描或未经批准即公开推送）；登记 D-117~D-122。本次为硬规则变更，用户已明确裁决实质内容；**措辞由处理 Agent 依用户 2026-09-05 授权定稿（R20-1，CR-20260905-01），非用户逐字审阅**）**；再上一版 2026-06-29（整体重写：吸收卡帕西工程原则，硬规则与执行方法重新分割——本文档只留硬规则/禁止事项/判断边界，执行方法全部移至 Skill）
> 来源：卡帕西提示词 `产物/草稿/卡帕西提示词.md` 吸收 + 原 AGENTS.md R4-2 基线重构
> 审核状态：经用户审核通过（2026-06-25 / 2026-06-29 重写）
> 文档定位：rebuild 根目录 Agent 执行准则入口。定义硬规则、禁止事项、必读路径。**不包含工作流 / 执行方法 / 工程原则细节**——全部由 `rebuild-work-guidelines` Skill 承载。
> 设计原则：本文档 = 宪法（什么必须做 / 什么禁止做）；Skill = 法律实施细则（怎么做）。

---

## 0. Agent 启动硬规则

任何 Agent 接手 rebuild 任务时，**必须**按以下顺序启动：

```text
1. 写下自检：我不能依赖旧窗口记忆，必须先查本地事实源。
2. 读取本 AGENTS.md（本文档）——理解硬规则与禁止事项。
3. 加载 rebuild-work-guidelines Skill（.claude/skills/rebuild-work-guidelines/SKILL.md）——获取可执行流程。
4. 按 Skill 指引读取 交接/当前/ 和项目治理材料，判断阶段与任务类型。
```

**Skill 是强制入口**：本文档只定义硬规则。执行方法、工程原则、工作流、模板、检查清单全部由 Skill 承载。Agent 不得跳过 Skill 直接凭本文档执行复杂任务。

---

## 1. 平台与版本硬规则

- **平台名称**：`rebuild`（全小写，不是版本号）
- **当前版本**：`V26.3`（基线发布号，禁止写死到长期路径）。演进序列：V26.1.1（收束于 R17.5）→ V26.2（R18~R22，总验收结论 `rework_required`，发布收口移至 V26.3 后）→ **V26.3**（R23 文档核对与优化 / R24 代码与架构核对与优化，D-123）
- **产品定位**：**软件重构平台（泛软件重构）**。V26.2 起进入第二阶段（D-117），**不再局限于信创替换**。场景体系分两层：**① 典型场景**——信创切换 `xinchuang_switch` / 软件现代化 `modernization` / 软件移植 `porting`，平台重点支持、**配套资源相对更丰富**；**② 开放扩展**——"**包括但不限于**"典型场景，**支持用户自由修改与自定义场景包**，新增场景须为"新增场景包目录、零代码改动"，**不得实现为固定枚举**。任何场景须满足**准入三判据**方可进入 P0-P6（D-118）：① 有源代码 ② 有可判定的目标态 ③ 产出物可机器验证。**仍禁止**扩展为泛 AI 办公 / 通用 Agent 市场 / 泛项目管理 / 无源码纯咨询平台 / 只展示 Agent 能力而不闭环。场景体系两层结构、典型场景包配套与准入三判据的**详述源** = `01-决策记录.md` D-117 / D-118；本文只保留硬规则摘要，不复制细节（§6.2）。
- **主编排底座**：LangGraph（D-037，硬规则，变更需 C4 申请）。dsh（DeepSeek Harness）**只吸收范式不吸收代码**，不引入 TS 运行时或第三方编排框架（D-120）。
- **开源与许可**：**MIT**；**沿用原有 git 仓库并保留全部提交历史**（D-119）。转公开前须过全 git 历史密钥扫描（绝对硬 Gate）；转公开与推送须用户明确批准。
- **当前阶段**：以 `交接/当前/` 和 `文档/00-项目治理/01-决策记录.md` D-010 为准

---

## 2. 阶段体系硬规则

### 2.1 两套体系严格分离

| 体系 | 用途 | 编号 |
|------|------|------|
| P 阶段 | 用户项目产品流程 | P0-P6 |
| R 阶段 | 平台自身建设 | R0-RN |

**禁止混用**。禁止使用旧 Phase / F0-F6 / S0-S7 / RunEngine 阶段名。

### 2.2 R 阶段定义

完整 R 阶段列表见 `01-决策记录.md` D-010。当前阶段状态以 `交接/当前/` 最新交接材料为准，不得凭记忆或旧窗口上下文判断。

### 2.3 P 阶段执行架构硬规则（目标驱动 Agent）

rebuild 是 AI 平台，不是确定性/规则引擎。每个 P 阶段必须是"目标驱动的 Agent 循环"：

1. 每阶段【固定】的只有：阶段目标、验收标准锚点、用户 Gate、Artifact/Evidence 契约、安全红线。计划、工具/命令/MCP/Skill 选择、执行、任务拆分、产物内容，一律由 Node Worker Agent(LLM) 过程中自主生成，不得写死为确定性规则或主线。
2. 确定性【只用于两处】：①采集（clone/列文件/读内容/跑命令/计数/脱敏扫描）；②验证（编译/测试/diff/等价检查）。识别、理解、规划、解读、风险研判、验收判定一律 LLM 推理。确定性能力以 Skill/Tool/Expert Agent/MCP 资源形式被 Agent 调用（D-063），不做阶段主线。
3. 通用 vs 样本实例分层：阶段的通用目标/验收锚点/证据/Gate 逻辑可沉淀为平台规则；样本实例值与项目特定命令（文件名/依赖数/SQL 方言/目标环境等）由 Agent 按项目生成，不得硬编码进 handler / Project schema / 前端固定字段 / 默认 Gate 规则。
4. 验收：验收标准是相对固定锚点，判定由独立 Acceptance Agent(LLM，D-082) 基于目标+证据自适应裁量；确定性只在其调用的验证工具层。No Evidence No Completed 不变。
5. 无 Key 不兜底：P 阶段识别/推理需有效模型 Key 且走 ModelGateway(D-098)；无有效 Key 时诚实 blocked，不得降级为确定性规则冒充完成。
6. 循环骨架复用既有 StageLoop / NodeLoop / ReviewPass / TaskGraph（D-091/D-065），不另造。

> 关联：D-063、D-065、D-091、D-098、产品定义基准 §5.3、01-用户旅程 §5、Q-R17.4-3-1（本条落实其裁决）。

---

## 3. 必读材料（五层事实源）

Agent 接手任务时**必须按需逐层读取**，不得凭记忆：

| 层级 | 路径 | 内容 |
|------|------|------|
| L1 任务入口 | `交接/当前/` | 当前任务提示词、阶段交接说明 |
| L2 项目治理 | `文档/00-项目治理/00~07` | 决策记录、术语表、阶段计划等 |
| L3 专题材料 | `文档/01~09/` | 按任务类型读取对应专题 |
| L4 产物证据 | `产物/` `证据/` | 当前草稿、产物、运行证据 |
| L5 历史参考 | `rebuild-archive/` | 只读参考，禁止直接复制 |

详细读取策略及按任务类型的专题路由表见 Skill。

---

## 4. 材料身份边界（硬规则）

| 身份 | 路径 | 性质 |
|------|------|------|
| 项目文档 | `文档/` | 长期事实源，状态为 accepted 后方可引用 |
| 交接材料 | `交接/当前/` → `交接/已完成/` | 协作传递，不自动成为项目文档 |
| 产物 | `产物/` | Agent 输出，不自动成为事实源 |
| 草稿 | `产物/草稿/` | 待正式化的中间产物 |
| 证据 | `证据/` | 支撑判断和验收的实证 |
| 运行记录 | 后续指定目录 | 不得混入正式文档 |

**晋升规则**：草稿 → 正式候选（待审核） → 用户审核 → accepted。交接材料需晋升为正式文档时必须走文档正式化流程。

---

## 5. 文档与交接流转规则

### 5.1 生命周期硬规则

- 草稿不得直接晋升为正式文档（必须走正式化流程）
- 产物不得自动成为事实源
- 交接材料不得自动成为项目文档
- 正式候选未经用户审核不得标记为 accepted
- 禁止为每个子文档生成零散独立报告（维护阶段总报告 + 总表）

### 5.2 流转路径

```
产物/草稿/  →  Agent 评估完善  →  文档/ (状态: 待审核)  →  用户审核  →  accepted
                                                                  ↘  需返工  →  rework
交接/当前/  →  本轮完成  →  交接/已完成/<轮次>/
产物/草稿/  →  处理完成  →  产物/已完成/  或晋升至 文档/ (走正式化流程)
```

---

## 6. 核心执行原则

### 6.1 事实 / 推断 / 建议分离（硬规则）

```
事实：来自已读取文件、代码、目录或用户明确裁决
推断：基于事实的判断，必须标注不确定性
建议：Agent 的处理方案，必须说明理由和风险
```

**禁止**：把推断写成事实、把建议伪装成已确认决策、把用户举例当作完整范围。

### 6.2 单一事实源 + 引用（防漂移）

每个事实 / 规则 / 定义 / 枚举只在唯一详述源文档中完整展开，其余文档**引用而不复制**。修改时只改详述源。发现两处以上全文复制 → 收敛或登记待确认项。

### 6.3 分步执行

```text
列路径 → 读关键文件 → 摘取事实 → 形成判断 → 输出计划 → 分步写入 → 分段校验
```

复杂任务不得跳过计划直接施工。

---

## 7. 历史版本使用规则

历史版本（`rebuild-archive/`）**只读参考**。

| 允许 | 禁止 |
|------|------|
| 吸收设计思想、字段经验、交互心智、失败反例 | 直接复制旧代码、直接复制旧文档 |
| | 复活旧 RunEngine |
| | 复用旧 S0-S7 / F0-F6 为主流程 |

### 7.1 模块建设三步法 —— **已废止（2026-09-15，用户裁决，D-131）**

```text
原规则（现已废止）：建设任一模块必须 ① 先独立提自有方案 → ② 再深度吸收历史方案
                    → ③ 用吸收到的经验完善方案后施工，三段产物缺一不可进入施工。
```

**废止依据**：用户 2026-09-15 明确裁决「不需要三步法了。历史素材的价值我觉得基本不大了，后面不用继续按照这个模式」。

**废止范围（严格限定，勿扩大解读）**：

| 一并废止 | **仍然有效，不受本次废止影响** |
|---|---|
| 强制产出 ①自有方案 / ②历史吸收 / ③完善方案 三段产物 | **§6.3 分步执行**：复杂任务不得跳过计划直接施工 |
| 强制"深度吸收历史方案"作为施工前置 | **Skill §4.1**：涉及代码施工必须**先输出施工计划**（模板见 `references/templates.md` §3） |
| — | **§7 本节主体**：历史版本仍为只读参考，仍禁止直接复制旧代码/旧文档、禁止复活旧 RunEngine、禁止复用旧 S0-S7 / F0-F6 为主流程 |
| — | **§10-24**：架构层决策仍须向用户说明方案选择、权衡与风险 |

> **要点**：废止的是"**必须**产出三段产物"这一强制流程，**不是**"施工前不用出计划"。施工计划仍为硬要求，只是不再强制拆成三段、不再强制做历史吸收章节。Agent 若判断某次施工确实能从历史材料获益，**可以**自行参考（§7 允许），但不再是义务。

> 被替代决策：D-067、D-062 中关于"模块建设三步法为强制前置"的部分 → `superseded by D-131`。两决策的其余内容（历史版本只读、禁止复制）不受影响。
>
> 已产出的三步法材料不作废：`产物/草稿/批次一-①②③*.md`（V26.2 返工批次一）与 `产物/已完成/R22/R22-①②③*.md` 等既有三段产物仍为有效参考材料，不因本次废止而失效或需回收。

---

## 8. 安全与密钥（硬规则）

### 绝对禁止

输出或写入：Key、Token、Secret、Password、`.env` 明文、API Key 明文、认证凭据。

禁止写入目标：报告、日志、Trace、Audit、前端页面、截图说明、交接材料、正式文档、Git 提交。

### 安全记录写法

```
发现某类配置文件，内容已脱敏，需用户或本地环境自行确认。
```

---

## 9. Skill 体系规则

### 9.1 强制使用

Agent 执行 rebuild 任务时**必须**加载 `rebuild-work-guidelines` Skill。Skill 提供完整的工作流、工程原则、模板与检查清单。

### 9.2 AGENTS.md 与 Skill 边界

```
AGENTS.md = 硬规则 / 禁止事项 / 判断边界（本文档）
Skill     = 工程原理 / 执行方法 / 模板 / 检查清单 / 操作流程
```

冲突时以 AGENTS.md 为准，并将冲突登记为待确认项。

### 9.3 分类

- **R 系列**：建设用 Skill，位于 `skills/R-建设执行/`
- **P 系列**：平台运行用 Skill，位于 `平台资源/skills/P系列/`

两者不得混放。

---

## 10. 禁止事项总表

```text
 1. 凭记忆回答项目事实
 2. 跳过 Skill 直接凭本文档执行复杂任务
 3. 直接复制历史版本文档为当前正式文档
 4. 把草稿直接搬入正式区
 5. 把正式候选在未经用户审核的情况下标记为已审核或已完成
 6. 把 V26.1.1 当平台名称
 7. 混用 P 阶段和 R 阶段
 8. 复活旧 RunEngine 或使用旧阶段名
 9. 将密钥写入任何文档、报告、日志、Trace、前端或提交
10. 将产物自动晋升为项目文档或事实源
11. 跳过任务要求的评估、计划、检查和校验
12. 为每个小文档生成零散独立报告（应维护总报告 + 总表）
13. 遇到不确定项仍继续执行
14. 以"后续补齐"绕过当前必须确认的问题
15. 用用户举例替代完整范围判断
16. 因内容长而省略关键检查
17. 只写 service/API 而无任何页面或入口实际调用，却把"已写接口"当作"已联调"标记阶段完成（D-074）
18. 在未更新 证据/进度追踪/ 功能表、阻塞项、待确认项、施工记录和验收证据索引的情况下标记阶段 accepted
19. 在代码/配置/脚本/.env 中各自另设服务端口（后端统一 8000、前端 dev 5173 / 容器 8080）
20. 用 placeholder / mock / build-only 冒充 implemented — 失败不得回退 mock（D-097）
21. 静默 except — 异常必须发声（公理 3）
22. 硬编码模型名 / endpoint — 必须走 ModelGateway → 动态策略（D-098）
23. "顺手"重构与任务无关的代码 — 只改要求的一处（精准修改原则）
24. 在未向用户说明方案选择、权衡和风险的情况下做出架构层面决策
25. 把 P 阶段的识别/理解/规划/解读/风险研判/验收判定做成确定性规则或写死主线（须由 Node Worker Agent LLM 推理；确定性只限采集与验证，D-063/§2.3）
26. 把样本实例值或某项目特定命令硬编码进 handler/schema/前端/默认 Gate（通用可沉淀、样本随项目替换，§2.3）
27. **把场景做成固定枚举或封闭清单**（场景分典型 + 开放两层，"包括但不限于"；准入靠 D-118 三判据；新增场景须为"新增场景包目录、零代码改动"；场景包须允许用户修改/改写，不得锁为只读；不得静默套用信创口径，D-117）
28. **未过全 git 历史密钥扫描即转公开仓库，或未经用户明确批准执行公开推送**（D-119）
```

---

## 11. 任务完成自检

```text
 1. 是否加载并遵循了 rebuild-work-guidelines Skill？
 2. 是否识别了当前阶段和任务类型？
 3. 是否读取了必要事实源？
 4. 是否区分事实、推断和建议？
 5. 是否执行了分步读取、分步写入、分段校验？
 6. 是否更新了必要的文档、交接材料、总报告或总表？
 7. 是否存在待确认项？
 8. 是否存在安全或密钥风险？
 9. 是否误用历史术语或旧版本事实？
10. 是否把草稿、产物、证据、正式文档混淆？
11. 是否可以交给用户审核？
12. 若本任务属于阶段收口：是否已执行追踪一致性检查并更新全部追踪文件？
```

---

## 12. 模型配置与 ModelGateway 使用规范（R5-3 起）

### 12.1 密钥安全（硬规则，不可绕过）

- Key **仅从环境变量读取**，不得硬编码在代码 / 配置文件 / 文档中
- Key 存放位置：`backend/.env`（`.gitignore` 保护）
- 配置文件中只存 Provider 非敏感信息，Key 走 env
- API 响应只返 `credential_status`，不返 Key 值
- 所有报告/日志/Trace/Audit/前端/截图中的 Key 统一标记为 `[REDACTED]`

### 12.2 Agent 接手模型相关任务的标准流程

```text
1. 读取 backend/app/config/model_profiles.yaml 了解已配置 Provider/Profile/Strategy
2. 检查 backend/.env 中对应环境变量是否已填写（仅判断存在性，不读取值）
3. 通过 GET /api/model/providers 查看 Provider 配置状态
4. 通过 POST /api/model/self-test 验证连通性
5. 所有模型调用必须经 ModelGateway → LiteLLM Adapter，不得直连 Provider
6. 不得在任何输出中暴露 Key 值
```

---

## 13. 进度追踪强制要求（R5-1 起）

### 13.1 强制更新

每个 R 阶段方案、施工、验收、收口时，**必须**检查并更新以下追踪文件：

| 文件 | 更新内容 |
|------|---------|
| `01-功能表.md` | 能力状态、证据路径、缺口 |
| `02-阻塞项.md` | 发现/解决的阻塞 |
| `03-待确认项.md` | 发现/确认的待确认项 |
| `04-施工记录.md` | 本阶段每轮施工/验收动作 |
| `05-验收与证据索引.md` | 验收证据索引 |

### 13.2 阶段收口硬 Gate

**若未更新功能表、阻塞项、待确认项、施工记录和验收证据索引，阶段不得标记 accepted。** 收口前必须执行追踪一致性检查（清单见 `证据/进度追踪/06-状态口径与更新规则.md` §3）。

### 13.3 防最小化施工

核心原则：不得用 placeholder / mock / service-only / build-only 冒充 implemented / accepted；不得将本阶段可完成能力无理由标记 future；不得在未更新追踪材料的情况下结束阶段。

---

## 14. 本文档自维护规则

1. AGENTS.md 只含硬规则——工作流 / 工程原则 / 模板 / 检查清单变更只改 Skill，不改本文档
2. 硬规则修改（C2 及以上）须经用户审核，更新头部修订说明
3. 版本号 V26.1.1 保持不变（除非正式发布新基线）
4. 阶段状态不写死在本文档——以 `交接/当前/` 最新材料为准
5. 与决策记录冲突时以决策记录为准，登记冲突为待确认项
6. 进度追踪规则的详述源为 `证据/进度追踪/`，本文档只定义硬 Gate，不复制操作细节
