# rebuild —— 容器化（docker compose）环境变量示例
#
# 用法：  cp .env.example .env   然后按需取消注释并填写，再 docker compose up --build
#
# 本文件被 docker-compose.yml 以 env_file 方式注入 backend 服务，
# 同时为 compose 文件里的 ${VAR:-default} 占位提供取值。
# 本地开发（非容器）请改用 backend/.env.example → backend/.env。
#
# ── 安全红线（AGENTS.md §8/§12）───────────────────────────────────────────
# 本文件只列「变量名 + 说明 + 明显占位」，**不含任何真实密钥值**。
# 真实 Key / Token / Secret 只写在本机 .env（已被 .gitignore 保护），
# 绝不提交、绝不写入镜像、绝不出现在日志 / issue / 报告 / 截图中。
# ─────────────────────────────────────────────────────────────────────────
#
# 端口标准（单一事实源，禁止另设）：后端统一 8000；前端 dev=5173 / 容器=8080。
# 启动后：前端 http://localhost:8080 ，后端 http://localhost:8000/api/health
#
# 图例：  [必填] 缺失则相关功能不可用    [可选] 有默认值，不设即用默认
#
# 说明：容器内路径类变量（数据目录、资源目录、工作区）已由 docker-compose.yml
# 的 environment 段直接给定，且 environment 优先于 env_file，在本文件中设置无效，
# 故此处不再列出。


# ═══════════════════════════════════════════════════════════════════════
# 1. 凭据加密主密钥
# ═══════════════════════════════════════════════════════════════════════

# REBUILD_MASTER_KEY  [必填，若要使用凭据(BYOK)功能]
#   平台内保存的模型 / 集成凭据的 AES-256-GCM 加密主密钥。
#   缺省时走开发派生密钥，仅供本地试跑；容器化部署必须显式设置。
#   生成方式（在你自己的终端执行，把输出填到本机 .env，不要提交）：
#       python -c "import secrets; print(secrets.token_hex(32))"
# REBUILD_MASTER_KEY=<32-byte-hex>


# ═══════════════════════════════════════════════════════════════════════
# 2. 数据库
# ═══════════════════════════════════════════════════════════════════════

# 默认 SQLite（compose 已指向命名卷，开箱即用、零外部依赖），无需在此设置。

# REBUILD_DATABASE_URL  [可选] 改用 PostgreSQL 时设置（同时以 pg profile 启动：
#   docker compose --profile pg up --build）
# REBUILD_DATABASE_URL=postgresql+psycopg://<user>:<password>@db:5432/rebuild

# 以下三项仅在启用 pg profile 时用于初始化 postgres 容器。
# 请勿沿用示例占位作为真实口令。
# POSTGRES_USER=rebuild
# POSTGRES_PASSWORD=<choose-a-strong-password>
# POSTGRES_DB=rebuild


# ═══════════════════════════════════════════════════════════════════════
# 3. 跨域 / 前端地址（`REBUILD_` 前缀由后端 Settings 读取）
# ═══════════════════════════════════════════════════════════════════════
#
# 本文件面向容器化拓扑：前端 nginx 在 8080。

# REBUILD_CORS_ORIGINS  [可选] 允许的前端来源，逗号分隔
REBUILD_CORS_ORIGINS=http://localhost:5173,http://localhost:8080

# REBUILD_FRONTEND_URL  [可选] OAuth 登录完成后跳回的前端地址
REBUILD_FRONTEND_URL=http://localhost:8080

# REBUILD_DEBUG  [可选] 调试模式；对外部署请设为 false
# REBUILD_DEBUG=false


# ═══════════════════════════════════════════════════════════════════════
# 4. 模型接入（BYOK —— 自带模型凭据）
# ═══════════════════════════════════════════════════════════════════════
#
# 所有模型调用统一经 ModelGateway → LiteLLM 适配层，不直连 Provider、不硬编码
# 模型名与 endpoint。Provider 的非敏感信息（含各自使用哪个环境变量名取 Key）
# 定义在 backend/app/config/model_profiles.yaml，Key 本身只从环境变量读取，
# 只经 env_file 注入，绝不写进镜像。
#
# 无有效 Key 时，需要推理的阶段会诚实报告 blocked，不会降级为规则模板冒充完成。

# LLM_API_KEY  [必填之一] 通用兜底 Key（单 Provider 部署最省事的一种）
# LLM_API_KEY=your-api-key-here

# LLM_BASE_URL  [可选] 通用兜底 Key 对应的 OpenAI 兼容 endpoint
# LLM_BASE_URL=https://<your-openai-compatible-endpoint>/v1

# 按 Provider 分别配置时使用 {PROVIDER}_API_KEY 形式：
# DEEPSEEK_API_KEY=your-api-key-here
# AGNES_API_KEY=your-api-key-here

# OPENAI_API_KEY / OPENAI_BASE_URL  [可选]
#   部分 OpenAI 兼容 SDK 调用路径会读取这两个标准变量名。
# OPENAI_API_KEY=your-api-key-here
# OPENAI_BASE_URL=https://<your-openai-compatible-endpoint>/v1


# ═══════════════════════════════════════════════════════════════════════
# 5. 模型调用重试与超时（均 [可选]，不设即用代码内默认值）
# ═══════════════════════════════════════════════════════════════════════

# LLM_MAX_RETRIES            单次模型调用的最大重试次数
# LLM_REQUEST_TIMEOUT        单次模型请求超时（秒）
# LLM_RETRY_BASE_DELAY       重试退避基准延迟（秒）
# LLM_STREAM_TOTAL_TIMEOUT   流式响应总超时（秒）
# LLM_MAX_RETRIES=
# LLM_REQUEST_TIMEOUT=
# LLM_RETRY_BASE_DELAY=
# LLM_STREAM_TOTAL_TIMEOUT=

# STAGE_TRANSIENT_MAX_RETRIES        阶段执行遇瞬时错误的最大重试次数
# STAGE_TRANSIENT_RETRY_BASE_DELAY   阶段重试退避基准延迟（秒）
# STAGE_TRANSIENT_MAX_RETRIES=
# STAGE_TRANSIENT_RETRY_BASE_DELAY=

# P3_PLANNING_MAX_TOKENS   规划阶段单次生成的最大 token 数
# P3_PLANNING_TIMEOUT      规划阶段调用超时（秒）
# P4_GEN_MAX_TOKENS        执行阶段代码生成的最大 token 数
# P3_PLANNING_MAX_TOKENS=
# P3_PLANNING_TIMEOUT=
# P4_GEN_MAX_TOKENS=

# LITELLM_LOG  [可选] LiteLLM 自身日志级别，排查模型调用问题时临时打开
# LITELLM_LOG=DEBUG


# ═══════════════════════════════════════════════════════════════════════
# 6. 执行模式
# ═══════════════════════════════════════════════════════════════════════

# EXECUTION_MODE  [可选] 命令执行提供方：local（容器内子进程，默认）| container
#   切到 container 还需按 docker-compose.yml 注释同时完成另两步（挂载 docker.sock
#   并构建沙箱镜像）。挂载 docker.sock 等同把宿主 Docker 控制权交给 backend，
#   仅建议在可信单机环境启用。
# EXECUTION_MODE=local

# P5_EXECUTION_MODE  [可选] 验证阶段命令执行模式，默认 workspace_local
# P5_EXECUTION_MODE=workspace_local

# SKILL_SOURCE_ROOT  [可选] Skill 资源加载根目录，覆盖默认推导路径
# SKILL_SOURCE_ROOT=/app/source/skills

# WORKSPACE_DIR  [可选] 执行环境内部的工作区挂载路径
# WORKSPACE_DIR=/workspace


# ═══════════════════════════════════════════════════════════════════════
# 7. GitHub OAuth（可选，代码托管集成用）
# ═══════════════════════════════════════════════════════════════════════
#
# 回调地址在 dev 与容器两套拓扑下相同（后端都在 8000），GitHub OAuth App
# 只需注册一次：
#   http://localhost:8000/api/integrations/git/oauth/github/callback

# REBUILD_GITHUB_CLIENT_ID=your-client-id-here
# REBUILD_GITHUB_CLIENT_SECRET=your-client-secret-here
# REBUILD_GITHUB_REDIRECT_URI=http://localhost:8000/api/integrations/git/oauth/github/callback


# ═══════════════════════════════════════════════════════════════════════
# 8. 可选社区服务（profile=community，默认不启动）
# ═══════════════════════════════════════════════════════════════════════

# COMMUNITY_DATABASE_URL  [可选] 社区服务独立数据库连接串（不与主平台共库）
# COMMUNITY_DATABASE_URL=sqlite:////app/.data/community.db
