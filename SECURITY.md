# 安全策略

感谢你帮助 rebuild 变得更安全。本文档说明**如何上报漏洞**、**我们承诺的响应时限**，以及**我们如何处理**你的报告。

---

## 请不要在公开 issue 中报告漏洞

**安全漏洞不要提交到公开 issue、Pull Request 或 Discussions。** 公开描述可利用的缺陷等于把利用路径先交给攻击者，而修复与发布需要时间。

若你已经不小心公开了，请立即告知我们（走下方私密渠道），我们会协助尽快处置。

---

## 如何上报

请使用 **GitHub Security Advisory** 私密上报：

1. 进入本仓库的 **Security** 标签页；
2. 选择 **Report a vulnerability**；
3. 填写描述并提交。

直达链接：`https://github.com/RedKingLu/rebuild/security/advisories/new`

该渠道的内容仅维护者与你可见，可在私密环境中讨论、协作与验证补丁，修复发布后再一并公开披露。

> 我们**不提供**专用安全邮箱。请不要向任何声称是本项目安全邮箱的地址发送报告——本项目从未设置过。日常使用问题、功能讨论请走 **GitHub Discussions**（非安全议题）。

### 报告中请尽量包含

- 受影响的组件与版本（例如 `backend/app/...`、平台基线版本号）
- 复现步骤，或最小复现用例
- 你观察到的实际影响（读取任意文件？执行任意命令？越权访问？信息泄漏？）
- 运行环境（操作系统、Python / Node 版本、是否容器部署）
- 可选：你建议的修复方向

### 请不要在报告中附带真实密钥

如果复现需要凭据，请使用**你自己的、可立即吊销的测试凭据**，并在报告中脱敏为 `[REDACTED]`。本项目对密钥有硬规则：任何报告、日志、审计记录、前端页面均不得留存明文密钥。若你怀疑某个密钥已经泄漏，请在报告中说明"存在某类凭据泄漏"，**不要粘贴其值**。

---

## 响应时限承诺

当前项目由个人维护，我们承诺一个**能真正做到**的时限，而不是好看的时限：

| 环节 | 承诺 |
|---|---|
| **首次响应**（确认收到、说明后续安排） | **5 个工作日内** |
| 初步评估结论（是否受影响、严重度判定） | 首次响应后 10 个工作日内 |
| 修复排期与进展同步 | 至少每 14 天同步一次，直至关闭 |
| 公开披露 | 修复发布后，与报告者协商时间点后一并披露 |

修复时限按严重度分级，**评估后给出具体日期**，不做统一承诺：

| 严重度 | 典型特征 | 修复目标 |
|---|---|---|
| **Critical** | 无需认证即可远程执行代码、读取任意文件、获取密钥或完全绕过风险 Gate | 尽最大努力优先修复，评估后给出具体日期；期间提供缓解措施（配置规避、临时禁用相关能力） |
| **High** | 需要一定前置条件的越权、路径穿越、凭据泄漏、危险操作绕过审批 | 排入最近一个建设阶段修复 |
| **Medium** | 影响有限或利用条件苛刻的问题（如仅本机、仅特定配置） | 排入常规迭代 |
| **Low** | 加固建议、深度防御改进、无实际可利用路径 | 视情况纳入，或记录为已知项 |

**如果我们无法在承诺时限内响应**（个人维护，可能遇到不可用期），我们会在渠道内如实说明并给出新的时间点，而不是让报告静默悬置。

---

## 处理流程

```
你私密上报  →  我们确认收到（≤5 个工作日）
            →  复现与严重度评估
            →  在 Advisory 内同步结论与修复计划
            →  开发补丁（必要时请你验证）
            →  发布修复
            →  协商披露时间，公开 Advisory 并致谢
```

我们不运营漏洞赏金计划，也没有奖金预算。我们会在 Advisory 与发布说明中**具名致谢**（除非你要求匿名）。

---

## 支持范围

| 在范围内 | 不在范围内 |
|---|---|
| 后端 API 与服务（`backend/`） | 你自行修改后引入的问题 |
| 前端应用（`frontend/`） | 第三方依赖自身的漏洞——请上报给上游；若我们的用法放大了影响，则在范围内 |
| 部署与容器编排配置（`deploy/`、`docker-compose.yml`） | 需要已获得宿主机 root 权限才能触发的问题 |
| 凭据存储与脱敏机制 | 缺少加固项但无实际利用路径（欢迎作为普通 issue 提出） |
| 风险分级与审批 Gate 的绕过 | 社会工程、物理攻击、对我们基础设施的压力测试 |
| 执行沙箱的隔离绕过 | 自动化扫描器原始报告（无人工验证与复现步骤） |

**只支持默认分支的最新提交。** 项目仍在活跃建设中，尚未提供长期支持版本；历史标签不回溯打补丁。

---

## 请勿进行的测试

- 不要对任何非你自有的部署实例进行测试
- 不要进行拒绝服务 / 压力测试
- 不要尝试访问、修改或删除他人数据
- 请在你本地自建的实例上验证

---

## 关于本项目的安全特性

rebuild 会**读取并处理用户提供的源代码仓库**，并在受控条件下执行构建与测试命令。因此以下方向的问题我们尤其关注：

- **路径穿越**：项目源码目录、上传解包、导出下载等环节的路径逃逸
- **命令注入 / 沙箱逃逸**：执行阶段跳出隔离边界
- **凭据处理**：Key 只从环境变量读取，不落盘、不入日志、不回显前端；API 仅返回凭据状态而非凭据值
- **高风险操作审批**：不可逆 / 生产级操作（风险分级 L5）必须经用户显式批准，绕过审批即为漏洞

如果你发现上述任一机制可被绕过，请按本文档上报——即使你不确定影响有多大。**不确定也请报**，判定是我们的工作。
