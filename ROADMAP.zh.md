# ROADMAP —— rebuild 下一步做什么

> **语言 / Language**：中文（当前页，**权威版本**） · [English](./ROADMAP.md)
> 本仓库对外文档以**中文为权威版本**：英文版是中文版的翻译，两版表述冲突时**以中文版为准**。
> 语言变体清单见 [`.i18n.yaml`](./.i18n.yaml)。本页内的文档链接指向中文版。

> 平台：rebuild · 版本：V26.2（在建） · 许可：[MIT](./LICENSE)
> 配套阅读：[VISION.md](./VISION.zh.md)（为什么存在与明确做不到什么）、[ARCHITECTURE.md](./ARCHITECTURE.zh.md)（怎么实现的）、[README.md](./README.zh.md)（如何跑起来）

---

## 0. 怎么读这份路线图

**三条阅读约定，请先看完再往下：**

**① 状态分三档，不把规划写成已实现。**

| 标注 | 含义 |
|---|---|
| **已实现** | 有真实运行证据，可以按现有功能预期 |
| **部分实现** | 能力在场但有明确边界（环境依赖、证据不完整、覆盖不全） |
| **规划中** | 尚未实现。**不要按已有功能预期**，也不代表一定会做 |

**② 不承诺时间表。** 本文档按"优先级方向"组织，不给日期、不给版本号承诺。rebuild 由极少数人维护，任何日期都会变成失信。已排定优先级的事项写在前面，条件未满足的写在后面。

**③ 路线图会因为"发现问题"而改变顺序。** 平台的核心卖点是"结论可信"，因此**修正一处失真的判定，优先级高于新增一项功能**。如果你报告了一个"平台把不可用说成可用"的缺陷，它会插到所有新功能前面。

---

## 1. 当前状态快照

**rebuild 目前处于可运行、可端到端跑通、但仍在积极建设中的阶段。它不是一个开箱即用的全自动迁移平台。**

**已实现**（有真实运行证据）：

- 前后端可本地启动，项目可创建并按 P0–P6 推进（P0 接入 → P1 建档 → P2 评估 → P3 规划 → P4 执行 → P5 验证 → P6 交付），流程可裁剪。
- 阶段编排、用户 Gate、证据与执行轨迹、任务图、模型网关、工作区隔离与执行沙箱、代码托管集成等主干链路已实现并有自验证据。
- 容器内真实构建：宿主无对应 SDK 但 Docker 可用时，按需拉取官方 SDK 镜像在隔离环境真实执行构建，产出真实编译结论与诊断明细。
- 臆造依赖识别：对包仓库做存在性与版本可解析性校验，把模型编造的依赖尽早暴露。
- 抗伪造判定：产物立不住时，验证阶段不会因为"构建成功"就放行 —— 这条链路已在真实运行中验证有效。
- 场景包运行期动态发现：新增或删除场景包目录无需重启后端、无需改任何代码即刻生效。
- 后端测试套件约 1600 个用例，可全量运行。

**部分实现**（边界见 [VISION.md](./VISION.zh.md) 第 6.2 节与 [ARCHITECTURE.md](./ARCHITECTURE.zh.md) 第 16 节）：依赖校验的覆盖面、验证维度的环境覆盖、长时任务观测、生命周期挂点、场景知识的决策权重。

**当前需要人工介入的环节**（这是设计，不是缺口）：阶段晋级需用户 Gate 授权；改造产物需人工复核后采纳；模型凭据须自备；真实构建结论依赖你的环境具备 Docker 或 SDK。

---

## 2. 优先方向一：把"可信度"收敛完整

这一组是当前的第一优先级。它们不新增功能，而是把已有结论的可信区间收窄 —— 这比加功能更重要。

### 2.1 千文件级真实规模项目的端到端验证（规划中）

**现状（请准确理解）**：

- **当前代码基线上已完成的完整端到端真跑（P0 到 P5/P6），源码规模只有 5 个文件** —— 一个为端到端验收准备的微型示例桩。它证明整条链路能走通、抗伪造门禁会真的拦下立不住的产物，**不证明规模承载能力**。
- **更早的代码基线上，确实在一套约 1050 个文件、84 个 C# 源文件的真实 ASP.NET WebForms 系统上产出过阶段执行轨迹与执行阶段的 PoC 产物**（数十个改造后代码文件与补丁）。这段历史真实存在，但属更早基线、且是阶段性 PoC。
- ⇒ **当前基线尚未在千文件级真实项目上做过完整端到端验证。**

**为什么排第一**：规模会同时压出三类未验证的失败模式 —— 单阶段耗时、上下文预算压力、以及长链路中途中断后的恢复行为。现在对这三者的了解是"设计上应该可以"，不是"实测确认"。

**做完之后能回答什么**：一次真实规模改造大致需要多久、在哪个阶段最可能卡住、需要多少次人工裁决。目前这些问题**没有可信答案**，任何人给你一个数字都是猜的。

> 附一条自己踩过的坑：上述两个样本**同名**、规模相差两百倍。引用样本时必须核对实际内容而非名字 —— 这是成熟度描述失真的常见来源。


### 2.2 依赖真实性校验的覆盖面（部分实现）

**已有实测证据（正反两面）**：真实 registry 可达时，一个模型编造的 NuGet 包被判 `package_not_found`（HTTP 404），同批另 7 个坐标判 `resolvable` —— 即"识别假包"与"确认真包"都有真实响应支撑。

**已守住的红线**：网络不可达或被重定向到非白名单主机时，平台标 `indeterminate` 而**不给结论**，并明确记录未修改依赖清单、未降级、未推荐替代包。**绝不把网络失败误判为"包不存在"。**

**缺口（规划中）**：npm / Maven 的语义化版本范围表达式（`^` / `~` / 属性占位 / BOM / 快照版本）当前**不做求解**，一律诚实标"版本无法判定"。要给出确定结论需要引入新的求解依赖，属独立评估，尚未决定是否做。


### 2.3 产出代码规模的对比观察（规划中）

**目标**：把"源码规模 vs 产出代码规模"做成一个**非阻断的观察指标**，比例异常偏低时标注"需人工复核"。

**明确不做**：不设阈值拦截、不接入 Gate、不改变阶段晋级判定。**任何情况下这个指标都不会让阶段失败或阻塞。**

**为什么不设阈值**："多低算异常"必须由真实规模样本决定，在有样本之前定阈值就是拍脑袋。因此阈值本身依赖 2.1 完成。

### 2.4 若干已登记的实现不整齐处（规划中）

这些都不影响当前运行正确性，但会让"读代码的人"产生误判，因此登记在案：

- 上下文预算裁剪可能丢弃配方声明为必需的层而没有显著发声。
- 审计记录的持久化标记字段被无条件写成"文件+内存"，且写在文件落盘之前 —— 项目 ID 缺失或写盘失败时该字段与事实不符（轨迹记录的同名字段实现是正确的，两者不一致）。
- 风险阶梯常量在两处存在同值副本（当前取值一致，无缺陷，但属易漂移模式）。
- Gate 类型常量清单在代码中没有强制校验点，实际使用的类型比清单更多。

### 2.5 前端运行期证据补全（部分实现）

**现状**：部分前端改动缺浏览器级的运行期证据（截图或录屏）。页面确实调用了真实接口并渲染真实响应，但证据形态不完整。

**边界**：这不影响功能，影响的是"我们能否证明它联调过"。

---

## 3. 优先方向二：让开放扩展真正好用

**场景体系是"典型场景 + 开放扩展"两层，典型场景不是全集。** 平台支持"包括但不限于"信创切换 / 软件现代化 / 软件移植这三类场景。这一节是让第二层更好用。

### 3.1 场景包生态（部分实现）

**已实现**：新增场景 = 新增一个目录，零代码改动，运行期即刻生效（实测无需重启后端）；用户可新建场景包，也可复制典型场景包后改写；无专用场景包时走通用兜底并明确标注"无专用场景知识"，不静默套用某个既有场景的口径。

**规划中**：

- **场景包编写指引与模板** —— 目前新建一个高质量场景包需要读现有典型包才知道该写什么。
- **典型场景以外的示例包** —— 云原生化改造、单体拆分微服务等类型目前需要用户自己从零写。
- **场景知识与迁移目标的权重问题** —— 实测显示，当场景包规则与显式填写的迁移目标冲突时，模型倾向跟随迁移目标，并把"是否改挂场景包"交还用户裁决。这不是缺陷，但意味着**场景包不能替代把目标态写清楚**；如何让两者的关系更明确，是待解决的设计问题。

### 3.2 生命周期挂点接线（部分实现）

定义了 10 个挂点，当前只有工具调用前后 2 个真正接线。其余 8 个是"已定义未接线"，**不应被读作已生效的安全控制点**。逐个接线是渐进工作，按实际需要推进，不承诺全量。

### 3.3 长时任务的观测与自愈（部分实现）

**已实现**：心跳注册表可以**发现**任务挂起。

**明确边界**：它**从不重试、不恢复、不接管**。这是刻意的 —— 一个会自动接管的组件很容易长成第二个编排器，而平台只允许存在一个编排底座。

**规划中**：更好的挂起可见性（前端能看到"这个任务多久没心跳了"）。**不规划**自动接管。

---

## 4. 优先方向三：让外部的人能用起来

### 4.1 双语文档（已落地）

**方案**：轻量的源文档 + 语言变体文件（`README.md` / `README.zh.md` 这样的一对，配一份根目录对照清单 [`.i18n.yaml`](./.i18n.yaml)），**不引入任何 i18n 框架或运行时依赖**。

**当前状态**：仓库根的四份对外文档（本文档、项目入口、愿景、架构）均已提供中英两个语言变体。**中文为权威版本**——英文版是中文版的翻译，两版表述冲突时以中文版为准，该规则记在对照清单里。

**明确边界**：这只覆盖仓库根的**对外文档**。**平台内前端的用户可见文案仍然中文优先** —— 前端多语言是独立的设计变更，不由文档双语化自动带出来。翻译只覆盖上述四份文档，其余对外文件（贡献指南、行为准则、安全上报说明）目前仍只有中文，对照清单里已如实登记。

### 4.2 预构建发行物（规划中）

**现状**：尚未发布 PyPI 包、npm 包或公共镜像，需从源码本地构建。

**前置条件**：在发行物之前，至少要先有 2.1 的真实规模验证结论 —— 否则等于把一个未验证规模上限的工具做成一键安装，会制造错误预期。

### 4.3 多用户与权限体系（规划中，条件未满足）

**现状**：当前面向**单机单用户**本地使用，**没有登录、账号、租户隔离与权限体系**。**不要把服务暴露到公网。**

**为什么排在后面**：多用户会牵动编排状态存储（当前默认落本地 SQLite）、凭据归属、工作区隔离与审计主体四条线，是一次结构性改动。在单用户形态尚未被真实规模项目验证之前先做多用户，属于在流沙上盖楼。

**触发条件**：出现真实的多人协作使用诉求，且单用户形态的可信度已收敛（第 2 节完成）。

---

## 5. 社区：当前形态与启用条件

这一节回答"我在哪里找人讨论"。**请先看清当前状态，避免白跑一趟。**

### 5.1 当前状态：交流渠道尚未开启

**仓库目前仍是私有状态，GitHub Discussions 尚未开启。**

- **计划**：以 **GitHub Discussions 作为过渡期的交流社区**，**待仓库转公开后开启**，首帖内容为项目愿景与参与方式。
- **现在还不能做什么**：Discussions 未开启，因此**现在没有可用的公开讨论入口**，本文档也不提供链接。
- **转公开本身是一个需要显式批准的动作**，有前置条件（安全修复与全历史密钥扫描已完成），不会顺手执行。

在此之前，参与方式以 [CONTRIBUTING.md](./CONTRIBUTING.md) 为准；**安全问题请按 [SECURITY.md](./SECURITY.md) 的私密渠道上报，不要开公开 issue**。

### 5.2 `community/` 目录的定位与启用条件

仓库里有一个 `community/` 目录，它是一套**独立的社区服务**（独立后端 + 前端，通过 HTTP 接口与主平台解耦，容器编排里作为独立 profile，**默认不启动**）。

**它现在的状态：暂缓，不是废弃。**

| 项 | 说明 |
|---|---|
| **是什么** | 一个独立的社区门户服务，与主平台零代码混合，可独立启停 |
| **当前状态** | **暂缓建设**。已实现的能力不回退、不删除，但本阶段不投入新建设 |
| **默认行为** | 容器编排中不默认启动，不影响主平台运行 |
| **为什么暂缓** | 在没有真实参与者之前建设社区门户属于过早投入。GitHub Discussions 零成本、可沉淀、与开源仓库天然一体，足够承担过渡期 |
| **启用条件** | 出现**稳定的真实外部参与者**与**内容沉淀需求**时启用 —— 即讨论量与内容积累超出 Discussions 的承载能力，且有人真的需要一个独立门户 |
| **保留它的理由** | 保留既有资产，使后续"把社区做大"有东西可承接，而不是从零重做 |

**请不要把 `community/` 读作"已废弃的死代码"**，也不要读作"已经有社区门户了"。它是一个已建成、按条件待启用的组件。

---

## 6. 明确不在路线图上的事

这些不是"以后再说"，是**明确不做**：

| 不做 | 理由 |
|---|---|
| 通用 AI 办公平台 / 通用 Agent 市场 / 泛项目管理平台 | 超出定位红线，与平台的准入判据直接冲突 |
| 无源代码情况下的纯咨询式输出 | 违反准入判据第一条（有源代码）；无源码则无从验证 |
| 面向所有软件工程任务的通用自动化平台 | **"泛软件重构"不等于"泛软件工程"** —— 平台解除的是"改造类型"的限定，不把新建开发、需求管理、运维、测试外包纳入范围 |
| 无人值守的全自动迁移 | 与"关键节点由人裁决"的设计信条直接冲突。**这不是路线图上的远期目标，是明确的非目标** |
| 内置模型 / 代管模型额度 | 凭据自备（BYOK）是设计选择 |
| 引入第二个 Agent 编排框架 | 单一编排底座是硬约束 |
| 引入运行时的 TypeScript 依赖 | 同上 |
| 在能力不可用时回退到 mock 或占位内容 | 这会直接损害平台唯一真正的卖点 |
| 自动接管挂起的长时任务 | 容易长成第二个编排器（见 3.3） |
| 前端多语言（由文档双语化自动带出） | 属独立设计变更，须单独评估（见 4.1） |

---

## 7. 这份路线图明确不承诺什么

即使一项工作写在上面，也**不构成**以下承诺：

- **不承诺日期。** 本文档没有任何时间表，任何"预计某月发布"的说法都不是出自这里。
- **不承诺"规划中"的一定会做。** 优先级会因为发现新问题而重排；条件未满足的（例如多用户）可能长期不动。
- **不承诺向后兼容。** 平台仍在积极建设中，数据模型、接口字段与产物结构都可能变化。当前**没有稳定 API 承诺**，也没有版本化的接口契约。
- **不承诺完成 2.1 就等于"生产可用"。** 千文件级验证会回答"能不能跑完"，不回答"你的项目能不能用"—— 后者取决于你的代码形态、目标态清晰度与验证环境。
- **不承诺解决业务语义等价。** 平台能证明可编译、测试通过、diff 可读；**语义等价永远需要你的领域知识把关**，这不是一个会被路线图消灭的限制，而是分工。

---

## 8. 怎么影响这份路线图

按对项目帮助从大到小：

1. **报告"平台把不可用说成可用"的缺陷。** 这类问题优先级高于一切新功能。请附上你看到的状态标记与实际环境。
2. **在真实项目上试用并报告卡在哪里。** 尤其欢迎大规模项目的失败报告 —— 那正是第 2.1 节缺的东西。
3. **贡献场景包。** 一个写得好的自定义场景包（目标态词表 + 验收锚点 + 风险清单）对其他人的价值很高，且不需要改任何代码。
4. **提交代码。** 请先读 [CONTRIBUTING.md](./CONTRIBUTING.md) 与 [ARCHITECTURE.md](./ARCHITECTURE.zh.md) 第 7 节（能力接缝三角色）与第 17 节（改代码须知）。

安全问题请走 [SECURITY.md](./SECURITY.md) 的私密渠道，**不要开公开 issue**。

---

> 本文档的写法约定与 [VISION.md](./VISION.zh.md)、[ARCHITECTURE.md](./ARCHITECTURE.zh.md) 一致：状态分三档；每写一条能力，同时写它的边界；核实不到的不写。如果你发现文中描述与实际情况不符，请提 issue。
