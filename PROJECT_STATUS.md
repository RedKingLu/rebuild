# PROJECT_STATUS.md — 项目进展与现状（给第一次接触 rebuild 的人）

> 版本：V26.3 | 最后更新：2026-09-17
> 面向读者：**刚看到这个仓库、想在 5 分钟内判断"它做到哪一步了、值不值得投入"的人**
> 与其它文档的关系：`README` 讲怎么跑起来，`ARCHITECTURE` 讲怎么设计的，`ROADMAP` 讲要去哪里，**本文讲"现在真实到哪了、哪些地方会硌到你"**。
> 本文的写法：所有能力标注都对应可核查的实测证据或已登记的缺陷编号。**没验过的一律不写"已支持"。**

---

## 1. 一句话

rebuild 是一个**软件重构/迁移平台**：你给它一个有源代码的旧项目，它按 P0–P6 七个阶段（接入 → 建档 → 评估 → 规划 → 执行 → 验证 → 交付）在人工 Gate 把关下推进迁移，全程留证据。

它**不是**通用 AI 办公平台、不是 Agent 市场、不是项目管理工具。没有源代码它什么也做不了。

---

## 2. 最重要的一件事：它的核心设计取向是"宁可诚实失败，不许假装成功"

这个取向不是口号，是被反复验证过的机制。**如果你只看一段，看这段。**

平台内建大量"抗伪造"守卫，它们的作用是让失败**必须**表现为失败：

| 机制 | 它拦什么 |
|---|---|
| `No Evidence, No Completed` | 没有落盘证据的阶段不得标记完成 |
| 阶段验收由独立 Acceptance Agent 判定 | 干活的 Agent 不能给自己打分 |
| 空壳晋级校验 | 阶段没有真实产物就不许晋级 |
| Gate 决策必须绑定被决策对象 | 不能"批准 A 却生效在 B 上" |
| 动作审批必须绑定被审阅的入参 | 你批准的命令 ≠ 实际执行的命令，这种情况被禁止 |
| 内容合法性硬拦 | 模型的工具调用协议文本不得被当成代码写盘 |
| 无 Key 不兜底 | 没有可用模型就诚实 blocked，**不许降级成规则引擎冒充完成** |

**2026-09-17 的一次真实全流程运行可以证明这不是纸面规则**：那次运行里 P4 执行阶段只完成了 18 个任务节点中的 4 个，平台**报的就是 blocked，不是"完成"**；P5 验证阶段在容器里真跑 `dotnet build` 失败，平台**判 `rework_required`**。全程没有一处把不可用说成可用。

反过来说：**你会经常看到 `evidence_gap` / `blocked` / `rework_required`。这是设计如此，不是坏了。**

---

## 3. 现在真实能用到什么程度

### 3.1 已被真实项目验证的（有实测证据）

验证样本：一个 2010 年代的 ASP.NET Web Forms 项目（1018 个文件、约 20 万行、含 10MB T-SQL），目标是迁到 .NET 8 + Linux 容器。

| 阶段 | 实际做到 | 证据 |
|---|---|---|
| **P0 接入** | 源码物化 1018 文件；LLM 识别出 13 项技术事实中的 12 项 | 正确识别出"这是**无 `.csproj` 的 Web Site 项目模型**、App_Code 动态编译"——这是该迁移最核心的结构性难点，不是表面信息 |
| **P1 建档** | 24 份领域产物 | 唯一为空的是 CI/CD 清单，而源项目**确实没有**任何 CI/CD 配置（经独立核实） |
| **P2 评估** | 13 份评估产物，判 `accepted` | 单次 LLM 结构化输出达 21,545 tokens |
| **P3 规划** | 18 节点 / 17 边的任务图，逐边带风险级与 Gate 策略 | 节点标题是真实的迁移工序（脚手架 → 依赖建模 → 配置外移 → 宿主替换），互为硬前置 |
| **P4 执行** | 生成 27 个文件 / 2405 行迁移代码（含 `.csproj` / `Program.cs` / 配置类） | 节点会**先用工具真实读源码再写**（日志可见 "Let me read the MicroUserInfo class definition..."），不是照标题套模板 |
| **P5 验证** | 在 `mcr.microsoft.com/dotnet/sdk:8.0` 容器内**真跑** restore/build/test/format | 容器有完整加固：`cap_drop=ALL`、只读根文件系统、非 root、不挂 docker socket；出网需求被如实标注为"弱化项" |

### 3.2 已知做不到 / 会硌到你的地方

**① 生成的代码目前编译不过。** 这是当前最大的短板，必须说清楚。

上述那次运行中，生成的 `.csproj` 里有一行 ASCII 分隔线 `------` 写在了 XML 注释内，而 XML 规范禁止注释含 `--` ⇒ 项目文件连解析都过不了（`MSB4025`），四条构建命令同时死在第一步。

更值得注意的是：平台现有的检测器（跨节点重名符号检查、未声明依赖检查、协议文本泄漏检查）**没有一个校验生成文件本身的语法合法性**。已登记为 `B-V263-GENFILE-SYNTAX-UNCHECKED`（P1），解除条件要求"落盘前按扩展名分派最小解析器，解析失败即拒绝写盘"。

**② P4 不会跑完所有节点。** 上述运行 18 个节点只完成 4 个就 blocked，根因是宿主命令白名单不含 `dotnet`，节点无法自验构建。这是设计上的安全取舍（白名单只放 `python3/python/echo/cat/ls/pwd/which`），构建验证归 P5 的容器通道。

**③ 只在一个场景上做过完整验证。** 平台声称支持三类典型场景（信创切换 / 软件现代化 / 软件移植）+ 用户自定义场景，但**只有"软件现代化"跑过完整链路**。信创场景最远到过 P4。

**④ 依赖检查有两个已知盲区。**
- 中国区网络下 `api.nuget.org` 会 302 重定向到镜像，平台拒绝跟随未预期重定向（安全行为正确），结果是"这个包存在吗"经常只能答 `indeterminate`
- 传递依赖的版本冲突不检查

**⑤ 出网未做 egress 白名单。** 容器构建档为了 `restore` 允许出网，这一项在执行元数据里被如实标注为 `network_weakened`，但确实没有域名级管控。

**⑥ 前端有若干页面尚未接真实后端。** 这类位置一律显式标记（14 种能力状态标记，含 `mock` / `static_demo` / `not_connected` / `future` 四档区分），**不会伪装成可用**。看到"规划中"标签就是真的还没做。

### 3.3 完全没做的

- P6 交付阶段在真实规模项目上**从未跑通过**
- 生成代码的"能否启动运行"从未验证（连编译都还没过）
- 多 run 并发场景下的 Gate 归属有一处已知理论缺陷（`B-ACC-PROMOTE-DIRECT-RUNBLIND`，P1，代码推导未实测触发）

---

## 4. 技术栈与规模

| 项 | 值 |
|---|---|
| 后端 | Python 3.12 + FastAPI + SQLAlchemy 2.0；**LangGraph 作为唯一主编排底座**（硬规则，换它需走架构变更申请） |
| 前端 | React + Vite + TypeScript |
| 模型接入 | 统一走内部 ModelGateway → LiteLLM，**禁止任何代码直连 Provider**；模型名/endpoint 禁止硬编码 |
| 数据库 | SQLite（默认，可换）+ Alembic 迁移 |
| 后端规模 | 约 205 个文件 / 5.2 万行 |
| 测试 | **1976 passed / 4 skipped / 0 failed**，约 48 分钟跑完 |
| 前端规模 | 约 80 个文件 / 1.5 万行 |

**跑测试不需要任何环境变量前缀** —— 运行前提（`PATH` 补齐、LLM 打桩开关）已在 `conftest.py` 里机器化自愈，且结尾会打印横幅告诉你当前跑的是桩模式还是真实模型模式。这件事之前踩过 4 次坑才机器化。

---

## 5. 项目的工程纪律（决定你读代码时会看到什么）

这个项目的一些做法可能和你习惯的不同，先说明免得困惑：

**① 注释里有大量"为什么"和实测数据。** 例如某个 `max_tokens` 常量上方会写着"实测触顶值 16383、该模型有 reasoning 通道开销、completion_tokens=185 中 reasoning_tokens=146"。这是刻意的：本项目反复吃过"后人不知道当初为什么这么定，随手改回去"的亏。

**② 有些地方明确写着"勿删"。** 那通常意味着删掉会复现一个已经修过的真实缺陷，且注释里会说明是哪个。

**③ 安全检查一律没有开关。** 你不会找到 `if not settings.enable_xxx_check` 这种旁路。fail-closed 是默认。

**④ 状态词有严格定义。** `resolved` 表示该问题自己定义的全部解除条件逐条达成；`mitigated` 表示主体已修但取证未齐（且必须写明还差什么、归口哪里）；`open` 就是没修。不允许用含糊状态词掩盖实情。

**⑤ 缺陷台账里记着"谁在什么情况下犯了什么错"，包括维护者自己的。** 例如有一条记录着"主窗口执行用户批准的 Gate 拒绝时亲身触发并当场坐实"某缺陷、以及"这是核查不完整，不是不可预见的意外"。这不是自我批评表演，是因为**失败模式本身是最有价值的资产**。

---

## 6. 当前缺陷台账概览

总计 **7 条 open**：

| 编号 | 级别 | 一句话 |
|---|---|---|
| `B-V263-GENFILE-SYNTAX-UNCHECKED` | P1 | 生成文件语法非法致工程无法加载，无落盘前语法校验 |
| `B-ACC-PROMOTE-DIRECT-RUNBLIND` | P1 | 直连晋级路径不按 run_id 过滤 Gate（推导，未实测触发） |
| `B-V263-RSTAGE-STALE` | P2 | 运行期自报阶段号会落后于实际（"为什么会过期"未根治） |
| `B-RW-P5CLASSIFY-FALLTHROUGH` | P1 | P5 失败分类误判 —— **维护者已明确裁决接受为已知缺口** |
| `B-FE-01` / `B-ORCH-02` / `B-ORCH-03` / `B-DEBT-SILENT-EXCEPT` | 混合 | 早期版本（R5–R11 世代）遗留，待专项核对 |

另有 6 条 `mitigated`（主体已修、取证未齐），逐条写明了还差什么。

完整台账在 `证据/进度追踪/02-阻塞项.md`（**注意：该目录不在 git 中**，见下节）。

---

## 7. 仓库里有什么、没有什么（重要）

`.gitignore` 排除了四个中文目录，它们**不在这个仓库里**：

| 目录 | 内容 | 为什么不在 git 里 |
|---|---|---|
| `交接/` | 阶段交接材料、完成报告、验收报告 | 内部协作过程材料，体量大且含大量过程叙述 |
| `产物/` | 草稿、方案、施工计划 | 同上 |
| `证据/` | **缺陷台账、施工记录、验收证据索引** | 同上 |
| `工作区/` | 用户项目的运行时数据 | 运行产物，含用户源码 |

**这意味着**：本文引用的缺陷编号、实测数据、台账，你在这个仓库里**看不到原文**。这是维护者的既有安排（这些是内部治理材料，不是产品的一部分）。如果你需要它们来做贡献判断，可以提 issue 要求公开特定材料。

仓库里有的是：`backend/` `frontend/` `文档/`（79 份设计文档）`skills/` `source/`（平台运行期资源）以及根目录的对外文档。

---

## 8. 版本演进（读代码注释时会遇到这些编号）

```
V8 / V10 / V26.0   历史版本，已归档，只读参考（不在本仓库）
V26.1.1            收束于 R17.5，P0–P6 全阶段完成 skill-first 重构
V26.2              R18–R22：安全修复与开源就绪 / 能力地基 / 泛软件重构场景化 /
                   dsh 范式吸收 / 发布冻结
                   ⚠ 总验收结论为 rework_required，发布收口推迟到 V26.3 之后
V26.3（当前）       R23 文档核对与优化 / R24 代码与架构核对与优化
```

**两套阶段编号，不要混**：`R0–RN` 是平台自身的建设阶段；`P0–P6` 是用户项目在平台里走的流程。代码和文档里到处是这两种编号。

---

## 9. 如果你想参与

**先读**：`README`（跑起来）→ `ARCHITECTURE`（设计）→ `CONTRIBUTING`（提交规范与测试基线）。

**最有价值的贡献方向**（按维护者视角的优先级）：

1. **生成文件的落盘前语法校验**（`B-V263-GENFILE-SYNTAX-UNCHECKED`）—— 这是当前阻塞"生成代码能编译"的第一道关，且解除条件已经写清楚了
2. **依赖检查的镜像/重定向适配** —— 让"这个包存在吗"能在中国区网络下给出确定答案
3. **场景包**：新增一个场景只需新建 `source/skills/scenarios/<id>/` 目录，**零 Python 与前端改动**（这是硬规则，做成固定枚举是被明令禁止的）
4. **早期世代遗留缺陷的专项核对**（那 4 条 R5–R11 遗留项）

**提交前**：跑全量测试（`cd backend && uv run pytest -q`，不需要环境变量前缀），确认 `0 failed`，并注意结尾横幅显示的模式与你以为的一致。

**一个请求**：如果你发现平台把某个不可用的东西报成了可用，**请优先报这个**。在本项目的价值排序里，"谎报成功"比"功能缺失"严重得多。

---

## 10. 许可与来源

MIT。

**关于提交历史（措辞需要精确，此处如实说明）**：本仓库**未做 squash、未重建仓库**，V26.1.1 之后的全部迭代过程都在 git 历史里（125 个提交，可逐个查看）。

但要说清一件事：**仓库的根提交就是 `V26.1.1 baseline`**，它没有父提交。也就是说 **V26.1.1 之前的开发历史（V8 / V10 / V26.0 三个世代）不在这个仓库里** —— 那些版本被归档在仓库之外的 `rebuild-archive/`（V10 世代保留了它自己的完整 git 历史）。

⇒ 你在这里能看到的是"**V26.1.1 → V26.2 → V26.3 的完整迭代过程**"，不是项目从零开始的全部历史。前者是本项目"以迭代过程作为持续改进证明"这一取向的实际范围。

**分支说明**：默认分支为 `main`。远端另有 `master`（内容与 `main` 完全相同，只多一个历史合并提交）与 `r7-closure`（早期 R7 收口分支，含 `main` 中没有的内容，故保留）。向 `main` 提 PR 即可。
