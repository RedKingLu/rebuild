# 00-项目总览

> 文档路径：`文档/00-项目治理/00-项目总览.md`
> 文档状态：**accepted**
> 平台名称：rebuild
> 当前版本：V26.2
> 来源草稿：`产物/草稿/00-项目总览-v0.3.md`
> 本次处理人 / Agent：Claude (deepseek-v4-pro) / R2 收口流程
> 最后更新时间：**2026-09-02（V26.2 启动，用户裁决三条主线：① 平台进入第二阶段——由"首期信创迁移"扩展为泛软件重构，场景体系为「典型场景 + 开放扩展」两层——典型场景为信创切换/软件现代化/软件移植（平台重点支持、配套资源相对更丰富），并"包括但不限于"这三类、支持用户自由修改与自定义场景，准入靠三判据不靠枚举，禁写死；② 一并解决软件栈存量问题并吸收 DeepSeek Harness（dsh = `deepseek-ai/deepseek-harness`，MIT，只吸收范式不吸收代码，LangGraph 主编排 D-037 不变）；③ 以 MIT 协议彻底开源，沿用原有 git 仓库并保留全部提交历史作为持续迭代证明，GitHub Discussions 作过渡期交流社区。新增阶段 R18~R22；规划 `交接/当前/V26.2-总体规划.md`、验收口径 `交接/当前/V26.2-验收标准.md`、首阶段入口 `交接/当前/R18-交接提示词.md`；待登记决策 D-117~D-122。§3.1 与 §6.2 的 V26.2 新增段落实质内容由用户 2026-09-02 裁决，措辞由处理 Agent 依用户 2026-09-05 授权定稿（R20-1，CR-20260905-01），非用户逐字审阅）**；上一版 2026-07-27（R17.5-P6 交付阶段 skill-first + capability-first 重构验收通过并收口 accepted，登记 D-116——R1 skill-first 转向（P-migration-delivery skill + p6_delivery_agent analysis_only advisory）/ R2 capability-first（p6_capability_service 四能力，工具在场仍诚实 evidence_gap 绝不伪造 available，沙箱 environment_available=0）/ R3 交付定级·验收结论确定性映射（license_notice 只读扫描 + scope_level PoC/生产候选 + 四档 acceptance_result，Q-P6-2 口径：license 不明默认 accepted_with_warning 仅 distribution 升 blocked）/ R4 有机全绿全链回归测 test_r175_p6_r4_organic_green.py（P5 通过→P6 携三轨真跑、P5 未通过→P6 blocked 双路径）+ StagePageP6 联调；分 4 轮独立 agent 施工、主窗口逐轮真跑验收；证实 status="completed" 硬编码常量、gate 不被 advisory/capability 翻转；全量 pytest 1100 passed/3 skipped/0 failed + 前端 build 绿。至此 R17.5 P0-P6 全阶段 skill-first + capability-first 重构收口完成。**R17.5 收口后真跑基线**：主窗口用真实 Key（美团 LongCat-2.0）真跑 MicroOA 迁移项目 P0-P6 完整链路，产出实际轨基线（`产物/草稿/实际/P0-P6实际产出基线-run08f298.md`）——证实抗伪造机制端到端有效（P4 代码立不住→P4 rework→P5 blocked→P6 不伪造交付），并暴露两大真实能力缺口（G1 验证/构建从不用可用容器真构建、G2 P4 依赖臆造无可构建性校验），构成 R17.6 修复靶子（交接：`交接/当前/R17.6-交接提示词.md`）。**§6 R 阶段表已于 2026-07-28 校正至实际进度**）
> 审核状态：经用户审核通过（2026-06-24）
> 文档定位：rebuild 项目总入口，供项目负责人、Copilot、本地 Agent、独立验收 Agent、新窗口接手时快速理解平台定位、当前版本、目录结构、阶段体系、事实源和执行边界。
> 修订说明：R2 审核校准（2026-06-24）：目录树更新为磁盘真实 10 子目录（00-09）；代码根为顶层 `backend/frontend/demo-projects`；阶段口径更新为 R0/R1/R2 已完成、R3 准备开工；V26.1.1 基线收口。

---

## 1. 平台一句话定义

**rebuild 是一个面向软件重构与迁移的软件重构平台（泛软件重构）；在用户拥有源代码的前提下，帮助用户完成项目迁移、重构、验证与交付。场景体系分「典型场景 + 开放扩展」两层：信创切换 / 软件现代化 / 软件移植为典型场景（平台重点支持、配套资源相对更丰富），并"包括但不限于"这三类，支持用户自由修改与自定义场景；准入以三判据把门（D-117 / D-118）。**

rebuild 的核心不是泛化任务自动化平台，也不是单纯的多 Agent 展示系统，而是围绕"有源代码的软件项目如何安全、可控、可追溯地完成迁移和重构"建立产品流程、Agent 协作、模型调用、受控执行、验证评审和交付归档能力。

---

## 2. 平台名称与版本号

### 2.1 平台名称

平台名称是：

```text
rebuild
```

后续所有长期目录、产品文案、架构文档和用户可见描述，应优先使用平台名称 `rebuild`。

### 2.2 当前版本号

当前版本号是：

```text
V26.2
```

V26.2 是当前基线发布号。rebuild 后续会持续迭代和升级，因此不应把版本号写死到长期目录、API、数据库表、包名、模块名、前端路由或运行时路径中。

建议表达方式：

```text
平台：rebuild
当前版本：V26.2
当前建设阶段：R0~R17 已完成（V26.1.1 基线收束于 R17.5，commit 42ba7eb/D-116，pytest 1100 绿）；**V26.2 已启动（2026-09-02）；R18 安全修复与开源就绪、R19 能力地基、R20 泛软件重构场景化 均已施工完成（R20 全量 1371 绿，12 个提交未推送，待独立验收）；当前阶段 = R21 dsh 范式深度吸收（待启动）**（R18~R22 见 §6.2）
```

> **阶段状态维护规则（2026-09-07 补，`Q-R20-1-7`）**：本行与 `01-决策记录.md §1.1` 的同类字段**必须成对校正**——R20-1 施工时实测两处互相矛盾（此处写 R18、那处写"R3 准备开工"）。阶段的权威定义在 `06-R阶段总计划.md`，权威进度在 `证据/进度追踪/`；本行是速查冗余，冲突时以那两处为准。

不建议表达方式：

```text
平台名：rebuild 26.1
产品名：V26.1 平台
长期模块名：v26_1_runtime
长期 API：/api/v26.1/...
```

---

## 3. 产品定位

### 3.1 平台定位

rebuild 的定位是：

```text
软件重构平台
```

重点应用领域是：

```text
软件重构与迁移（泛软件重构）——典型场景：信创切换 / 软件现代化 / 软件移植；
并"包括但不限于"这三类（详见 §3.1 下方 V26.2 段与 D-117）
```

> **V26.2 第二阶段扩展（2026-09-02 用户裁决实质内容；措辞由处理 Agent 依用户 2026-09-05 授权定稿，R20-1，非用户逐字审阅）**：平台进入第二阶段，**不再局限于信创替换，成为泛软件重构平台**。场景体系分两层——
>
> **① 典型场景**（平台重点支持，**配套资源相对更丰富**：P 系列 skill / 知识条目 / 案例 / 资源库引用 / 选型候选）：
>
> ```text
> xinchuang_switch  信创切换      向国产化软硬件栈替换（原主模式）
> modernization     软件现代化    OS / 运行时 / 框架 / 前端组件版本过低的现代化改造
> porting           软件移植      跨平台移植（Windows→macOS、Android→iOS、Android→鸿蒙 等）
> ```
>
> **② 开放扩展**："**包括但不限于**"上述典型场景，**支持用户自由修改与自定义场景包**（可复制典型场景包后改写）；**不得实现为固定枚举**，新增场景须为"新增场景包目录、零代码改动"。
>
> 任何场景须同时满足**准入三判据**方可进入 P0-P6：**① 有源代码 ② 有可判定的目标态 ③ 产出物可机器验证**。三判据的完整定义、判定口径与拒绝准入路径的**详述源 = `01-决策记录.md` D-118**；场景体系两层结构与场景包结构的**详述源 = D-117**。本文不复制细节（AGENTS §6.2）。

典型前提是：

```text
用户拥有待迁移或待重构项目的源代码。
```

平台要解决的问题是：

```text
在已有源代码的情况下，帮助用户完成项目盘点、迁移评估、方案规划、受控改造、验证评审和交付归档。
```

### 3.2 核心场景动作

无论落在哪一类场景（典型场景或用户自定义场景），核心场景动作一致，包括但不限于：

```text
1. 对已有项目进行源码接入；
2. 建立项目事实源；
3. 识别技术栈、依赖、运行环境和迁移风险；
4. 制定迁移或重构方案；
5. 在受控执行机制下完成代码、配置、依赖或工程结构调整；
6. 验证改造结果；
7. 形成可交付、可追溯、可继续维护的成果。
```

### 3.3 不应偏离的边界

rebuild 不应扩展为泛软件工程平台——定位扩展为泛软件重构（D-117）不改变本节任何边界。

以下内容始终不作为核心目标：

```text
1. 通用 AI 办公平台；
2. 通用 Agent 市场；
3. 泛项目管理平台；
4. 无源代码情况下的纯咨询平台；
5. 面向所有软件工程任务的通用自动化平台；
6. 只展示 Agent 能力而不完成迁移和重构闭环的平台。
```

---

## 4. 第一性原理

rebuild 从以下问题出发：

```text
用户拥有一个旧项目或待迁移项目的源代码，希望借助 AI 和 Agent 降低迁移、重构、验证与交付成本，同时避免失控、误改、返工和不可追溯。
```

因此，平台必须优先保障：

```text
1. 项目事实可建立；
2. 风险和范围可评估；
3. 方案和计划可审核；
4. 执行动作可控制；
5. 产物和证据可追溯；
6. 验收和交付可复核；
7. 不确定项可停下并交给用户确认。
```

平台的核心闭环不是"Agent 自动干完所有事"，而是：

```text
接入源代码
→ 建立事实源
→ 评估风险
→ 制定方案
→ 受控执行
→ 验证评审
→ 交付归档
```

---

## 5. 当前版本状态

### 5.1 当前版本

```text
平台名称：rebuild
当前版本：V26.2
主目录：/home/king/rebuild/
归档目录：/home/king/rebuild-archive/
当前建设阶段：R0~R19 已完成；V26.2 在建，R20 施工完成待独立验收，当前阶段 = R21 dsh 范式深度吸收（待启动）
```

### 5.2 R0 已完成

R0（基线冻结与启动）已于 2026-06-23 完成，成果包括：

```text
1. 历史版本完整归档至 /home/king/rebuild-archive/（V8 / V10 / V26.0）；
2. archive 详细索引建立；
3. 当前版本主目录骨架建立（文档/ 交接/ 产物/ 证据/ backend/ frontend/ demo-projects/ 测试/ 脚本/ 配置示例/ 工作区/）。
```

R0 完成报告见：`交接/当前/R0-完成报告.md`

### 5.3 当前阶段目标

R1 的目标不是写占位草稿，而是输出一套 **基本可用的 rebuild 文档基线草稿**。

基本可用的含义：

```text
1. 能指导后续本地 Agent 完善；
2. 能给独立 Agent 审核；
3. 能作为用户与 Copilot 最终验收的基础；
4. 只有现场路径、真实代码状态、真实 API 字段、实际技术栈细节等需要在 R2 微调；
5. 不能只是目录占位或泛泛说明。
```

---

## 6. P0-P6 与 R0-RN 的区别

### 6.1 P0-P6：平台产品流程

P0-P6 是 rebuild 面向用户项目的可裁剪产品流程。

```text
P0 接入
P1 建档
P2 评估
P3 规划
P4 执行
P5 验证
P6 交付
```

P 阶段用于描述用户项目如何在平台中推进，不用于描述 rebuild 自身如何建设。

P 阶段可以按 Project 引导模板裁剪。例如：

```text
只做评估：P0 → P1 → P2 → P6
只做规划：P0 → P1 → P2 → P3 → P6
完整迁移/重构交付：P0 → P1 → P2 → P3 → P4 → P5 → P6
```

裁剪规则：

```text
1. 被裁剪掉的阶段不得伪装为已完成；
2. 被启用的阶段必须有 Gate、Artifact、Evidence 和完成条件；
3. P6 可以作为任意裁剪流程的收尾；
4. 正式交付建议包含验证或明确的风险确认。
```

### 6.2 R0-RN：平台建设阶段

R 阶段用于管理 rebuild 当前版本的自身建设。

当前 V26.1.1 规划包括（R0~R7 已完成，R8 为下一阶段）：

```text
R0  基线冻结与启动                                                   ✅ 已完成（2026-06-23）
R1  文档草稿全量建立与正式化                                         ✅ 已完成
R2  文档完善、独立审核与验收                                         ✅ 已完成（2026-06-24）
R3  前端主干与体验壳                                                 ✅ 已完成（2026-06-25）
R4  基础工程骨架与 API 契约落地                                      ✅ 已完成（2026-06-25 验收通过 accepted）
R5  模型网关与模型策略                                               ✅ 完成（accepted）
R6  Agent / Skill / 资源基础                                         ✅ 完成（accepted）
R7  集成与项目接入                                                   ✅ 完成（2026-06-26 accepted）
R8  工作区真实化                                                     ✅ 已完成（2026-06-27 accepted）
R9  P0-P1 最小真实链路                                               ✅ 已完成（2026-06-29 accepted）
R10 P2-P3 评估与规划链路                                             ✅ 已完成（并入 P0-P4 真实链路）
R11 P4 执行链路                                                      ✅ 已完成（2026-07-04 accepted）
R12 P5-P6 验证与交付                                                 ✅ 已完成（R12-18 conditional_pass；后由 R17.5 重构收口）
R13 Fusion 功能开发及合入                                            ✅ 已完成（R13-3 基线+施工）
R14 远程资源调用开发及合入                                           ✅ 已完成
R15 社区功能建设及合入（含文档/知识化，R16 已并入）                   ✅ 已完成（R15-5 accepted，2026-07-10）
R16 文档功能建设及合入                                               ⟲ 已并入 R15（第二期增强标签）
R17 全量联调、体验打磨与发布冻结                                     ✅ 已完成（R17.2 联调 accepted；R17.5 P0-P6 skill-first+capability-first 全阶段重构 accepted 收口 2026-07-27；V26.1.1 基线在此收束）
  └ R17.6 实际能力对齐与修复                                         ⟲ 已吸收进 V26.2（未独立开工，十 WP 按 Q-26.2-9 分流至 R18-1/R19）

────── V26.2 阶段（2026-09-02 用户裁决启动，基线在建；本段状态：待审核）──────
R18 安全修复与开源就绪（MIT 开源前置，全局阻塞关键路径）             📋 计划
R19 能力地基（G1 容器真构建 / G2 依赖真实性 / 产物卫生 / 韧性）       📋 计划
R20 场景化：泛软件重构（典型场景 + 开放扩展 + 准入三判据）           📋 计划
R21 DeepSeek Harness（dsh）范式深度吸收                              📋 计划
R22 V26.2 发布冻结与社区启动                                         📋 计划
```

> **R15/R16 合并（2026-07-09，R15-4-C0，Q-R15-1）**：R16 文档功能并入 R15 核心闭环，R16 仅为第二期增强标签，不复活独立 Docs/Help 中心。详见 `06-R阶段总计划.md` §2 与 `01-决策记录.md` D-060 执行注。

P 阶段和 R 阶段不得混用。

---

## 7. 当前事实源与参考源

### 7.1 当前事实源

rebuild 当前版本的正式事实源从以下目录开始建立：

```text
/home/king/rebuild/文档/       ← 项目文档与设计规范（R1 起逐步写入）
/home/king/rebuild/交接/       ← 任务交接、阶段报告、Agent 提示词、总报告、总表
/home/king/rebuild/产物/       ← 草稿、设计、报告、Demo、发布包
/home/king/rebuild/证据/       ← 调研证据、验收证据、运行记录
```

### 7.2 历史参考源

历史参考源位于：

```text
/home/king/rebuild-archive/V8/
/home/king/rebuild-archive/V10/
/home/king/rebuild-archive/V26.0/
```

历史版本只作为参考，不在本文档中展开叙述。其详细用法应放入：

```text
文档/00-项目治理/03-版本与目录策略.md
文档/09-参考索引/
AGENTS.md（✅ accepted）
```

使用历史参考源时必须遵守：

```text
1. 可阅读；
2. 可索引；
3. 可总结；
4. 可对比；
5. 可提取设计思想；
6. 不得直接复制；
7. 不得自动成为当前版本决策；
8. 任何吸收都必须写明理由、适配方式和风险。
```

---

## 8. 目录说明

### 8.1 根目录（当前实际状态）

以下为 `/home/king/rebuild/` 的当前实际目录结构。标注 `[实际]` 的为已存在目录/文件，标注 `[规划]` 的为计划建立但尚未创建的目录/文件。

```text
/home/king/rebuild/
├── 文档/           [实际] 10 子目录（00-项目治理 ~ 09-参考索引），目录事实真值
├── 交接/           [实际] 当前/ + 已完成/
├── 产物/           [实际] 草稿/ + 设计/ + 报告/ + Demo/ + 发布包/
├── 证据/           [实际] 调研证据/ + 验收证据/ + 运行记录/
├── backend/        [实际] 后端工程目录（R4 已施工：Python+FastAPI+uv，16 域 API/SSE/Trace/Audit，纳入 Git）
├── frontend/       [实际] 前端工程目录（R3 体验壳已建成：React+Vite，纳入 Git）
├── demo-projects/  [实际] 演示项目目录（空骨架，按需创建样例）
├── 测试/           [实际] unit/ + integration/ + e2e/ + fixtures/
├── 脚本/           [实际] 开发/ + 测试/ + 构建/ + 发布/
├── 配置示例/       [实际]
├── 工作区/         [实际] projects/ + runs/ + artifacts/ + evidence/ + temp/
├── skills/         [实际] R-建设执行/（仅 R 系列建设 Skill，置于 source/ 之外与运行期隔离）
├── source/       [实际] 平台运行期资源素材统一根（D-103：工具/P系列Skill/agents/mcp/cases/knowledge/resources）以实际为准
├── AGENTS.md       [实际] ✅ accepted
└── README.md       [实际] 项目说明文件
```

### 8.2 关键目录职责

#### `文档/`

长期项目文档和设计规范。R1 起逐步写入基本可用版本。

#### `交接/`

任务交接、阶段报告、外部 Agent 提示词、完成报告、待确认项。

#### `产物/`

草稿、设计、报告、Demo、发布包等。产物不自动成为正式文档。

#### `证据/`

调研证据、验收证据、运行记录。

#### `backend/`、`frontend/`、`demo-projects/`（工程代码事实根）

rebuild 当前版本的工程代码以**工程惯例**为准，根目录为**顶层** `backend/`、`frontend/`、`demo-projects/`（用户裁决 ）。不得直接复制历史版本代码。

```text
backend/        — 后端代码（Python + FastAPI，R4 已施工：16 域 API + SSE + Trace/Audit + graph 占位；LangGraph 仅边界，未编译）
frontend/       — 前端代码（React + Vite + TypeScript，R3 体验壳 + R4 service 层与项目列表页最小联调）
demo-projects/  — 演示/样例项目（空骨架，按需创建）
```

> 文档目录以**可读性**为准（中文 00-09），代码目录以**工程惯例**为准（英文顶层），两者分线，互不混用。

#### `skills/`

R 系列建设用 Skill 目录（仅 R 系列）。当前包含 `R-建设执行/rebuild-work-guidelines/`（SKILL.md + references/）。

P 系列平台运行用 Skill 不在此处，已按 D-008-REV1 移入 `平台资源/skills/P系列/` 统一管理。

---

## 9. 核心设计原则

### 9.1 源代码前提

平台面向用户拥有源代码的项目——"有源代码"是准入三判据第 ① 条，也是平台第一性前提（D-118）。

这意味着平台能力应围绕以下对象展开：

```text
源码目录
Git 仓库
ZIP 源码包
项目依赖
配置文件
构建脚本
运行环境
测试与验证材料
```

### 9.2 事实源优先

任何评估、规划、执行和验证，都必须基于项目事实源，而不是基于 Agent 的想象。

核心原则：

```text
No Evidence, No Decision.
No Artifact, No Completed.
No Trace, No Trusted Result.
```

### 9.3 少 Agent，多 Skill

默认不为计划、编排、执行、评审等相似职责过度拆 Agent。

优先使用：

```text
同一 Agent + 不同 Skill + 不同上下文 + 不同工具权限
```

只有在以下情况才切换 Agent：

```text
1. 需要干净上下文；
2. 需要更换模型；
3. 权限边界不同；
4. 责任边界不同；
5. 并行任务需要隔离；
6. 需要专家能力；
7. 独立验收。
```

### 9.4 Fusion 只是模型

Fusion 是一种模型能力或模型策略，不是特殊流程、特殊阶段或特殊评审引擎。

平台可以提示 Fusion 更适合评审、建议、方案比较等任务，但不为 Fusion 设计额外流程特权。

### 9.5 Gate 必须有明确裁决主体

P 阶段晋级 Gate 必须由用户授权。

阶段内部 Gate 根据执行模式区分：

```text
Manual：用户审计划、任务和授权动作。
Plan：用户审 Stage Plan 和 Task Plan；计划内动作由 Hook + Policy 检查。
Auto：Agent 审 Stage Plan 和 Task Plan；阶段内授权由 Hook + Policy + Auto Review Agent 处理。
```

---

## 10. 当前不做什么

R1 不做以下事项：

```text
1. 不写业务代码；
2. 不迁移旧代码；
3. 不复刻旧前端；
4. 不恢复旧 Phase / F / S 流程；
5. 不把历史版本文档直接复制成当前版本文档；
6. 不把社区资源默认设为可执行；
7. 不把 Fusion 特殊化为流程；
8. 不跳过文档基线直接开工。
```

---

## 11. 新 Agent 阅读顺序

任何新 Agent 接手时，建议先读：

```text
1. 文档/00-项目治理/00-项目总览.md          ← 本文档
2. 文档/00-项目治理/01-决策记录.md
3. 文档/00-项目治理/02-术语表.md
4. 文档/00-项目治理/03-版本与目录策略.md
5. 文档/00-项目治理/04-变更控制规范.md
6. 文档/00-项目治理/05-交接与报告规范.md
7. 文档/00-项目治理/06-R阶段总计划.md
8. AGENTS.md                                  （根目录，唯一 Agent 准则入口）
9. skills/R-建设执行/rebuild-work-guidelines/SKILL.md （必须 Skill）
10. 交接/当前/ 下的最新任务说明
```

---

## 12. 待确认项

以下内容需在后续阶段确认：

```text
1. R13-R16 增强功能的实际优先级（D-060 已定序，具体安排接近 R13 时确认）；
2. 典型场景（尤其信创切换）应优先覆盖的技术栈范围（当前策略"默认不限制"，后续运行后评估）；
3. VERSION 文件创建时机（当前标记 [规划]，按需创建）。
```

---

## 13. 本文档验收标准

本文档达到 R1 基本可用标准，当且仅当：

```text
1. 明确平台名称是 rebuild；
2. 明确 V26.1 只是当前版本号；
3. 明确 rebuild 是软件重构平台；
4. 明确定位为泛软件重构，场景体系为「典型场景 + 开放扩展」两层且"包括但不限于"；
5. 明确以用户拥有源代码为典型前提；
6. 能区分 P0-P6 与 R0-RN；
7. 能说明当前事实源与参考源；
8. 不在总览中过多展开旧版本；
9. 能给出新 Agent 阅读顺序；
10. 能明确当前不做什么；
11. 能列出待确认项；
12. 目录树与当前实际磁盘状态一致；
13. 标注了规划中但尚未创建的文件/目录；
14. 引用了 R0 完成事实。
```
