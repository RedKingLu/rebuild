# 06-容器化部署与执行隔离规范

> 文档路径：`文档/02-架构设计/06-容器化部署与执行隔离规范.md`
> 文档状态：**accepted**（2026-06-26 用户随 R7 收口接受）
> 平台名称：rebuild
> 当前版本：V26.1.1
> 来源：R7-5 返工 + 容器化施工（用户附加任务）；评估稿 `产物/草稿/容器化部署评估-R7-4期间.md`
> 本次处理人 / Agent：Claude (Opus 4.8) / R7-5 施工
> 最后更新时间：2026-06-26
> 审核状态：待用户审核
> 文档定位：定义 rebuild 平台容器化部署形态、镜像与编排基线、**执行隔离规范**，并约束后续 Workspace / 执行 / 集成相关施工必须遵循的容器化思路。
> 上级依据：`02-架构设计/04-Workspace-Environment-Execution架构.md`、`02-架构设计/05-集成与外部执行架构.md`、`07-安全与权限/`、`AGENTS.md §8/§12`、`01-决策记录.md`（D-037 LangGraph 主编排不变）
> 边界：本文为部署/执行隔离架构规范，不替代具体 Dockerfile/compose 实现（实现在仓库根 `docker-compose.yml`、`backend/Dockerfile`、`frontend/Dockerfile`、`deploy/execution-sandbox/`）。

---

## 0. 为什么容器化（结论）

适合，主因是 rebuild 的两项固有属性，而非追新：

1. **混合运行时**：后端 Python 3.12 + uv，集成层 shell 调用系统 `git`/`ssh`，OpenCode 走 Node CLI。同宿主机依赖易互相干扰。容器按模块隔离依赖。
2. **平台会执行代码**：Agent/OpenCode 生成的代码当前以宿主机 `subprocess` 运行，仅靠 deny-list 防护。将执行迁入**受限、网络隔离、非 root、资源受限**的一次性容器，是与执行风险分级 L4/L5 契合的安全升级，也是本规范的核心。

执行顺序硬约束：**先完成功能/安全返工（含 P0 连接池泄漏），再容器化**，不得把缺陷固化进镜像。

---

## 1. 部署拓扑（Phase C1：基础编排）

```
┌─────────────┐      ┌──────────────┐      ┌──────────────────┐
│ frontend     │/api │ backend       │ DB   │ sqlite(卷) / pg   │
│ nginx:80     ├────▶│ uvicorn:8000  ├─────▶│ (profile=pg)      │
│ SPA + 反代   │      │ FastAPI       │      └──────────────────┘
└─────────────┘      │  + git/ssh    │
                     │  ExecutionProvider
                     └──────┬───────┘
                            │ EXECUTION_MODE=container（C2）
                            ▼
                     ┌──────────────────────┐
                     │ execution-sandbox      │ 无网络/只读根/非root/限额
                     │ 一次性、disposable     │
                     └──────────────────────┘
```

- **frontend**：多阶段构建 → nginx 托管 `dist`，`/api` 反代至 `backend:8000`，SSE 关闭缓冲。
- **backend**：多阶段 uv 构建；运行层装 `git`+`openssh-client`；非 root（uid 10001）；`/api/health` healthcheck；数据卷 `/app/.data`（sqlite db、执行沙箱、仓库克隆）。
- **db**：默认 SQLite（卷），开箱即用；`--profile pg` 切 PostgreSQL（`DATABASE_URL` 覆盖）。
- **execution-sandbox**：Phase C2 隔离执行镜像，默认只构建（`--profile sandbox`）。

---

## 1.1 运行端口标准（硬规则 · 单一事实源）

> 背景：历史上后端在「本地开发」用 8765、「容器化」用 8000 两套并存，导致端口反复漂移、GitHub OAuth 回调因端口不一致而失效。自此**本节为 rebuild 全平台端口的唯一事实源**；任何代码、配置、脚本、文档、`.env` 不得各自另设端口，新增服务须在此登记。

| 角色 | 本地开发 | 容器化 (compose) | 说明 |
|------|---------|------------------|------|
| 后端 uvicorn | **8000** | **8000**（`8000:8000`） | **统一 8000；废弃 8765**。dev 启动：`uvicorn app.main:app --port 8000` |
| 前端 | Vite **5173** | nginx **8080**（`8080:80`） | dev server 与 nginx 静态托管是两类进程，端口不同属正常 |
| 浏览器 → 后端 | 经 Vite proxy `/api` → `127.0.0.1:8000` | 经 nginx `/api` → `backend:8000` | **浏览器只认前端源**，用同源相对路径 `/api`，免 CORS、不绑定后端端口 |
| GitHub OAuth `redirect_uri`（注册值，**不可变**） | `http://localhost:8000/api/integrations/git/oauth/github/callback` | 同左 | 后端两套拓扑都在 8000 → GitHub OAuth App **注册一次即两套通用**，根治回调失效 |
| GitHub OAuth `frontend_url`（登录后落地，env 驱动） | `http://localhost:5173` | `http://localhost:8080` | **唯一允许随拓扑变化**的值，经 `REBUILD_FRONTEND_URL` 覆盖，不注册到 GitHub |
| CORS origins | `http://localhost:5173,http://localhost:8080` | 同 | 同时允许两套前端 |

**配置落点（改端口只改这些，且必须同步本表）**：

| 位置 | 键 |
|------|----|
| `backend/app/core/config.py` | `github_redirect_uri`(8000) / `cors_origins`(5173,8080) / `frontend_url`(dev 5173) |
| `frontend/vite.config.ts` | `VITE_BACKEND_TARGET` 默认 `http://localhost:8000` |
| `backend/.env` / `backend/.env.example` | `REBUILD_GITHUB_REDIRECT_URI`(8000) / `REBUILD_CORS_ORIGINS` |
| 根 `.env.example` / `docker-compose.yml` | 容器前端 8080 / `REBUILD_GITHUB_REDIRECT_URI`(8000) |
| `frontend/nginx.conf` | `proxy_pass http://backend:8000` |

**一次性人工动作（GitHub 侧）**：GitHub OAuth App 的 Authorization callback URL 须登记为 `http://localhost:8000/api/integrations/git/oauth/github/callback`（由 8765 迁移而来）。平台代码无法代改 GitHub 端注册。

---

## 2. 镜像与编排基线（硬规则）

1. **密钥绝不入镜像**：`LLM_API_KEY`/各 Provider Key/Webhook/OAuth Secret 只经 `env_file`（`.env`，已 `.gitignore`）注入。镜像、`docker history`、构建日志均不得含明文（AGENTS §8/§12）。
2. **非 root 运行**：所有应用容器以非 root 用户运行。
3. **数据持久化用命名卷**：DB 与 `.data/` 必须落卷，容器重建不丢数据。
4. **可复现构建**：后端依赖锁定 `uv.lock`（`uv sync --frozen`），前端 `npm ci`。
5. **健康检查**：每个常驻服务定义 `HEALTHCHECK`。
6. **`.dockerignore` 卫生**：排除 `.venv/`、`.data/`、`node_modules/`、`dist/`、`.env`、`.git/`。
7. **LangGraph 主编排底座不变**（D-037）：容器化只改"在哪运行/如何隔离"，不改编排内核。

---

## 3. 执行隔离规范（Phase C2 — 安全核心）

### 3.1 ExecutionProvider 强制接口

平台内**一切代码执行**（Workspace、OpenCode 集成、未来 Run/Task 执行）**必须经 `ExecutionProvider`**（`backend/app/services/execution_provider.py`），不得在调用点直接 `subprocess`/直连 `opencode_adapter`：

| 模式（`EXECUTION_MODE`） | Provider | 说明 |
|--------------------------|----------|------|
| `local`（默认） | `LocalSubprocessExecutionProvider` | 当前行为：宿主机受控子进程 + deny-list，向后兼容 |
| `container` | `ContainerExecutionProvider` | 一次性、网络隔离、非 root、资源受限容器内执行；**R8 已用 docker-py 真实接线**（镜像 `rebuild-execution-sandbox:local`，uid 10002，`network=none`/`read_only`/`cap_drop=ALL`/`no-new-privileges`）；失败返回显式错误，**绝不静默降级为宿主机执行** |

### 3.2 沙箱容器硬化基线（compose 已落地）

`read_only` 根文件系统、`network_mode: none`、`cap_drop: ALL`、`no-new-privileges`、`pids_limit`、`mem_limit`、`cpus`、`tmpfs` 工作目录、非 root。沙箱**不得**装 `git`/`ssh`/网络工具——不可外联代码托管/远程。

### 3.3 纵深防御（不可删除）

容器隔离是**第一层**；现有 `opencode_adapter` 的 deny-list / allowlist / timeout / stdout-stderr 截断 / env 脱敏作为**第二层**保留。

---

## 4. 对后续工作区（Workspace）及相关施工的约束（预留要求）

> 本节为对未来 R 阶段施工的**硬约束**，确保"工作区及相关部分"按容器化思路落地。

1. **R8+ Workspace/Run 执行施工**：项目内代码运行、测试、迁移脚本执行，**必须**走 `ExecutionProvider`，并优先支持 `container` 模式；新增执行入口不得绕过该 seam。
2. **每个项目工作区 = 一个隔离执行上下文**：以容器/命名空间为边界，工作区文件挂载为卷；不同项目工作区互不可见。
3. **远程/Git 操作**：保留在 backend 容器或专用 worker，凭据经卷/secret 挂载，不入镜像；沙箱本身无外联能力。
4. **信创交付（Phase C3）**：基础镜像替换为信创合规镜像（openEuler/UOS 系），产出离线镜像包，目标环境做端到端联调（D-074 同等标准）。
5. **变更控制**：部署/执行架构变更按 `04-变更控制规范` C2（编排）～C4（技术栈）提交；本规范 accepted 后作为该领域单一事实源。

---

## 5. 落地现状（R7-5）

| 项 | 状态 | 证据 |
|----|------|------|
| backend/frontend/sandbox Dockerfile | implemented | 仓库根与各目录 |
| docker-compose（sqlite 默认 + pg/sandbox profile） | implemented | `docker-compose.yml`，`docker compose config` 通过 |
| ExecutionProvider seam（local 默认 / container 预留） | implemented | `backend/app/services/execution_provider.py`，OpenCode 路由已接 seam |
| 沙箱镜像构建 + 无网络运行 | verified | `docker build` + `docker run --network none` 通过 |
| backend 镜像构建 | verified | `docker build` 通过 |
| 端到端 `compose up` 真实联调 | verified | docker compose 起栈：frontend 200 / nginx→backend 200 / 非root appuser / git+ssh / 0 QueuePool（R7-5 自验报告 §4.2） |
| container 执行真实接线 | implemented（R8，本地验证） | `execution_provider.py` docker-py 接线；本地实测 `print(6*7)`→exit0/`42`、shell 以 uid 10002 运行、出站网络不可达；compose 启用见 §1.1/§3 的 docker.sock opt-in |
| 信创合规镜像 | future（C3） | 本规范 §4.4 |

---

## 6. 待确认项

- **Q-CT-1**：默认数据库口径——dev 用 SQLite、prod 用 PostgreSQL 是否作为正式约定？（建议：是）
- **Q-CT-2**：`container` 执行接线（C2）排入哪个 R 阶段（建议与 R8 Workspace 真实化合并）。
- **Q-CT-3**：信创基础镜像选型（openEuler / UOS）与离线交付形态，待交付节点确认。
