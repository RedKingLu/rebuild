# rebuild backend —— 本地开发环境变量示例
#
# 用法：  cp .env.example .env   然后按需取消注释并填写。
#
# ── 安全红线（AGENTS.md §8/§12）───────────────────────────────────────────
# 本文件只列「变量名 + 说明 + 明显占位」，**不含任何真实密钥值**。
# 真实 Key / Token / Secret 只写在本机 .env（已被 .gitignore 保护），
# 绝不提交、绝不写入镜像、绝不出现在日志 / issue / 报告 / 截图中。
# ─────────────────────────────────────────────────────────────────────────
#
# 读取方式：`REBUILD_` 前缀变量由 pydantic-settings（backend/app/core/config.py，
# env_prefix=REBUILD_）读取；其余无前缀变量由代码直接从环境读取，后端启动时
# 会把本文件所在目录的 .env 预加载进进程环境（shell 中已设置的键不会被覆盖）。
#
# 端口标准（单一事实源，禁止另设）：后端统一 8000；前端 dev=5173 / 容器=8080。
#
# 图例：  [必填] 缺失则相关功能不可用    [可选] 有默认值，不设即用默认


# ═══════════════════════════════════════════════════════════════════════
# 1. 凭据加密主密钥
# ═══════════════════════════════════════════════════════════════════════

# REBUILD_MASTER_KEY  [必填，若要使用凭据(BYOK)功能]
#   用途：平台内保存的模型 / 集成凭据的 AES-256-GCM 加密主密钥。
#   缺省时走开发派生密钥，仅供本地试跑；任何正式使用都必须显式设置。
#   生成方式（在你自己的终端执行，把输出填到本机 .env，不要提交）：
#       python -c "import secrets; print(secrets.token_hex(32))"
# REBUILD_MASTER_KEY=<32-byte-hex>


# ═══════════════════════════════════════════════════════════════════════
# 2. 基础运行配置
# ═══════════════════════════════════════════════════════════════════════

# REBUILD_DATA_DIR  [可选] 本地数据目录（SQLite 库、执行沙箱、仓库克隆）
REBUILD_DATA_DIR=./.data

# REBUILD_DATABASE_URL  [可选] 数据库连接串。不设则用 backend/.data 下的 SQLite。
#   仅在需要覆盖默认库位置或改用 PostgreSQL 时设置：
# REBUILD_DATABASE_URL=sqlite:////absolute/path/to/rebuild.db
# REBUILD_DATABASE_URL=postgresql+psycopg://<user>:<password>@localhost:5432/rebuild

# REBUILD_CORS_ORIGINS  [可选] 允许的前端来源，逗号分隔（同时放行 dev 5173 与容器 8080）
REBUILD_CORS_ORIGINS=http://localhost:5173,http://localhost:8080

# REBUILD_DEBUG  [可选] 调试模式；生产部署请设为 false
REBUILD_DEBUG=true

# REBUILD_FRONTEND_URL  [可选] OAuth 登录完成后跳回的前端地址（dev=5173 / 容器=8080）
REBUILD_FRONTEND_URL=http://localhost:5173

# REBUILD_SOURCE_DIR  [可选] 平台运行期资源根目录（Skill / 案例 / 资源清单）。
#   不设则用仓库内 source/ 的默认绝对路径；仅在把仓库放在别处时需要覆盖。
# REBUILD_SOURCE_DIR=/absolute/path/to/repo/source

# REBUILD_WORKSPACE_DIR  [可选] 各项目隔离工作区的根目录。
# REBUILD_WORKSPACE_DIR=/absolute/path/to/workspaces

# REBUILD_COMMUNITY_BASE_URL  [可选] 可选社区服务的 base URL（默认不启用该服务）
# REBUILD_COMMUNITY_BASE_URL=http://localhost:8001

# REBUILD_R_STAGE  [可选] 平台自身版本阶段标识，仅用于 /api/health 与 /api/version 输出，
#   一般保持默认即可。
# REBUILD_R_STAGE=unknown


# ═══════════════════════════════════════════════════════════════════════
# 3. 模型接入（BYOK —— 自带模型凭据）
# ═══════════════════════════════════════════════════════════════════════
#
# 所有模型调用统一经 ModelGateway → LiteLLM 适配层，不直连 Provider、不硬编码
# 模型名与 endpoint。Provider 的非敏感信息（含各自使用哪个环境变量名取 Key）
# 定义在 backend/app/config/model_profiles.yaml，Key 本身只从环境变量读取。
#
# 无有效 Key 时，需要推理的阶段会诚实报告 blocked，不会降级为规则模板冒充完成。

# LLM_API_KEY  [必填之一] 通用兜底 Key（单 Provider 本地开发最省事的一种）
# LLM_API_KEY=your-api-key-here

# LLM_BASE_URL  [可选] 通用兜底 Key 对应的 OpenAI 兼容 endpoint
# LLM_BASE_URL=https://<your-openai-compatible-endpoint>/v1

# 按 Provider 分别配置时使用 {PROVIDER}_API_KEY 形式：
# DEEPSEEK_API_KEY=your-api-key-here
# AGNES_API_KEY=your-api-key-here

# OPENAI_API_KEY / OPENAI_BASE_URL  [可选]
#   部分 OpenAI 兼容 SDK 调用路径会读取这两个标准变量名。
# OPENAI_API_KEY=your-api-key-here
# OPENAI_BASE_URL=https://<your-openai-compatible-endpoint>/v1


# ═══════════════════════════════════════════════════════════════════════
# 4. 模型调用重试与超时（均 [可选]，不设即用代码内默认值）
# ═══════════════════════════════════════════════════════════════════════

# LLM_MAX_RETRIES            单次模型调用的最大重试次数
# LLM_REQUEST_TIMEOUT        单次模型请求超时（秒）；流式调用同时用作
#                            「首字节超时」与「chunk 间读超时」阈值（B-R18-1-STREAM-HANG）
# LLM_RETRY_BASE_DELAY       重试退避基准延迟（秒）
# LLM_STREAM_TOTAL_TIMEOUT   流式响应总超时（秒）；覆盖整次调用（含全部重试）
# LLM_MAX_RETRIES=
# LLM_REQUEST_TIMEOUT=
# LLM_RETRY_BASE_DELAY=
# LLM_STREAM_TOTAL_TIMEOUT=

# STAGE_TRANSIENT_MAX_RETRIES        阶段执行遇瞬时错误的最大重试次数
# STAGE_TRANSIENT_RETRY_BASE_DELAY   阶段重试退避基准延迟（秒）
# STAGE_TRANSIENT_MAX_RETRIES=
# STAGE_TRANSIENT_RETRY_BASE_DELAY=

# P3_PLANNING_MAX_TOKENS   规划阶段单次生成的最大 token 数
# P3_PLANNING_TIMEOUT      规划阶段调用超时（秒）
# P4_GEN_MAX_TOKENS        执行阶段代码生成的最大 token 数
# P3_PLANNING_MAX_TOKENS=
# P3_PLANNING_TIMEOUT=
# P4_GEN_MAX_TOKENS=

# LITELLM_LOG  [可选] LiteLLM 自身日志级别，排查模型调用问题时临时打开
# LITELLM_LOG=DEBUG


# ═══════════════════════════════════════════════════════════════════════
# 5. 执行模式与路径
# ═══════════════════════════════════════════════════════════════════════

# EXECUTION_MODE  [可选] 命令执行提供方：local（宿主子进程，默认）| container
#   container 模式需另行准备沙箱镜像并授予 Docker 访问权（等同交出宿主 Docker
#   控制权），仅建议在可信单机环境启用。
# EXECUTION_MODE=local

# P5_EXECUTION_MODE  [可选] 验证阶段命令执行模式，默认 workspace_local
# P5_EXECUTION_MODE=workspace_local

# WORKSPACE_DIR  [可选] 容器/沙箱内工作区挂载路径（与上面 REBUILD_WORKSPACE_DIR
#   不是同一个变量：此项面向执行环境内部路径）
# WORKSPACE_DIR=/workspace

# SKILL_SOURCE_ROOT  [可选] Skill 资源加载根目录，覆盖默认推导路径
# SKILL_SOURCE_ROOT=/absolute/path/to/repo/source/skills


# ═══════════════════════════════════════════════════════════════════════
# 6. GitHub OAuth（可选，代码托管集成用）
# ═══════════════════════════════════════════════════════════════════════
#
# 回调地址在 dev 与容器两套拓扑下相同（后端都在 8000），GitHub OAuth App
# 只需注册一次：
#   http://localhost:8000/api/integrations/git/oauth/github/callback

# REBUILD_GITHUB_CLIENT_ID=your-client-id-here
# REBUILD_GITHUB_CLIENT_SECRET=your-client-secret-here
# REBUILD_GITHUB_REDIRECT_URI=http://localhost:8000/api/integrations/git/oauth/github/callback
