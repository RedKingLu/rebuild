# rebuild V26.3 发布说明

> 文件身份：发布说明
> 日期：2026-09-17
> 发布类型：**优化基线**（不是功能版本）
> 平台：rebuild | 版本：V26.3
> 许可：MIT
>
> ⚠ **先说清"发布"在这里是什么意思**：本版本**未走完发布收口**（用户亲自前端验收 `V-09` 未执行）。它是一次**以优化为核心目标的现代化重构**的收口记录，同时是仓库转公开时的版本标记。当前真实现状与已知缺陷请以 `PROJECT_STATUS.md` 为准 —— 那份文档比本文更适合用来判断"能不能用"。

---

## 1. V26.3 做了什么

V26.3 **不是功能版本**，是一次针对"历史多轮迭代 + 开源大变更"留下的沉积做的清理（D-123）。两条主线：

### R23 文档逐项核对与优化（83 个文件）

| 类别 | 处数 |
|---|---|
| 过程注记收敛至 `文档/09-参考索引/修订史.md` | 89（含一段 1615 字符的叙事段） |
| 过期结论更正 | 28 |
| 死链修复 | 75 |
| 重复详述源收敛 / 口径漂移统一 | 2 / 9 |
| 决策登记（D-123~D-131） | 9 条 |

**一处值得记录的方案选择**：原计划是"修正某个错误的提交数快照"，实际改为**删掉 6 处此类速查快照、全部改为指向权威源**，并把原先"两处必须成对校正"的规则改成"**禁止再回填**"。理由是这类字段本质必然过期（同一字段先后实测为 0 / 4 / 5 三个值，三个都"对"，只是时点不同）。⇒ 修的是"为什么会过期"，不是"这次的数字"。

同时解决一处互指的权威源死循环：A 文档写"权威在 B"、B 文档写"权威在 A"。

### R24 代码与架构逐项核对与优化（两遍）

**第一遍**：把 V26.2 遗留的 13 项缺陷台账一次修完（原计划分四个批次，用户裁决合并）。5 项全部解除条件闭合，8 项部分闭合（未闭合的 12 条解除条件按原因归三类：权限边界 6 条、需真跑或浏览器环境 5 条、安全取舍 1 条 —— 无一条是"忘了"）。

主要修复：
- **Gate 决策必须绑定被决策对象**：此前对同 run 内任意一个待决 Gate 提交决策，都会被应用到"图自己暂停的那个 Gate"上（真跑中已实际造成一次超出授权范围的状态变更）
- **动作审批必须绑定被审阅的入参**：此前授权只按 `(run_id, 工具名)` 匹配 ⇒ 用户签核的是入参 X、实际授权的是后来的入参 Y
- **晋级 Gate 文案按真实验收结论分派**：此前把"已完成并通过独立验收"写成无条件断言，`rework_required` 时产出自相矛盾且会诱导误批的文案
- **P 环节取消平台侧硬编码 token 天花板**：改为交还给模型自身输出上限 + 三层时间护栏（详见 §2）
- **P0 解析失败不得判完成**：此前 P0 识别产出 `parse_error` 后仍置 `completed`、阶段仍判 `accepted / 0 问题`
- 新增跨节点重复符号检查、未声明依赖检查、被挂起动作可见性、L1 只读递归文件名检索工具
- 建 Gate 前先过命令白名单 —— 不再请用户为一条注定被执行层挡回的命令签核

**第二遍**：21 处不发声的宽 `except` **全部改为发声**，按语义分档（"文件不存在"是正常语义 → debug；"文件在却读不出来"是真问题 → warning），每处写明控制流为何不变、以及为什么静默不可接受。零控制流语义改动、零测试改动。

长函数拆了 0 个、超长文件拆了 0 个 —— **有数据支撑而非偷懒**：55 个长函数里去重后 52 个，其中"长而扁"的拆了是负收益，真正有控制流复杂度的 11 个全部落在安全边界或图主链路上，按授权口径不得触碰。

### 测试

`1810 → 1976 passed / 0 failed`。每一步的账目（passed / skipped / failed / 收集总数）四项同时闭合。

---

## 2. 一次完整真实项目运行（本版本最硬的证据）

样本：2010 年代 ASP.NET Web Forms 项目，1018 文件物化 / 约 20 万行 / 含 10MB T-SQL，目标 .NET 8 + Linux 容器。走规范主入口，141 次模型调用、约 780 万 token、**0 失败、0 provider fallback**。

**到达 P5**，最终判定 `rework_required`（正确 —— 产出代码确实编译不过）。

### 取消 token 天花板的直接证据（跨三个服务）

| 阶段 | 单次结构化输出 | 该处原硬编码上限 | 倍数 |
|---|---|---|---|
| P2 评估 | 21,545 tokens | 16,384 | 1.3× |
| P3 规划 | 22,058 tokens | 16,384 | 1.3× |
| **P4 执行** | **30,633 tokens** | **4,096** | **7.5×** |

三者均完整落地。⇒ 若旧天花板仍在，这三次必被截断；P4 那次会把一个代码文件砍成不到七分之一，且是**静默写盘半截文件**。

同时**反面证据消失**：141 次调用中落在"紧贴天花板"区间（16380–16390）的为 **0**，而上一轮真跑的实锤是 16383 / 16384 / 16387 三个。

### 与上一轮真跑的同位置对照

| | 上一轮 | 本轮 |
|---|---|---|
| P0 识别 | `parse_error`，12 个识别键全空，**却判 accepted / 0 问题** | 无 `parse_error`，12/13 键有值 |
| P2 | 撞顶截断，五个清单产物全部"未产出"，`rework_required` → **全轮中止** | 13 份产物无一解析失败，判 `accepted` |
| P4 | 报 **12/12 节点完成**，产出代码却不可编译 | **4/18 就报 blocked**，不谎报 |
| 代码可运行性 | 三层缺陷剥洋葱才见真因 | 一条诊断，精确到行列 |

### 容器构建是真验，不是声称

P5 在 `mcr.microsoft.com/dotnet/sdk:8.0`（含镜像 digest）内真跑 restore / build / test / format 四条命令。容器加固：`cap_drop=ALL`、只读根文件系统、非 root 运行、不挂 docker socket、内存与进程数限制。出网需求（`restore` 需要包源）被**如实标注为弱化项**，并写明未做 egress 白名单。

---

## 3. 本版本没有解决的（请连同 §2 一起读）

**① 生成的代码目前编译不过。** 上述运行中，生成的 `.csproj` 有一行 ASCII 分隔线写在 XML 注释内，而 XML 禁止注释含 `--` ⇒ 项目文件连解析都过不了，四条构建命令同时死在第一步。

更值得注意的是**这个缺陷落在所有现有检测器之外**：跨节点重名检查、未声明依赖检查、协议文本泄漏检查 —— 没有一个校验生成文件本身的语法合法性。已登记为 P1，解除条件要求"落盘前按扩展名分派最小解析器，解析失败即拒绝写盘"，并明确"给模型加提示词说别这么写"**不算修好**。

**② P6 交付阶段从未在真实规模项目上跑通。**

**③ 只在"软件现代化"一个场景上做过完整验证。** 信创切换场景最远到过 P4。

**④ 用户亲自前端验收（`V-09`）未执行** ⇒ 本版本严格说未完成发布收口。

**⑤ R24 第一遍刻意改变了 6 项用户可见行为**（Gate 文案、`/gates/active` 返回结构、onboarding 校验失败由 200 改 422、P1 占位计数、P5 页请求时机、一处兜底删除后的报错）。均在授权范围内，但**缺少产物级 diff 证据** —— 因为用于对照的 golden 基线是在优化**之后**才建立的（排期被压缩所致，已如实登记）。该缺口无法追溯补齐。

完整缺陷台账（7 条 open + 6 条部分闭合）见 `PROJECT_STATUS.md` §6。

---

## 4. 与 V26.2 的关系

V26.2 的总验收结论是 `rework_required`，其发布收口按裁决移至 V26.3 之后。V26.3 完成了该返工 + 优化，但**发布收口仍未走完**（见 §3④）。

⇒ 演进序列：**V26.1.1**（首次基线）→ **V26.2**（能力基线，总验收未过）→ **V26.3**（优化基线，本版本）。

三份发布说明都保留，各自记录其时点的判断 —— 包括后来被推翻的那些。`V26.2-RELEASE.md` 已补记"后续事实更正"段说明这一点。

---

## 5. 本版本同时完成的事

- 仓库转为公开（MIT），沿用既有 git 仓库、未 squash、未重建
- 转公开前置的全 git 历史密钥扫描通过（真实凭据从未进入历史，`.gitignore` 从一开始就挡住了 `.env*`）
- 新增 `PROJECT_STATUS.md` —— 面向外部读者的现状报告，短板写在显眼处
- 建立 V26.2 基线快照归档（供"修改 accepted 文档须保留旧版本"的合规留存）
