# R13 Fusion 一等模型接入 — 收口说明

> 创建日期：2026-07-06
> 阶段：R13-9（收口）
> 关联：D-060（R13 Fusion 功能开发及合入）、D-035（Fusion 作为模型能力）、D-098（模型策略）
> 状态：**accepted（收口）**

## 1. 本轮做了什么

R13 把 **Fusion 聚合模型** 做成系统里的**一等模型**：在 ModelGateway 后台把多个真实模型聚合成一个可被平台当作单一模型使用的能力，并对上层（Agent / Skill / P0-P6 / 用户模型选择器 / 平台小助手）**与普通模型没有差别**。

### 1.1 用户三条硬要求（全部达成）
- **AC-U1 参与模型/数量可调**：`FusionProfile.panel_participants` 在 API（create/PUT config）与前端（`FusionConfigPanel` add/remove/update）均可增删改。
- **AC-U2 不限制环节**：`enabled_stages` 仅作元数据，调用路径无任何环节 gate；经 `_project_preferred_ref` 的普通调用即可触发。
- **AC-U3 对 Agent 无差别**：经既有 `user_override="fusion/{id}"` 或项目/Agent `preferred_ref` 选择 Fusion，返回形态与普通模型等价；**未新增任何上层专属参数**。

### 1.2 架构基线（方案 E，R13-2C 裁决）
- **引擎** `FusionExecutionEngine`：Panel 并发 fan-out → Judge（temp=0，五字段 JSON）→ Synthesizer（**不执行工具**，方案 E）+ Self-MoA 标准降级 + 预算/超时/degraded。
- **防递归三重 + 选择层过滤**：participant 不含 fusion + `max_fusion_depth=1` + `_raw_call()` 不经 `resolve_model()`；前端参与器过滤 `provider=rebuild-fusion`。
- **一等模型接入**（R13-8-FIX）：`model_gateway._soft_fusion_profile()` 软优先级链（preferred_ref → strategy 默认）识别 `fusion/` 引用并透明分派；`/api/model/providers`+`/api/model/profiles` 并入 `rebuild-fusion` 虚拟服务商与各 FusionProfile 虚拟模型；ModelsPage / Strategy / Agent 默认 / 平台小助手四处可见并可选。
- **配置变更审计**（F3 修复）：`routes_fusion._svc` 注入共享 `AuditWriter`，create/update/toggle 落 `fusion_config_change`。

## 2. 独立验收结论

- **R13-8R-2 二次独立验收：accepted（修复轮）**。
- 我（独立验收 Agent）**亲自真实 E2E**：将某项目 `global_model_ref=fusion/fp-*` 经普通 `call_stream(user_override=None)` 触发 → 透明跑完整聚合、流式回吐单一 content **2578 字符**、done=True；`_soft_fusion_profile(preferred=普通已配置)→None`（不劫持普通模型）。
- 后端全量 `pytest` **766 passed / 2 skipped / 0 failed**（独立复跑）；`test_r13_5_live_e2e` **1 passed**；前端 `vite build` exit 0。
- 方案 E / 防递归 / 返回等价 / 脱敏（rebuild-fusion 无明文 Key）均保持。

## 3. 收口操作（R13-9）

- **进度追踪更新**：
  - `01-功能表.md`：ENH-01 `in_progress` → **accepted**；MOD-05 `planned` → **accepted**。
  - `02-阻塞项.md`：B-R13-2C-ENV-1（WebSearch 故障）→ **mitigated/resolved**（社区 C 级已被 A 级官方文档覆盖，未阻塞 R13）。
  - `03-待确认项.md`：Q-R13-8R-1 → **resolved**（装配已打通，二次验收 accepted）；Q-R13-2C-1~7 随收口闭环。
  - `04-施工记录.md`：追加 R13-8R-2、R13-9 条目。
  - `05-验收与证据索引.md`：追加 EVD-R13-9-01（R13 全量收口证据，位于 R17 条目之后，未触碰 R17）。
- **文件归档**：`交接/当前/R13-*.md`（19 份）→ `交接/已完成/R13/`。**未触碰任何 R17 文件**。
- **Git 提交**：见下方提交信息。

## 4. 已知残留（不阻断 accepted，属 REC / 后续轮）

- `resolve_call_target`（OpenCode 外部编码子进程路径）不分派 Fusion（边界，选中诚实报错）。
- 平台小助手对 fusion 模型的"连通性自测"按钮诚实报错（应经触发验证）。
- F6（同 provider 未标 `homogeneous_panel` degraded）/ F7（Judge 首调用未落库）——R13-8R 既有 REC。
- 正式文档更新（09-聚合页 2→3 tab、02-Fusion 作为模型能力、01-ModelGateway）已登记，归后续文档对账轮正式化。

## 5. 给后续 Agent 的提示

- Fusion 一等模型已打通：**新增"把某能力做成模型"类需求** 可复用 `rebuild-fusion` 虚拟服务商 + `_soft_fusion_profile` 软链 + `_raw_call` 防递归原语。
- 方案 E 红线（Fusion 不执行工具）与防递归三重+选择层过滤**不可退化**。
- R14（远程资源）/ R15（社区）/ R16（文档）按 D-060 串行推进。
