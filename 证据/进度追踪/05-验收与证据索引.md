# 05-验收与证据索引

> 文件路径：`证据/进度追踪/05-验收与证据索引.md`
> 文件身份：进度追踪 / 验收证据统一索引
> 平台名称：rebuild
> 当前版本：V26.1.1
> 创建时间：2026-06-25
> 最后更新：2026-06-25
> 更新来源：R5-1 进度追踪体系建设
> 文档定位：将验收证据、截图、测试结果、Playwright/network、pytest、build、grep 等统一索引，连接功能表与真实证据。

---

## 0. 证据类型

| 类型 | 含义 |
|------|------|
| pytest | 后端 Python 测试 |
| build | 前端/后端构建 |
| Playwright | 浏览器端到端测试 |
| screenshot | 截图证据 |
| network | 网络请求捕获 |
| grep | 代码搜索验证 |
| audit | 审计日志 |
| trace | 调用追踪 |
| report | 验收/自验报告 |
| user_acceptance | 用户验收确认 |

## 1. 验收证据清单

### EVD-R3-01：R3 体验壳验收

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R3-01 |
| **证据类型** | build + Playwright + screenshot + report |
| **关联功能ID** | UX-01~08, WS-01~07 |
| **关联施工记录ID** | REC-R3-01, REC-R3-02 |
| **证据路径** | 交接/已完成/R3/R3-4-体验验收报告.md, 交接/已完成/R3/R3-3-前端页面壳施工报告.md, 证据/验收证据/R3-关键页面截图/ |
| **证据摘要** | npm run build exit 0；Playwright 全部主路由 0 崩溃；逐路由边界测试通过；项目区收敛、路由精简、404回落验证 |
| **证明了什么** | 前端工程可构建；所有主路由不崩溃；Workspace 七区布局正确；信息架构三层落地 |
| **不能证明什么** | 任何真实后端交互（R3为体验壳，显式Mock边界）；真实数据渲染；Agent/Gate/模型真实能力 |
| **是否可复现** | 是（npm run build + Playwright） |
| **验收状态** | user_accepted |

### EVD-R4-01：R4 后端测试

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R4-01 |
| **证据类型** | pytest + report |
| **关联功能ID** | API-01~04, SSE-01, AET-03~04, ENV-01, ENV-03~05 |
| **关联施工记录ID** | REC-R4-02, REC-R4-03, REC-R4-04 |
| **证据路径** | 交接/已完成/R4/R4-4-基础工程与API契约独立验收报告.md, 交接/已完成/R4/R4-修复轮-自验报告.md, 证据/验收证据/R4-4-独立验收证据/ |
| **证据摘要** | 46 passed (venv Python 3.12.13)；16 域 API 端点全部可访问；统一 envelope + Problem Details；Trace 自动写入、Audit 记录 |
| **证明了什么** | 后端骨架可运行；16 域 API 端点存在且返回合法响应；SSE snapshot+heartbeat 正常；Trace/Audit 写入正常 |
| **不能证明什么** | API 真实业务逻辑（大部分为 mock/占位）；LangGraph 编译运行（仅占位）；真实模型/Agent/Git 操作 |
| **是否可复现** | 是（uv sync --dev + pytest） |
| **验收状态** | user_accepted |

### EVD-R4-02：R4 最小前后端联调（项目列表页）

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R4-02 |
| **证据类型** | Playwright + network + report |
| **关联功能ID** | PRJ-03, UX-03 |
| **关联施工记录ID** | REC-R4-04 |
| **证据路径** | 交接/已完成/R4/R4-修复轮-自验报告.md |
| **证据摘要** | Playwright 实测项目列表页运行期真实发起 GET /api/projects；经 Vite proxy 命中后端；页面渲染后端返回的项目数据与来源标签；后端不可用时回退本地 mock 不崩溃 |
| **证明了什么** | 前端→Vite proxy→后端→前端 闭环通；D-074 最小前后端联调要求可行 |
| **不能证明什么** | 其他页面的前后端联调（仅项目列表页一条） |
| **是否可复现** | 是（需后端运行中） |
| **验收状态** | user_accepted |

### EVD-R4-03：R4 独立验收

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R4-03 |
| **证据类型** | report + pytest + build + audit |
| **关联功能ID** | API-01~04, SSE-01, AET-01~04, ENV-01~05, PRJ-01, GAT-01 |
| **关联施工记录ID** | REC-R4-03 |
| **证据路径** | 交接/已完成/R4/R4-4-基础工程与API契约独立验收报告.md, 证据/验收证据/R4-4-独立验收证据/ |
| **证据摘要** | 独立验收 Agent 检查：后端启动、46 passed、16域API逐域、SSE、Trace/Audit、LangGraph边界、无真实能力伪装、无旧术语、无密钥；前端 build exit 0、7路由0崩溃 |
| **证明了什么** | R4 交付物符合"基础工程骨架与API契约"范围；有条件通过（C-A/C-B 后修复） |
| **不能证明什么** | 真实业务能力（验收报告明确标注仅骨架/占位） |
| **是否可复现** | 部分（需相同环境） |
| **验收状态** | independently_verified |

### EVD-R5-01：R5-1 方案

> 注：R5-1~R5-4 的验收证据条目统一详列于 §3「验收证据统计」之后（EVD-R5-01~EVD-R5-04），此处不重复，避免重复编号与状态冲突（R5-4 修复 C-2）。

---

## 2. 证据充分性评估

### 2.1 已验证证据链（2026-06-25 R5-3 更新）

| 能力 | 证据强度 | 评估 |
|------|---------|------|
| 前端可构建 | build exit 0 | ✅ 充分 |
| 前端路由不崩溃 | Playwright 逐路由 | ✅ 充分 |
| 后端可运行 | 启动 + 46+43 passed | ✅ 充分 |
| 16 域 API 端点存在 | 逐域 curl/检查 | ✅ 充分 |
| SSE snapshot+heartbeat | 手动验证 | ✅ 充分 |
| Trace/Audit 写入 | 代码检查 + 运行验证 | ✅ 充分 |
| 项目列表页前后端联调 | Playwright network捕获 | ✅ 充分 |
| ModelGateway 全量能力 | 43 pytest + 8 API端点 + 3 Provider真实reachable | ✅ 充分 |
| 前端模型页真实联调 | 5 tab全部真实调用/api/model/* | ✅ 充分 |
| Workspace ModelGateway chip | fetch /api/model/status 真实状态 | ✅ 充分 |
| 平台助手真实调用 | /api/assistant/chat 77 tokens | ✅ 充分 |
| 密钥脱敏工程化 | 全端点零Key + call log/Trace/Audit脱敏 | ✅ 充分 |
| no-key/wrong-key/provider-down边界 | 3边界测试43/43 passed | ✅ 充分 |

### 2.2 证据不足警示（2026-06-25 R5-3 修正）

| 能力 | 当前证据 | 不足原因 |
|------|---------|---------|
| 资源/集成/聚合/案例/知识/设置/社区/文档页面 | 仅截图（静态Mock） | R5范围外，非R5阻塞 |
| Gate API | 仅端点存在 | 无真实决策逻辑、无集成测试 |
| Workspace 非chip区域 | 仅截图（UI壳） | R5仅接chip，其余归R8 |
| Artifact/Evidence API | 仅端点存在 | 无真实存储/检索测试 |
| LangGraph | 仅占位代码 | 未编译、未运行 |
| Agent 对话 | 仅UI壳 | 无后端 Agent 服务（R6范围） |

**证据不足的能力不应标记为 `implemented` 或 `verified`**，当前功能表中已按实际状态标记为 `partial`。

---

## 3. 验收证据统计

| 验收状态 | 数量 |
|---------|------|
| user_accepted | 5 (EVD-R3-01, EVD-R4-01, EVD-R4-02, EVD-R5-01, EVD-R5-04) |
| independently_verified | 1 (EVD-R4-03) |
| self_verified | 2 (EVD-R5-02, EVD-R5-03) |
| candidate | 0 |
| stale | 0 |
| **合计** | **8** |

### EVD-R5-01：R5-1 模型网关自有方案

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R5-01 |
| **证据类型** | report |
| **关联功能ID** | MOD-01~06, LIT-01~04 |
| **关联施工记录ID** | REC-R5-01 |
| **证据路径** | 交接/当前/R5-1-模型网关与模型策略方案报告.md, 产物/草稿/R5-模型网关与模型策略方案.md |
| **证据摘要** | R5-1 方案覆盖 19 章节（ModelGateway设计/Provider/Profile/Strategy/LiteLLM/Self-test/API/前端联调/容器化/测试/不做事项）；18 条阶段边界自检+6条事实源自检+9条模型网关自检+6条安全脱敏自检+6条前后端联调自检+6条容器化自检=全部通过 |
| **证明了什么** | R5 ModelGateway 自有方案完整、边界清晰、不违反任何红线 |
| **不能证明什么** | 方案可行性（待 R5-3 施工验证）；历史兼容性（R5-2 验证） |
| **是否可复现** | N/A（方案文档） |
| **验收状态** | user_accepted（有条件通过，Copilot C-1~C-8） |

### EVD-R5-02：R5-2 模型网关历史吸收

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R5-02 |
| **证据类型** | report |
| **关联功能ID** | MOD-01~06, LIT-01~04 |
| **关联施工记录ID** | REC-R5-02 |
| **证据路径** | 交接/当前/R5-2-模型网关历史吸收记录.md, 交接/当前/R5-2-模型网关历史吸收报告.md, 产物/草稿/R5-模型网关与模型策略完善建议.md |
| **证据摘要** | R5-2 读取 V26.0（3 份）+ V10（15+ 文件）历史材料；识别 7 项可吸收 + 3 项不吸收 + 2 项改写后吸收；回答 15 个必答问题；发现 3 个反例（单provider硬编码/模型名前缀缺失/Key掩码片段）；给出 7 项 R5-1 方案修订建议 + R5-3 18 工作包施工计划 |
| **证明了什么** | R5-1 方案 Route A 被两个历史版本验证可行；历史反例已被识别并规避；R5-3 施工输入完整 |
| **不能证明什么** | 施工可行性（待 R5-3） |
| **是否可复现** | N/A（历史吸收记录） |
| **验收状态** | self_verified（待用户审核） |

### EVD-R5-03：R5-3 模型网关全量施工

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R5-03 |
| **证据类型** | pytest + report + network |
| **关联功能ID** | MOD-01~04, MOD-06, LIT-01~04, HLP-01~02, SEC-05, WS-03, UX-06 |
| **关联施工记录ID** | REC-R5-04 |
| **证据路径** | 交接/当前/R5-3-模型网关全量施工报告.md, R5-3-模型网关全量施工自验报告.md, backend/tests/test_model_gateway.py (43 passed) |
| **证据摘要** | 后端: 3 Provider/9 Profile/1 Strategy YAML配置, LiteLLM Adapter(171行), ModelGateway Service(269行), 8 API端点全真实, 43 pytest passed, no-key/wrong-key/provider-down三边界通过; 前端: ModelsPage 5 tab(240+行)全真实调用后端, Workspace ModelGatewayChip真实/api/model/status, modelService.ts 7 API函数; 真实Key: 3 Provider全部configured+reachable, DeepSeek self-test 1169ms; 平台助手: POST /api/assistant/chat completed 77 tokens; 密钥安全: 全端点零Key泄露, call log/Trace/Audit/前端均[REDACTED] |
| **证明了什么** | R5 模型网关模块全量真实实现；前端模型接入板块和Workspace chip真实前后端联调；平台助手通过ModelGateway完成真实最小调用；密钥脱敏工程化落地；D-074最小前后端联调达成 |
| **不能证明什么** | R5-4 独立验收结论（待执行）；持久化/加密存储/组织级rate limit/完整BYOK（属后续阶段） |
| **是否可复现** | 是（uv sync --dev + pytest + npm run build；真实Key需用户填写） |
| **验收状态** | self_verified（自验通过；待 R5-4 独立验收） |

### EVD-R5-04：R5-4 独立验收 + 优化施工 + 收口

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R5-04 |
| **证据类型** | report + pytest + Playwright + network + screenshot |
| **关联功能ID** | MOD-01~04, MOD-06, LIT-01~04, HLP-01~02, SEC-05, WS-03, UX-06, GOV-12 |
| **关联施工记录ID** | REC-R5-04, REC-R5-05 |
| **证据路径** | 交接/已完成/R5/R5-4-二轮自验报告.md, R5-4-模型网关全量施工独立验收报告.md, 证据/验收证据/R5-3-模型网关全量施工证据/R5-4-*.png |
| **证据摘要** | 后端 uv run pytest 100 passed/0 failed(含导入/凭据/策略/能力标记/不落盘/三边界100%); 前端 npm run build exit 0; Playwright浏览器级实证: 模型页4面板中文+14能力标记+统计框=tab/导入POST providers 200+用户导入标记+删除200/助手浮窗级联服务商→模型+PONG·来源ModelGateway·1462ms+自测200/Workspace chip 网关·3/3 providers/banner已隐藏/设置界面字号 大→zoom=1.1; console error=0; Key泄露=0; 截图R5-4-v3-*.png齐全 |
| **证明了什么** | R5模型网关模块全量真实可用; 前端模型页/导入/助手/Workspace chip真实前后端联调; 14能力标记落地; 中文优先+线性图标规范固化; 平台助手级联切换+连通自测; Key volatile不落盘工程化; 策略编辑持久化; 用量聚合诚实标记; C-1~C-6全部修复; D-074最小前后端联调达标; 进度追踪体系全覆盖 |
| **不能证明什么** | 持久化用量库/成本统计/趋势图表(R8+); BYOK加密存储Key持久化(R6+,见Q-009); 非DeepSeek模型元数据准确性(Q-010); 阶段级override/fallback rule(R9); 完整组织级rate limit(R8+) |
| **是否可复现** | 是(uv sync --dev + pytest + npm run build + Playwright; 真实Key需用户填写) |
| **验收状态** | user_confirmed（R5收口验收通过，2026-06-25） |

## 7. R6 Agent / Skill / 资源基础 验收证据

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R6-01 |
| **证据类型** | report + pytest + Playwright + network + screenshot + key-leak-scan |
| **关联功能ID** | ASR-01~05, TKT-01~06, MOD-02, UX-06 |
| **关联施工记录ID** | REC-R6 |
| **证据路径** | 交接/已完成/R6/R6-4-独立验收报告.md, R6-收口总报告.md, 证据/验收证据/R6-3-Agent-Skill-资源基础/, 证据/验收证据/R6-4-独立验收证据/ |
| **证据摘要** | 后端 uv run pytest 133 passed(含31 R6新增+BYOK credential_ref e2e); 前端 npm run build exit 0; Playwright /resources/{agents,skills,tools} 全链路200 API+0 console error; Key泄露扫描(API/DB/日志/Audit)零命中; 5 Agent+27 Skill(3R+24P)+30+ Resource种子; 24 P系列真实SKILL.md(source/skills/)+3 R系列(skills/); BYOK AES-256-GCM+HKDF加密+credential_ref解码链路+L5 Audit |
| **证明了什么** | R6 Agent/Skill/Resource Registry全量真实可用; ResourcesPage 12类目录后端驱动; BYOK加密存储零泄露; ECC策展24 P系列真实Skill(信创语境改写); 数据库编写规范11章; D-074最小前后端联调达标 |
| **不能证明什么** | BYOK前端入口(仅后端/api/credentials→R7); 显式Provider不跨fallback强测试(R5策略→R7); R6数据库规范正式化晋升(→R7) |
| **是否可复现** | 是(uv sync --dev + rm .data/rebuild.db + uvicorn + pytest; Playwright截图可复跑) |
| **验收状态** | user_confirmed（R6收口验收通过 accepted，2026-06-26） |

---

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R7-02 |
| **证据类型** | self-verify report + pytest + build + API loop + Playwright + docker compose e2e + key-leak-scan |
| **关联功能ID** | P0-02 相关(项目/集成真实化)、凭据管理、容器化 |
| **关联施工记录ID** | REC-R7-02 |
| **证据路径** | 交接/当前/R7-5-返工与容器化自验报告.md；docker-compose.yml；backend/Dockerfile；frontend/Dockerfile；文档/02-架构设计/06-容器化部署与执行隔离规范.md |
| **证据摘要** | pytest 139 passed/0 failed；npm build exit 0；P0：220+ DB 请求 0 失败/0 QueuePool（本地+容器）；P1：归档过滤返回非空、飞书 webhook 仅返掩码无明文、凭据 create/rotate masked_key、Settings 凭据 UI 浏览器可见；P2：OpenCode 回退真实执行 6*7=42、概览移除未真实 Gate；浏览器 7/7 页 0 console error；docker compose 端到端 frontend 200/nginx→backend 200/非root/git+ssh |
| **证明了什么** | R7-4 全部 P0/P1/P2 返工项已修复并实证；平台可容器化一键起栈；ExecutionProvider seam 预留容器化执行 |
| **不能证明什么** | container 执行真实接线(C2 预留→R8)；真实外部 Git 平台/远程 VM/真实飞书群(待用户凭据)；信创合规镜像(C3) |
| **是否可复现** | 是（uv run pytest；npm run build；docker compose up --build；Playwright 脚本可复跑） |
| **验收状态** | completed（待 R7-6 独立复验，不得自行 accepted） |

---

## 8. R8 工作区真实化 验收证据

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R8-01 |
| **证据类型** | independent-review + pytest + build + REST API + Playwright + filesystem + container exec + trace/audit + key-leak-scan |
| **关联功能ID** | WS-01/04/07/08, AET-05, WKE-01/02/03/04, WKE-02(Environment Profile) |
| **关联施工记录ID** | REC-R8-03/04/05 |
| **证据路径** | 交接/当前/R8-4-工作区地基独立验收报告.md（含附录A修复轮）, R8-工作区真实化-汇总施工报告.md, R8-引导方案归属判定与实施方案.md |
| **证据摘要** | 后端 uv run pytest **152 passed**（139基线+13新增R8端点测试：文件读写/源码只读/路径越界/终端执行/deny-list/会话/Environment Profile）；前端 npm run build **exit 0**；浏览器 Playwright 工作区聚合/文件树/材料/会话/文件预览/终端/Trace面板/首启占位均真实，**0 console error**；容器执行 EXECUTION_MODE=container 实测 print(6*7)→exit0/42、uid10002 非root、network=none；Environment Profile(WP-A) 默认生成+GET/PUT持久化+非法enum 400；持久化文件无密钥泄露；conftest 修复 workspace_dir 隔离 |
| **证明了什么** | Workspace 从 mock/static demo → 真实可复用地基：文件树真实扫描/文件预览/材料编辑保存闭环/受控终端/ExecutionSession 文件持久化/Trace-Audit 文件持久化/容器执行真实接线/AET 检视真实化/Environment Profile(D-051)/路径越界与 deny-list 防护；端口统一(后端8000单一事实源)/容器与OAuth缺陷修复 |
| **不能证明什么** | 引导向导本体(5步模态+接P0)→R9；执行模式三模式真实策略(GAT-04)→R9；真实Agent对话(WS-06)→R9；D-078 coding_agent_ref字段+选择器→R8-5收尾/R9；LangGraph编排+Run/Gate/AET DB持久化→R9-R12 |
| **是否可复现** | 是（uv run pytest；npm run build；uvicorn :8000 + curl env/file/execute；EXECUTION_MODE=container 真跑；Playwright 脚本可复跑） |
| **验收状态** | **accepted**（用户裁决 2026-06-27；R8-4 独立验收 rework_required → 修复轮闭合 P0-11/P0-14 + 引导地基 WP-A/WP-B 补建后收口） |

---

## 9. R9 P0-P1 最小真实链路 验收证据

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R9-01 |
| **证据类型** | independent-review + pytest + build + REST API(live) + Playwright + filesystem + trace/audit + key-leak-scan |
| **关联功能ID** | PRJ-01/04/05/06, GAT-01/04, WS-04(GateBanner) |
| **关联施工记录ID** | REC-R9-01 |
| **证据路径** | 交接/当前/R9-4-P0-P1最小真实链路独立验收报告.md（rework_required 裁决+最小返工清单）, 交接/当前/R9-3F-返工与二次自验报告.md, backend/tests/test_r9_chain.py |
| **证据摘要** | R9-4 独立验收实测裁决 **rework_required**（6 P0：profiler SyntaxError/stage uuid 500/Gate 状态不一致/coding-agents 500/ReviewPass 未接线/decide 不闭环）。R9-3F 返工后二次自验：后端 **pytest 182 passed, 2 skipped, 0 failed**；前端 **build exit 0**；实跑服务真实 API：onboarding→Run+intake+P0Gate(waiting_decision)、promotion-decision approve→current_stage=p1+active_gate清除、非法决策→400、/profile→12 项产出+P2 manifest+Review passed、materialize git→deferred/manual→empty(诚实)、三模式 manual→require_confirmation/auto→auto_approved 执行；Playwright：OnboardingWizard 渲染、**GateBanner 渲染并点击批准后横幅消失/chip 转无待决 Gate**、**0 console error** |
| **证明了什么** | P0 接入 + P1 建档最小真实链路端到端可用：引导→Run/Artifact/Evidence→P0→P1 Gate→真实决策推进；14 项全量识别真实+脱敏+P2 输入包；Review Pass 真实接线(execute→review→retry≤2→escalate)；三模式真实行为差异+Auto 授权代理标准字段+L4 不代理；materialization 诚实标记；ZIP/local_dir 安全加固；关键链路有测试覆盖(防绿色掩盖断链) |
| **不能证明什么** | git/github 真实 clone(待凭据，deferred)；真实 AI 编码 Agent 调用(归 R11)；LangGraph 真实编排(graph_status 仍 mock 占位)；Gate/Run DB 持久化(当前内存，重启失)；Plan 模式完整计划编排与 effective_mode 不可变历史 |
| **是否可复现** | 是（uv run pytest -q；npm run build；EXECUTION_MODE=local uvicorn :PORT + curl 链路；VITE_BACKEND_TARGET 指向后端跑 Playwright） |
| **验收状态** | **rework_required→已修复二次自验通过（待用户最终裁决 accepted）** |

---

## 10. R9-5 + R9-6 P0-P1 真实化专项修补 验收证据

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R9-5-01 |
| **证据类型** | independent-review + pytest + G10 Playwright(S1-S4) + live HTTP + DB/EvidenceStore 查询 + 后端重启恢复 |
| **关联功能ID** | R96-WP1~WP7, ORCH-06~09, ROUTE-W9, TRUTH-01~08 |
| **关联施工记录ID** | REC-R9-5-1~REC-R9-5-8, REC-R9-6-CLOSE |
| **证据路径** | 交接/当前/R9-5-真实收口报告.md（R9-5 独立验收 ◐）, 交接/当前/R9-6-施工自验报告.md（R9-6 施工自验）, frontend/e2e/r96-s1-ideal.mjs~r96-s4-persist.mjs |
| **证据摘要** | **R9-5 独立验收（亲手驱动平台）**：P0 理想路径真实跑通（产物落盘+Run/Gate入库+P0→P1晋级）；发现 F-1~F-8 运行期缺陷（git卡死/Evidence空/幂等缺失/envelope污染/静默except/图不走/G10缺失/未入Git）。**R9-6 施工自验**：WP-1~7 全部完成；Playwright G10 金标准：S1 8/8 PASS（graph_driven=true+checkpoint_ref+evidence 2条+p1晋级），S2 7/7 PASS（source_pending+retry_action+reason非空），S3 9/9 PASS（幂等+400+409），S4 10/10 PASS（重启后 current_stage/evidence/checkpoint_ref 全恢复）；**pytest 407 passed, 2 skipped, 0 failed**（120s）；全部 0 console error；Git commit a70ebb3（156 files, 19831 ins） |
| **证明了什么** | P0-P1 真实链路最终收口：LangGraph 接管执行（graph_driven=true），Evidence 真实落可查询存储，Gate 幂等+409，git 失败→source_pending gate（有 reason 和 retry_action），静默 except 消灭，持久化跨重启恢复，G10 金标准 4 场景全通 |
| **不能证明什么** | W8 旧 onboarding 入口委托 FlowRuntime（延 R10，STOP-3）；git/github 真实 clone（待凭据，deferred）；真实 AI 编码 Agent 调用（归 R11）；T5 astream 域事件（延 R10）；T8 广面静默 except 余下降级类 |
| **是否可复现** | 是（`DATABASE_URL=sqlite:////tmp/rebuild_test_g10.db ... uv run uvicorn app.main:app --port 8010`；`node frontend/e2e/r96-s{1,2,3,4}.mjs`；`uv run pytest tests/ -x -q`） |
| **验收状态** | **R9-6 施工自验完成（407/2/0）；待独立验收 Agent 裁决 R9-5+R9-6 联合 accepted** |

---

## 11. R10-4 P2-P3 评估规划链路 独立验收证据

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R10-4-01 |
| **证据类型** | independent-review（D-082，未参与 R10-3 施工）+ pytest + npm build + 真实 git clone + 真实 LLM(P2) + Playwright 浏览器 + 代码级 grep + DB/产物/Evidence/Trace 查询 + 后端重启恢复 |
| **关联施工记录ID** | REC-R10-3（T0-T21 自验，参考不直采） |
| **证据路径** | 交接/当前/R10-4-P2-P3评估规划链路独立验收报告.md, 交接/当前/R10-5-修复计划.md, frontend/e2e/r104_acc_browser.mjs, frontend/e2e/r104_ws.mjs, /tmp/r104_shots/ |
| **证据摘要** | 独立隔离环境(临时DB/WS/DATA，真实LLM Key)。**pytest 511/2/0**（独立重跑，与自验一致）；**build EXIT0**。真实 git clone(file://Oracle+WebLogic样例)→source/真实源码+source_index✅。P0/P1真实(tech_stack Java/JSP/maven+12识别项+24 artifact_refs Gate附审)✅。**P2 真实 LLM(deepseek-v4-flash 经ModelGateway)产出有意义六类产物**(risk L2-L4含source/basis+blocker+gaps+resource, overall L4, analysis_only)+**5 Evidence 落库可追溯**+P2→P3 Gate✅。重启持久化(current_stage=p2/Evidence/assessment 恢复)✅。幂等✅。安全无泄露✅。代码级：register_handler p0-p3真实/P4 stub/ORM分表/无绕过ModelGateway/无自建StateGraph/8维边策略/必生TaskGraph 全✅。 |
| **发现问题** | **P1-A** `/workspace` 聚合 500（EvidenceGapResponse gap_id/evidence_type vs list_evidence_gaps evidence_id 字段不一致→真实源项目过P1后工作区离线降级）；**P1-B** `/promotion-decision` 非法decision返回200+创建重复P0 Gate(S6.5违反,与/gates/{id}/decision 400不一致)；**P1-C** P2/P3 handler未调 context_assembler、未用Skill(S4未满足；T7仅孤立测试未接线)；P2-A schemas/taskgraph.py mock死值+docstring不符；P2-B 静默except 44处vs称38；P2-C Gate graph_capability_status死值"not_connected"。 |
| **不能证明什么** | P3 真实产物运行时受限未完整验证（DeepSeek端侧延迟致P3三段LLM链超窗未落库，非P3代码缺陷；必生逻辑代码已验）——须R10-5真实LLM复跑补齐；P3→P4 Gate（依赖P3 completed）；真实远程凭据clone（file://已验，远程https连通性已探测）。 |
| **是否可复现** | 是（隔离DB拷贝+REBUILD_WORKSPACE_DIR/DATA_DIR于/tmp；git file://样例；node frontend/e2e/r104_*.mjs；真实LLM需DeepSeek可达） |
| **验收状态** | **rework_required（返工）** — 无P0阻断(P2/P3非stub非假实现)；3 P1需修复后独立复验；详见 R10-4 报告 + R10-5 修复计划 |

---

## 12. R10-6 二轮独立验收（R10-5 修复结果）证据

| 字段 | 内容 |
|------|------|
| **证据ID** | EVD-R10-6-01 |
| **证据类型** | independent-review（D-082，未参与 R10-5 修复）+ pytest + build + 真实 git clone + 真实 LLM P2/P3 + Playwright 浏览器 + 代码级 + DB 查询 |
| **关联** | R10-5 修复施工（REC-R10-5）；上轮 R10-4（EVD-R10-4-01, rework_required） |
| **证据路径** | 交接/当前/R10-6-二轮独立验收报告.md, 交接/当前/R10-4-*.md, 交接/当前/R10-5-修复计划.md, frontend/e2e/r104_ws.mjs, /tmp/r104_shots/ |
| **证据摘要** | **pytest 517/2/0**（独立重跑, +6 回归测试）；build✅。**P1-A✅**(/workspace 200, 浏览器0 console error, gap schema对齐)；**P1-B✅**(非法decision→400+gate不重复)；**P1-C✅**(P2/P3 execute 调 assemble_context, Evidence context_refs=C0-C6+skill_refs 8个, 渐进披露 metadata 仅注入 name+description, skill not_connected 发声)；**P2-A✅**(taskgraph.py删+无残留importer)；**P2-B✅**(44处静默except台账登记B-DEBT-SILENT-EXCEPT)；**P2-C✅**(gate graph_capability_status=live)。核心真实链路无回归：真实clone/P0/P1/真实LLM P2六类产物+5Evidence/真实LLM P3 StagePlan(sp-864a23d1 meaningful)+9 TaskPlan/重启持久化/P4 stub。 |
| **不能证明什么** | P3 真实-LLM **TaskGraph 必生落库 + P3→P4 Gate** 运行时证据未取得——第3次LLM调用(TaskGraph边)DeepSeek端侧连续超时(3轮retry timeout)→按公理3 failed(未伪造)，current_stage停p2未越权。**环境限制非代码缺陷**；TaskGraph必生由 test_graph_p3/test_p3_planning(517绿)+代码覆盖。OBS-1：隔离环境Skill为测试占位(Test P-Skill 全not_connected)，P1-C机制正确但注入占位元数据。 |
| **是否可复现** | 是（隔离DB拷贝+/tmp WS/DATA；git file://样例；node e2e/r104_ws.mjs；真实P3需DeepSeek端侧稳定） |
| **验收状态** | **accepted（通过，2026-07-02）** — R10-4 全部P1/P2缺陷修复验证；条件(a)已由真实LLM完整跑通P3满足：StagePlan(sp-3d97b60a)+9TaskPlan+TaskGraph必生(tg-e67b570b,9节点8边,degraded=False,8维边策略显式,高风险Gate红线)+P3→P4 Gate+P4 stub。根因glm-5.2死源默认profile已改可达模型(OBS-2)。详见 R10-6 §13。 |
