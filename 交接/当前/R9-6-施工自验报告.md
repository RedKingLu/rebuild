# R9-6 施工自验报告

> 文件路径：`交接/当前/R9-6-施工自验报告.md`
> 文件身份：**施工自验报告**（施工 Agent 出具，非独立验收；待独立验收 Agent 最终裁决）
> 施工 Agent：Claude (sonnet-4-6)，R9-6 施工 Agent（跨两个 context 窗口，context 摘要接续）
> 施工日期：2026-06-29
> 上游依据：`交接/已完成/R9/R9-6-施工-交接提示词.md`、`R9-5-真实收口报告.md`、`R9-6-规划报告.md`
> 铁律声明：本报告为施工自验，不可自行标 accepted；需独立验收 Agent 按 D-082 亲手驱动平台复核后裁决。

---

## 0. 身份与方法声明

本人为 R9-6 施工 Agent（sonnet-4-6），跨两个 context 窗口完成 WP-1~WP-8 全部工作包。
自验方法：

1. 亲手启动后端（独立测试 DB `sqlite:////tmp/rebuild_test_g10.db`），真实 HTTP 调用
2. G10 金标准 Playwright 4 场景全部以 `node r96-s{1,2,3,4}.mjs` 驱动真实跑通
3. `uv run pytest tests/ -x -q` 全量基线确认
4. 逐 WP 核查真实落点（DB 行 / Evidence 存储 / Gate 字段 / console error）

**安全红线**：全程未输出/写入任何 Key/Token/Secret/密码明文，测试使用独立隔离 DB。

---

## 1. WP 完成情况

| WP | 对应缺口 | 完成状态 | 核心改动 |
|----|---------|---------|---------|
| WP-1 | F-1 git 失败分流 | ✅ | `nodes.py` empty_source→source_pending gate_type；`gate_backend.py` reason+retry_action；`routes_projects.py` SSE retry_action |
| WP-2 | F-2 Evidence 落库 | ✅ | `core/evidence_store.py` 新建 SQLite+aiosqlite；execute P0 写真实 Evidence；GET /evidence 真实查询 |
| WP-3 | F-3 P0 幂等 | ✅ | active gate 短路 idempotent；阶段>p0 → 409；invalid decision → 400 |
| WP-4 | F-4 envelope 真实性 | ✅ | SSE complete 含 graph_driven/graph_capability_status/checkpoint_ref 真实字段；去 Meta() 默认 mock |
| WP-5 | F-6 静默 except | ✅ | gate_backend/source_materializer/knowledge_search/opencode_acp/main 10 处→log+raise/显式降级 |
| WP-6 | F-5 运行实例切 LangGraph | ✅ | `state.py`+source_type/source_config GraphState 字段；`routes_stages.py` decide_promotion 重写（图驱动+partial exec 检测）；graph_driven=true 实证 |
| WP-7 | F-7 G10 金标准 | ✅ | 4 场景 Playwright 全绿（S1 8/8, S2 7/7, S3 9/9, S4 10/10 含重启），全部 0 console error |
| WP-8 | F-8/F-9 收口 | ✅ | Git commit a70ebb3；追踪五件更新；交接/产物归档；本报告出具 |

---

## 2. G10 金标准 Playwright 真实验证结果

> 后端：`DATABASE_URL=sqlite:////tmp/rebuild_test_g10.db DATA_DIR=/tmp/rebuild_g10_data WORKSPACE_DIR=/tmp/rebuild_g10_workspace uvicorn app.main:app --port 8010`
> 前端：`vite preview --port 5173`（代理 → 8000 同测试 DB）

### 场景一：本地理想路径（r96-s1-ideal.mjs）8/8 PASS

| 步骤 | 验证点 | 结果 |
|-----|--------|------|
| 1 | 前端首页加载 0 console error | PASS |
| 2 | Manual 项目创建 | PASS |
| 3 | onboarding/complete → run_id | PASS |
| 4 | execute SSE: graph_driven=true, checkpoint_ref=run_id | PASS |
| 5 | Gate DB: checkpoint_ref=run_id, type=stage_promotion | PASS |
| 6 | Evidence 落库: 2 条（WP-2） | PASS |
| 7 | Gate 批准 graph_driven=true → current_stage=p1（WP-6 + WP-3） | PASS |
| 8 | 前端渲染 0 console error | PASS |

### 场景二：Git 失败路径（r96-s2-git-fail.mjs）7/7 PASS

| 步骤 | 验证点 | 结果 |
|-----|--------|------|
| 1 | 前端首页 0 console error | PASS |
| 2 | Git 项目创建（不可达 URL） | PASS |
| 3 | complete → run_id（不卡死） | PASS |
| 4 | execute SSE: source_pending + retry_action（WP-1 DoD） | PASS |
| 5 | Gate DB: gate_type=source_pending, reason 非空 | PASS |
| 6 | Manual 对照组 stage_promotion 不受影响 | PASS |
| 7 | 0 console error | PASS |

Gate reason 实际值（脱敏节选）：`"p0 源码导入失败：源码目录为空。请补充源码凭据或切换为手动导入后重新执行 [retry_action=update_source_config]"`

### 场景三：负路径集（r96-s3-negative.mjs）9/9 PASS

| 步骤 | 验证点 | 结果 |
|-----|--------|------|
| 1 | 前端加载 | PASS |
| 2 | 首次 execute → gate_id | PASS |
| 3 | 重复 execute → idempotent=true, gate_id 相同（WP-3） | PASS |
| 4 | DB 只有 1 waiting_decision P0 Gate | PASS |
| 5 | 非法 decision `frobnicate` → HTTP 400 | PASS |
| 6 | 非法 decision 后 Gate 仍 waiting_decision | PASS |
| 7 | Gate 批准 → current_stage=p1 | PASS |
| 8 | 阶段>p0 时 execute → 409（WP-3 阻断） | PASS |
| 9 | 0 console error | PASS |

### 场景四：持久性验证（r96-s4-persist.mjs）10/10 PASS（含重启）

| 步骤 | 验证点 | 结果 |
|-----|--------|------|
| 1 | 前端加载 | PASS |
| 2 | 项目创建 + run_id | PASS |
| 3 | execute: gate_id + checkpoint_ref=run_id | PASS |
| 4 | Evidence 基准: 2 条（WP-2） | PASS |
| 5 | P0 Gate 批准 → current_stage=p1 | PASS |
| 6 | 后端 SIGTERM → 重启成功（25s 内恢复） | PASS |
| 7 | 重启后 current_stage=p1（DB 持久化） | PASS |
| 8 | 重启后 Evidence 恢复: 2 条（WP-2 落库通过） | PASS |
| 9 | 重启后 Gate checkpoint_ref=run_id（WP-6 通过） | PASS |
| 10 | 前端刷新 0 console error | PASS |

---

## 3. pytest 基线

```
407 passed, 2 skipped, 0 failed  (119.55s)
```

（2 skipped = 有效 LLM key 依赖的真实调用，非代码缺陷；含全部 R9-5-1~R9-5-8 + R9-6 新增测试）

---

## 4. Git 提交

- commit: `a70ebb3`
- 范围: R8 收口后全部未入库代码（首次全量入库）
- 统计: 156 files changed, 19831 insertions(+), 1151 deletions(-)
- 关键新增: `backend/app/graph/`（完整 LangGraph 编排），`backend/app/core/evidence_store.py`，`frontend/e2e/r96-s{1,2,3,4}.mjs`

---

## 5. 已知残留（登记在案，非阻塞）

| 项 | 说明 | 归属 |
|----|------|------|
| W8 旧入口委托 FlowRuntime | onboarding/complete·execute 仍走旧内联路由，不委托 FlowRuntime.start；STOP-3 用户裁决延 R10 | R10 |
| T5 astream 域事件 | event_service 仅 snapshot+heartbeat（诚实），完整 astream 域事件延 R10（图成主驱动时） | R10 |
| git/github 真实 clone | 待用户提供凭据环境；source_type=git→deferred/source_pending gate | R10+ |
| 前端 ProjectCreatePage 接入 precheck | precheck 端点已建，前端接入待补 | R10 |

---

## 6. 独立验收 Agent 需核查项（D-082）

1. **S1 graph_driven=true**：`BACKEND_URL=http://localhost:8010 node frontend/e2e/r96-s1-ideal.mjs`，核查 execute SSE complete.graph_driven, checkpoint_ref, evidence 条数, Gate 批准后 current_stage
2. **S2 source_pending**：`node frontend/e2e/r96-s2-git-fail.mjs`，核查 Gate DB gate_type=source_pending + reason 非空
3. **S3 幂等**：`node frontend/e2e/r96-s3-negative.mjs`，核查 idempotent 返回 + DB Gate 唯一性 + 400/409
4. **S4 持久性**：`BACKEND_PID=<pid> BACKEND_START_CMD="..." node frontend/e2e/r96-s4-persist.mjs`，核查重启后恢复
5. **pytest**：`cd backend && uv run pytest tests/ -x -q`，期望 407+/2/-0

---

## 7. 施工自验结论

R9-6 全部 WP（WP-1~WP-8）施工完成，G10 金标准 4 场景 Playwright 全绿（34/34 PASS，0 console error），pytest 407/2/0，代码首次全量入 Git（commit a70ebb3）。

**本报告不能自行标 accepted**——需独立验收 Agent（未参与本期施工）亲手驱动平台复核后裁决。建议独立验收 Agent 至少跑 G10 S1+S4（核心路径+持久化），结合 pytest 基线，出具 R9-5 + R9-6 联合验收报告。
