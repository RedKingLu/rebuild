# R9-5 + R9-6 联合验收报告

> 文件路径：`交接/当前/R9-5+R9-6-联合验收报告.md`
> 文件身份：**独立验收报告**（D-082 合规：本 Agent 未参与 R9-6 任何施工）
> 验收 Agent：Claude (sonnet-4-6)，独立验收 Agent
> 验收日期：2026-06-29
> 验收对象：R9-5（历史缺陷 F-1~F-9）+ R9-6（WP-1~WP-8 专项修复 + G10 金标准）
> 铁律：施工 Agent 自验结论仅作参考，本报告全程亲手驱动平台取证。

---

## 0. 验收身份与方法声明（D-082 合规）

本验收 Agent 未参与 R9-6 任何子期施工（WP-1~WP-8）。本报告基于以下方法出具：

1. **亲手启动独立后端实例**（端口 8020，独立测试 DB `sqlite:////tmp/rb_acc_v2_1782726652.db`）
2. **真实 HTTP 调用**驱动 F-1~F-6 全部修复项
3. **独立重跑 G10 金标准 Playwright 4 场景**（使用端口 8020 独立后端）
4. **独立重跑 pytest 全量基线**
5. **代码级 grep + AST 分析**（F-6 静默 except 审计）

安全红线：全程未输出/写入任何 Key/Token/Secret/密码明文，测试使用独立隔离 DB。

> 上游依据：`R9-5-真实收口报告.md`（F-1~F-9 缺陷定义）、`R9-6-施工自验报告.md`（施工 Agent 自验，参考不直采）、`R9-中立重设计与验证规范-v0.1.md`（V1-V6 验收框架）

---

## 1. 独立后端启动实证

| 项 | 实测值 |
|----|--------|
| 启动命令 | `REBUILD_DATABASE_URL=sqlite:////tmp/rb_acc_v2_*.db uv run uvicorn app.main:app --port 8020` |
| 进程 cwd | `/proc/86540/cwd -> /home/king/rebuild/backend`（确认运行当前源码） |
| 健康检查 | `GET /api/health → {"status":"ok","platform":"rebuild","version":"V26.1.1"}` |
| 测试 DB 表 | `['credential','project','run','p_gate','call_log',...]`（14 表，独立初始化） |
| Git commit | `657250d`（最新）/ `a70ebb3`（R9 收口全量入库） |

---

## 2. pytest 独立运行结果

```
407 passed, 2 skipped, 0 failed   (134.09s)
```

- 2 skipped = 有效 LLM API Key 依赖的真实调用，非代码缺陷
- 0 failed：基线完全通过
- 施工自验声明 407/2/0，**与独立重跑结果一致**

---

## 3. F-1~F-6 修复状态（逐项亲手驱动验证）

### F-1：git 失败分流（WP-1）

| 核查项 | 实测结果 |
|--------|---------|
| 创建 `source_type=git` 不可达仓库 | `workspace_status=blocked`（正确标异常） |
| onboarding/complete 响应 | `materialized.success=false`，run_id 正常返回 |
| onboarding/execute SSE | `event: complete`，`gate_type=source_pending`，`graph_driven=true` |
| SSE `retry_action` | `update_source_config`（非空，有具体引导动作） |
| SSE `gate_reason` | `"p0 源码导入失败：源码目录为空。请补充源码凭据或切换为手动导入后重新执行 [retry_action=update_source_config]"` |
| DB `p_gate` 行 | `gate_type=source_pending, gate_status=waiting_decision, reason 非空, checkpoint_ref=run_id` |
| **旧 F-1 症状** | "event:error + issues=[]" 卡死：**未出现** |

**判定：✅ F-1 修复**

与施工自验的差异：无差异，均 PASS。

---

### F-2：Evidence 落库（WP-2）

| 核查项 | 实测结果 |
|--------|---------|
| 项目类型 | `source_type=manual` |
| onboarding/complete evidence_candidates | 2 条（`ev-p0-ws-*`, `ev-p0-onb-*`）|
| onboarding/execute SSE | `event: complete` 正常 |
| `GET /api/projects/{id}/evidence` 响应 | `{"data":{"evidence":[{id,type,status},{id,type,status}]}}` |
| Evidence 条数 | **2 条**（`workspace_initialized` + `onboarding_completed`）|
| Evidence 字段完整性 | 含 `evidence_id/type/status/source/claim/stage/created_at` |
| **旧 F-2 症状** | `{"evidence":[]}` 空列表：**未出现** |

**判定：✅ F-2 修复**

---

### F-3：P0 幂等（WP-3）

| 核查项 | 实测结果 |
|--------|---------|
| 首次 execute → gate_id | `gate-ae5f27`，`gate_type=stage_promotion` |
| **重复 execute** SSE 响应 | `{"message":"P0 Gate 已存在，无需重复执行。","gate_id":"gate-ae5f27","idempotent":true}` |
| DB Gate 数量（重复后） | **1 条**（无重复 Gate） |
| 非法 decision `frobnicate` | HTTP 400（返回错误，Gate 状态不变） |
| Gate 批准 → current_stage | `p1` |
| **对 p1 项目再次 execute** | HTTP 409：`"项目当前阶段已是 p1，不能重新执行 P0 onboarding"` |
| **旧 F-3 症状** | 重复造 Gate / P1 项目重建 P0 Gate：**未出现** |

**判定：✅ F-3 修复**

---

### F-4：envelope 真实性（WP-4）

| 核查项 | 实测结果 |
|--------|---------|
| `GET /api/projects` meta.source_status | `real` |
| `GET /api/projects` meta.persistence | `durable` |
| `GET /api/projects` meta.capability_status | `available` |
| `GET /api/projects` meta.not_connected_reason | `""（空）` |
| 禁止字符串搜索 | `"R4 provides API skeleton"`: 未出现 |
| | `"mock"`: 未出现 |
| | `"volatile"`: 未出现 |
| | `"in-memory data"`: 未出现 |
| **旧 F-4 症状** | `Meta` 默认污染（capability_status:not_connected / persistence:volatile）：**未出现** |

**判定：✅ F-4 修复**

---

### F-5/WP-6：运行实例切 LangGraph（graph_driven 真实性）

| 核查项 | 实测结果 |
|--------|---------|
| SSE complete `graph_driven` | `true` |
| SSE complete `graph_capability_status` | `live` |
| SSE complete `checkpoint_ref` | `run-967bf3`（= run_id，非 null）|
| DB `p_gate.checkpoint_ref` | `run-967bf3`（非 null，DB 持久化）|
| SSE 事件序列 | `graph_start → node_start(LangGraph) → node_start(p0_work) → node_end(p0_work) → node_start(p0_gate) → node_end(LangGraph) → complete` |
| **旧 F-5 症状** | `graph_capability_status=not_connected, checkpoint_ref=null`：**未出现** |

**判定：✅ F-5/WP-6 修复**

---

### F-6：静默 except（WP-5）

WP-5 声明修复的 10 处目标位置（gate_service×3 / source_materializer×2 / knowledge_search×2 / opencode_acp×2 / main×1）：

| 文件 | 验收结果 |
|------|---------|
| `app/services/gate_service.py` | ✅ 无静默（所有 except 含 log+exc_info=True） |
| `app/services/source_materializer.py` | ✅ 无静默（所有 except 含 log 或 raise） |
| `app/services/knowledge_search.py` | ✅ 无静默 |
| `app/services/opencode_acp_client.py` | ✅ 无静默（_trace_permission / _audit_permission 降级已 log） |
| `app/main.py` | ✅ 无静默（两处 except 均含 logging.getLogger+exc_info=True） |

**WP-5 范围内：全部修复。**

**全库新增发现（非 WP-5 范围，P2 级别，登记备查）：**

发现 ~47 处 `except: pass` 分布于以下文件（部分有注释说明为 advisory/non-fatal，部分无注释）：

- `app/graph/gate_backend.py:60`（retry_action DB 更新失败 — non-fatal，但无 log）
- `app/graph/stage_handlers.py:65,144`（context assembly advisory — 有注释，可理解）
- `app/graph/checkpoint.py:75`
- `app/api/routes_stages.py:87,92,122,138`
- `app/services/aet_service.py:46,108,127,138`
- `app/services/mcp_service.py:173,179,314,337`
- `app/services/execution_provider.py:263,273`
- `app/core/audit_writer.py:71,107,109`，`app/core/trace_writer.py:69,125,127`
- 其他：routes_workspace, routes_gates, routes_projects, context_assembler, full_stack_profiler 等

这些均不在 R9-6 WP-5 声明的修复范围内，属于平台历史债务，建议 R10 专项清理。**不阻断本次验收。**

**判定：◐ F-6 WP-5 范围内修复；全库仍有 ~47 处静默 except（P2，R10 建议清理）**

---

## 4. G10 Playwright 4 场景结果（独立运行）

> 后端：端口 8020（独立测试 DB，cwd=backend）
> 前端：vite preview（端口 5173，已运行）
> 环境变量：`BACKEND_URL=http://localhost:8020`

### 场景一：本地理想路径（r96-s1-ideal.mjs）— 8/8 PASS

| 步骤 | 验证点 | 独立实测 |
|-----|--------|---------|
| 1 | 前端首页 0 console error | PASS |
| 2 | Manual 项目创建 | PASS |
| 3 | onboarding/complete → run_id | PASS |
| 4 | SSE: graph_driven=true, checkpoint_ref=run_id | PASS |
| 5 | Gate DB: checkpoint_ref=run_id, type=stage_promotion | PASS |
| 6 | Evidence 落库: 2 条 | PASS |
| 7 | Gate 批准 graph_driven=true → current_stage=p1 | PASS |
| 8 | 前端渲染 0 console error | PASS |

```
总计: 8 步 | 通过: 8 | 失败: 0 | Console errors: 0
```

### 场景二：Git 失败路径（r96-s2-git-fail.mjs）— 7/7 PASS

| 步骤 | 验证点 | 独立实测 |
|-----|--------|---------|
| 1 | 前端首页 0 console error | PASS |
| 2 | Git 项目创建（不可达 URL） | PASS |
| 3 | complete → run_id（不卡死） | PASS |
| 4 | SSE: source_pending + retry_action（WP-1 DoD） | PASS |
| 5 | Gate DB: gate_type=source_pending, reason 非空 | PASS |
| 6 | Manual 对照组 stage_promotion 不受影响 | PASS |
| 7 | 0 console error | PASS |

```
总计: 7 步 | 通过: 7 | 失败: 0 | Console errors: 0
```

实测 Gate reason（脱敏）：`"p0 源码导入失败：源码目录为空。请补充源码凭据或切换为手动导入后重新执行 [retry_action=update_source_config]"`

### 场景三：负路径集（r96-s3-negative.mjs）— 9/9 PASS

| 步骤 | 验证点 | 独立实测 |
|-----|--------|---------|
| 1 | 前端加载 | PASS |
| 2 | 首次 execute → gate_id | PASS |
| 3 | 重复 execute → idempotent=true, gate_id 相同（WP-3） | PASS |
| 4 | DB 只有 1 waiting_decision P0 Gate | PASS |
| 5 | 非法 decision → HTTP 400 | PASS |
| 6 | 非法 decision 后 Gate 仍 waiting_decision | PASS |
| 7 | Gate 批准 → current_stage=p1 | PASS |
| 8 | 阶段>p0 时 execute → 409 | PASS |
| 9 | 0 console error | PASS |

```
总计: 9 步 | 通过: 9 | 失败: 0 | Console errors: 0
```

### 场景四：持久性验证（r96-s4-persist.mjs）— 10/10 PASS（含后端重启）

| 步骤 | 验证点 | 独立实测 |
|-----|--------|---------|
| 1 | 前端加载 | PASS |
| 2 | 项目创建 + run_id | PASS |
| 3 | execute: gate_id + checkpoint_ref=run_id | PASS |
| 4 | Evidence 基准: 2 条 | PASS |
| 5 | P0 Gate 批准 → current_stage=p1 | PASS |
| 6 | 后端 SIGTERM → 重启成功 | PASS |
| 7 | 重启后 current_stage=p1（DB 持久化） | PASS |
| 8 | 重启后 Evidence 恢复: 2 条 | PASS |
| 9 | 重启后 Gate checkpoint_ref 恢复 | PASS |
| 10 | 前端刷新 0 console error | PASS |

```
总计: 10 步 | 通过: 10 | 跳过: 0 | 失败: 0 | Console errors: 0
```

**G10 总计：34/34 PASS，0 console error（与施工自验 34/34 结果一致，独立确认）**

---

## 5. V3 持久化验证

V3 持久化已在 G10 场景四中全量验证（步骤 7~9）：

| 验证项 | 实测 |
|--------|------|
| P0 → P1 Gate/Evidence/stage 入 DB | ✅ PASS（G10 S1 步骤 5~7 + S4 步骤 3~5） |
| SIGTERM 后端进程 → 重启 | ✅ PASS（G10 S4 步骤 6） |
| 重启后 `current_stage` 恢复 | ✅ `p1`（G10 S4 步骤 7） |
| 重启后 Evidence 条数恢复 | ✅ 2 条（G10 S4 步骤 8） |
| 重启后 Gate `checkpoint_ref` 恢复 | ✅ `run_id` 不变（G10 S4 步骤 9） |

---

## 6. 独立验收 vs 施工自验差异汇总

| 项 | 施工自验（参考） | 独立实测 | 差异 |
|----|----------------|---------|------|
| pytest | 407/2/0 (119.55s) | 407/2/0 (134.09s) | 无差异，均通过 |
| G10 S1 | 8/8 PASS | 8/8 PASS | 无差异 |
| G10 S2 | 7/7 PASS | 7/7 PASS | 无差异 |
| G10 S3 | 9/9 PASS | 9/9 PASS | 无差异 |
| G10 S4 | 10/10 PASS | 10/10 PASS | 无差异 |
| F-6 全库 | "10 处→log+raise/显式降级" | WP-5 5 文件✅；全库仍有 ~47 处 pass | **差异**：WP-5 原始目标达成，但全库存在 R9-5 报告未发现的其他静默 except（历史债务，P2） |
| 独立 DB 测试 | 使用 8010 端口 | 使用 8020 端口 | 端口不同，独立实例 |

---

## 7. 总判定

### R9-6 判定：✅ **accepted**

R9-6 WP-1~WP-8 全量施工成果通过独立验收：

- **F-1（git 失败分流）**：✅ 修复 — SSE 输出 `gate_type=source_pending + gate_reason 非空 + retry_action`，DB Gate 正确落库，旧卡死症状消除
- **F-2（Evidence 落库）**：✅ 修复 — `GET /evidence` 返回 2 条真实 Evidence（含 id/type/status），非空列表
- **F-3（P0 幂等）**：✅ 修复 — 重复 execute 返回 `idempotent=true + 同 gate_id`，P1 项目 execute 返回 409，DB 无重复 Gate
- **F-4（envelope 真实性）**：✅ 修复 — meta 字段全真实（real/durable/available），无 mock/volatile/R4 骨架文案
- **F-5/WP-6（graph_driven）**：✅ 修复 — SSE complete 含 `graph_driven=true + checkpoint_ref=run_id`，DB Gate `checkpoint_ref` 非 null
- **F-6（WP-5 范围内）**：✅ 修复 — 5 个目标文件全部无静默 except（AST 级验证）
- **G10 金标准**：✅ 34/34 PASS，0 console error（独立 Playwright 运行，含后端重启）
- **Git 收口**：✅ 代码入库（commit a70ebb3 + 657250d）
- **pytest 基线**：✅ 407/2/0

### R9-5 + R9-6 联合判定：✅ **accepted**（Q-R96-2 用户裁决落实）

R9-5 有条件通过时（上轮验收，2026-06-29）登记的 F-1~F-9 缺陷，已由 R9-6 全部修复并通过独立验收：

| 缺陷 | R9-5 状态 | R9-6 修复结果 |
|------|----------|-------------|
| F-1 git 卡死 | ❌ | ✅ 修复 |
| F-2 Evidence 假数据 | ❌ | ✅ 修复 |
| F-3 幂等缺失 | ❌ | ✅ 修复 |
| F-4 envelope 污染 | ❌ | ✅ 修复 |
| F-5 运行实例 G1（W8 延 R10） | ❌（W8 用户裁决延 R10）| ✅ WP-6 真实 graph_driven；W8 旧内联路由→R10（R9-6 已在 onboarding/execute 直接走图，完成核心真实化） |
| F-6 静默 except（WP-5 范围） | ❌ | ✅ WP-5 目标文件全部修复 |
| F-7 G10 缺失 | ❌ | ✅ 4 场景 Playwright 全绿 34/34 |
| F-8 代码未入 Git | ❌ | ✅ commit a70ebb3 全量入库 |
| F-9 交接材料 stale | ❌ | ✅ WP-8 收口归档 |

**R9（P0-P1 最小真实链路）= accepted**

---

## 8. 已知残留（登记在案，非阻断）

| 项 | 说明 | 归属 |
|----|------|------|
| W8 旧入口委托 FlowRuntime | onboarding/execute 已直接走 LangGraph 图（WP-6 真实化），W8 委托 FlowRuntime.start 架构统一延 R10 | R10 |
| T5 astream 域事件 | event_service 仅 snapshot+heartbeat，完整 astream 域事件 R10 | R10 |
| git/github 真实 clone | source_pending gate 路径已通；真实凭据 clone 待用户补凭据测试 | R10+ |
| 前端 ProjectCreatePage precheck | precheck 端点已建，前端接入待补 | R10 |
| 全库静默 except ~47 处 | WP-5 范围外历史债务（audit_writer/trace_writer/mcp_service 等），建议 R10 专项清理 | R10 建议 |

---

## 9. 收口动作确认

| 动作 | 状态 |
|------|------|
| ① Git 代码入库（R8 后全量） | ✅ commit a70ebb3 (R9) + 657250d (WP-8) |
| ② 独立验收报告出具 | ✅ 本报告 |
| ③ R9-5 + R9-6 一并标 accepted | ✅ 本报告 §7 |
| ④ 交接材料归档（当前 → 已完成/R9） | ⏳ 待用户确认后归档 |
| ⑤ 追踪文件更新 | ⏳ 待 R10 规划接手更新 |

---

> **本报告结束。** 独立验收，亲手驱动取证（HTTP + Playwright + DB + pytest），施工自验未直接采信。
> D-082 合规 ✅ | pytest 407/2/0 ✅ | G10 34/34 PASS ✅ | F-1~F-6 修复确认 ✅
> **R9-5 + R9-6 联合判定：accepted | R9（P0-P1 最小真实链路）= accepted**
