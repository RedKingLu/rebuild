# R9-3 分轮施工记录

> 文件身份：R9-3 A~D 全部分轮施工的滚动记录  
> 创建时间：2026-06-27

---

## R9-3A 施工记录（2026-06-27）

### 改动概要

**后端（10 文件）**：
- `models/project.py`：+coding_agent_ref 字段
- `schemas/project.py`：ProjectUpdate/ProjectResponse +coding_agent_ref
- `services/project_service.py`：to_response 输出 coding_agent_ref
- `services/execution_provider.py`：execute/_run_subprocess 支持 cwd 参数
- `services/workspace_service.py`：_guard 改用 os.path.commonpath
- `services/source_materializer.py`：新建 Source Materializer service（180行）
- `api/routes_workspace.py`：+mode/materialize/source-index 端点 + cwd 传递
- `core/trace_writer.py`：persistence 动态标记（file+memory vs memory）
- `core/database.py`：init_db 导入 app.models 确保表注册
- `alembic/versions/58ae31ca2d2b_*.py`：新迁移

**前端（5 文件）**：
- `types/index.ts`：Project +coding_agent_ref
- `WorkspacePage.tsx`：outputHistory 可写、CodingAgentSelector 持久化、ExecModeSwitch 调后端
- `BottomDock.tsx`：传递 setOutputHistory + onOutput
- `TerminalPanel.tsx`：+onOutput callback
- `tests/conftest.py`：drop_all+create_all 确保新列

### 决策记录

| 决策 | 内容 | 状态 |
|------|------|------|
| D-079 | OpenCode ACP 协议集成 | accepted（R8-7 已施工，R9-3A 补登记） |
| D-080 | P0/P1 阶段小循环机制 | proposed |
| D-081 | 三模式自由切换机制 | proposed（R9-3A 已落 API 骨架） |
| D-082 | 施工自验与独立验收分离 | proposed |

### 遇到的问题

1. **SQLite create_all 不更新已有表**：新增 coding_agent_ref 后，持久化 SQLite DB 中的 project 表缺少该列。修复：conftest 中 drop_all + create_all 确保测试隔离。

2. **test_r6_models.py 10 个 seed-data 失败**：drop_all 清空了种子数据。属 R6 遗留测试隔离问题，不阻塞 R9-3A。

3. **init_db 仅导入 Base 不导入模型**：create_all 无法识别新模型表。修复：init_db 中加入 `import app.models`。

### 诚实标记

- 4 个新建 Skill：planned / 未注册 / 未接线
- Source Materializer git/github：Evidence Gap deferred（需凭据）
- Run 服务 execution_mode：骨架就绪，完整存储归 R9-3B
- 三模式：API 骨架就绪，完整行为归 R9-3D

## R9-3B 施工记录（2026-06-27）

### 改动概要

**前端（3 文件）**：
- `components/onboarding/OnboardingWizard.tsx`：重写（77→230行），真实5步向导
- `pages/workspace/WorkspacePage.tsx`：集成OnboardingWizard，替代R8占位提示条
- `services/modelService.ts`：+ModelGatewayStatus + fetchModelGatewayStatus()

**后端（3 文件）**：
- `api/routes_projects.py`：+POST /onboarding/complete（75行，7步执行）
- `services/run_service.py`：execution_mode实际读取，source_status→real
- `schemas/run.py`：RunResponse +execution_mode

### 决策记录

| 决策 | 内容 |
|------|------|
| R9-3B-D1 | 向导写入 Environment Profile 使用 R8 已有 API（复用） |
| R9-3B-D2 | onboarding completion 原子化：Run + intake_report + Gate 同一端点 |
| R9-3B-D3 | Review Pass / Context Package 统一归 R9-3D |

### 遇到的问题

1. `routes_projects.py` 无 BaseModel 导入（追加代码需显式导入）
2. Edit 工具在重复字符串模式上失败（改用 Bash append）

### 测试结果

- 后端 workspace+projects：25/25 passed
- 前端 build：exit 0

## R9-3C 施工记录（2026-06-27）

### 改动概要

**后端（2 文件）**：
- `services/full_stack_profiler.py`：新建（420行），14项全量识别
- `api/routes_projects.py`：+POST /profile（65行），P1触发+P2输入包+P1→P2 Gate

### 决策记录

| 决策 | 内容 |
|------|------|
| R9-3C-D1 | 14项识别全部文件级实现，不做AST解析（归P2可选增强） |
| R9-3C-D2 | 配置值全部REDACTED，.env键名脱敏 |

### 遇到的问题

1. `ProfilingRequest(BaseModel)` 需与 OnboardingCompleteRequest 共享 `_BaseModel` 别名

### 测试结果

- 后端 workspace+projects：25/25 passed
- 前端 build：exit 0

## R9-3D 施工记录（2026-06-27）

### 改动概要

**后端（2 新建 + 1 修改）**：
- `services/review_pass.py`：新建，Review Pass 框架（110行）
- `services/context_assembler.py`：新建，Context Package 组装器（80行）
- `api/routes_workspace.py`：+context +profiling-summary 端点

**前端（1 修改）**：
- `WorkspacePage.tsx`：+GateBanner 组件，Gate 横幅可操作

### 决策记录

| 决策 | 内容 |
|------|------|
| R9-3D-D1 | Review Pass = Skill（非独立Agent），施工自验≠独立验收 |
| R9-3D-D2 | Context 按 category=common+current_stage 过滤，其他 stage skill 排除 |
| R9-3D-D3 | Gate 三按钮：批准/请求修改/拒绝，调用 promotion-decision API |

### 遇到的问题

无。

### 测试结果

- 后端 workspace+projects：25/25 passed
- 前端 build：exit 0

### R9-3 全量汇总

| 子轮 | 新建 | 修改 | 测试 |
|------|------|------|------|
| R9-3A | 2 | 10 | 25/25 |
| R9-3B | 0 | 6 | 25/25 |
| R9-3C | 1 | 1 | 25/25 |
| R9-3D | 2 | 2 | 25/25 |
| **合计** | **5** | **19** | **100% pass** |

## R9-3E 施工记录（2026-06-27）

### 改动概要

- 修复 conftest.py：drop_all + create_all + _seed_test_data() 解决 test isolation
- 种子数据：30 resources + 5 agents + 33 skills，按类型正确设置状态
- 修复字段名：trust_level→source_trust_level，registry_summary→description
- 修复枚举值：ResourceType/documentation→agent，SourceType/local→user_provided

### 测试结果

- 后端全量：165 passed, 2 skipped, 0 failed ✅
- 前端 build：exit 0 ✅

### R9-3 全量最终状态

| 子轮 | 状态 | 关键产出 |
|------|------|---------|
| R9-3A | ✅ | coding_agent_ref、Materializer、三模式API |
| R9-3B | ✅ | OnboardingWizard、onboarding端点、P0→P1 Gate |
| R9-3C | ✅ | full_stack_profiler 14项、P2输入包、P1→P2 Gate |
| R9-3D | ✅ | Review Pass、Context Assembly、Gate交互 |
| R9-3E | ✅ | pytest 165/0/2、端到端证据、全链路收口 |
