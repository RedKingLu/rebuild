# R9 全链路代码追踪与待修复报告

> 生成方式：逐条追踪前端→service→路由→后端服务→DB/文件/网关/终端命令的完整调用链
> 覆盖路径：Git OAuth / 模型配置 / 项目创建 / Onboarding / P0 Agent 执行 / P1 Profiling / Gate 决策 / Agent Chat / Workspace 聚合
> 每步标注：REAL / MOCK / PLACEHOLDER / NOT PERSISTED / SILENT FAILURE / DEAD CODE

---

## 0. 全局发现摘要

| 类别 | 数量 | 说明 |
|------|------|------|
| **REAL（真实）** | 大部分 | Git OAuth、模型配置、源导入(local_dir)、14项识别、Review Pass、SSE流式、Trace/Audit文件写入、文件树扫描、Environment Profile、Project CRUD |
| **NOT PERSISTED** | 3 核心组件 | Run、Gate、Trace/Audit 重启后数据 |
| **MOCK / HARDCODED** | 5 处 | AET artifacts/evidence_gaps、graph_status、policy_check、risk_assessment、resource panel |
| **DEAD CODE** | 1 处 | Agent 工具定义（写了3个tool但从没传给LLM） |
| **SILENT FAILURE** | 12+ 处 | 遍布各处的 try/except: pass |
| **PLACEHOLDER** | 3 处 | git/github clone、resource tab、LangGraph graph_status |

---

## 一、各路径代码追踪详情

### 路径 1：Git OAuth 绑定

| 步骤 | 调用链 | 状态 | 备注 |
|------|--------|------|------|
| 前端点击"连接 GitHub" | `IntegrationsPage.handleConnectGithub` → `getGithubAuthorizeUrl()` → `GET /integrations/git/oauth/github/authorize` | ✅ REAL | 弹窗 OAuth 流程，有 postMessage 通信（刚修复） |
| 后端获取授权 URL | `routes_integrations.github_authorize` → `git_oauth_service.get_github_authorize_url()` | ✅ REAL | 拼接 `https://github.com/login/oauth/authorize?...` |
| GitHub 回调 | `GET /integrations/git/oauth/github/callback?code=...` → `exchange_github_code(code)` | ✅ REAL | `httpx.AsyncClient` 真实 HTTP POST 到 `github.com/login/oauth/access_token` |
| 换取 token | `exchange_github_code()` → HTTP POST | ✅ REAL | 返回 `{access_token, token_type, scope}` |
| 获取用户信息 | `fetch_github_user(access_token)` → HTTP GET `api.github.com/user` | ✅ REAL | 返回 login, avatar_url 等 |
| 加密存储 token | `encrypt_api_key(access_token, "default")` → `cryptography.hazmat.primitives.ciphers.aead.AESGCM` | ✅ REAL | AES-256-GCM，12字节随机 nonce |
| 写入 DB | `GitAccountService.create_account()` → `db.add(acct)` → `db.commit()` | ✅ REAL | SQLite `git_account` 表 |
| 前端刷新账号 | `listGitAccounts()` → `GET /integrations/git/accounts` → `GitAccountService.list_all()` | ✅ REAL | 查询 DB，返回真实数据 |
| 查看仓库 | `listAccountRepos(id)` → `GET /integrations/git/accounts/{id}/repos` → `fetch_github_repos(token)` | ✅ REAL | HTTP GET `api.github.com/user/repos` |

**结论**：全链路真实，无 mock。

---

### 路径 2：模型服务商配置

| 步骤 | 调用链 | 状态 | 备注 |
|------|--------|------|------|
| 点击"+ 添加供应商" | `ModelsPage` → `AddProviderModal` | ✅ REAL | Modal 表单：名称/endpoint/API格式/Key/模型列表 |
| 提交 | `createProvider(input)` → `POST /model/providers` | ✅ REAL | 配置写入 `user_providers.yaml` |
| API Key 存储 | `set_credential()` → `CredentialService.create()` → `encrypt_api_key()` | ✅ REAL | AES-256-GCM → SQLite `credential` 表 |
| Key 注入内存 | `os.environ[envVar] = api_key` | ✅ REAL | 进程内存，仅存活于当前进程（按 AGENTS §12.1 设计） |
| 自检连通 | `POST /model/self-test` → `ModelGateway.self_test()` → `LiteLLMAdapter.test_connectivity()` | ✅ REAL | 真实 HTTP 调用 `litellm.acompletion()` 发送 "Hi" → 测延迟 |
| 调用日志 | `CallLog` 模型 → SQLite `call_log` 表 | ✅ REAL | 持久化调用记录+token 计数 |

**结论**：全链路真实。Key 存内存（设计如此），非敏感配置存 YAML 文件，加密凭据存 DB。

---

### 路径 3：Onboarding（引导 → 源码导入 → Gate）

| 步骤 | 调用链 | 状态 | 备注 |
|------|--------|------|------|
| 环境更新 | `update_environment()` → 写 `.rebuild/environment.json` | ✅ REAL | 文件 I/O |
| 项目状态 | `project_service.update(onboarding_done=True)` → SQLAlchemy commit | ✅ REAL | DB 持久化 |
| **Run 创建** | `RunService.create()` → `self._runs[rid] = RunResponse(...)` | ❌ **NOT PERSISTED** | 内存 dict，重启即失 |
| intake_report | `write_text()` → `artifacts/intake_report.json` | ✅ REAL | 文件 I/O |
| 源码导入 | `SourceMaterializer.materialize()` | ⚠️ 混合 | 见下表 |
| source_index | `generate_source_index()` → `Path.rglob("*")` → `artifacts/source_index.json` | ✅ REAL | 文件 I/O |
| Review Pass | `ReviewPass.run()` → execute_fn + review_fn + retry loop | ✅ REAL | 真实异步循环，可升级 |
| Gate 材料 | 写 4 个文件：execution_record / construction_report / review_pass | ✅ REAL | 文件 I/O |
| **Gate 创建** | `gate_service.create()` → `self._gates[gid] = g` | ❌ **NOT PERSISTED** | 内存 dict，重启即失 |
| Agent 执行 (SSE) | `/onboarding/execute` → SSE 4 阶段 | ⚠️ **前端未消费** | SSE 流未被 AgentChat 渲染 |
| Trace 写入 | `TraceWriter.write()` → deque + `.rebuild/traces.jsonl` | ⚠️ PARTIAL | 文件写入有，**但不回读**——重启后 trace 不可见 |
| Audit 写入 | `AuditWriter.write()` → deque + `.rebuild/audits.jsonl` | ⚠️ PARTIAL | 同上，重启后 audit 不可见 |

**Source Materialization 按类型**：

| source_type | 真实行为 | 状态 |
|-------------|---------|------|
| `local_dir` | `shutil.copy2()` 逐文件拷贝 + `_guard` 边界保护 + `SENSITIVE_LOCAL_ROOTS` 拒绝 | ✅ REAL |
| `zip` | 从 `extracted_path` 拷贝（ZIP 解压已提前在 `/projects/upload` 完成） | ✅ REAL |
| `git` | **不 clone**——写 warning + evidence gap + `materialization_status="deferred"` | ⚠️ DEFERRED |
| `github` | **不 clone**——同 git | ⚠️ DEFERRED |
| `manual` | 不导入，`materialization_status="empty"` | ✅ REAL（诚实） |

---

### 路径 4：P1 全量识别（Profiling）

| 步骤 | 调用链 | 状态 | 备注 |
|------|--------|------|------|
| 14项识别 | `FullStackProfiler.profile()` → 14个 `_item()` 方法 | ✅ **ALL REAL** | 每个都真正读文件系统 |
| 文件索引 | `_item1_file_index` → `source.rglob("*")` | ✅ REAL | 记录 path/size/ext，上限2000文件 |
| 目录结构 | `_item2_source_structure` → `source.iterdir()` | ✅ REAL | 顶层目录+子文件数 |
| 模块结构 | `_item3_module_structure` → 查找 build files | ✅ REAL | 检测 monorepo |
| 语言识别 | `_item4_language_id` → 扩展名统计 + LANG_EXTENSIONS(60+) | ✅ REAL | confidence: high/medium/low |
| 框架识别 | `_item5_framework_id` → **真实读文件内容**（pom.xml / package.json / requirements.txt） | ✅ REAL | 检测 Spring Boot/React/Django/Flask/FastAPI |
| 构建系统 | `_item6_build_system_id` → BUILD_FILES 匹配 | ✅ REAL | 30+ 构建文件模式 |
| 依赖清单 | `_item7_dependency_id` → **真实读 package.json/requirements.txt** | ✅ REAL | 上限500条 |
| 入口点 | `_item8_entry_point_id` → 文件名匹配 + **读 Dockerfile** | ✅ REAL | 提取 ENTRYPOINT/CMD |
| 测试目录 | `_item9_test_inventory` → 目录名+测试框架匹配 | ✅ REAL | pytest/jest/JUnit/NUnit |
| CI/CD | `_item10_cicd_inventory` → 文件路径匹配 | ✅ REAL | Docker/GitHub Actions/Jenkins |
| 基础设施 | `_item11_infra_clues` → 文件名+**读配置文件内容** | ✅ REAL | 值脱敏输出，只记"检测到连接配置" |
| 配置文件 | `_item12_config_inventory` → **读 .env/JSON/YAML 文件** | ✅ REAL | `.env` 键名输出为 `[ENV_KEY]_xxx`，敏感键输出为 `[REDACTED]_xxx`，**值永远不输出** |
| 文档清单 | `_item13_doc_inventory` → 文件匹配 | ✅ REAL | README/CHANGELOG/docs/ |
| 不确定性 | `_item14_uncertainty_manifest` → 汇总 gaps | ✅ REAL | 所有 confidence<high 项注册 |
| profiling_summary | `_build_summary()` → 真实数据生成 Markdown | ✅ REAL | 动态数据，非硬编码模板 |
| p2_input_manifest | `_build_p2_manifest()` → 包含所有 artifact refs + gaps | ✅ REAL | 结构完整，可供 R10 使用 |
| Review Pass | `ReviewPass.run()` review_fn 校验产物落盘 | ✅ REAL | 失败不创建 Gate |
| P1 Gate | `gate_service.create()` | ❌ **NOT PERSISTED** | 内存 dict |

**结论**：P1 Profiling 是 R9 最扎实的组件。14 项全真、值脱敏真、产物完整。唯一问题：Gate 不持久。

---

### 路径 5：Gate 决策

| 步骤 | 调用链 | 状态 | 备注 |
|------|--------|------|------|
| 前端点批准/请求修改/拒绝 | `GatePanel.handleDecision` → `POST .../promotion-decision` | ✅ REAL | 真实 HTTP |
| 后端路由 | `routes_stages.decide_promotion` → `stage_service.promote()` | ✅ REAL | 找对应 Gate → 调 `gate_service.decide()` |
| 决策校验 | `VALID_DECISIONS = {"approve","reject","request_changes"}` | ✅ REAL | 非法→ValueError→HTTP 400 |
| Gate 状态更新 | `g.gate_status = "approved"/"rejected"/"changes_requested"` | ✅ REAL | 但状态在**内存 dict** |
| 阶段推进 (approve) | `_apply_promotion` → `project_service.update(current_stage=nxt)` | ✅ REAL | **DB 持久化** ✅ |
| Run stage_status (approve) | `run_service.set_stage_status(run_id, cur, "completed")` | ❌ **NOT PERSISTED** | 内存 dict 更新 |
| Run stage_status (reject) | `run_service.set_stage_status(run_id, cur, "blocked")` | ❌ **NOT PERSISTED** | 内存 dict 更新 |
| Run stage_status (request_changes) | `run_service.set_stage_status(run_id, cur, "changes_requested")` | ❌ **NOT PERSISTED** | 内存 dict 更新 |
| active_gate 清除 | `project_service.update(active_gate="")` | ✅ REAL | **DB 持久化** ✅ |
| Audit 写入 | `audit_writer.write()` → deque + `.rebuild/audits.jsonl` | ⚠️ PARTIAL | 文件有，重启不读 |
| policy_check | `PolicyCheckResponse(allowed=True, reason="R4 mock: no real policy evaluation")` | ❌ **HARDCODED MOCK** | |
| risk_assessment | `RiskAssessmentResponse(risk_level="L0", summary="R4 mock")` | ❌ **HARDCODED MOCK** | |

**结论**：Project 级状态（current_stage, active_gate）已持久化。但 Run stage_status、Gate 自身、Trace/Audit 回读均不持久。policy_check 和 risk_assessment 是显式 mock。

---

### 路径 6：Agent Chat

| 步骤 | 调用链 | 状态 | 备注 |
|------|--------|------|------|
| 前端输入发送 | `AgentChat.handleSend` → `POST /projects/{id}/agent/chat` | ✅ REAL | SSE Reader 解析 `event:` / `data:` 行 |
| 后端组装上下文 | `routes_agent_chat.agent_chat` → 读 artifacts/ 目录 + profiling_summary.md + source/ 文件数 | ✅ REAL | 文件系统真实读取 |
| **LLM 调用** | `AgentLoop.run()` → `litellm.acompletion(stream=True, model="openai/deepseek-chat", api_key=DEEPSEEK_API_KEY)` | ✅ REAL | 真实 token 级流式，验证通过 |
| SSE 流式返回 | `yield f"event: delta\ndata: ...` → `StreamingResponse(text/event-stream)` | ✅ REAL | 逐 token 推送前端 |
| API Key 缺失 | `api_key = DEEPSEEK_API_KEY or LLM_API_KEY`，无 Key 时仍调 litellm（会失败） | ⚠️ BUG | 无 Key 时应走 ImportError 回退或更早提示 |
| **工具调用** | `AGENT_TOOLS` 列表（3个工具）定义了但**从未传给 litellm.acompletion** | ❌ **DEAD CODE** | `get_project_info`/`read_artifact`/`run_profiling` 完全不可用 |
| 错误回退 | `ImportError` → echo 回复；通用 `Exception` → error 消息 | ✅ REAL | 非静默，用户能看到错误 |

**Agent Chat 上下文组装细节**（每次对话都包含）：

```python
system_prompt = f"""你是 rebuild 平台的 AI 助手，负责协助用户完成软件迁移项目的接入和建档。
当前项目: {project_name} (ID: {project_id})
当前阶段: {stage}
执行模式: {mode}
{file_count} 个源码文件已导入
项目技术栈信息: {profiling_summary[:2000]}
阶段产物: {', '.join(artifacts[:20])}
最近活动: {trace_summary}
"""
```

**结论**：核心流式对话真实可用（需 Key）。工具调用是死代码。

---

### 路径 7：Workspace 聚合（页面加载）

| 组件 | 调用链 | 状态 | 备注 |
|------|--------|------|------|
| Project | `project_service.get()` → SQLAlchemy | ✅ REAL | DB 持久 |
| **Run** | `run_service.get()` → `self._runs.get(run_id)` | ❌ **NOT PERSISTED** | 内存 dict |
| **Gate** | `gate_service.get_active()` → `self._gates` 遍历 | ❌ **NOT PERSISTED** | 内存 dict |
| **Artifacts** | `aet_service.list_artifacts()` → `seed_artifacts()` | ❌ **MOCK** | 3个硬编码对象（tech_stack_report/risk_matrix/migration_plan），不是实际产物！ |
| **Evidence Gaps** | `aet_service.list_evidence_gaps()` → `seed_evidence_gaps()` | ❌ **MOCK** | 2个硬编码对象，不是实际 gaps |
| Traces | `aet_service.list_traces()` → `TraceWriter.query()` | ⚠️ PARTIAL | deque 中有数据，但**重启后丢失**（不回读 jsonl） |
| Audits | `aet_service.list_audits()` → `AuditWriter.query()` | ⚠️ PARTIAL | 同上 |
| File Tree | `scan_file_tree()` → 真实文件系统遍历 | ✅ REAL | source/ + artifacts/ + patches/ |
| Material Tree | `scan_material_tree()` → 真实文件系统遍历 | ✅ REAL | materials/ + artifacts/ + evidence/ + reports/ + runs/ + logs/ |
| **graph_status** | `GraphStatus(graph_capability_status="not_connected", transition_mode="mock")` | ❌ **HARDCODED MOCK** | 写死的常量，永不变 |
| **source_status** | `"source_status": "real"` | ⚠️ HARDCODED | 写死为 "real"，不是从实际能力评估的 |
| **capability_status** | `"capability_status": "available"` | ⚠️ HARDCODED | 同上 |

**InspectPanel 各 Tab 真实度**：

| Tab | 状态 | 数据来源 |
|-----|------|---------|
| Gate | ⚠️ 数据来自内存 dict | GateService in-memory |
| Evidence | ❌ MOCK | seed_evidence_gaps() 硬编码 |
| Trace | ⚠️ 数据 volatile | TraceWriter deque（重启失） |
| Audit | ⚠️ 数据 volatile | AuditWriter deque（重启失） |
| Context | ✅ REAL | `GET /projects/{id}/context` → context_assembler |
| Resource | ❌ PLACEHOLDER | `GET /api/resources` 返回空——注释："Resource registry planned for R6" |
| Model | ✅ REAL | `GET /api/model/providers` + `/api/model/status` |

---

## 二、SILENT FAILURE 清单（全仓 try/except: pass）

| # | 文件 | 行 | 被静默吞掉的操作 | 影响 |
|---|------|----|-----------------|------|
| 1 | routes_projects.py | 421 | `update_environment()` | 环境配置保存失败无感知 |
| 2 | routes_projects.py | 438 | `RunService.create()` | Run 创建失败无感知 |
| 3 | routes_projects.py | 454 | `intake_report.json` 写入 | 产物缺失无感知 |
| 4 | routes_projects.py | 484 | `SourceMaterializer.materialize()` | 源码导入失败无感知 |
| 5 | routes_projects.py | 541 | Gate 创建 | Gate 创建失败返回 None |
| 6 | routes_projects.py | 567 | `ps.update(active_gate=gate_id)` | active_gate 设置失败 |
| 7 | routes_projects.py | 733-734 | profiling 中 materialize | 源码缺失继续 profiling |
| 8 | routes_projects.py | 877-878 | P1 Gate 创建 | P1 Gate 缺失无感知 |
| 9 | gate_service.py | 144-146 | `project_service.update(current_stage=nxt)` | 阶段推进失败 |
| 10 | trace_writer.py | 69-70 | 文件持久化写入 | trace 降级为 memory-only |
| 11 | audit_writer.py | 71-72 | 文件持久化写入 | audit 降级为 memory-only |
| 12 | WorkspacePage.tsx | 71-73,81 | fileTree/materialTree/sessions/mode 加载 | 面板空/模式不回读 |

---

## 三、待修复问题清单（按严重度排列）

### P0：阻断用户使用

| # | 问题 | 位置 | 修复方案 |
|---|------|------|---------|
| **P0-1** | Git/GitHub 项目创建后**源码不 clone**，source/ 为空，P0 实质失败 | `source_materializer.py:120-147` `_materialize_git` / `_materialize_github` | 有 OAuth token 时真实 `git clone`；无 token 时在 UI 阻塞并引导绑定 |
| **P0-2** | `/onboarding/execute` SSE 流**前端未消费**——Agent 执行过程在 AgentChat 中不可见 | `OnboardingWizard.tsx:117-122` | `handleFinalize` 中改为消费 SSE 流，逐步渲染到 AgentChat |
| **P0-3** | AET 面板 **Evidence** tab 显示硬编码 seed 数据，不是真实 Evidence Gaps | `aet_service.py:30-33` `list_artifacts()` / `aet_service.py:49-50` `list_evidence_gaps()` | 改为从 `artifacts/` 目录读取实际产物列表 + 从 `uncertainty_manifest.json` 读取真实 gaps |
| **P0-4** | InspectPanel **Resource** tab 返回空——placeholder | `routes_registry.py` / InspectPanel ResourcePanel | 调已有 `GET /api/registry/resources` 端点。该端点当前返回空 `ResourceRegistryResponse()`——需使其返回 DB 中真实注册的 Agent/Skill/Resource |
| **P0-5** | Agent 聊天**工具调用死代码**——3个 tool 定义但从未传给 LLM | `agent_loop.py:17-48` + `run()` | 将 `tools=AGENT_TOOLS` 传给 `litellm.acompletion()`；添加 tool call 响应处理循环 |
| **P0-6** | `agent_loop.py:108-112` 无 Key 时仍调 litellm（无 api_key 参数），导致静默错误 | `agent_loop.py:104-112` | 无 Key 时走 ImportError 回退或提前返回友好错误 |

### P1：影响数据可靠性和用户体验

| # | 问题 | 位置 | 修复方案 |
|---|------|------|---------|
| **P1-1** | **Run 不持久**——`RunService._runs` 是内存 dict，重启全丢 | `run_service.py:17` | 建立 `run` DB 表（SQLAlchemy model + migration）或至少写 JSON 文件持久化 |
| **P1-2** | **Gate 不持久**——`GateService._gates` 是内存 dict | `gate_service.py:36` | 建立 `gate` DB 表，或写 JSON 文件（与 Run 同方案） |
| **P1-3** | **Trace/Audit 不回读**——jsonl 文件写了但重启后 query() 只看 deque | `trace_writer.py:93-112` / `audit_writer.py` | query() 时合并 jsonl 文件中的历史记录 |
| **P1-4** | **graph_status 永为 mock**——硬编码 `"not_connected"` | `workspace_service.py:415-417` | 若 LangGraph 未就绪（归 R10），诚实标注 `"future_r10"` 而非 `"not_connected"` |
| **P1-5** | **policy_check / risk_assessment 硬编码 mock** | `gate_service.py:168-182` | 若有 mode_policy.py，接入真实风险评估；否则诚实标注为 `not_implemented` |
| **P1-6** | **12 处 try/except: pass** 静默吞错 | 见上表 | 改为 at least log error + 设置错误标记返回给前端 |
| **P1-7** | `_apply_promotion` 中 Run stage_status 更新是内存操作，重启后 NodeRail 状态丢失 | `gate_service.py:139-157` | 若 Run 持久化（P1-1），同步持久化 stage_status |

### P2：体验缺陷

| # | 问题 | 位置 | 修复方案 |
|---|------|------|---------|
| **P2-1** | GatePanel 批准后 P1 profiling loading 在 Modal 内，用户看不到 Agent 对话区过程 | `GatePanel.tsx:103-122` | profiling 触发后关闭 Modal，切换 AgentChat tab，在对话中显示进度 |
| **P2-2** | Manual 模式 AgentChat 的「确认执行」「拒绝」按钮不调用后端 | `AgentChat.tsx:232-260` | 按钮应真正发消息给后端 Agent 执行或拒绝 |
| **P2-3** | `source_status` / `capability_status` 写死为 `"real"` / `"available"` | `workspace_service.py:399-400` | 应从实际能力矩阵动态计算 |
| **P2-4** | 前端加载失败静默（fileTree/materialTree/sessions/mode） | `WorkspacePage.tsx:71-81` | 至少 console.warn；重要数据加载失败应显示重试按钮 |

---

## 四、已确认真实且无需修复的部分

以下经逐行追踪确认为全链路真实，无需修复：

- ✅ Git OAuth 绑定与账号列表
- ✅ GitHub 仓库列表拉取
- ✅ 模型服务商配置与连通性自检
- ✅ 模型调用日志（CallLog DB 表）
- ✅ API Key 加密存储（AES-256-GCM + HKDF-SHA256）
- ✅ 项目 CRUD（DB 持久化）
- ✅ Environment Profile 读写（文件持久化）
- ✅ OnboardingWizard 5步引导
- ✅ Source Materialization（local_dir / zip / manual）
- ✅ source_index 生成
- ✅ P1 14 项全量识别（全部真文件读取）
- ✅ P1 产物生成（13 JSON + profiling_summary.md + p2_input_manifest.json）
- ✅ 配置值脱敏（真 REDACTED）
- ✅ Review Pass 框架（execute→review→retry≤2→escalate）
- ✅ Gate 决策（approve/reject/request_changes 三态 + 非法 400）
- ✅ Project 阶段推进（current_stage 持久化到 DB）
- ✅ Agent Chat SSE 真正的 token 级流式（需 Key）
- ✅ Audit 写入（每次 Gate 决策）
- ✅ Trace 写入（每个关键操作）
- ✅ 文件树 / 材料树（真文件系统扫描）
- ✅ Workspace 文件读写（带路径边界保护）
- ✅ 终端命令执行（ExecutionProvider + deny-list）
- ✅ 执行模式切换与持久化
- ✅ 编程 Agent 选择器持久化
