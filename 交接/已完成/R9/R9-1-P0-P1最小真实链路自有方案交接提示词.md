# R9-1 P0-P1 最小真实链路自有方案交接提示词

文件身份：新窗口 / 本地 Agent 一次性交接提示词  
适用项目：rebuild  
项目路径：`/home/king/rebuild/`  
当前阶段：R9-1 P0-P1 最小真实链路自有方案  
任务类型：方案阶段；先核查实际状态，再提出当前版本自有方案；默认不进入 R9-3 施工  
输出原则：报告数量尽量少，便于交接；优先输出 1 份主方案报告，必要时最多 1 份附录。  
强制节奏：**先自有方案 → 再历史吸收与完善 → 最后施工**。本轮只做 R9-1 自有方案与状态核查，不直接施工。

---

## 0. 启动自检

我正在执行 rebuild 当前版本 R9-1：P0-P1 最小真实链路自有方案任务。我不能依赖旧窗口记忆，不能假设前面阶段已经完整完成，也不能假设后续阶段会替我补救。

本轮必须先核查当前实际状态，再提出方案。即使 R8 已 accepted，也不能全盘认为所有 R8 地基能力已经全面、正确、可直接使用。必须逐项核查 R8 工作区、文件/材料、编辑、终端、ExecutionSession、Trace/Audit、容器执行、AET、Environment Profile、首启占位、进度追踪和相关文档的实际完成度。

如果发现阻塞、缺陷、文档不同步、状态不诚实或关键能力不满足 R9 复用要求，不得跳过；应当提出修补方案。若属于 R9 启动前必须修复的阻塞，应明确登记并要求先修复；若可在 R9 工作包中顺手修复，应纳入 R9 工作包，而不是写“后续补齐”。

本轮不应过度强调“不能施工后续阶段”。如果在 P0-P1 链路中遇到需要提前准备的后续接口、占位、字段、轻量状态或联调 seam，可以提前施工或占位；但必须诚实标记，不能伪装完成。重点是：**不能后延、不能部分实现冒充完成、不能以阶段边界为借口绕过当前链路所需能力。**

---

## 1. 当前阶段定位

R9 的目标是：在 R8 已收口的工作区地基上，打通 **P0 接入 + P1 建档的最小真实链路**。

R9 不只是做一个引导 UI，也不只是写一份建档报告。R9 必须让一个 Project 在 Workspace 中完成从“接入确认/补充信息”到“形成初始项目档案”的真实闭环，并产生可追踪的状态、材料、Artifact/Evidence、Trace/Audit 和可进入 P2 的输入基础。

R9 可以适度提前准备后续 P2-P6 会依赖的字段、状态、接口或占位，但不得把 P0-P1 本该闭合的能力后延到后续阶段。

---

## 2. 本轮任务目标：R9-1 自有方案

本轮只做 R9-1，不做代码施工。必须输出一份可供 R9-2 历史吸收和 R9-3 施工使用的自有方案。

必须完成：

1. 核查 R8 accepted 后的实际可复用能力，而不是直接采信汇总报告。
2. 明确 P0-P1 最小真实链路的当前版本自有设计。
3. 明确 R9 与 R8 地基、R10-R12 后续阶段之间的关系。
4. 明确哪些 R8 遗留或 P2 小项需要在 R9 开工前修补或纳入 R9 工作包。
5. 明确 P0 接入引导向导本体如何落地。
6. 明确 P1 建档最小产物如何生成、保存、展示、编辑、追踪和进入 P2。
7. 明确三模式 Manual / Plan / Auto 在 P0-P1 的最小真实语义。
8. 明确 D-078 `coding_agent_ref` / Coding Agent 选择器 UI 的 R9 处理方式。
9. 明确 R9-2 历史吸收范围。
10. 明确 R9-3 候选施工工作包和 R9-4 验收标准建议。

---

## 3. 必读事实源

执行前必须读取。若路径不存在，以本地实际路径为准，并在报告中说明。

### 3.1 硬规则与工作准则

- `/home/king/rebuild/AGENTS.md`
- `/home/king/rebuild/skills/R-建设执行/rebuild-work-guidelines/SKILL.md`

### 3.2 R8 收口材料

- `交接/当前/R8-工作区真实化-汇总施工报告.md` 或实际归档位置
- R8-4 独立验收报告与修复轮材料，若存在
- R8-5 执行集成专项材料，若存在
- R8 相关证据目录

注意：R8 汇总报告可作为收口声明，但 R9-1 必须核查实际代码和运行状态，不能只引用报告。

### 3.3 项目治理与阶段规划

- `/home/king/rebuild/文档/00-项目治理/00-项目总览.md`
- `/home/king/rebuild/文档/00-项目治理/01-决策记录.md`
- `/home/king/rebuild/文档/00-项目治理/02-术语表.md`
- `/home/king/rebuild/文档/00-项目治理/06-R阶段总计划.md`
- `/home/king/rebuild/文档/00-项目治理/07-版本演进与历史资产吸收.md`
- `/home/king/rebuild/证据/进度追踪/01-功能表.md`
- `/home/king/rebuild/证据/进度追踪/02-阻塞项.md`
- `/home/king/rebuild/证据/进度追踪/03-待确认项.md`
- `/home/king/rebuild/证据/进度追踪/04-施工记录.md`
- `/home/king/rebuild/证据/进度追踪/05-验收与证据索引.md`
- `/home/king/rebuild/证据/进度追踪/06-状态口径与更新规则.md`

### 3.4 产品、流程、架构、UX、API、安全、测试专题

至少读取与 P0-P1、Project、Workspace、引导、建档、Artifact/Evidence/Trace/Audit、Gate、三模式相关的文件：

- `/home/king/rebuild/文档/01-产品定义/00-产品定义基准.md`
- `/home/king/rebuild/文档/01-产品定义/01-用户旅程.md`
- `/home/king/rebuild/文档/01-产品定义/03-典型项目场景与引导模板.md`
- `/home/king/rebuild/文档/02-架构设计/00-架构总纲.md`
- `/home/king/rebuild/文档/02-架构设计/03-Project-Run-TaskGraph状态架构.md`
- `/home/king/rebuild/文档/02-架构设计/04-Workspace-Environment-Execution架构.md`
- `/home/king/rebuild/文档/06-UX与前端/01-信息架构与项目创建.md`
- `/home/king/rebuild/文档/06-UX与前端/03-ProjectWorkspace布局规范.md`
- `/home/king/rebuild/文档/06-UX与前端/05-Evidence-Trace-Audit与Gate交互.md`
- `/home/king/rebuild/文档/05-API与集成契约/` 中与 Project / Workspace / Stage / Gate / AET / P0-P1 相关内容
- `/home/king/rebuild/文档/07-安全与权限/` 中与权限、密钥、命令、写盘、审计相关内容
- `/home/king/rebuild/文档/08-测试与验收/` 中与 D-074 和阶段验收相关内容

### 3.5 当前代码事实源

必须读取实际代码，不得只看文档：

- `backend/app/api/`
- `backend/app/services/`
- `backend/app/models/`
- `backend/app/schemas/`
- `frontend/src/pages/projects/`
- `frontend/src/pages/workspace/`
- `frontend/src/services/`
- `frontend/src/stores/`
- `source/skills/`
- `skills/R-建设执行/`
- `工作区/`
- `docker-compose.yml`
- `deploy/`

重点核查：

- R8 Workspace 地基是否仍可运行。
- P0/P1 是否已有任何占位或旧实现。
- Project 创建与 Workspace 首启占位现状。
- Environment Profile 读写现状。
- Trace/Audit 和 AET 是否可用于 P0-P1。
- 三模式当前只是前端开关还是已有后端字段。
- Coding Agent 配置、D-078 字段/UI 是否存在。
- 进度追踪是否已更新 R8 accepted 与 R9 planned。

---

## 4. R9-1 必须先做的实际状态核查

### 4.1 R8 accepted 承接核查

不能只写“R8 已 accepted”。必须核查：

1. R8 accepted 是否已同步到 R 阶段总计划。
2. 进度追踪六件套是否记录 R8 完工和证据。
3. R8 汇总报告中的遗留项是否登记。
4. R8 P2 小项是否已修复、登记或仍存在。
5. R8 地基能力是否可被 R9 直接复用。

### 4.2 R8 地基复用核查

至少核查以下能力：

1. Workspace 页面是否能打开。
2. 文件树是否真实。
3. 材料视图是否真实。
4. 文件预览和材料编辑是否可用。
5. 终端是否有受控交互式最小闭环。
6. ExecutionSession 是否写入。
7. Trace/Audit 是否持久化。
8. Environment Profile 是否真实存在并可读写。
9. Container 模式是否仍可用。
10. AET 检视是否可读真实数据。

如发现 R8 地基能力实际失效，不得跳过。必须在 R9-1 方案中列为开工前修补项或 R9-3 前置工作包。

### 4.3 R8 遗留项核查

必须核查并给出处理建议：

1. D-078 `Project.coding_agent_ref` 字段是否存在。
2. Workspace Coding Agent 选择器 UI 是否存在。
3. 三模式 Manual / Plan / Auto 是否有后端真实差异。
4. 终端 cwd 是否仍非工作区。
5. 输出面板 outputHistory 是否仍是死态。
6. `_guard` 前缀安全问题是否仍存在。
7. persistence 标记是否诚实。
8. 引导向导本体是否仍未实现，仅有首启占位。

---

## 5. R9 自有方案必须回答的问题

### 5.1 P0 接入最小真实链路

必须回答：

1. P0 的输入是什么？
2. 用户在向导中需要确认什么？
3. 哪些信息来自已有 Project / Workspace / Environment Profile？
4. 哪些信息由用户补充？
5. P0 结果保存在哪里？
6. 是否生成 Artifact/Evidence？
7. Trace/Audit 如何记录？
8. P0 完成后状态如何推进到 P1？
9. Manual / Plan / Auto 三模式在 P0 中有什么不同？
10. 遇到信息缺失、环境未知、模型不可用、材料缺失时如何处理？

### 5.2 P1 建档最小真实链路

必须回答：

1. P1 最小建档产物是什么？
2. 项目初始档案保存为文件、DB 记录、Artifact，还是组合？
3. 档案是否可在 Workspace 材料视图中查看和编辑？
4. 是否生成技术栈初步识别或用户声明？
5. 是否更新 Environment Profile？
6. 是否生成风险/待确认项初始清单？
7. 是否形成进入 P2 的输入包？
8. P1 完成状态如何记录？
9. Trace/Audit 如何记录？
10. 不确定项是否阻塞进入 P2，还是作为待确认项进入 P2？

### 5.3 三模式最小真实策略

R8 报告中三模式真实策略归 R9。R9-1 必须提出当前版本自有方案，至少定义：

1. Manual：哪些 P0/P1 动作必须用户确认？
2. Plan：是否先生成接入/建档计划，再由用户确认？
3. Auto：哪些低风险动作允许自动完成？
4. 三模式状态保存在哪里？
5. 三模式如何写 Trace/Audit？
6. 三模式如何影响 Gate 和用户确认？

### 5.4 D-078 Coding Agent 选择机制

R8 汇总报告中 D-078 字段/UI 尚未出现。R9-1 必须提出处理方案：

1. 是否在 R9 补 `Project.coding_agent_ref` 或等价字段？
2. 是否在 Workspace 增加 Coding Agent 选择器 UI？
3. 选择结果是否保存到 Project 或 Project Profile？
4. 真实 AI 编码调用是否仍归 R11？
5. R9 只做配置与选择机制时，如何避免伪装成真实编码 Agent 调用？

### 5.5 提前占位和轻量施工原则

R9 可以为了 P0-P1 链路提前准备后续会用到的字段、接口、状态或占位，例如：

- P2 输入包字段。
- P3 plan placeholder。
- P4 execution readiness 标记。
- Gate 状态枚举。
- TaskGraph seam。
- LangGraph future hooks。

但必须做到：

1. 明确标记为 future / placeholder / not_connected / planned。
2. 不伪装成 completed。
3. 不影响 P0-P1 真实闭环。
4. 不以占位代替当前必须完成的 P0-P1 能力。

---

## 6. R9-2 历史吸收范围建议

R9-1 只提出自有方案，不做历史吸收。但必须列出 R9-2 应深读范围，至少包括：

1. V10 / V26.0 中 P0/P1 接入、建档、项目档案、引导、阶段推进相关实现。
2. V10 LangGraph StateGraph 中接入/建档节点、interrupt/resume、checkpoint 思路；只吸收模式，不复活 S0-S7。
3. V26.0 / V10 中项目初始化、source import、workspace metadata、environment profile 与 project documentation 相关经验。
4. 历史版本中假 completed、skeleton/real_llm 伪完成、OpenCode 模拟器等反例。
5. 历史 UX 中引导向导、项目接入、建档页面或流程。

禁止吸收：旧 RunEngine、旧 F0-F6/S0-S7、Mission 产品层、含密钥材料、旧代码直接复制。

---

## 7. 建议输出文件

本轮输出尽量控制为 1 份主报告：

- `R9-1-P0-P1最小真实链路自有方案.md`

如内容过长，可附 1 份：

- `R9-1-实际状态核查附录.md`

不得再输出 6-8 份碎片报告。

主报告必须包含：

1. 启动自检。
2. 已读取事实源。
3. R8 accepted 承接核查结果。
4. R8 地基实际可复用性核查。
5. R8 遗留项处理建议。
6. R9 阶段定位。
7. P0 接入最小真实链路自有方案。
8. P1 建档最小真实链路自有方案。
9. 三模式最小真实策略方案。
10. D-078 Coding Agent 选择机制方案。
11. 可提前占位/轻量施工项与诚实标记规则。
12. R9-2 历史吸收范围建议。
13. R9-3 初步施工工作包建议。
14. R9-4 验收标准建议。
15. 阻塞项、风险项、待确认项。
16. 文档/进度追踪需要同步的事项。

---

## 8. R9-1 完成标准

R9-1 可接受，当且仅当：

1. 已核查实际状态，而不是只采信 R8 报告。
2. 已明确 R8 地基可复用程度和缺口。
3. 已对阻塞项提出修补方案，未跳过。
4. 已提出当前版本自己的 P0-P1 方案。
5. 已明确三模式最小真实策略。
6. 已处理 D-078 字段/UI 遗留方案。
7. 已列出 R9-2 历史吸收范围。
8. 已给出 R9-3 工作包和 R9-4 验收建议。
9. 未把 P0-P1 当前必须完成的能力后延。
10. 允许必要提前占位，但均诚实标记。
11. 报告数量少、结构清楚、可交接。
12. 未泄露任何密钥或敏感配置。

---

## 9. 禁止事项

1. 禁止直接跳到 R9-3 施工。
2. 禁止只写“R8 已完成”而不核查实际状态。
3. 禁止遇到 R8 地基缺陷时跳过。
4. 禁止用“后续会补”绕过 P0-P1 闭环。
5. 禁止将 P0/P1 的 UI 向导做成空壳。
6. 禁止将 P1 建档只写在聊天报告或交接报告中，而不形成 Workspace 可见产物。
7. 禁止把 placeholder / not_connected / mock 写成 completed。
8. 禁止复活旧 RunEngine、旧 F0-F6/S0-S7、Mission 产品层。
9. 禁止泄露 Key/Token/Secret/Password/.env 明文。
10. 禁止输出大量零散报告。

---

## 10. 最终提醒

R9-1 的核心不是证明“后续不能做什么”，而是规划“P0-P1 现在必须真实闭合什么”。如果为了 P0-P1 闭环需要提前准备后续字段、接口、状态或占位，可以做，但必须诚实标记。真正不能接受的是：该在 P0-P1 闭合的内容继续后延，或者只做部分实现却标记完成。

先提出当前版本自己的方案，再进入 R9-2 历史吸收与完善，最后才进入 R9-3 施工。
