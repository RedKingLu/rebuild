# R9-3G 产品级端到端返工施工交接提示词

文件身份：新窗口 / 独立施工 Agent 一次性交接提示词
适用项目：rebuild
项目路径：`/home/king/rebuild/`
当前阶段：R9-3G 施工（五轮：G-A → G-B → G-C → G-D → G-E）
任务类型：返工施工 / 产品级端到端真实化 / 非验收
施工计划：`/home/king/rebuild/交接/当前/../.claude/plans/synchronous-growing-bee.md`（详细施工手册）
输出原则：每轮施工完成后输出简短的轮次自检摘要。最终输出 1 份施工收口报告。证据（截图）集中在 G-E 轮。
强制边界：R9-3G 是施工，不是验收。不得把自检当独立验收。验收将由独立验收 Agent 后续执行。

---

## 0. 启动自检

我正在执行 R9-3G 产品级端到端返工施工。我不是验收 Agent。我不能依赖旧窗口记忆，不能假设 R9 已经完成。

本轮施工必须以用户真实使用路径为唯一验收标准：
> 用户登录平台 → 绑定 Git → 绑定模型 → 基于 Git 仓库创建项目 → 进入工作区 → 真实完成 P0（源码导入+可见产物+Gate）→ 批准进入 P1 → 真实完成 P1（14项识别+可见产物+Gate）→ P1→P2 Gate 可操作。

---

## 1. 必读事实源

执行前必须读取（按需，不要求全部通读）：

- 施工手册：`/home/king/.claude/plans/synchronous-growing-bee.md`（**必读**，包含每轮详细施工指令）
- 决策记录：D-083（Gate 附审材料制度，`文档/00-项目治理/01-决策记录.md`）
- 根规则：`AGENTS.md`
- 当前代码状态：git status --short（工作树为 R9 所有修改，HEAD=0826297 R8 收口）

关键代码参考点（施工时使用）：
- Modal pattern：`frontend/src/components/models/AddProviderModal.tsx` 第 94-101 行
- 后端路由 pattern：`backend/app/api/routes_projects.py` 的 `SuccessEnvelope` 模式
- 前端 service pattern：`frontend/src/services/workspaceService.ts` 的 `get`/`post`/`put` helpers
- 前端 store：`frontend/src/stores/index.ts`（useWorkspaceStore, useSettingsStore）
- 类型定义：`frontend/src/types/index.ts`（StageId, Gate, Artifact, Evidence, Trace, Audit）
- 路由定义：`frontend/src/routes.tsx`（workspace 路由='/projects/:id/workspace'）

---

## 2. 五轮施工总览

严格顺序执行。每轮完成后自检通过方可进入下一轮。

### R9-3G-A：占位文案清零 + 用户入口闭环

**施工项**：
1. **InspectPanel 三个占位 tab 真实化**（修改 `frontend/src/pages/workspace/InspectPanel.tsx`）
   - `context` tab（line 30）：新增 ContextPanel 子组件，调用 `GET /projects/{id}/context` API。展示过滤后的 Skill 列表（category=common + current_stage）+ 项目上下文（name/source_type/workspace_status/onboarding_done/coding_agent_ref）
   - `resource` tab（line 32）：新增 ResourcePanel 子组件，调用 `GET /api/registry/resources`。展示资源列表（名称/类型/风险级别/状态）
   - `model` tab（line 34）：新增 ModelPanel 子组件，调用 `GET /api/model/status`。展示 Provider 列表（名称/健康状态/模型数）

2. **Agent 对话占位替换**（修改 `frontend/src/pages/workspace/WorkspacePage.tsx` 第 253-268 行）
   - 不再显示"将在 R9 接入"。改为展示：
     - 当前阶段摘要卡片（项目名、阶段、Run 状态、文件数）
     - 最近 10 条 Trace（从 `data.recent_traces` 获取）
     - 底部诚实文案："Agent 流式对话将在 R9-3G-C 接入（本轮后续）"

3. **Git 绑定验证**（`frontend/src/pages/projects/ProjectCreatePage.tsx`）
   - 验证 `listGitAccounts()` → `listAccountRepos()` 链路可用
   - 验证仓库选择后 `source_config` 写入 `clone_url`/`branch`
   - 任何断裂点修复

4. **模型绑定验证**（`frontend/src/pages/models/ModelsPage.tsx`）
   - 验证 Provider 配置 UI 正常
   - 验证 `fetchModelGatewayStatus()` 正常返回

**G-A 自检**：InspectPanel 的 context/resource/model 三个 tab 显示真实数据（非占位）；Agent tab 显示项目摘要+Trace；Git 绑定和模型绑定入口可用。

---

### R9-3G-B：P0 浮窗引导 + 源码导入 + Gate 附审材料

**施工项**：
1. **Modal 组件**（新增 `frontend/src/components/ui/Modal.tsx`）
   - Props: `{ open, onClose, title?, width?, children }`
   - 样式：`position:fixed; inset:0; background:rgba(0,0,0,.35); display:flex; align-items:center; justify-content:center; z-index:1200`
   - 内部 card：`background:var(--color-surface); border-radius:12px; max-width:640px; max-height:90vh; overflow:auto; padding:24px`
   - backdrop onClick→onClose, card onClick→e.stopPropagation()

2. **OnboardingWizard 浮窗化**（修改 `frontend/src/components/onboarding/OnboardingWizard.tsx`）
   - 用 Modal 包裹整个内容
   - WorkspacePage 第 229-238 行：改为渲染 `<OnboardingWizardModal>`（或直接让 OnboardingWizard 内部渲染 Modal）
   - 顶部状态栏新增"重新引导"按钮（onboarding_done 后显示，onClick 重置 onbDismissed 状态）

3. **materialize 确认**（修改 `frontend/src/components/onboarding/OnboardingWizard.tsx` `handleFinalize`）
   - 确认第 115-123 行的 materialize 调用正常
   - sourceType 为 local_dir/zip 时调 POST /materialize，manual 时跳过
   - onDone() → loadData() 刷新文件树

4. **Gate 附审材料——后端**（修改 `backend/app/api/routes_projects.py` `complete_onboarding`）
   - Gate 创建时 artifact_refs 填入 4 个材料路径：
     - `artifacts/intake_report.json`（已有）
     - `artifacts/p0_execution_record.json`（新建：7步操作每步的 status/timestamp 摘要）
     - `artifacts/p0_construction_report.md`（新建：markdown 含阶段名、时间、产物清单、自检结论）
     - `artifacts/p0_review_pass.json`（新建：来自 ReviewPass 的 ReviewResult）
   - 这 4 个文件写入 `workspace/projects/{id}/artifacts/`
   - Gate 的 `evidence_refs` 填入 evidence_candidates

5. **GatePanel 前端**（新增 `frontend/src/components/gate/GatePanel.tsx`）
   - GateBanner（一行横幅）点击→展开 Modal
   - Modal 内：左栏材料列表（4项），右栏材料内容预览
   - JSON 内容用 `<pre>` 格式化，.md 简单渲染（标题/列表/粗体/代码块）
   - 底部：批准/请求修改/拒绝按钮
   - 替换 WorkspacePage.tsx 第 224-227 行原有 GateBanner

6. **StagePageP0**（新增 `frontend/src/pages/workspace/StagePageP0.tsx`）
   - 4 个卡片区：接入状态 / 源码导入摘要 / 产物 / 最近活动
   - 数据来源：workspace aggregate（data.project, data.active_run, data.recent_traces, data.recent_audits, data.file_index）
   - WorkspacePage 第 269-275 行：`tab.kind === 'stage'` 分支改为按 tab.data 分派

**G-B 自检**：浮窗引导可用；source/ 有源码；材料树可见 intake_report 和 3 个新 P0 材料；GateBanner 点击展开 GatePanel，4 项材料可查看；批准后 P0 节点变绿；P0 阶段页展示四卡片。

---

### R9-3G-C：P1 自动触发 + Agent 对话 + 执行可视化

**施工项**：
1. **GatePanel 批准→自动触发 P1**（修改 `frontend/src/components/gate/GatePanel.tsx`）
   - approve 成功且 gate.stage === 'p0' → 调 POST /profile
   - profiling 期间显示 loading（"P1 全量识别进行中…"）
   - 返回后 onDecided() → loadData()

2. **Agent 对话真实化**
   - 新增 `frontend/src/components/agent/AgentChat.tsx`：消息列表+输入框+发送按钮
   - 新增 `backend/app/api/routes_agent.py`：`POST /api/projects/{id}/agent/chat` SSE 端点
   - 新增 `backend/app/services/agent_loop.py`：简单的 context→LLM→streaming response loop
   - 不使用 LangGraph checkpointing（归 R10），但支持基础 tool calling（可选）
   - WorkspacePage 第 253-268 行：替换为 AgentChat 组件

3. **StagePageP1**（新增 `frontend/src/pages/workspace/StagePageP1.tsx`）
   - 4 个卡片区：技术栈 / 14项识别摘要 / 产物列表 / Evidence Gaps
   - 数据来源：GET /profiling-summary API
   - WorkspacePage 分派增加 p1→StagePageP1

4. **P1→P2 Gate 附审材料**（修改 `backend/app/api/routes_projects.py` `run_profiling`）
   - 同 B4 模式，生成 p1_stage_plan.json / p1_execution_record.json / p1_construction_report.md / p1_review_pass.json
   - Gate 的 artifact_refs 填入这些材料

**G-C 自检**：P0 Gate 批准→P1 profiling 自动开始→Agent 对话区显示过程；P1 阶段页可见 profiling_summary+14项卡片+artifacts 列表+Evidence Gaps；P1→P2 Gate 展开→4 项材料可查看；Agent 对话可流式问答。

---

### R9-3G-D：Gate / 三模式 / Review 产品体验收口

**施工项**：
1. **Gate request_changes 返工回路**
   - GatePanel：request_changes 后显示返工提示文字
   - StagePageP0/P1：在 stage_status==='changes_requested' 时显示"重新执行"按钮
   - NodeRail：changes_requested 节点变橙色

2. **Gate reject 状态可视化**
   - NodeRail 变红（blocked），顶部栏状态 chip 变红
   - 阶段页显示拒绝原因

3. **三模式 UI 行为差异**（修改 `frontend/src/components/agent/AgentChat.tsx`）
   - Manual：agent 提议动作时显示确认按钮（"确认执行"/"拒绝"）
   - Plan：agent 先生成计划摘要列表，用户确认后批量执行
   - Auto：agent 直接执行低风险动作，对话显示自动执行日志
   - 模式从 useWorkspaceStore.execMode 读取

4. **Review Pass 可视化**（修改 AgentChat.tsx + routes_projects.py）
   - profiling 过程中每轮 Review 的结果以消息卡片插入 Agent 对话
   - passed：绿色卡片 ✓；failed：琥珀色卡片 ✗ + issues；escalate：红色卡片 + 原因

**G-D 自检**：request_changes→返工→重新执行→再次 Gate 通过；reject→blocked 红点；Manual 模式 Agent 确认按钮可见；Auto 模式自动执行；Review 失败→Agent 对话显示问题和返工进度。

---

### R9-3G-E：产品级端到端浏览器验收

**施工项**：
1. Playwright 脚本（新增 `frontend/e2e/r9_acceptance.mjs`）
   - 15 步验收路径逐项执行（参见计划 §G-E）
   - 每步截图保存到 `/tmp/r9_e2e/`
   - console error 收集并报告
2. 验收报告（新增 `交接/当前/R9-3G-端到端验收报告.md`）
   - 15 步逐项结果 + 截图引用 + pytest/build 结果 + 缺陷清单
   - 裁决：accepted / conditional / rework

---

## 3. 全局禁止事项

1. 禁止做 P2-P6 阶段页（文案改为"将在后续阶段接入"，不写"R9"）
2. 禁止把 git/github deferred 写成 completed
3. 禁止改动 `mode_policy.py` 核心授权逻辑（只读使用 `authorize_action` 函数）
4. 禁止引入任何 UI 框架或 CSS 库
5. 禁止删除现有文件（只新增和修改）
6. 禁止用任何 mock 数据——必须调用真实后端 API
7. 禁止跳过任何一轮的自检验收标准
8. 禁止擅自修改决策记录 D-080/081/082 的状态
9. 禁止把施工自检当独立验收

## 4. 关键后端 API 速查

施工时直接调用以下已有端点，不要重新实现：

- `GET /projects/{id}/workspace` → 完整 WorkspaceAggregate
- `GET /projects/{id}/context?stage=p0|p1` → Context Package（Skill+项目上下文）
- `POST /projects/{id}/onboarding/complete` → P0 完成（7步原子操作）
- `POST /projects/{id}/profile` → P1 全量识别（14项+Review+Gate）
- `GET /projects/{id}/profiling-summary` → profiling_summary.md + artifacts 列表
- `POST /projects/{id}/materialize` → 源码导入（已有，B3 只确认不重写）
- `GET /projects/{id}/source-index` → source_index JSON
- `POST /projects/{id}/runs/{rid}/stages/{stage}/promotion-decision` → Gate 决策
- `PUT /projects/{id}/mode` / `GET /projects/{id}/mode` → 模式读写
- `GET /api/model/status` → 模型网关状态
- `GET /api/coding-agents` → 编码 Agent 列表
- `GET /api/registry/resources` → 资源/Skill/Agent 注册表
- `GET /projects/{id}/gates` / `GET /projects/{id}/gates/active` → Gate 列表

## 5. 施工完成输出

1. `交接/当前/R9-3G-施工收口报告.md`（施工摘要 + 每轮自检结果 + 改动文件清单）
2. `交接/当前/R9-3G-端到端验收报告.md`（G-E 输出，15步结果 + Playwright 截图 + 裁决建议）
3. Playwright 截图目录 `/tmp/r9_e2e/`

## 6. 最终提醒

R9-3G 的目标是让用户能从登录一路走到 P1 完成，在 Workspace 中看到过程、产物、状态和 Gate。任何占位、空白、无反应、不可见产物的页面，都意味着本轮未完成。以用户体验为标准，不以 API 存在为标准。
