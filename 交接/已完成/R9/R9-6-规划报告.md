# R9-6 规划报告 — P0-P1 真实链路收口 + 运行期缺陷专项修复

> 文件路径：`交接/当前/R9-6-规划报告.md`
> 文件身份：**交接材料 / 阶段规划报告**（施工前计划，待用户 Gate）
> 出具 Agent：Claude（opus-4-8），独立验收 Agent
> 日期：2026-06-29
> 上游依据：`R9-5-真实收口报告.md`（◐ 有条件通过）、`R9-现状缺口报告.md`、四份摸排材料、D-084（全量真实化）、D-085（P0/P1 真实节点）、D-037（LangGraph）、D-074（最小联调）
> 阶段归属：R9「P0-P1 最小真实链路」（D-010）下的收口子期。R9-5 = 历史阻塞修补；**R9-6 = R9-5 真实化欠账的诚实修复 + R9（P0-P1 链路）最终闭环 + 收口**

---

## 1. R9-6 定位与目标

### 1.1 定位

R9-5 亲手驱动验收确认：**P0 理想路径真实可跑通**，但暴露 4 项 P0-P1 应然能力的真实运行期缺陷（F-1~F-4）+ 1 项静默失败残留（F-6）+ 运行实例未走图（F-5），且收口三件（G10/Git/追踪）未完成。按 D-084"R9-5 不得以后续补齐绕过本应完成的真实化"，这些欠账**必须在进入 R10（P2-P3）前由 R9-6 补齐**。

### 1.2 目标（成功标准，可验证）

```
G-R96-1  git/github 失败分流真实化：失败在创建/precheck 阶段明确阻断，或 P0 给出明确
         "源码导入失败 → 去补凭据/改地址 → 可重试" 的恢复动作；非 manual 空源不再卡死无出口。
G-R96-2  Evidence 真实落可查询存储：P0 产出的 Evidence 为真实对象（id/type/status/来源/claim），
         GET /evidence 能查到，Gate evidence_refs 指向真实对象。绝无内存假 list。
G-R96-3  P0 幂等：重复 complete/execute 不产生重复 Run/Gate/Artifact；已晋级阶段不可被重新造低阶 Gate。
G-R96-4  响应真实性：去除 common.py Meta 的 R4 默认污染；真实能力端点 meta 反映真实状态（real/available/durable）。
G-R96-5  消灭剩余静默 except（10 处）：失败发声（log+raise/降级标记），不静默吞错。
G-R96-6（建议）运行实例 P0/P1 切 LangGraph：LIVE 路径经图执行，Gate 携真实 checkpoint/interrupt/resume，使 G1 在真实路径成立。
G-R96-7  G10 金标准：4 场景 Playwright 全绿 + 0 console error（场景见 §4）。
G-R96-8  收口：Git 提交 + 追踪五件同步 + 06§3 一致性检查 + 交接/产物归档 + R9-5/R9-6 accepted。
```

---

## 2. 工作包（按优先级与依赖）

> 三步法（AGENTS.md §7.1）：每个工作包先独立提自有方案 → 吸收历史/参考材料 → 完善后施工。施工前不得跳过计划。

| 工作包 | 范围 | 对应缺口 | 关键文件（线索，施工前须通读） | 依赖 |
|---|---|---|---|---|
| **WP-1 git 失败分流** | precheck 真实拦截不可达/无凭据；或 P0 execute 在 empty+非manual 时产出"导入失败+恢复动作"的明确态（可建带"源码待补充"缺口的特殊 Gate 或显式 blocked 态 + 重试入口），透出具体 reason | F-1 | `routes_projects.py`(precheck/execute/_review_p0)、`source_materializer.py` | 无 |
| **WP-2 Evidence 落库** | 定义 Evidence 存储（DB 表或落盘 + 查询）；onboarding/complete/execute 写真实 Evidence；GET /evidence 读真实；Gate evidence_refs 校验存在 | F-2 | `routes_aet.py`(evidence)、`routes_projects.py`、AET/Evidence service、models | 无 |
| **WP-3 P0 幂等** | complete/execute 幂等键（按 project+stage+run）；已有未决 Gate 不重复建；已晋级阶段拒绝回退建 Gate | F-3 | `routes_projects.py`、`gate_service.py`、`run_service.py` | WP-1 |
| **WP-4 envelope 真实性** | 重构 common.py Meta：去 R4 默认，改为按端点真实能力赋值（real/available/durable）；审计各 router 是否显式传 meta | F-4 | `schemas/common.py`、各 `routes_*.py` | 无 |
| **WP-5 消灭静默 except** | 逐处评估 10 个 except:pass → 改为发声/显式降级标记；登记不可避免者并说明 | F-6 | gate_service / source_materializer / knowledge_search / opencode_acp / main | 无 |
| **WP-6（建议）运行实例切图** | LIVE P0/P1 委托 FlowRuntime/graph；Gate 用 interrupt 表达，resume 走 checkpoint；与 R9-5-7 P121 HITL 对齐 | F-5 | `routes_graph.py`、`app/graph/*`、`routes_projects.py` | WP-1/2/3（避免在缺陷态上切图） |
| **WP-7 G10 金标准** | 4 场景 Playwright（§4）；需先处理运行实例（建议独立测试 DB，勿污染凭据） | F-7 | `frontend/e2e/`、webapp-testing skill | WP-1~4（先修后测） |
| **WP-8 收口** | Git 提交（R9 全量首次入库）；追踪五件同步；06§3 13 项一致性检查；交接/产物归档；标 R9-5/R9-6 accepted（用户 Gate） | F-8/F-9 | `证据/进度追踪/*`、`交接/`、`产物/` | 全部 |

**建议施工顺序**：WP-1 → WP-2 → WP-3 →（WP-4 / WP-5 可并行）→ WP-6 → WP-7 → WP-8。每个 WP 完整收口（自验 + 证据）再进下一个，**不重蹈 R9-5 乱序覆辙**。

---

## 3. 验收方法（每 WP 收口必做，对齐验证规范 V1-V6）

```
V1 服务/单元：函数级落点真实（DB 行/文件/Evidence 对象存在）
V2 API 契约 ：请求/响应 schema、状态码、错误码枚举、脱敏断言、幂等断言
V3 持久化/恢复：重启后端 + 刷新前端，状态从后端恢复一致
V4 端到端   ：Playwright 真点击 → 渲染真实响应 → 0 console error
V5 证据闭合 ：每步 Artifact/Evidence/Trace/Audit 可索引且内容真实
V6 负路径   ：失败/无凭据/无Key/重复/越界 → 诚实降级或阻断，绝不伪装完成
强 Gate：任一步 L6 负路径会"看起来成功"则该步不得标完成（高于 V4 通过的门槛）
```

> **铁律（用户指令）**：施工 Agent 自验与 pytest 仅作参考，**收口验收必须亲手驱动平台真实跑通**（本轮已证明"测试绿 ≠ 真实可用"）。

---

## 4. G10 金标准 4 场景（必须真实数据 + 0 console error）

```
场景一 本地理想路径：配模型→自检→ZIP/local 创建→引导(Plan 模式)→P0 接入→P0→P1 Gate 批准→P1 识别→P1→P2 Gate。
场景二 Git 路径    ：绑 Git→创建→无凭据/失败时在创建/precheck 明确阻断引导（验证 F-1 修复，不卡 P0）。
场景三 负路径集    ：无 Key 对话 / 错误 Key 自检 / 损坏 ZIP / 重复决策（验证 F-3 幂等）→ 逐一诚实降级或阻断。
场景四 持久性      ：完成到 P1 后重启后端 + 刷新前端 → 所有状态/阶段/Gate/Evidence 完整恢复（验证 F-2 落库 + V3）。
```

---

## 5. 与红线/决策对齐

- **D-084 全量真实化**：R9-6 修复 F-1~F-6 即兑现"P0-P1 不得后延的真实化"。
- **D-037/D-085**：WP-6 使主编排红线在真实路径成立（建议本期完成；若用户裁决维持 W8 延 R10，须在收口报告显式标注"运行实例 G1 待 R10"）。
- **D-097**：F-4 envelope 去 mock 是去 Mock 红线的延伸。
- **不扩范围**：P2-P6/Fusion/远程/社区/文档维持 R10-R16，R9-6 不碰。

## 6. 待用户裁决（Gate）

```
Q-R96-1 R9-6 范围确认：是否同意"必修 F-1~F-6 + 收口 F-7~F-9"，WP-6（运行实例切图）本期做还是维持延 R10？
        建议：WP-6 本期做（否则主编排红线在用户真实路径上始终不生效，且 G10 场景一/四更接近真实）。
Q-R96-2 R9-5 收口方式：◐ 有条件通过收口（确认真实成果 + 缺口入 R9-6），还是要求 R9-5 先补齐再 accepted？
        建议：◐ 有条件通过收口，R9-5 与 R9-6 在 R9-6 收口时一并标 accepted。
Q-R96-3 测试用 DB：G10 需运行实例，建议用独立测试 DB（勿污染现有凭据库）；确认是否允许。
```

> **规划报告结束。** 待用户就 §6 裁决后，按 WP-1 起按序施工，每 WP 亲手驱动验收并完整收口。
