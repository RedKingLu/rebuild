# R9-4 独立验收报告：P0-P1 最小真实链路

> 文件路径：`交接/当前/R9-4-P0-P1最小真实链路独立验收报告.md`
> 文件身份：独立验收报告（非施工 / 证据驱动 / 非迎合式评审）
> 验收对象：R9-3A/B/C/D/E 最终施工结果（工作树未提交，HEAD=0826297 R8 收口）
> 验收人：独立验收 Agent（新窗口，未参与 R9-3 施工）
> 验收日期：2026-06-27
> 验收方法：代码审查 + 后端全量 pytest + 前端 build + **后端实跑 + 真实 API 实测 + Playwright 浏览器实测** + 文件系统验证 + Trace/Audit 检查 + 进度追踪一致性检查

---

## 0. 验收裁决

# 裁决：rework_required（返工）

R9 **未**真正完成 P0 接入 + P1 建档最小真实链路，**不能进入 accepted，不能作为 R10 P2-P3 评估/规划的输入基础**。

裁决依据（命中 §12 rework_required 多条触发条件）：

| 触发条件 | 命中 | 证据 |
|---|---|---|
| #2 P0 或 P1 端到端链路失败 | ✅ | P1 profiler 模块 **SyntaxError 无法导入**，`POST /profile` 运行时 500；UI Gate 路径 500 |
| #3 Review Pass 只是框架，未接真实流程 | ✅ | `ReviewPass` 类全仓库零调用、零测试，P0/P1 流程未接入 |
| #4 Gate 决策链路失败 | ✅ | UI 路径 `/promotion-decision` 500（uuid 未导入）；真实 `decide()` 不推进阶段、接受非法决策；GateBanner 永不渲染 |
| #5 三模式只是 UI/API 切换，无行为差异 | ✅ | `/execute` 完全不读 mode；无 Auto 授权代理；模式刷新后不恢复 |
| #8 浏览器级证据缺失 | ✅ | 施工方自述 Playwright 脚本未写；本轮实测证明关键链路浏览器不可用 |
| #9 进度追踪未同步 | ✅ | 施工记录无 R9-3A~E 条目；功能表未反映 R9（GAT-04 仍 planned/mock） |

诚实说明：**前端 build exit 0、后端 pytest 165 passed/0 failed 均为真实通过**，但绿色测试**掩盖了链路断裂**——没有任何测试覆盖 profiler 导入、`/profile`、`/onboarding/complete`、`/promotion-decision` 端点（见 §9）。这正是交接提示词 §0.6「不把局部测试通过当全量通过」与 §0.4「不把 API 存在当链路闭合」所警示的情况。

---

## 1. 已读取事实源

- 硬规则：`AGENTS.md`、`skills/R-建设执行/rebuild-work-guidelines/SKILL.md`
- R9 方案/施工材料：R9-1 / R9-1R / R9-2 / R9-3A~E 收口报告 / R9-3 分轮施工记录（作为施工声明与线索，未采信其结论）
- 治理与追踪：`文档/00-项目治理/01-决策记录.md`（D-079~082）、`证据/进度追踪/01-功能表.md`、`02-阻塞项.md`、`04-施工记录.md`
- 代码事实源（实读）：`source_materializer.py`、`full_stack_profiler.py`、`review_pass.py`、`context_assembler.py`(仅确认接线)、`workspace_service.py`、`gate_service.py`、`stage_service.py`、`dependencies.py`、`routes_projects.py`、`routes_workspace.py`、`routes_gates.py`、`routes_stages.py`、`OnboardingWizard.tsx`、`WorkspacePage.tsx`、`stores/index.ts`、`routes.tsx`、`vite.config.ts`

---

## 2. 实跑命令与结果

| 命令 | 退出码 | 结果 |
|---|---|---|
| `uv run pytest -q`（backend） | 0 | **165 passed, 2 skipped, 0 failed**（83.8s）。2 skipped 为模型网关连通性测试（无 LLM Key），可接受。 |
| `npm run build`（frontend） | 0 | 构建成功；仅 chunk>500KB 与动态导入提示，非错误。 |
| `uvicorn app.main:app`（实跑服务，端口 8000） | 启动成功 | health 200。**实测中触发 4 类运行时 500**（见下） |
| Playwright Chromium 实测（前端 5173 → 代理 8000） | — | 见 §8 |

实跑期间服务端实际抛出的运行时错误（server log 实录）：

```
POST /api/projects/{id}/profile                       → 500  SyntaxError: full_stack_profiler.py:517
POST /api/projects/{id}/runs/{rid}/stages/p0/promotion-decision → 500  NameError: name 'uuid' is not defined
GET  /api/coding-agents                               → 500  AttributeError: 'generator' object has no attribute 'query'
```

直接导入验证：
```
$ uv run python -c "import app.services.full_stack_profiler"
  File ".../full_stack_profiler.py", line 517
    f"- 语言分布：{', '.join(f'{l[\"language\"]}({l[\"file_count\"]})' for l in langs[:5])}",
SyntaxError: unexpected character after line continuation character
```

---

## 3. P0 验收逐项结果

| 项 | 结果 | 证据 / 说明 |
|---|---|---|
| **P0-01** OnboardingWizard 真实化 | ✅ pass | `WorkspacePage.tsx:229` 实际引用；`!onboarding_done` 时渲染；5 步均调真实 API（fetchEnvironment/updateEnvironment/fetchModelGatewayStatus）；提交调 `/onboarding/complete`；跳过路径可用；无 mock。浏览器实测：新项目显示「项目引导/环境选择/摘要确认」。 |
| **P0-02** Environment Profile 更新 | ✅ pass | 实测 `.rebuild/environment.json` 写入 `status=declared, env_kind=local, language_hint=Python, framework_hint=FastAPI`；刷新可读；`update_environment` 写 Trace+Audit。 |
| **P0-03** Project 状态更新 | ◐ partial | `onboarding_done=true` 真实写入（实测）；`coding_agent_ref` 持久化经 `updateProject` 真实；UI 诚实标注「R11 实现真实调用」。**但** `GET /api/coding-agents` 运行时 500（session bug），编程 Agent 下拉列表加载失败，仅余「平台自有 Agent」兜底项。 |
| **P0-04** P0 Run 创建 | ✅ pass | 实测 `run_id=run-409378` 创建；`execution_mode/run_goal` 真实读回；持久化于 DB（非仅前端 state）。 |
| **P0-05** intake_report 与 Evidence candidates | ✅ pass | 实测 `artifacts/intake_report.json` 落盘真实；2 个 evidence candidates 返回；写 Trace+Audit（jsonl 实录）。AET 右栏可见。 |
| **P0-06** P0→P1 Gate | ❌ fail | Gate 创建成功（`gate-94877f`, stage=p0, type=stage_promotion）。**但**：①`create()` 置状态 `created`，而 `get_active()` 只认 `waiting_decision` → `active_gate` 实测返回 **None** → 前端 GateBanner **永不渲染**；②approve/request_changes/reject 的实际可用性见 §5 GATE 项（UI 路径 500）。Gate 决策后状态不可恢复推进。 |

---

## 4. P1 验收逐项结果

| 项 | 结果 | 证据 / 说明 |
|---|---|---|
| **P1-01** source materialization | ◐ partial（含诚实性缺陷） | 是后端 service（`SourceMaterializer`），非终端脚本；local_dir 实测真实导入 28 文件；`_guard`/commonpath 路径边界稳健；失败生成 Evidence Gap。**缺陷**：`materialize()` 末尾 `if not result["errors"]` 无条件改写状态为 `completed`，**实测 git 源 → `materialization_status=completed, success=True`（应为 deferred）、manual 源 → `completed`（应为 empty）**，违反 §0.9 / §11.9「deferred/empty 不得写成 completed」诚实红线。 |
| **P1-02** source_index | ✅ pass | 实测 `/source-index` 返回全部要求字段（file_count/directory_count/top_level_dirs/key_files/skipped_dirs/binary_files_count/too_large_files_count/generated_at/source_type/materialization_status）。 |
| **P1-03** 14 项全量识别 | ❌ fail（运行时死） | 代码层 `full_stack_profiler.py` 14 项实现完整、含脱敏与 Evidence Gap，质量良好。**但 line 517 SyntaxError 导致模块无法导入**，`POST /profile` 运行时 500，14 项识别在真实运行中**完全不可用**。 |
| **P1-04** P1 Artifact 与 P2 输入包 | ❌ fail | 因 profiler 不可导入，`profiling_summary.md` / 13 Artifact / `p2_input_manifest.json` **实测均未生成**（`/profiling-summary` 返回 `available=false`）。P2 输入包不存在 → 无法供 R10 读取。 |
| **P1-05** P1→P2 Gate | ❌ fail | Gate 创建代码在 `/profile` 内，但该端点 500，Gate 不会被创建；即便创建也复用同样的 `created` 状态/UI 500 缺陷（同 P0-06）。 |

---

## 5. Review / Gate 验收结果

### Review Pass（REV-01/02/03）— ❌ 全部 fail
`ReviewPass` / `ReviewResult` 类仅在 `review_pass.py` 内自引用，**全仓库零调用**（`grep` 确认 `routes_*`、`*service.py`、`tests/` 均无引用），无任何测试。`/onboarding/complete` 与 `/profile` 流程中均未接入 execute→review→retry→escalate。属「只有框架、未接真实流程」，命中 rework #3。max_rounds=2 与 escalate 逻辑虽写出但无路径触达。

### Gate 决策链路（GATE-01~04）
| 项 | 结果 | 实测证据 |
|---|---|---|
| GATE-01 approve | ❌ fail | 真实端点 `/gates/{id}/decision` approve→`gate_status=approved`+写 Audit ✅；**但 `current_stage` 实测仍为 `p0`（未推进）、active_gate 未清除**。Project/Run/Stage 状态不推进。 |
| GATE-02 request_changes | ❌ fail | `decide()` 将 request_changes 映射为 `resolved`（语义错误，非 changes_requested）；**不创建返工任务、不回小循环**。 |
| GATE-03 reject | ◐ | `decide()` 置 `rejected` + 写 Audit；但阶段状态不变 blocked/halted。 |
| GATE-04 非法决策 | ❌ fail | 实测 `decision="frobnicate"` → **HTTP 200**，状态被改为 `resolved`、decision=frobnicate。应返 400/403 并拒绝。`GateDecisionRequest.decision` 无枚举校验。 |
| **UI 实际路径** | ❌ fail（致命） | 前端 GateBanner 调的是 `/runs/{rid}/stages/{stage}/promotion-decision`（→`stage_service.promote`，**`NameError: uuid` 运行时 500**），而非真实 `decide()`。即用户点击批准/请求修改/拒绝**全部 500**。且 GateBanner 因 active_gate=None 根本不渲染（§3 P0-06）。 |

结论：Gate 决策链路在浏览器路径下**完全不可用**；后端 `decide()` 虽部分工作但不推进状态、接受非法值，且不是 UI 调用的端点。

---

## 6. 三模式 / Auto 授权验收结果

| 项 | 结果 | 证据 |
|---|---|---|
| MODE-01 自由切换 | ◐ partial | 顶部三按钮可切换；`PUT /mode` 写 workspace.json + Trace + Audit（真实）。**但** `execMode` store 默认 `plan`、未持久化 localStorage、加载时不从后端回读 → **刷新后模式不恢复**（MODE-01.4 fail）。effective_mode 不可变历史未实现（RunAction 无 effective_mode 落地）。 |
| MODE-02 行为差异 | ❌ fail | `/execute` 等动作**完全不读取 mode**；Manual/Plan/Auto 无任何执行行为差异。三模式当前仅为一个被存储并挂到 Run 的字符串。 |
| MODE-03 Auto 授权边界 | ❌ fail | **不存在 Auto 授权代理**：无 `auto_approve`/授权决策输出（decision/reason/risk_level/evidence_refs/trace_refs/audit_ref/mode/reviewer），无 L0-L3 自动处理逻辑。Auto 不绕过 Gate「成立」仅因 Gate 机制本身未运转，并非有意约束。L4+ 不代理无证据。 |

注：`/execute` 的 deny-list（`rm -rf /` 实测被拦截，记 L4 deny Audit）是**与模式无关**的硬拦截，不构成三模式行为差异。

---

## 7. 浏览器级证据摘要（Playwright Chromium，前端 5173 → 代理 8000）

| 验证点 | 结果 |
|---|---|
| 应用根加载 | ✅ title「rebuild · 软件重构与迁移平台」 |
| Workspace 外壳（`/projects/:id/workspace`） | ✅ 渲染：返回平台、ExecMode 三按钮（手动/计划确认/自动）、AET 右栏（过程追踪/审计/证据） |
| OnboardingWizard（新项目） | ✅ 渲染「项目引导 / 环境选择 / 摘要确认」5 步 |
| GateBanner「阶段晋级 Gate」 | ❌ **不渲染**（active_gate=None）；状态 chip 显示「无待决 Gate」 |
| P1 profiling_summary / Artifact 可见 | ❌ 不可用（profiler 500，无产物） |
| Gate approve/request_changes 路径 | ❌ UI 端点 500 |
| console error | ⚠ **4 × 500（`GET /api/coding-agents`）**；其余 0 |

施工方 R9-3E 报告自述「浏览器级证据：GateBanner operable、Workspace loads real data」——经实测：Workspace 加载真实数据为真，但 **GateBanner 不可操作、coding-agents 500** 为不实陈述。R9-3E 同时自述「Playwright 脚本未写」，故该「浏览器证据」从未由施工方实际产出。

---

## 8. 安全检查结果

| 项 | 结果 | 说明 |
|---|---|---|
| Key/Token/Secret/.env 明文泄露 | ✅ 未发现 | Trace/Audit jsonl 实测无凭据；命中的 `token/api_key` 字样均为被分析的源码变量名，非泄露 |
| Git/GitHub 凭据入 Trace/Audit/stdout | ✅ | git/github 仅记 URL 前缀，未输出凭据 |
| 配置扫描脱敏 | ✅（代码层） | profiler `_item12` 对 SENSITIVE_KEYS 与 .env 键全部 REDACTED——惟因 profiler 不可运行，实际从不输出（fail-safe） |
| Auto 不代理 L4+ | ⚠ 不适用 | Auto 代理机制不存在 |
| source/ 默认只读 | ✅ | `READONLY_DIRS={source,patches}`，写入 403 |
| local_dir 防越界 | ◐ | 写入目标经 `_guard` 防越界 ✅；但**源目录 `config.path` 无敏感目录限制**（可指向 /etc 等系统目录读取），建议补白名单/敏感目录拒绝 |
| ZIP 防 zip slip | ◐ | `routes_projects.py:210` 的 `continue` 对 `extractall` **无效**（仍解压全部成员），实际防护依赖 Python stdlib `extractall` 自带的路径净化。建议改为逐成员安全解压 |
| deferred/planned/stub 写成 completed | ❌ | materialization git/manual 误报 completed（§4 P1-01） |

无 P0 级安全泄露；存在 2 项 P2 加固建议 + 1 项诚实性缺陷。

---

## 9. 文档与进度追踪一致性

| 检查 | 结果 |
|---|---|
| `01-功能表.md` 反映 R9 真实状态 | ❌ 未更新 R9。GAT-04 三模式仍 `planned`/「前端按钮静态Mock，后端未实现」；GAT-01 Gate 仍 `partial`/「仅占位骨架」。无 profiling/onboarding-completion/review 行。 |
| `04-施工记录.md` 记录 R9-3A~E | ❌ 最后条目为 REC-R8-07（D-079）；**无任何 R9-3 施工记录**。 |
| `02-阻塞项.md` | ⚠ 未登记 R9 运行时阻塞（profiler 语法错误/gate 500/coding-agents 500） |
| `05-验收与证据索引.md` | ❌ 未登记 R9-3/R9-4 证据 |
| D-079~082 状态 | ✅ D-080/081/082 均 `proposed`（未越权写 accepted），D-079 accepted。准确。 |
| 旧快照过时（总览/术语表/R阶段计划） | ⚠ 施工方 R9-3E 已自认部分过时，列为文档缺口 |

进度追踪未同步 → 命中 AGENTS §10 第 18 条与 rework #9：**未更新追踪材料的情况下不得收口 accepted**。

> 诚实补充：功能表虽未更新，但其**现存内容并未谎报** R9 完成（GAT-01/04 仍诚实标 skeleton/mock），与本轮实测结论一致。问题是「未按 §10 更新」，而非「谎报完成」。

---

## 10. 缺陷清单（按 P0/P1/P2 分级）

### P0（阻断，必须修复）
1. **[P1链路死] `full_stack_profiler.py:517` SyntaxError** —— 嵌套 f-string 转义引号致模块无法导入，`/profile` 500，P1 14 项识别/Artifact/profiling_summary/p2_input_manifest 全部不可用。
2. **[Gate UI 死] `stage_service.promote` `NameError: uuid`** —— `import uuid` 仅在 `submit_stage_plan` 内局部，`promote()`(line 69) 引用 uuid 即 500；前端 GateBanner 唯一调用的就是此端点。
3. **[Gate 不显] `create()` 状态 `created` ↔ `get_active()` 认 `waiting_decision` 不一致** —— active_gate 恒为 None，GateBanner 永不渲染，P0→P1 / P1→P2 用户确认入口实际缺失。
4. **[Agent列表死] `GET /api/coding-agents` `'generator' object has no attribute 'query'`** —— DB session/generator 误用（与历史 R7-4 get_session 池泄漏同源），workspace 顶部编程 Agent 选择器列表 500。
5. **[Review 未接线] Review Pass 全仓零调用** —— 三模式均要求经 Review Pass（方案要求），实际未接入任何 P 阶段流程。
6. **[Gate 决策不闭合] `decide()` 不推进 Project/Run/Stage 状态、接受非法决策(GATE-04)、request_changes 误映射 resolved 且无返工任务**。

### P1（影响最小真实链路质量）
7. **三模式无行为差异**：`/execute` 不读 mode；无 Auto 授权代理（MODE-02/03）。
8. **模式刷新不恢复**：execMode 未持久化、未从后端回读（MODE-01.4）。
9. **materialization 诚实性缺陷**：git→completed（应 deferred）、manual→completed（应 empty），违反诚实红线。
10. **进度追踪未同步**：施工记录无 R9-3、功能表未更新 R9、验收证据索引未登记。
11. **关键链路测试缺失**：无测试覆盖 profiler 导入 / `/profile` / `/onboarding/complete` / `/promotion-decision`，导致 165 绿色掩盖断链（TEST-03 fail）。

### P2（加固/诚实）
12. ZIP 解压 `continue` 防护无效（实际靠 stdlib 净化），建议逐成员安全解压。
13. local_dir 源目录无敏感目录/白名单限制。
14. `onboarding/complete` 与 `profile` 内大量 `try/except: pass` 静默吞错，会把 Run/Gate/Artifact 创建失败掩盖为 200 成功，建议显式错误上报。
15. workspace aggregate 中 `source_status="real"`、`capability_status="available"`、成功响应 `meta.persistence="volatile"`/`source_status="mock"` 等口径不一致，建议统一。
16. Gate 仅内存存储（`GateService._gates` dict，进程级单例），重启即失，非 DB 持久化——「刷新可见」仅在同进程生命周期内成立。

---

## 11. 是否建议 accepted / 进入 R10

- **不建议 accepted。**
- **不建议进入 R10。** R10 P2-P3 评估/规划的输入基础是 P1 的 `p2_input_manifest.json` + 13 Artifact + profiling_summary——这些产物在当前代码下**运行时完全不生成**。在 P0 缺陷 1（profiler 语法错误）修复前，R10 无可读输入。

---

## 12. 最小返工清单（R9-3F 建议）

按修复成本从低到高、且能快速翻绿链路排序：

1. **修 profiler 语法错误**（1 行）：`full_stack_profiler.py:517` 改写 f-string，避免嵌套转义引号（如预取 `l["language"]`/`l["file_count"]` 到局部变量，或改用 `.format`/拼接）。修后验证 `python -c "import app.services.full_stack_profiler"` 通过。
2. **修 `stage_service.promote` 的 uuid**：将 `import uuid` 提到模块顶部。并使 `promote()` 真正调用 `gate_service.decide()` + 推进阶段，或让前端 GateBanner 改调真实 `/gates/{gate_id}/decision`（二选一并打通）。
3. **统一 Gate 状态词表**：`create()` 用 `waiting_decision`（或令 `get_active` 兼容 `created`），使 active_gate 能被发现、GateBanner 能渲染。
4. **修 `GET /api/coding-agents` 的 session 使用**（generator 误用为 Session）。
5. **`decide()` 闭环**：approve 推进 stage；reject 置 blocked；request_changes 置 changes_requested 并建返工任务/回小循环；非法 decision 返 400（加枚举校验）。
6. **接线 Review Pass** 到 `/onboarding/complete` 与 `/profile`（execute→review→retry≤2→escalate），并补测试。
7. **三模式行为差异 + Auto 授权代理**：`/execute` 等按 mode 走确认/计划/自动 + L0-L3 自动授权输出标准字段；execMode 持久化并加载回读。
8. **修 materialization 诚实性**：保留 deferred/empty 状态，不被 `completed` 覆盖。
9. **补关键链路测试**：profiler 导入 + `/profile` + `/onboarding/complete` + `/promotion-decision` 端到端用例，使 pytest 真正覆盖链路。
10. **同步进度追踪**：施工记录补 R9-3A~F、功能表更新 PRJ/GAT/新增 profiling 行、验收证据索引登记 R9-3/R9-4。
11. （P2）ZIP 逐成员安全解压、local_dir 源目录限制、去除静默吞错。

修复后应能由验收 Agent 在一轮内复验（多数为运行时小修，profiler 与 promote 两个 500 一旦修复，链路即可真实跑通）。

---

## 13. 验收边界声明

本轮为独立验收，未替施工 Agent 补任何代码、证据或文档。验证期间在 gitignored 的 `工作区/` 与测试 DB 中创建的临时项目已清理；为产出浏览器证据临时安装的 Playwright 依赖已通过 `git checkout` 还原 `package.json`/`package-lock.json`。本报告所有结论均以实读代码、实跑命令、真实 API 响应、浏览器实测与文件系统/Trace 实证为依据。
