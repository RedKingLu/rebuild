# R9-5 移交：完工进度汇总 + 按序施工交接提示词 + 进度跟踪表

> 文件路径：`交接/当前/R9-5-移交-完工进度与按序施工交接提示词.md`
> 文件身份：**交接材料**（协作传递，不自动成为项目文档）
> 移交人 / Agent：Claude (opus-4-8) / R9-5 施工（本窗口）
> 移交日期：2026-06-28
> 接手对象：新窗口 Agent（接手 R9-5 历史阻塞专项修补工程的后续施工）
> 移交原因：本窗口施工**乱序**（在 R9-5-1/2/5/7/8 五个子期次各做了一块、无一完整收口），需新窗口按子期次顺序重新有序施工。

---

## 0. 给新 Agent 的第一句话（必读）

```
你不能依赖任何旧窗口记忆。先按 AGENTS.md 启动序列：
1) 读 /home/king/rebuild/AGENTS.md
2) 加载 Skill：skills/R-建设执行/rebuild-work-guidelines/SKILL.md
3) 读本交接材料（你正在读）
4) 读 R9-5 计划与基线（见 §3 资料路径）
本工程是 R9-5 历史阻塞专项修补工程（D-084），目标：把历史遗留的占位/mock/绕过设计/部分实现真实化落地。
本窗口的教训：不要"做下一件有用的事"，要"做计划里的下一期，并完整收口再进下一期"。
```

---

## 1. 项目上下文（自包含）

- **平台**：rebuild（V26.1.1）；软件重构/迁移平台，首期信创迁移。主编排底座 = **LangGraph（D-037 硬规则）**。
- **当前工程**：**R9-5 历史阻塞专项修补工程**（D-084 立项）。P0-P6 主流程；P 阶段晋级=用户 Gate；No Evidence No Completed；密钥全程脱敏。
- **R9-5 全量真实化原则（D-084）**：除明确划给 R10-R17 的内容外，所有功能在 R9-5 完整真实化，不得后延；R10-R17 可顺手提前的允许提前。
- **代码根**：`backend/`（FastAPI + uv，Python3.12）、`frontend/`（React+Vite+TS）。
- **测试基线**：`cd backend && uv run pytest -p no:cacheprovider -q` —— 当前 **199 passed, 2 skipped, EXIT=0**（约 66s）；前端 `cd frontend && npm run build` **EXIT=0**。
- **⚠ 运行实例 DB 注意**：本窗口给 Project 表新增了列（`model_strategy_mode`/`global_model_ref`）。**运行中的 dev 实例 DB 需 drop+recreate 才暴露新列**（隔离测试已用 drop+recreate 覆盖，但运行实例未迁移，以免清空 DB 凭据）。新 Agent 启动运行实例做联调/浏览器测前，需处理 DB 迁移（recreate 或 alembic），并注意会清空 DB 内凭据（user_strategies.yaml 是文件不受影响）。

---

## 2. 完工进度汇总表（诚实，含乱序实情）

> 真相：本窗口**没有完整收口任何一个子期次**，而是跨 5 个子期次各做了一块。下表按子期次列出"已做 / 未做 / 证据"。

| 子期次 | 计划范围 | 已做（真实+测试绿） | 未做（缺口） | 证据/文件 |
|---|---|---|---|---|
| **R9-5-1** LangGraph 地基+P0/P1 | 见 01 材料 | 真实 LangGraph spine（state/checkpoint/stage_loop/stage_reports/nodes/graph/runtime）+ P0/P1 真实业务节点 + 真实 GateBackend（DB Gate）+ 图驱动 HTTP 端点（/graph/start·resume·state）+ checkpoint/interrupt/resume | **阶段C-routes**：旧 onboarding/complete·execute·profile 仍走旧内联路径，未切到图；**阶段D**：gate 决策→graph.resume 未统一；run 生命周期未图驱动；真实事件推送 bus 未做 | `backend/app/graph/*.py`、`routes_graph.py`、`tests/test_graph_{spine,p0p1,api}.py`；中间施工报告 `产物/草稿/R9-5-施工总规划/R9-5-1-中间施工报告-阶段AB.md` |
| **R9-5-2** 模型网关统一 | 见 02 材料 | LLM 调用加硬超时（杜绝挂死）；`resolve_call_target` 单一事实源；agent_loop 去硬编码改走策略 | **真流式（网关 stream 仍伪实现）**、**fallback-on-failure 未接线**、**多作用域优先级（Task/Agent/Project/System）未完整**、统一 Key 解析（自检 vs 调用分叉）未处理、去 dev fallback 主密钥未做 | `litellm_adapter.py`(_REQUEST_TIMEOUT)、`model_gateway.py`(resolve_call_target)、`agent_loop.py`、`tests/test_model_strategy.py` |
| **R9-5-3** 上下文真实组装 | 见 03 材料 | **未开始** | 全部：context_assembler 接入执行链路、SKILL.md 正文注入、context_recipe/C0-C6、按阶段选 Agent+Skill+Tool 组装 | — |
| **R9-5-4** 资源运行回路 | 见 04 材料 | **未开始** | 全部：Registry→运行时调度、Tool 资源化调用、MCP tools/call、案例/知识注入、社区导入审核闭环、三阶优先级 | — |
| **R9-5-5** 外部平台 OpenCode | 见 05 材料 | opencode 去硬编码改走策略（resolve_model_defaults） | **上下文/材料注入、文件读写中介、命令执行审核、反馈契约（ACP）** 全未做；qcode/Aider stub | `opencode_server.py`(resolve_model_defaults)、`opencode_adapter.py` |
| **R9-5-6** 执行隔离与远程 | 见 06 材料 | **未开始** | 全部：构建 sandbox 镜像、container 模式验证、远程执行基础设施（SSH 真实执行+凭据注入） | — |
| **R9-5-7** 引导+执行模式+HITL | 见 07 材料 | D-098 模型策略 UI：引导界面 global/custom+全局模型下拉、资源Agent界面 custom 每-Agent 模型下拉+回退说明（前端 build 绿） | **引导提交方式本地/远端 Git 可写、模型配置落 Project 绑定（部分已做）、执行模式落库且与顶栏同源、Manual 接 P121 HITL、三模式真实差异** 未做 | `OnboardingWizard.tsx`、`ResourcesPage.tsx`(AgentModal) |
| **R9-5-8** 状态真实性 | 见 08 材料 | 删 placeholders.py、event_service 去 mock 定时器、graph_status 真实探测、get_event 诚实 404 | **工作区"产出代码"目录、precheck、StagePageP1 识别清单单一源、Evidence Gaps 读 manifest、SSE 真实事件+前端订阅、消灭静默 except、来源标签修正、物化失败不标 ready、去 mock/data.ts（D-097 全量）** 未做 | `event_service.py`、`graph/runtime.py`(probe)、`workspace_service.py`、`routes_health.py`、`routes_events.py` |
| **R9-5-9** 收口与独立验收 | 见 09 材料 | **未开始** | 全部：全量回归、独立 Acceptance、金标准 4 场景 Playwright、追踪同步、DOC-1~4、归档 | — |

**横切决策（已登记，跨子期次）**：
- **D-097** 全平台去 Mock + 失败不得回退 Mock（强红线）——event_service 已部分落实，**前端 mock/data.ts 全量清理未做**（归 R9-5-8）。
- **D-098** 模型策略体系（单一动态源/全局统一·自定义/可用性回退）——**后端已完整 + 前端两界面已做**，仅**自动模式占位**未真实化（用户认可后置）。动态源 = `user_strategies.yaml` override（用户经平台 UI 配置），出厂 yaml 仅 bootstrap 不固化用户选择。

---

## 3. 资料路径（必读事实源）

```
硬规则/工作流：AGENTS.md ; skills/R-建设执行/rebuild-work-guidelines/SKILL.md
四份已接收基线报告（产物/草稿/）：
  R9-设计文档驱动的用户使用全流程-应然基准-v0.1.md（应然）
  R9-实际代码状态用户操作全链路追踪-v0.1.md（实际）
  R9-中立重设计与验证规范-v0.1.md（应该+怎么验证）
  R9-全平台阻塞点与待完成项摸排-v0.1.md（阻塞+待完成；5 根因 B-ORCH-01~05）
R9-5 施工计划（产物/草稿/R9-5-施工总规划/）：
  00-总规划.md（子期次划分 + R10-17 路线 + 七节模板 + 贯穿验收 G1-G10）★必读★
  01~09-R9-5-{1..9}-*.md（各子期次材料：任务清单/实施路径/验收方法）
  10~17-R10~R17-*.md（后续期材料）
  R9-5-1-中间施工报告-阶段AB.md（本窗口已做的 R9-5-1 进展）
决策记录：文档/00-项目治理/01-决策记录.md（D-084~D-098 为本工程相关）
进度追踪：证据/进度追踪/{01 功能表,02 阻塞项,03 待确认项,04 施工记录,05 验收索引}
  —— 02 含 B-ORCH-01~05；03 含 Q-MODEL-1/2 已澄清；04 含 REC-R9-05/REC-R9-5-1/REC-R9-5-2-FOUND
```

---

## 4. 按序施工指引（核心：恢复纪律）

```
铁律：① 按 00-总规划 §2 的子期次顺序施工；② 每期完整做到其材料的 DoD + 验收方法（V1-V6）+ 独立验收，
      收口后才进下一期；③ 不得再"跳着做下一件有用的事"。
依赖（00-总规划 §2）：R9-5-1/2/6 无前序可并行；3 依赖 1,2；4 依赖 3；5 依赖 1,6；7 依赖 1；8 依赖 1,3；9 依赖全部。

【建议的第一步——先对账再施工】（强烈建议，避免重复本窗口错误）：
  Step A. 只读盘点：对照 §2 完工汇总表，逐项核实本窗口已做代码现状（git diff / 读文件），
          确认每期"已做/缺口"无误，更新到下方 §6 进度跟踪表。
  Step B. 与用户确认"重排还是顺序补齐"（本窗口遗留待确认，见 §7）：
          - 因 D-097/D-098/LLM 修复是中途横切、半成品散落多期，建议先小幅更新 00-总规划，
            把已做部分归位、明确每期剩余 DoD，再按序逐期收口。
  Step C. 从"最早未完整收口的子期次"开始（当前是 R9-5-1），把该期范围内剩余工作补齐 + 验收 + 收口，
          再进 R9-5-2…依此类推。已提前做的 R9-5-2/5/7/8 partial 在到达各期时并入收口。

【R9-5-1 收口要点】（你接手后第一个要收口的期）：
  - 难点：阶段D 的 gate 决策→graph.resume 与 R9-5-7 的 P121 HITL 强耦合（STOP-4）。
    这是本窗口乱序的根源——需先与用户对齐：R9-5-1 收口是否包含"旧端点切图+gate→resume"，
    还是把这部分明确移到 R9-5-7 并据此调整 R9-5-1 的 DoD。不要再含糊带过。
  - 现状：图路径已建+测试绿+启动注册，但 live app 的 P0/P1 仍走旧内联路由（未切换）。
```

---

## 5. 关键决策与红线（不可违背）

```
D-037 LangGraph 主编排（已在 spine+P0/P1 真实落地，禁止回退自建状态机）
D-084 R9-5 全量真实化（除 R10-R17 外不得后延）
D-085 P0/P1 真实 LangGraph 节点
D-086 引导可写 + 本地/远端 Git 两提交方式
D-087 Manual 确认/拒绝 接 P121 HITL
D-088 外部平台分阶段可选接入（自有全包/外部；外部范围 仅编码 or 全环节）
D-089 社区在线来源预留接口、当前静态
D-090 容器执行 sandbox R9-5 建设
D-091 每阶段(P0-P6)小循环
D-092 阶段三类审核报告（起始计划/中间施工/验收）
D-093 远程执行基础设施 R9-5 / 远程资源调用编排 R14
D-094 项目创建入口收敛 ZIP/Git
D-097 全平台去 Mock + 失败不得回退 Mock（直接展示失效项）★强红线★
D-098 模型策略：单一动态源 + 全局统一/自定义 + 可用性回退；禁任何调用点硬编码模型/endpoint ★
通用红线：P 阶段晋级用户 Gate；No Evidence No Completed；密钥全程脱敏；不得静默 except；不得 mock 冒充真实。
```

---

## 6. 进度跟踪表（接手后维护——每期收口即更新）

> 状态枚举：未开始 / 进行中 / partial(部分,本窗口遗留) / 已收口(accepted)。新 Agent 每完成一期更新此表 + 证据/进度追踪/04 施工记录。

| 子期次 | 状态 | 完成度 | 本期剩余 DoD（缺口） | 收口验收证据 | 负责窗口/更新时间 |
|---|---|---|---|---|---|
| R9-5-1 LangGraph地基+P0/P1 | partial | ~60% | 旧端点切图、gate→resume(与R9-5-7对齐)、run图驱动 | 待补 | 本窗口 2026-06-28 |
| R9-5-2 模型网关统一 | partial | ~40% | 真流式、failure-fallback、多作用域优先级、统一Key解析、去dev主密钥 | 待补 | 本窗口 2026-06-28 |
| R9-5-3 上下文真实组装 | 未开始 | 0% | 全部 | — | — |
| R9-5-4 资源运行回路 | 未开始 | 0% | 全部 | — | — |
| R9-5-5 外部平台OpenCode | partial | ~20% | 上下文/文件/命令/反馈契约；qcode/Aider | 待补 | 本窗口 2026-06-28 |
| R9-5-6 执行隔离与远程 | 未开始 | 0% | 全部 | — | — |
| R9-5-7 引导+执行模式+HITL | partial | ~30% | 本地/远端Git可写、执行模式落库、Manual接HITL、三模式差异 | 待补 | 本窗口 2026-06-28 |
| R9-5-8 状态真实性 | partial | ~25% | 产出代码目录、precheck、识别清单单一源、SSE真实事件+前端订阅、去mock/data.ts全量、消灭静默except | 待补 | 本窗口 2026-06-28 |
| R9-5-9 收口与独立验收 | 未开始 | 0% | 全部 | — | — |

> 完成度为本窗口诚实估计，新 Agent 盘点后可校正。

---

## 7. 待确认项（移交给新 Agent + 用户裁决）

```
Q-SEQ-1【最高】恢复施工秩序方式：重排计划(A) / 严格顺序补齐(B) / 先盘点再定(C)。
  本窗口建议 C→A。新 Agent 接手第一动作即处理此项（先盘点+对账，再与用户定 A/B）。
Q-SEQ-2 R9-5-1 收口边界：是否包含"旧端点切图+gate→resume"，还是移交 R9-5-7？（STOP-4 耦合，需用户拍板）
Q-MODEL（已澄清,备案）：模型默认动态来自 user_strategies.yaml override；运行实例 DB 需迁移暴露 Project 新列。
D-098 自动模式：当前占位，用户认可后置。
```

---

## 8. 禁止事项（本窗口踩过的坑，新 Agent 勿重蹈）

```
1. 不要"做下一件有用的事"——要做计划里的下一期，且完整收口再进下一期。
2. 不要把模型策略/默认模型硬编码进出厂 yaml——动态引用用户真实配置（D-098）。
3. 不要 mock 冒充真实、失败回退 mock（D-097）。
4. 不要静默 except（公理3 失败必发声）。
5. 改 Project/Agent 表列后，运行实例需 DB 迁移；隔离测试用 drop+recreate 已覆盖。
6. 编辑追踪/决策文件用唯一锚点，勿用 replace_all（本窗口曾误伤重复块）。
7. LLM 调用必走 ModelGateway 且有超时；本沙箱对 maas 推理可能不响应（超时已防挂死）。
```

---

## 9. 验证方法（每期收口必做）

```
后端：cd backend && uv run pytest -p no:cacheprovider -q   # 期望全绿且 EXIT=0（当前 199 passed/2 skipped）
前端：cd frontend && npm run build                          # 期望 EXIT=0
分层验收：见各子期次材料 §5（V1 服务/V2 API契约/V3 持久化恢复/V4 端到端/V5 证据/V6 负路径）
贯穿验收：00-总规划 §5 G1-G10
浏览器级：R9-5-9 金标准 4 场景 Playwright（需先处理运行实例 DB 迁移）
```

---

> 交接材料结束。新 Agent 接手后：先按 §0 启动 → §4 Step A 盘点对账 → 更新 §6 进度跟踪表 → 处理 Q-SEQ-1/2 → 从 R9-5-1 起按序收口。
