# R9-5 真实收口报告 — 独立验收 Agent（亲手驱动平台版）

> 文件路径：`交接/当前/R9-5-真实收口报告.md`
> 文件身份：**交接材料 / 独立验收报告**（Acceptance Agent 出具，非施工 Agent）
> 验收 Agent：Claude（opus-4-8）— **未参与 R9-5-1~9 任一子期施工**（落实 D-082）
> 验收日期：2026-06-29
> 验收对象：R9-5 历史阻塞专项修补工程全量（R9-5-1~9 + G1-G10）
> 验收依据（事实源）：
> - 应然基准：`产物/草稿/R9-设计文档驱动的用户使用全流程-应然基准-v0.1.md`
> - 实际追踪：`产物/草稿/R9-实际代码状态用户操作全链路追踪-v0.1.md`
> - 验证规范：`产物/草稿/R9-中立重设计与验证规范-v0.1.md`（V1-V6 + 金标准场景）
> - 阻塞摸排：`产物/草稿/R9-全平台阻塞点与待完成项摸排-v0.1.md`（B-ORCH-01~05）
> - 决策：D-082（独立验收）、D-074（最小联调）、D-084（R9-5 全量真实化）、D-085（P0/P1 真实节点）、D-037（LangGraph 主编排）
> 验收方法：**亲手把当前代码跑起来，真实驱动 P0 端到端流程**（区别于上一轮纯代码级 pytest+grep）+ 独立重跑 pytest + 独立 grep + 真实落点核查

---

## 0. 验收身份与方法声明（D-082 合规）

```
本人未参与 R9-5 任一子期施工。本报告的核心证据不是"读代码 + 跑测试"，而是：
  ① 用当前代码（editable 安装）真实启动后端实例（127.0.0.1:8010，cwd=backend）
  ② 通过真实 HTTP API 驱动 P0 全流程（创建→onboarding/complete→execute→Gate→批准→P1）
  ③ 逐项核查真实落点（磁盘产物 / DB 行 / Evidence 存储 / Gate 字段）
  ④ 主动跑负路径（git 无凭据 / manual 空源 / 重复触发）
施工 Agent 的自验结论与"测试绿"均仅作参考，不被直接采信。
所有判定标注证据来源。凡涉及凭据一律脱敏。
```

> **用户核心命题的回答**："平台现在能否真实开始 P0？" —— **能（理想路径），但有真实运行期缺陷（见 §3）。** 详见 §2.1。

---

## 1. 独立基线核查（参考）

| 项 | 结果 | 性质 |
|---|---|---|
| 后端 pytest（独立重跑） | **397 passed / 2 skipped / 0 failed，EXIT=0**（160.6s，含真实 LLM 测试） | 仅参考 |
| 前端 `dist/` | 存在（构建于 2026-06-29 08:47） | 仅参考 |
| 代码 Git 状态 | **R8 收口 commit（0826297）后全部未入 Git**（大量 M/D 未提交） | 收口阻断项 |
| 运行实例（8000 端口） | 陈旧 R4 进程（当前源码已无其 `R4 provides API skeleton` 文案以外的旧逻辑），与当前代码不同步 | 环境观察 |

> **关键纪律印证**：pytest 397 全绿，但我亲手驱动平台仍发现 4 类运行期缺陷（§3）。**"测试绿 ≠ 平台真实可用"** —— 正是用户要求"亲自跑、别只信测试"的价值所在。

---

## 2. 亲手驱动 P0 的真实实证

> 实例：当前代码后端 `127.0.0.1:8010`（进程 cwd 经 `/proc/<pid>/cwd` 确认 = `backend`，editable 安装确认运行当前源码）。

### 2.1 P0 理想路径（local_dir，3 个真实源码文件）—— ✅ 真实跑通

| 步骤 | 真实结果 | 落点证据 |
|---|---|---|
| 创建项目 `local_dir`（指向 /tmp/rb_src，3 文件） | `project_id=d4316276…`，`source_status=real / capability_status=available` | DB `project` 行真实写入 |
| `POST /onboarding/complete` | `onboarding_done=true`；`materialized={success:true, file_count:3}`；生成 **4 个产物**；`review.passed=true` | — |
| 产物真实落盘 | `工作区/projects/d4316276…/artifacts/{intake_report.json, p0_execution_record.json, p0_construction_report.md, p0_review_pass.json}` **真实存在** | V1 ✅ 磁盘文件 |
| `POST /onboarding/execute`（SSE） | `source_check(3文件)→artifact_check(4/4)→review(通过)→gate_creation` → **`event: complete` 携 `gate_id=gate-154021`** | SSE 真实流 |
| Gate DB 落点 | `p_gate` 行：`stage=p0, gate_status=waiting_decision, transition_mode=real` | V1/V3 ✅ DB |
| `POST /projects/{id}/gates/{gid}/decision {approve}` | `gate_status→approved`；**项目 `current_stage` 由 p0 → p1**，`active_gate` 清空 | V1 ✅ 晋级真实 |

**结论**：**平台能真实开始并走完 P0 理想路径**——源码真实物化、产物真实落盘、Run/Gate 真实持久化、P0→P1 用户 Gate 真实晋级。这是 R9-5 的实质性真实交付，独立确认成立。

### 2.2 manual 空源（合法空态）—— ✅ 符合应然

`source_type=manual` → execute → `file_count=0` 但正确建 Gate（`gate-2861a4`），`workspace_status=ready`。符合验证规范"manual 是合法空态，可建 Gate"。

### 2.3 负路径与落点核查（发现缺陷，见 §3）

| 探测 | 真实结果 | 判定 |
|---|---|---|
| github 无凭据/不可达仓库 | 物化 `success=false`、`workspace_status=deferred`（不再假标 ready）；execute → `event: error`（issues 为空）；**终态卡在 p0，无 Gate，无恢复动作** | ❌ 见 F-1 |
| Evidence 存储查询 | `onboarding/complete` 返回 2 个 evidence_candidates；但 `GET /projects/{id}/evidence` → `{"evidence":[]}`；Gate 的 `evidence_refs` 指向不存在的对象 | ❌ 见 F-2 |
| 重复触发 P0 | 重复 `complete` → 多 Run；对**已晋级 P1** 的项目重复 `execute` → **又造一个 P0 Gate（gate-9177a2）** | ❌ 见 F-3 |
| 响应 envelope | `GET /api/projects` 的 `meta` = `capability_status:not_connected / "R4...mock/in-memory data" / persistence:volatile`，与同响应 `data.capability_status:available` 自相矛盾 | ❌ 见 F-4 |
| Gate 图字段 | `graph_capability_status=not_connected`，`checkpoint_ref/interrupt_ref/resume_ref` 全 null | ❌ 见 F-5（已知 W8） |

---

## 3. 缺陷清单（亲手驱动发现，分档）

> 四级标准：P0 阻断 / P1 重要 / P2 次要 / REC 建议。**F-1~F-4 是上一轮代码级验收（pytest+grep）未发现的真实运行期缺陷。**

### 3.1 P0 / P1 缺陷

| 编号 | 缺陷 | 证据 | 违反 | 影响 |
|---|---|---|---|---|
| **F-1 [P0]** | **git/github 无凭据 → P0 卡死、无恢复路径**。物化诚实失败（deferred✅），但 execute 的 Review 失败仅发 `event:error`（issues 空，未透出"源码为空/去补凭据"具体原因），不建 Gate，终态停在 p0，无重试入口、无引导动作 | 亲手驱动 §2.3 | 验证规范 V6（git 失败须"创建阶段阻断 或 P0 明确去补凭据动作"，二者皆未满足）；实际追踪 P0-1 | **凡 git 来源凭据未就绪的项目，用户无法开始 P0，且无法自救** |
| **F-2 [P1]** | **Evidence 未落可查询存储**。evidence_candidates 仅内存返回，`GET /evidence` 为空；Gate 的 evidence_refs 引用空对象 → Gate 审材 Evidence 链断裂 | 亲手驱动 §2.3 | 验证规范 V5 / 应然§四4.3（"Evidence 必须落可查询存储，绝不用内存假 list 冒充"）；实际追踪 P1-3 | 用户在 Gate/检视区看不到真实 Evidence，"无 Evidence 不得 completed"红线名存实亡 |
| **F-3 [P1]** | **P0 幂等性缺失**。重复 complete 造重复 Run；对已晋级 P1 的项目重复 execute 又造 P0 Gate，可将状态拉回 | 亲手驱动 §2.3 | 验证规范 V6（重复触发不得产生重复 Run/Gate/Artifact） | 状态完整性破口；前端误触/重连即可制造脏 Gate |
| **F-4 [P1]** | **响应 envelope 真实性污染**。`app/schemas/common.py` 的 `Meta` 默认 `capability_status="not_connected" / not_connected_reason="R4...mock/in-memory data" / persistence="volatile"`，污染所有未显式覆盖 Meta 的端点（如 /projects） | grep + 亲手驱动 §2.3 | G7 真实性单调；D-097（去 mock）；上一轮 G7 仅查 workspace_service.py 漏此 | 真实 DB 持久化能力对外宣称"未连接/mock/易失"，自相矛盾，前端可能据此误显 |
| **F-5 [P1·已知]** | **运行实例 G1 不成立**：LIVE P0 走旧内联路由（routes_projects.py），不经 LangGraph；Gate 的 graph/checkpoint 字段全空。测试环境 `/graph/start` 中 G1 成立 | Gate 字段 + 代码 | D-037/D-085（W8 延期，用户已裁决延 R10） | 平台主编排红线在用户真实路径上未生效 |

### 3.2 P2 / 收口缺陷

| 编号 | 缺陷 | 证据 |
|---|---|---|
| **F-6 [P2]** | 静默 except 残留：typed `except …: pass` 共 10 处（gate_service×3 / source_materializer×2 / knowledge_search×2 / opencode_acp×2 / main×1）。无裸 `except:`（✅），但仍有静默吞错 | grep |
| **F-7 [P0·收口]** | **G10 金标准 Playwright 4 场景缺失**：`frontend/e2e/` 仅有 `r9_acceptance.mjs`，`r959-s{1..4}*.spec.ts` 不存在 | 文件核查 |
| **F-8 [P2·收口]** | **代码自 R8 后全部未入 Git**（收口六步含 git 提交，未执行） | git status |
| **F-9 [P2·收口]** | 交接材料 stale：`R9-5-移交…` 误标 R9-5-3/4/6 "未开始 0%"（实际已收口）；功能表 LNK-01 状态滞后 | 文件核查 |

### 3.3 独立确认为真实交付（非缺陷，R9-5 实质成果）

LangGraph spine + checkpoint/resume（测试环境真实）；模型调用全经网关、grep 无直连 litellm；上下文/资源/工具/案例/知识装配代码路径真实；P0 理想路径端到端真实（§2.1）；物化失败诚实标 deferred；**无 Key 泄露、无裸 except**；pytest 397 全绿。各子期 GAP 多数诚实登记。

---

## 4. R9-5 真实完成进度（诚实口径）

| 维度 | 应然 | 真实状态 | 完成度 |
|---|---|---|---|
| P0 接入（理想路径 local/zip/manual） | 真实编排+产物+Evidence+Gate | ✅ 真实（Evidence 落库除外） | ~85% |
| P0 负路径（git 失败分流） | 创建阻断 或 P0 给恢复动作 | ❌ 卡死无恢复（F-1） | ~30% |
| Evidence 真实落库 | 可查询存储 | ❌ 内存假 list（F-2） | ~20% |
| P0 幂等 | 重复不产生重复对象 | ❌ 缺失（F-3） | 0% |
| 响应真实性（envelope） | 真实状态 | ❌ Meta 默认污染（F-4） | 局部 |
| LangGraph 运行实例编排（G1） | LIVE 走图 | ❌ W8 延 R10（F-5） | 测试环境✅/运行实例❌ |
| 网关统一 / 上下文 / 资源装配 | 真实 | ✅ 代码级真实 | ~90% |
| 安全（脱敏/无裸 except） | 全满足 | ✅ | 100% |
| G10 金标准端到端 | 4 场景 Playwright | ❌ 缺失（F-7） | 0% |

---

## 5. 收口判定

### 5.1 总判定：**◐ 有条件通过（Conditional Pass）**

```
R9-5 在"P0 理想路径真实可跑通 + 编排/网关/上下文/资源代码级真实 + 安全红线满足"上，
是一次实质性的真实交付，远超占位/mock 阶段，独立确认成立。

但 D-084 明确要求"R9-5 全量真实化，不得以后续补齐/R10 再做绕过本应在 R9-5 完成的真实化"。
亲手驱动发现的 F-1（git 卡死）/F-2（Evidence 假数据）/F-3（幂等）/F-4（envelope 污染）
均属 P0-P1 真实链路范围、应在 R9-5 完成，且未被上一轮代码级验收发现。
加上 F-5（运行实例 G1）、F-7（G10 缺失）、F-8（未入 Git），收口六步未执行。

因此 R9-5 不可直接标 accepted。建议以"◐ 有条件通过收口"承接：
确认已交付的真实成果，将 F-1~F-9 登记为缺口，由 R9-6 专项修复后再标 R9-5 accepted（或随 R9-6 一并 accepted）。
```

### 5.2 收口六步状态（Skill §2.4）

| 步骤 | 状态 |
|---|---|
| ① 登记文档状态 | ◐ 本报告 + 缺口报告登记 |
| ② 归档交接材料 | ⏳ 待 R9-6 收口统一归档 |
| ③ 归档产物草稿 | ⏳ 同上 |
| ④ git 提交 | ❌ R8 后全部未提交（F-8） |
| ⑤ 清空当前区 | ⏳ 待 R9-6 |
| ⑥ 追踪一致性检查 + 更新追踪文件 | ⏳ 本轮更新功能表/缺口；完整 13 项检查待 R9-6 |

### 5.3 与上一轮独立验收（deepseek-v4-pro，2026-06-29）的差异

| 项 | 上一轮（代码级 pytest+grep） | 本轮（亲手驱动平台） |
|---|---|---|
| P0 能否真实开始 | 未实跑，仅推断 | **实跑确认理想路径✅** |
| F-1 git 卡死 | 未发现 | **实跑发现** |
| F-2 Evidence 假数据 | 未发现（G3 标✅） | **实跑发现存储为空** |
| F-3 幂等 | 未涉及 | **实跑发现重复 Gate** |
| F-4 envelope 污染 | 未发现（G7 仅查 workspace_service） | **实跑发现 Meta 默认污染** |
| G10 | 发现缺失（P0-01） | 一致确认缺失 |

---

## 6. 下一步

进入 **R9-6**（详见 `R9-6-规划报告.md`）：专项修复 F-1~F-6 运行期缺陷 + 执行 G10 金标准 + 收口六步，最终闭合 R9（P0-P1 真实链路）。统一缺口视图见 `R9-现状缺口报告.md`。

> **本报告结束。** 独立验收，亲手驱动取证，施工自验未直接采信。D-082 合规 ✅ | D-074：代码级 ✅ / 浏览器级 ❌（F-7）
