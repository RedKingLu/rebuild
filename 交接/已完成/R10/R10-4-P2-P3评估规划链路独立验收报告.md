# R10-4 P2-P3 评估规划链路独立验收报告

文件路径：交接/当前/R10-4-P2-P3评估规划链路独立验收报告.md
文件身份：独立验收报告（D-082 合规：本 Agent 未参与 R10-3 任何施工）
验收 Agent：Claude (opus-4-8[1M])，R10-4 独立验收 Agent
验收日期：2026-07-02
验收对象：R10-3 施工结果（T0–T21：横切底座 TaskGraph/NodeLoop/Acceptance + P2 评估 + P3 规划 + 前端 StagePageP2/P3 + 两道 Gate），及其对既有 P0/P1 主链路的影响
最终结论：**rework_required（返工）**

---

## 0. 独立验收声明

> 我执行 rebuild R10-4 独立验收。我未参与 R10-3 施工，不采信施工 Agent 自验结论作为验收结论。本报告全程亲手启动隔离服务、亲手发起真实 HTTP、亲手用浏览器(Playwright)驱动真实前端、亲手检查后端数据/产物/Evidence/Trace/Audit/ModelGateway 调用与代码。测试通过不等于验收通过。全程未输出/写入任何 Key/Token/Secret/.env 明文；测试使用隔离 DB/Workspace，未污染用户数据。

**方法**：
- 独立隔离后端 :8000（`REBUILD_DATABASE_URL`=临时 DB 拷贝、`REBUILD_WORKSPACE_DIR`/`REBUILD_DATA_DIR`=临时目录；`.env` 提供真实 LLM Key，仅判存在不读值），前端 :5173。
- 真实 git clone（`file:///tmp/legacy-erp-src`，自建 Oracle+WebLogic+JSP 信创迁移样例仓库）驱动 P0→P1→P2→P3。
- 真实 LLM（deepseek-official，configured/available）驱动 P2 评估与 P3 规划。
- 代码级 grep/read 核查 + 独立子代理复核。

---

## 1. 已读取材料

- 硬规则/流程：`AGENTS.md`、`skills/R-建设执行/rebuild-work-guidelines/SKILL.md`（Skill 已加载）
- R9 基线：`交接/已完成/R9/R9-5+R9-6-联合验收报告.md`（R9 accepted，407/2/0 基线，P0/P1 真实）
- R10 规划与前两轮：`交接/当前/R10-交接提示词.md`、`R10-1-实际状态核查报告.md`、`R10-2-历史吸收报告.md`
- R10-3 施工交接：`R10-3-续施工交接提示词.md`、`R10-3-续2-施工交接提示词.md`
- 进度追踪：`证据/进度追踪/04-施工记录.md`（REC-R10-1/2/3，含 T0–T21 落点与自验）
- 治理：`文档/00-项目治理/`（版本 V26.1.1、阶段体系）

---

## 2. 验收环境与服务状态

| 项 | 实测 |
|----|------|
| 后端 | :8000，`GET /api/health` → `{"status":"ok","platform":"rebuild","version":"V26.1.1","r_stage":"R10"}`（r_stage 已由 R4 更正为 R10，T12 生效 ✅） |
| 前端 | :5173，HTTP 200，标题 `rebuild · 软件重构与迁移平台` |
| DB | 隔离拷贝（自 dev `rebuild.db`，alembic head=`e5f6a7b8c9d0`），Workspace/Data 用 `/tmp` 隔离目录 |
| ModelGateway | `deepseek-official`：credential_status=configured、key_source=env、status=available、capability_marker=real_available（Key 未读取/未输出） |
| 代码状态 | R10-3 全量改动**未 git commit**（收口 T22 前统一提交，符合交接约定）；git head=`438628f`(R9 收口) |

> ⚠️ 迁移可移植性观察（REC）：对**全新空 SQLite** 执行 `alembic upgrade head` 失败于 `6f7a8b9c0d1e_005_skill_category_and_enabled`（`ALTER COLUMN ... SET NOT NULL` SQLite 不支持，未用 batch_alter）。属 R6 期历史迁移，非 R10 引入；测试与本验收改用 create_all/DB 拷贝规避，不阻断，但影响"全新环境一键起库"。

---

## 3. 测试命令结果

### 3.1 pytest
`cd backend && uv run pytest -p no:cacheprovider -q` → **511 passed, 2 skipped, 0 failed（318.71s）**。独立重跑与 R10-3 自验声明 511/2/0 **一致**。2 skipped = 依赖有效 LLM Key 的真实调用测试。

### 3.2 frontend build
`cd frontend && npm run build` → **✓ built（EXIT 0）**。仅非阻断告警（chunk >500KB、projectService 动态/静态混合导入提示）。

### 3.3 说明：测试通过不等于验收通过
pytest 全绿 + build 成功仅证明基础回归未破坏。R10 是否通过取决于真实浏览器链路、真实 clone、真实 ModelGateway、真实产物/Evidence 与负路径诚实——见下。**关键：R10-3 自验的集成测试全部使用离线 fake 网关；施工 §959 亦自述前端联调"用真实端点演示数据、非 LLM 真跑产物"。即 P2/P3 真实 LLM 完整链路此前无人验证——本报告首次真实驱动。**

---

## 4. 浏览器端到端主链路验证

浏览器：独立 Playwright 脚本 `frontend/e2e/r104_acc_browser.mjs` / `r104_ws.mjs`（自编，未采信施工 e2e）。截图存 `/tmp/r104_shots/`。

### 4.1 创建项目
- 前端 `/projects` 列表真实渲染，含本验收创建的 `信创迁移验收-ERP` 项目（真实后端数据，非 mock）。
- `/projects/new` 创建页真实渲染 3 个输入控件 + `创建项目`/`取消` 按钮，可填写。
- API 创建 git 源项目 → `source_type=git, workspace_status=ready, source_status=real, persistence=durable`。

### 4.2 真实源码接入 / clone 验证（S2）✅
- 后端真实执行 `git clone file:///tmp/legacy-erp-src`（后端日志：`ok=True, Cloned ...(branch=main)`）。
- Workspace `projects/{pid}/source/` 落真实源码：`README.md / pom.xml / src/main/java/com/acme/erp/{OrderDao,WlsJmsSender}.java / src/main/webapp/order.jsp / src/main/resources/weblogic.xml`（非空、非 mock fixture）。
- `artifacts/source_index.json` 真实生成。
- 另：真实**远程 https clone**（github 公共仓库）连通性探测通过（0.16s，HTTP 可达），佐证 clone 代码路径对远程亦有效。

### 4.3 P0 接入 ✅
- graph 驱动 `/onboarding/complete` → 建 P0→P1 Gate（返回 run_id、graph_driven）。
- P0 产物落盘（intake_report/p0_start_plan/p0_construction/p0_acceptance/p0_execution_record/p0_review_pass）。
- Evidence：`ev-p0-ws-* / ev-p0-onb-*` 落库；Trace/Audit（`.rebuild/traces.jsonl`/`audits.jsonl`）真实写入。

### 4.4 P0→P1 Gate ✅
- 前端 GatePanel 经 `/promotion-decision` 决策；批准后 graph resume 自动运行 P1。
- P0→P1 Gate 对象含真实 `artifact_refs`（附审材料可读）。

### 4.5 P1 建档 ✅（真实产物）
- 前端 P1 页真实渲染：技术栈"主要语言 Java、Java(2)/Unknown(.jsp)(1)、构建系统 maven"；12 识别项全 ✅。
- 后端 `tech_stack.json` 正确识别 Java/JSP/Maven；`p2_input_manifest.json` 引用真实 P1 制品。
- 观察（非 R10，REC）：`dependency_draft.json` deps=0，未从 pom.xml 解析 Oracle(ojdbc8)/WebLogic 依赖——P1(R9 accepted)剖析深度局限；P2 LLM 仍可从源码识别。

### 4.6 P1→P2 Gate ✅
- P1→P2 Gate 含 **24 条 artifact_refs**（p1_*/tech_stack/source_structure/p2_input_manifest/dependency_draft/… + profiling_summary.md），附审材料真实可读。
- ⚠️ 该 Gate 对象字段 `graph_capability_status:"not_connected"`（见 §15 P2-C：默认死值，与"图正在真实驱动"不符）。

### 4.7 P2 评估 ✅（真实 LLM，真实有意义产物 — 本轮首次真实验证）
- 批准 P1→P2 Gate → graph resume 同步运行**真实 P2 评估**（真实 LLM 调用，deepseek-v4-flash，经 ModelGateway）。
- 说明：真实 LLM 首次调用出现若干 `litellm timeout retry`（DeepSeek 端偶发慢/限流），但**重试后成功**（非不可用；见 §6/§12）。
- `assessment-summary` 真实内容（项目相关、有依据）：
  - 摘要："基于 P1 档案与 intake 报告…存在关键证据缺口(framework_unknown)及缺失 profiling_summary/environment.json。信创迁移需重点关注技术栈兼容性、依赖关系与基础设施适配，当前评估为高风险等级。"
  - `overall_risk_level: L4`，真实 recommendation。
  - `analysis_only: true`、`model_used: openai/deepseek-v4-flash`。
- 6 类产物落盘（`p2_assessment_report/risk_list/blocker_list/validation_gaps/resource_needs.json`）；风险项均含 `title/risk_level(L2-L4)/source/basis`，source 可追溯至 `p2_input_manifest.json -> pending_evidence_gaps[0]` / 缺失输入。
- 无占位/TODO/模板空文；符合 §4.7 五类 Evidence（见 §11）。P2 完成后 graph 建 **P2→P3 Gate**（current_stage=p2）。

### 4.8 P2→P3 Gate ✅
- P2 完成后自动创建 P2→P3 stage_promotion Gate（waiting_decision）。附审沿用 gate.artifact_refs 机制（同 P1→P2 已验证）。

### 4.9 P3 规划 —— ◐ 受限（代码已验；真实 LLM 三段链未在验收窗口内完成，见 §9）
- 批准 P2→P3 Gate → graph resume 触发真实 P3 规划（stage_plan→task_plans→task_graph 三次串行 LLM 调用）。
- 实测：DeepSeek 端侧对 P3 首个调用(stage_plan)反复 `litellm timeout retry`，验收窗口内(>15min)未成功落库（`stage_plan/task_plan/task_graph/task_node` 表均 0）。属 §2.3 LLM 端侧延迟环境限制——非 P3 代码缺陷（同类慢在 P2 单次调用经重试后成功；P3 需 3× 调用叠加放大延迟）。P3 必生/校验/降级逻辑经代码验证（§10）。
- 判定：P3 真实产物**运行时未完整验证（受限）**，列 R10-5 首验真实复跑项。前端 P3 页在 P3 未执行时诚实显示"P3 规划尚未执行。请先完成 P2 评估并批准 Gate 以触发 P3 规划。"（D-097 ✅）。

### 4.10 P3→P4 Gate / 4.11 P4 stub 边界
- P3 未完成，未观察到 P3→P4 Gate 创建（依赖 P3 completed）。P4 边界经代码+运行时验证：P4 handler 未注册（仅 p0–p3 注册），运行时 p4 stage_status=pending（future_r10 stub），未越界执行 ✅（§13）。

### 浏览器 Console / Network
- Console error：**2 条**，均为 `GET /api/projects/{pid}/workspace → 500`（见 §15 P1-A）；其余页面 0 error。
- Network：`/api/model/status`、`/coding-agents`、`/materials`、`/sessions`、`/files`、`/assessment-summary`、`/planning-summary`、`/profiling-summary` 均 200，真实发生；仅 `/workspace` 500。
- 前端对 500 诚实降级为"离线模式（使用缓存数据）"（D-097 行为正确），但底层 500 是真实缺陷。

---

## 5. 真实源码验证
见 §4.2。git clone 真实、source/ 真实源码、source_index 真实、P1 读取的是该真实源码（tech_stack 反映真实 Java/JSP/Maven）。✅

## 6. ModelGateway 调用验证
- P2/P3 模型调用**经 ModelGateway**：后端日志 `litellm call ...`、产物 `model_used=openai/deepseek-v4-flash`。
- 代码核查（§13）：`assessment_service.py`/`planning_service.py` 仅经 `get_services().model_gateway.call(...)`，`grep litellm|openai|httpx|litellm_adapter` 在这些业务服务中**零命中**，无绕过网关直连 provider，无硬编码 model/endpoint（D-098 ✅）。
- 无 Key 语义：`assessment_service.assess` 在 gateway `overall!=available` 时直接 `return status="blocked"（no_model_key，不降级规则评估）`（代码 :114-121 + 单测覆盖）。本验收环境 Key 存在，走真实调用正路径；blocked 语义经代码+单测验证。
- 模型输出标 `analysis_only=true`，不当作事实 Evidence ✅。安全：调用痕迹与产物**未泄露 Key**（§16）。

## 7. Agent 上下文组装与 Skill 使用验证 —— ❌ 缺口（S4 未满足，见 §15 P1-C）
- RealP0Handler/RealP1Handler.execute **调用** `assemble_context`（stage_handlers.py:61/141）。
- **RealP2Handler.execute(:210) / RealP3Handler.execute(:319) 均未调用 `assemble_context`**；`assessment_service`/`planning_service` 各自 `_gather_inputs`/`_gather_p2_inputs` 直接读 workspace 文件组装模型输入，**绕过统一上下文入口（C0-C6）**。
- P2/P3 **未加载/引用任何 Skill**（`grep skill|resource_loader|SKILL` 于两服务零命中）；使用硬编码 `_SYSTEM_PROMPT`/`_STAGE_PLAN_SYSTEM_PROMPT`/`_TASK_PLAN_SYSTEM_PROMPT`/`_TASK_GRAPH_SYSTEM_PROMPT`（各阶段 prompt 不同，非"同一段 prompt 冒充"，但绕过 context_assembler 的 C5 Skill 层）。
- T7 交付的 `tests/test_context_p2p3.py` 仅**孤立**调用 `assemble_context`/`build_system_prompt` 断言其"能"产 p2/p3 上下文，**未断言 handler 实际调用**——即能力存在+孤立测试通过，但真实 P2/P3 执行路径未接线（D-074 式"写了接口未联调"）。
- 判定：模型输入含真实项目事实（产物真实），但 R10-2 §1.1 T7 所要求的"P2/P3 execute 调 context_assembler"未落地，Skill 未参与。属 §8.3 rework 触发项"Agent 未真实组装上下文或未使用对应 Skill"。

## 8. P2 产物真实性检查 ✅
见 §4.7。评估报告/风险清单(L2-L4，含 source/basis)/阻塞项/验证缺口/资源需求六类真实落盘、与项目源码/P1 识别相关、无空/占位/模板；模型输出标 analysis_only。5 类 P2 Evidence 落库可追溯（§11）。

## 9. P3 产物真实性检查 —— ◐ 受限（未在验收窗口内完整验证）
- 代码级（§10/§13）：`planning_service.generate_stage_plan/generate_task_plans/generate_task_graph` 真实：无模型→blocked、模型失败→failed（公理3）、读 P2 五产出为输入（§5.7-1）、必生 TaskGraph（校验失败降级单链 sequence，degraded=True，绝不入库无效图）、落 stage_plan/task_plan/task_graph/task_node 表；RealP3Handler 串联三段、任一步 blocked/failed 短路、review 缺 stage_plan_ref/task_graph_ref/evidence_refs→fail。
- 运行时：真实批准 P2→P3 后 graph resume 触发真实 P3 三段 LLM 调用；DeepSeek 端侧对 stage_plan 调用反复超时重试，验收窗口(>15min)内未落库（4 表均 0），**未能捕获 P3 真实产物**。
- 判定：**受限（not fully verified）**——非 P3 代码缺陷（P2 单次真实调用已证 pipeline 可用；P3 3× 串行调用叠加 DeepSeek 延迟超窗）。**必须在 R10-5 以真实 LLM 复跑补齐 P3 真实产物证据（StagePlan/TaskPlan/TaskGraph 落库 + Evidence + P3→P4 Gate + P4 保持 stub）**。此受限不改变本轮 rework_required 结论（结论由 P1-A/B/C 独立成立）。

## 10. TaskGraph / NodeLoop / Acceptance 验证
- ORM 真实分表（代码 §13）：`task_graph`(定义)/`task_node`(定义)/`task_graph_run`(运行)/`task_node_run`(运行实例)/`stage_plan`/`task_plan`/`plan_delta`。
- 边策略：`task_graph_service.validate_edge_strategy` 8 维显式校验、缺省显式填充+warning（RK-4 不静默）、高风险边强制 gate_required=True。
- 必生 TaskGraph：`planning_service.generate_task_graph` 校验失败/空边→降级单链 sequence（degraded=True，记 validation_errors），绝不入库无效图（代码），无"无 TaskGraph+理由"路径。
- NodeLoop 9 步真实、Step8 调 `AcceptanceService.accept`（独立实例，Q-R10-4）；TaskGraphEngine 为节点内任务级调度、显式红线不建 StateGraph（不替代 LangGraph）。
- 注：NodeLoop/Acceptance/TaskGraphEngine 已建且单测覆盖，但 P2 不执行 TaskGraph、P3 只"生成不执行"（节点级执行属 R11/P4）——与施工声明 scope 一致。P3 生成的 TaskGraph 真实性见 §9。

## 11. Evidence / Trace / Audit / Gate 附审材料验证 ✅
- P2 Evidence 5 项落库（`ev-p2-{inputs,risk-basis,blocker-source,gap-source,model-analysis}.json`，§4.7），API `/evidence` 返回 7 条(2 P0 + 5 P2)，含 evidence_id/type/status/stage/created_at；内容真实可追溯（blocker source→"p2_input_manifest.json -> pending_evidence_gaps[0]"）。
- Trace/Audit：`.rebuild/traces.jsonl`(source_materialization/create_project/…)、`audits.jsonl` 真实写入。
- Gate 附审：P1→P2 Gate 24 条 artifact_refs 可读；P2→P3 Gate 已创建。

## 12. 负路径验证
| 场景 | 结果 |
|------|------|
| S6.1 无 Key → P2 blocked 不降级 | 代码+单测验证 blocked（本环境 Key 存在，走正路径） |
| S6.1' 真实 LLM 慢/超时 | 诚实：litellm retry；未伪造产物（超时期间 assessment-summary 返回 available:false"P2 评估尚未执行"）；重试成功后才产真实产物 ✅ |
| S6.4 重复 execute 幂等 | ✅ complete→1 gate、execute×2→仍 1 gate、响应含 `idempotent`（R9 F-3 保持） |
| S6.5 非法 Gate decision | ❌ **缺陷**：`/gates/{id}/decision` 拒绝返回 400 ✅；但**前端所用 `/promotion-decision` 对非法值返回 HTTP 200 并 resume 图 → 额外创建重复 P0 Gate**（neg2：complete+1 非法决策=2 gates）。current_stage 未被非法推进，但 gate 状态被污染。见 §15 P1-B。 |
| S6.6 重启持久化 | ✅ 后端 SIGTERM+重启后：current_stage=p2 恢复(DB)、P2 assessment-summary available=true(磁盘)、Evidence 7 条恢复——非内存态，durable 持久化 |
| 安全 | ✅ 未发现 Key/Token/Secret 泄露（§16） |

## 13. 代码级核查（§6 逐项，独立子代理 + 自验）
| 项 | 结论 |
|----|------|
| register_handler("p2"/"p3") 真实 handler | ✅（stage_handlers.py:414-417，RealP2/P3Handler） |
| P2/P3 不再走 future_r10 | ✅（get_handler 非 None → StageLoop 真实分支） |
| P4-P6 保持 stub 未注册 | ✅（仅注册 p0-p3；运行时 p4 pending stub） |
| TaskGraph ORM 真实/分表 | ✅（task_graph/task_node/task_graph_run/task_node_run 分表） |
| StagePlan/TaskPlan/PlanDelta 持久化 | ✅ |
| schemas/taskgraph.py 旧 mock | ◐ source_status="mock" 默认残留但已无 importer（死代码）+ docstring 与实现不符（P2-A） |
| get_stages / submit_stage_plan mock→real | ✅（读/写 stage_plan 表，source_status=real） |
| routes_health r_stage R4→R10 | ✅ |
| 新增 P2/P3 绕过 ModelGateway | ✅ 无绕过（仅经 gateway.call） |
| P2/P3 handler 调上下文组装 | ❌ 未调 context_assembler（P1-C） |
| Acceptance 参与 NodeLoop Step8 | ✅（node_loop.py:191-195） |
| R10 关键路径静默 except | ✅ 业务/落库路径无静默；辅助 trace 路径有注释降级 |
| 全库剩余静默 except | ◐ 44 处（排除 tests）vs 施工声明"约 38"（P2-B） |
| 无自建 StateGraph 替代编排 | ✅ |
| 前端 StagePageP2/P3 真实接后端 | ✅（assessment-summary/planning-summary） |
| 无 D-049 违规 mock 横幅 / 无 Key 回显 | ✅ |

## 14. 与 R10-3 自验差异
| 项 | R10-3 自验 | 独立实测 |
|----|-----------|---------|
| pytest | 511/2/0 | 511/2/0 一致 ✅ |
| build | ✅ | ✅ 一致 |
| P2/P3 真实 LLM 完整链 | 仅离线 fake 网关测试/演示数据（§959 自述） | **本报告首次真实 LLM 驱动**：P2 真实产物✅；P3 见 §9 |
| /workspace 端点 | 未报告 | ❌ 真实源项目过 P1 后 500（P1-A，施工未发现） |
| /promotion-decision 非法值 | 未报告 | ❌ 返回 200+重复 Gate（P1-B，施工未测） |
| P2/P3 context_assembler | T7 声称"通用支持已锁定" | ❌ handler 未接线（P1-C） |
| 静默 except | "约 38 处" | 44 处 |

## 15. 问题清单

### P0 阻断问题
无。P2/P3 非 stub、非假实现：P2 真实 LLM 产出有意义六类产物 + 5 Evidence；ORM/NodeLoop/Acceptance/边策略/必生 TaskGraph 代码真实；ModelGateway 未绕过；无密钥泄露；P4 保持 stub。P0/P1 主链路未被破坏。

### P1 重要问题
- **P1-A `/workspace` 聚合端点 500**：`aet_service.list_evidence_gaps()` 产 `{evidence_id,summary,type,item}`，而 `WorkspaceAggregateResponse.pending_evidence_gaps: list[EvidenceGapResponse]` 要求 `gap_id`+`evidence_type`（无默认）→ 凡 `uncertainty_manifest.json` 有 evidence_gaps（如 framework_unknown）即 `ValidationError` 500。后果：真实源项目过 P1 后工作区头部恒为"离线模式"。浏览器 2 条 console 500 皆此因。（可能 R9 期潜伏，经真实源项目首次触发；按 P0/P1 即时修复原则须修。）
- **P1-B `/promotion-decision` 非法 decision 不校验**：接受任意 decision 值 → resume 图 → 返回 HTTP 200 且**创建重复 P0 Gate**（gate 污染），与 `/gates/{id}/decision`(400) 不一致。违反 S6.5。
- **P1-C P2/P3 未接 context_assembler / 未用 Skill**：违反 S4 与 R10-2 §1.1 T7 意图；T7 仅交付孤立断言测试未验 handler 接线。P2/P3 模型调用缺 C0-C6 统一上下文与 C5 Skill 层（虽 ad-hoc 输入真实、产物真实）。

### P2 次要问题
- **P2-A** `schemas/taskgraph.py` `source_status="mock"` 默认死值 + docstring("T12/T13 flip to real")与实现不符（已无 importer，非运行事实源，建议清理/翻正）。
- **P2-B** 全库静默 `except: pass` 44 处 vs 施工声明"约 38"，债务台账数字需校正。
- **P2-C** Gate 对象 `graph_capability_status:"not_connected"`（schemas/common.py:36 默认死值）与"图真实驱动"不符——能力诚实性小瑕（state.py 明令不得写死 not_connected）。

### REC 建议项
- REC-1 全新空 SQLite `alembic upgrade head` 失败（R6 期 `SET NOT NULL` 未用 batch_alter）——影响一键起库，建议修迁移可移植性。
- REC-2 真实 LLM 慢/超时：批准 Gate 后 resume 同步内联跑 LLM（分钟级），HTTP 长阻塞（施工已登记 R11 异步化）——建议 R11 评估后台任务化。
- REC-3 P1 `dependency_draft` 未解析 Maven pom.xml 依赖（R9 剖析深度）——影响 P2 结构化依赖输入质量。

## 16. 安全与密钥检查 ✅
全程未输出/写入 Key/Token/Secret/.env 明文（`.env` 仅判存在性：DEEPSEEK/LLM/AGNES_API_KEY、GITHUB client id/secret、MASTER_KEY 存在，值未读）。产物/Evidence/Trace/Audit/后端日志经正则扫描**未发现明文密钥**。前端无 Key 回显。

## 17. 进度追踪检查
- R10-3 未收口（T22 未执行）：代码未 git commit、`01-功能表`/`05-验收索引` P2/P3 能力状态未更新、`02-阻塞项` 未回迁 R9 残留。属预期（R10-4 在收口前）。本报告为 R10-5 修复输入，不代施工方收口。

## 18. 最终结论与理由

**结论：rework_required（返工）**。

不得 accepted 的理由（§8.1 核心项未全部成立）：
1. **S4 未满足**：P2/P3 未经 context_assembler 组装上下文、未使用 Skill（P1-C）——§8.3 明列为 rework 触发项。
2. **真实前端链路部分失败**：`/workspace` 500（P1-A）使真实源项目工作区恒入离线模式。
3. **负路径 S6.5 失败**：`/promotion-decision` 非法值不拒绝且污染 Gate（P1-B）。

但须公允记录 R10-3 的真实成就（非假实现，故非最重返工档）：真实 git clone、真实 P0/P1、**真实 LLM 驱动的 P2 有意义六类产物 + 5 Evidence + P2→P3 Gate**、重启持久化恢复(S6.6)、ORM 分表/NodeLoop/Acceptance/8 维边策略/必生 TaskGraph 代码真实、ModelGateway 无绕过、P4 stub 边界正确、pytest 511/2/0、build 通过、幂等(S6.4)、无密钥泄露。P3 真实产物运行时验证受限（DeepSeek 延迟超窗，§9），须 R10-5 真实复跑补齐——但不改变返工结论（结论由 P1-A/B/C 独立成立）。

修复计划见 `交接/当前/R10-5-修复计划.md`。

## 19. R10-5 修复计划输入摘要
P1-A（/workspace 500 schema 对齐）、P1-B（/promotion-decision 校验+防重复 Gate）、P1-C（P2/P3 接 context_assembler + Skill）为 R10-5 P0/P1；P2-A/B/C 为 P2；REC-1/2/3 建议项。详见 R10-5。
