# R10-3 续2 施工交接提示词 — 组4 前端+Gate+收尾（T18 起）

> 文件路径：`交接/当前/R10-3-续2-施工交接提示词.md`
> 文件身份：**交接材料 / 新窗口启动材料**（非事实源、非实现契约）
> 出具人：Claude (opus-4-8[1M])，R10-3 施工 Agent（组2/组3 施工者）
> 出具日期：2026-07-02
> 平台版本：rebuild V26.1.1 | 当前阶段：**R10-3**（组0/组1 横切底座 + 组2 P2 + 组3 P3 全完成；进入组4）
> 服务状态：后端 :8000 已起（P0-P3 handler 全注册 live、r_stage=R10、HTTP 200）、前端 :5173 已起
> 测试基线：`uv run pytest -p no:cacheprovider -q` → **500 passed / 2 skipped / 0 failed**（约 3m40s）
> Alembic head：**e5f6a7b8c9d0**（T8 PlanDelta）
> ⚠️ 代码状态：R10-3 全部改动**未 git commit**（R10 收口时统一提交，见 §7）

---

## 0. 给你的第一句话（必读）

```
你是 R10-3 续2 施工 Agent（组4：前端 + Gate + 收尾）。你不能依赖任何旧窗口记忆。

必须先按以下顺序执行启动序列：
  1. 读 /home/king/rebuild/AGENTS.md
  2. 加载 Skill：skills/R-建设执行/rebuild-work-guidelines/SKILL.md
  3. 读本文件（你正在读）
  4. 读主蓝图：交接/当前/R10-2-历史吸收报告.md（§2 WBS + §3 施工顺序 + §5 已锁决策）
  5. 读事实基线：交接/当前/R10-1-实际状态核查报告.md
  6. 读施工进度：证据/进度追踪/04-施工记录.md 的 REC-R10-3（T0-T17 全部已完成任务的落点+证据）
  7. 前端施工前必读 06-UX 章节（Skill §4.4，见 §4）
  8. 读待确认项：证据/进度追踪/03-待确认项.md（Q-R10-1~5 均已 resolved）

然后从 §3「下一步 T18」开始，逐任务施工，每任务：三步法→施工→pytest/前端构建不降基线→更新 REC-R10-3。
铁律：先读权威契约/规范→再写；无 Evidence 不 completed；失败发声不伪造；不硬编码模型；前端中文优先/脱敏/线性图标/mock区分。
```

---

## 1. 项目上下文（自包含）

- **平台**：rebuild（V26.1.1）；软件重构/迁移平台，首期信创迁移。
- **主编排底座**：**LangGraph（D-037 硬红线）**。TaskGraph 是节点内部任务级 DAG，绝不替代 LangGraph。
- **阶段体系**：P0-P6=用户项目流程；R0-RN=平台自建。当前 **R10**（P2/P3 stub 灌真实业务 + 横切底座）。
- **代码根**：`/home/king/rebuild/`（`backend/` FastAPI+uv+Python3.12；`frontend/` React+Vite+TS）。
- **R10 使命**：P2 评估 + P3 规划两个 stub → 真实阶段业务，补齐 TaskGraph 引擎 / NodeLoop 9 步 / Acceptance 执行器。**后端服务链 P0-P3 现已全真实**。

---

## 2. R10-3 已完成（本轮交付基线，代码级已验证，pytest 全绿）

> 全部有 pytest 证据 + REC-R10-3 施工记录登记。**组0/组1/组2/组3 全完成**。

| 组 | 任务 | 交付 |
|----|------|------|
| 组0 | T0 | 静默 except 横扫（关键路径4文件） |
| 组1 | T4/T1/T3/T2/T5/T6/T6b/T7/T8 | TaskGraph+StagePlan+TaskPlan ORM / NodeLoop运行实例 / Acceptance执行器 / NodeLoop 9步 / 边策略8维校验 / 调度引擎 / W8全量cutover / Context适配 / PlanDelta |
| **组2 P2** | **T9-T12** | 评估服务(6类产出/无Key blocked/analysis_only) + Evidence落库(§4.7五项) + StageHandler真实接线(RealP2Handler) + API/schema真实化(get_stages/submit_stage_plan mock→real + r_stage R4→R10) |
| **组3 P3** | **T13-T17** | StagePlan生成 + TaskPlan(Batch)生成 + **TaskGraph必生**(降级单链+过T5校验) + Evidence(§5.7六项) + StageHandler注册(RealP3Handler，P4保持future_r11 stub) |
| 裁决 | Q-R10-5 | Node Status 补 blocked（12态） |

**新增/改动核心文件**：`services/{assessment_service,planning_service,acceptance_service,node_loop,task_graph_service}.py`、`models/{task_node_run,task_graph,stage_plan,plan_delta}.py`、`graph/stage_handlers.py`（RealP2/P3Handler + bootstrap 注册 p0-p3）、`services/stage_service.py`、`api/routes_health.py`、`schemas/gate.py`。对应测试：`test_p2_assessment / test_graph_p2 / test_stages_r10 / test_p3_planning / test_graph_p3 / test_health` 等。

### 已锁设计决策（直接执行，不再重确认）
- **Q-R10-1**：TaskGraph 定义态/运行态**分表**。**Q-R10-2**：LLM 必真实，**无 Key=blocked 不降级**（P2/P3 全适用）。**Q-R10-3**：P3 **每次必生 TaskGraph**；P4 handler 不注册保持 future_r11 stub。**Q-R10-4**：Acceptance 仅 Agent 实例独立。**Q-R10-5**：Node Status 含 blocked（12态）。
- **P0/P1 即时修复原则**（用户裁决）：施工中发现 P0/P1 bug 立即修复，不留阻塞。

### ⚠️ 关键陷阱（组4 必知）：注册 handler → 批准上一阶段 Gate 会让图**自动跑下一阶段的真实业务**
- LangGraph 跑至下一 interrupt。批准 P1→图跑真实 P2；批准 P2→图跑真实 P3。
- **`backend/.env` 存在 LLM Key（脱敏，仅判存在性）→ 测试网关 overall=available → 真实 LLM 调用挂起**（曾致全量套件从 231s 卡死 >15min）。
- **化解范式**（见 `tests/test_graph_api.py`）：任何驱动 LangGraph 图进入 P2/P3 的测试，在对应 resume 前 `monkeypatch model_gateway.get_status→SimpleNamespace(overall_status="not_configured")`，令该阶段走**确定性离线 blocked 路径**（真实 Q-R10-2 语义，无网络）。**T21 集成测试若驱动图到深阶段务必套用此范式**。

---

## 3. 下一步：T18 → T22（R10-2 §2 组4 剩余 WBS）

**施工顺序**（R10-2 §3）：`T18 + T19（前端可并行）→ T20（Gate 前端集成）→ T21（测试套件）→ T22（追踪更新+R10 收口）`。

| 任务 | 描述 | 量 | 依赖 | 关键约束 |
|------|------|----|------|---------|
| **T18** | StagePageP2（前端） | M | T12 | `frontend/src/pages/workspace/StagePageP2.tsx`；6 类 P2 评估产出展示（评估报告/风险/阻塞/不确定/验证缺口/资源需求）；风险/阻塞/gap 分组；接真实后端；**中文优先/线性图标(Icon.tsx)/脱敏/mock区分(D-049)/最小前后端联调(D-074 留浏览器级证据)** |
| **T19** | StagePageP3（前端） | L | T17 | `frontend/src/pages/workspace/StagePageP3.tsx`；StagePlan/TaskPlan(Batch)/TaskGraph 可视化（**DAG 简化视图**）；接真实后端 |
| **T20** | Gate 前端集成（P2→P3 / P3→P4 两道 Gate） | M | T18 T19 | 复用现有 `GatePanel` 组件；决策按钮 approve/reject/request_changes；决策经 `/projects/{pid}/runs/{rid}/stages/{stage}/promotion-decision` 或 `/gates/{gate_id}/decision`（graph-driven resume） |
| **T21** | 测试套件（P2/P3 集成路径） | L | T11 T17 | 集成路径测试；**目标基线继续增长不降 500/2/0**；驱动图到 P2/P3 的测试务必套用 §2 离线 blocked 范式避免真实 LLM 挂起 |
| **T22** | 追踪更新 + R10 收口 | S | T21 | 收口六步（Skill §2.3），见 §7 |

### 后端数据来源（前端接线用，真实端点）
- **阶段列表/单阶段**：`GET /api/projects/{pid}/runs/{rid}/stages`、`/stages/{stage}`（T12 已真实化：`source_status=real`、真实 `stage_plan_ref`）。
- **P2/P3 产出 Artifact**：落在 `workspace/{pid}/artifacts/`（P2：`p2_assessment_report/risk_list/blocker_list/validation_gaps/resource_needs.json`；P3：`p3_stage_plan/task_plans/task_graph.json`）。经 AET 路由 `list_artifacts(project_id, stage)` 读取（确认路由前缀：`app/api/` 下 AET/workspace 路由；**先 grep 确认真实端点路径，勿臆造**）。
- **Evidence**：`workspace/{pid}/evidence/`（P2 五项 `ev-p2-*`、P3 六项 `ev-p3-*`），经 `list_evidence(project_id, stage)`。
- **StagePlan/TaskPlan/TaskGraph 结构化数据**：目前落 DB（`stage_plan`/`task_plan`/`task_graph`/`task_node` 表），暂无专用只读 API。**T19 若需 TaskGraph nodes/edges 可视化，可能需新增只读端点**（读 task_graph.edges JSON + task_node 行）——先确认现有端点，缺则最小新增（归 T19 范围，遵薄编排/不扩 scope）。

---

## 4. 前端施工专项规范（Skill §4.4，T18/T19/T20 开工前必读）

| 何时 | 必读章节（`文档/06-UX与前端/`） |
|------|------|
| 任何前端施工 | `00-UX与前端总览.md`、`07-视觉系统与前端文案.md` |
| Workspace/IDE 布局 | `03-ProjectWorkspace布局规范.md` |
| 模型/资源/能力可视化 | `06-模型资源可视化与真实能力标记.md`（14 能力标记 / mock·static_demo·not_connected 区分 / 脱敏） |
| Evidence/Trace/Gate 交互 | `05-Evidence-Trace-Audit与Gate交互.md` |

**硬要求**：① 用户可见文案中文优先（技术标识符保留原文）；② 真实能力标记状态来自后端；③ mock/static_demo/not_connected 显式区分（D-049），已真实接入页不挂全局 mock 横幅；④ Key/Token 全程脱敏；⑤ 施工后最小前后端联调（D-074，留浏览器级证据，可用 webapp-testing skill / Playwright）；⑥ 图标用线性图标组件 `frontend/src/components/ui/Icon.tsx`，禁 emoji 当功能图标。

---

## 5. 关键事实源路径

```
硬规则/流程：  /home/king/rebuild/AGENTS.md ；skills/R-建设执行/rebuild-work-guidelines/SKILL.md
主蓝图：      交接/当前/R10-2-历史吸收报告.md（WBS/顺序/锁定决策）
事实基线：    交接/当前/R10-1-实际状态核查报告.md
P2/P3 契约：  文档/03-流程与运行时/01-P0-P6阶段契约.md §4(P2)/§5(P3)
对象/边规范： 文档/03-流程与运行时/02-StagePlan-TaskPlan-TaskGraph规范.md（§8 边策略/§9 版本）
状态架构：    文档/02-架构设计/03-Project-Run-TaskGraph状态架构.md（§5-9 字段/§8.3 Node Status）
前端规范：    文档/06-UX与前端/（见 §4）
进度追踪：    证据/进度追踪/{01-功能表,02-阻塞项,03-待确认项,04-施工记录,05-验收索引,06-状态口径与更新规则}.md
核心代码区：  backend/app/{services,models,graph,api}/ ；frontend/src/pages/workspace/、components/
```

---

## 6. 服务与验证

```
后端：http://localhost:8000 （P0-P3 handler 全注册；重启：cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port 8000）
前端：http://localhost:5173 （cd frontend && npm run dev -- --host 0.0.0.0 --port 5173）
测试：cd backend && uv run pytest -p no:cacheprovider -q  → 必须维持 500+/2/0（新增任务应增长）
迁移：uv run alembic upgrade head  → head=e5f6a7b8c9d0
前端构建：cd frontend && npm run build（不降；施工后须最小联调）
⚠️ 重启陷阱：pkill -f 会匹配自身 shell；改用 `ps -eo pid,args|grep uvicorn|grep -v grep` 取 PID 精确 kill
⚠️ 后端非 --reload 运行：改后端代码后须重启才生效（改 handler 注册尤其）
⚠️ conftest 用 create_all 建表 + autouse bootstrap 注册 p0-p3 handler；真实 LLM 陷阱见 §2
```

---

## 7. 收口要求（T22，R10 全部任务完成后）

Skill §2.3 收口六步全执行：① 追踪五件登记（`01-功能表`[P2/P3 能力状态+证据路径，本轮一直延后至收口统一更新]、`02-阻塞项`[回迁 R9 残留：静默except~38、W8已cutover核对；登记 R10 新残留见下]、`03-待确认项`、`04-施工记录` REC-R10-3 收口、`05-验收索引`）② 交接材料归档（当前/→已完成/R10/）③ 产物草稿归档 ④ **Git 提交**（代码+追踪+交接，force-add；R10-3 全部改动至今未提交）⑤ 清空当前区 ⑥ 追踪一致性检查（06§3）。

**独立验收**：收口验收须**独立验收 Agent**（未参与施工，D-082）亲手驱动 G10 全场景 + R9-5 规划 §5 V1-V6。

**残留（诚实登记，须在 02-阻塞项.md 登记）**：
- complete 内联 P0 管道与图节点部分重叠，彻底移除留 **R11 旧端点下线**（T6b 登记）。
- 全库非关键路径静默 except ~38 处（R10-2 §1.4 明确延 R11 CI）。
- 批准阶段 Gate 会同步触发下一阶段真实 LLM 调用（P2/P3 评估/规划在 resume 内联跑）——**体验/架构注记**：真实运行时用户批准 P1/P2 后 HTTP resume 会等待 LLM（分钟级）。是否改异步后台任务，建议 R11 评估（登记为待确认或 R11 债务）。

---

## 8. 安全红线（绝对遵守）

```
绝对禁止输出/写入：Key、Token、Secret、Password、.env 明文、API Key 明文、认证凭据
禁止写入目标：报告、日志、Trace、Audit、前端页面、截图说明、交接材料、正式文档、Git 提交
安全记录写法：发现某类配置文件，内容已脱敏，需用户或本地环境自行确认
（backend/.env 含 DeepSeek Key，credential_status=configured/key_source=env；仅判断存在性不读值）
```

---

> **交接材料结束。** 新窗口接手后：按启动序列读材料 → 前端施工前读 06-UX → 从 §3「T18」开始 → 逐任务三步法施工 + 维持 pytest 500+/2/0 + 前端 build/联调 + 更新 REC-R10-3。**绝不跳过契约/UX 阅读直接施工；无 Evidence 不 completed；失败发声不伪造；驱动图到 P2/P3 的测试务必用 §2 离线 blocked 范式。**
