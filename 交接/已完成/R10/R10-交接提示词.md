# R10 交接提示词 — P2 评估 + P3 规划链路 + 横切底座（TaskGraph/NodeLoop/Acceptance）

> 文件路径：`交接/当前/R10-交接提示词.md`
> 文件身份：**交接材料 / 新窗口启动材料**
> 出具人：Claude (sonnet-4-6)，R9 收口 Agent，2026-06-30
> 平台版本：rebuild V26.1.1 | 当前阶段：**R10**（R9 accepted 后首期业务灌注）
> 服务状态：后端 :8000 已起（`backend/app/main.py`），前端 :5173 已起（`npm run dev`）
> 代码最新 commit：`438628f`（R9 彻底收口 accepted，2026-07-01）

---

## 0. 给你的第一句话（必读）

```
你是 R10 施工 Agent。你不能依赖任何旧窗口记忆。

必须先按以下顺序执行启动序列：
  1. 读 /home/king/rebuild/AGENTS.md
  2. 加载 Skill：skills/R-建设执行/rebuild-work-guidelines/SKILL.md
  3. 读本文件（你正在读）
  4. 读 R10 规划材料：产物/已完成/R9/R9-5-施工总规划/10-R10-P2评估与P3规划链路.md
  5. 读 R9 accepted 收口材料：交接/当前/R9-5+R9-6-联合验收报告.md（了解 R9 交付基线）
  6. 读 产物/已完成/R9/R9-5-施工总规划/00-总规划.md（贯穿验收 G1-G10 + 各期边界）

然后按本文 §3 的「五步流程」逐步推进，不可跳步。
铁律：先状态核查→再立报告→再吸收历史→再施工→再验收。
```

---

## 1. 项目上下文（自包含）

- **平台**：rebuild（V26.1.1）；软件重构/迁移平台，首期目标：信创迁移。
- **主编排底座**：**LangGraph（D-037 硬红线，不可绕过）**。
- **R 阶段哲学**：R 阶段 = 平台自建；P 阶段 = 用户项目产品流程（P0-P6）。
- **当前阶段**：**R10**，定位：把 R9-5 建成的管道里**灌 P2/P3 的水**——将 P2/P3 节点从 `future_r10` stub 替换为真实阶段业务，并补齐横切底座（TaskGraph 执行引擎 / NodeLoop 9 步 / Acceptance 执行器）。
- **代码根**：`/home/king/rebuild/`（`backend/` FastAPI+uv+Python3.12；`frontend/` React+Vite+TS）。
- **测试基线**：`cd /home/king/rebuild/backend && uv run pytest -p no:cacheprovider -q` → **407 passed / 2 skipped / 0 failed**（当前基线，R10 施工必须维持）。
- **前端基线**：`npm run build` → EXIT=0（必须维持）。
- **服务地址**：后端 http://localhost:8000，前端 http://localhost:5173（已在运行，新窗口接手时确认在线）。

---

## 2. R9 accepted 现状（已完成的基线）

### 2.1 已真实交付（R9-6 独立验收 accepted，407/2/0）

| 能力 | 代码落点 | 状态 |
|------|---------|------|
| LangGraph 主脊 + 14 节点图（P0-P6 框架） | `backend/app/graph/` | ✅ 真实，P0/P1 节点已灌入业务 |
| P0 接入（source 物化/产物落盘/Gate） | `routes_projects.py` + `aet_service.py` | ✅ 真实，含 git 失败分流 + Evidence 落库 |
| P1 识别（FullStackProfiler 14 项） | `full_stack_profiler.py` + `stage_handlers.py` | ✅ 真实 |
| P0→P1、P1→P2 Gate（interrupt/resume） | `gate_service.py` + `graph/nodes.py` | ✅ 真实，checkpoint 持久化 |
| 模型网关统一（D-098） | `model_gateway.py` + `litellm_adapter.py` | ✅ 真实流式 + fallback |
| 上下文组装（C0-C6 分层） | `context_assembler.py` | ✅ 真实 |
| 资源运行回路（Tool/MCP/案例/知识） | `resource_loader.py` + `tool_registry.py` | ✅ 真实 |
| StageLoop 小循环框架（D-091） | `graph/stage_loop.py` | ✅ 框架存在，P2/P3 节点将复用 |
| StageReports 三报告框架（D-092） | `graph/stage_reports.py` | ✅ 框架存在，P2/P3 节点将复用 |
| Evidence 落库（workspace/evidence/ JSON） | `aet_service.write_evidence()` | ✅ 真实 |
| HITL Gate（P121，三模式） | `stage_handlers.py` + `mode_policy.py` | ✅ 真实 |
| 前端工作区 P0/P1 页 + Gate 交互 | `StagePageP0.tsx` / `StagePageP1.tsx` | ✅ 真实接后端 |

### 2.2 P2/P3 节点当前状态（R10 要替换的 stub）

```
backend/app/graph/nodes.py:
  p2_node → 当前为 future_r10 stub（返回 {"stage": "p2", "stub": True}）
  p3_node → 同上（返回 {"stage": "p3", "stub": True}）

backend/app/services/stage_service.py:
  submit_stage_plan() line 53-64 → 当前 mock 返回（待本期真实化）

backend/app/schemas/taskgraph.py:
  TaskGraph / TaskNode / TaskEdge → 纯 Pydantic schema，无 DB ORM（待本期建 ORM）
```

### 2.3 已知残留（非阻断，已诚实登记）

| 项 | 归属 |
|----|------|
| WP-5 覆盖文件外的历史静默 except（~47 处） | R10 P2 阶段专项清理（建议 R10-2 横扫） |
| opencode serve 路径（ACP 协议） | R11（外部编程平台真实接入） |
| P4-P6 节点业务 | R11（P4 执行）/ R12（P5/P6）|
| git/github 真实 clone（需凭据） | R10+（precheck 端点已建，前端接入待补） |
| `r_stage:"R4"` 字段（health 接口仍返回旧值） | 本期顺手更正为 R10 |

---

## 3. R10 五步流程（必须按序，不可跳步）

### 步骤 1：R10-1 实际状态核查 + 立报告

**目标**：亲手探测平台当前真实状态，不能仅凭报告推断。  
**方法**：
1. 启动后端（确认 :8000 在线）；独立重跑 `uv run pytest -p no:cacheprovider -q`，记录实际数字
2. 逐项核查以下落点（HTTP 调用 + 代码 grep，不信报告）：
   - `GET /api/projects` → 确认 meta 无 R4 污染（F-4 修复验证）
   - `nodes.py` → 确认 p2_node/p3_node 确实是 stub（`future_r10`）
   - `schemas/taskgraph.py` → 确认是纯 Pydantic（无 ORM）
   - `stage_service.submit_stage_plan` → 确认是 mock 返回
   - `graph/stage_loop.py`、`graph/stage_reports.py` → 确认框架接口真实存在
3. 扫描全量阻塞点（与 `证据/进度追踪/02-阻塞项.md` 对账）
4. 出具「**R10-1 实际状态核查报告**」（`交接/当前/R10-1-实际状态核查报告.md`）：
   - P2/P3 stub 清单（文件+行号）
   - 横切底座现状（TaskGraph ORM 缺失 / NodeLoop 未实现 / Acceptance 未接线）
   - 已确认可直接复用的真实底座清单
   - 残留静默 except 数量（grep 实测）
   - 当前 pytest 数字（不引用旧报告）

**出具「R10-1 自立报告」**（同文件或单独 `R10-1-立报告.md`）：
- R10 范围确认（P2 评估服务 + P3 规划 + TaskGraph 引擎 + NodeLoop + Acceptance 执行器 + 前端 P2/P3 页）
- 估计施工量（参照 10-R10-P2评估与P3规划链路.md §3 WBS）
- 待确认项（如有技术路线分歧，列 Q-R10-* 待用户裁决）
- **注意**：若实际探测发现与本文 §2 描述有出入，以实探为准，更新到报告

---

### 步骤 2：R10-2 历史吸收（材料审读 + 方案完善）

**目标**：深度吸收 R10 规划材料 + 相邻期契约，建立完整施工方案。  
**必读材料**（三步法第②步）：

```
产物/已完成/R9/R9-5-施工总规划/10-R10-P2评估与P3规划链路.md   ← 主规划（WBS + 验收方法）
产物/已完成/R9/R9-5-施工总规划/00-总规划.md                   ← 贯穿验收 G1-G10 + 期次边界
文档/01-平台功能设计/01-P0-P6阶段契约.md §4(P2) §5(P3)      ← 12 要素契约（若文件存在）
文档/01-平台功能设计/02-StagePlan-TaskPlan-TaskGraph规范.md   ← TaskGraph 规范（若文件存在）
文档/01-平台功能设计/03-NodeLoop与执行模式.md §2-§3           ← NodeLoop 9 步（若文件存在）
证据/进度追踪/01-功能表.md                                     ← 确认 P2/P3 能力当前状态
证据/进度追踪/02-阻塞项.md                                     ← 当前阻塞项清单
```

**输出**：「R10-2 历史吸收与方案完善报告」（`交接/当前/R10-2-历史吸收报告.md`）：
- 规划材料哪些直接采用 / 哪些需调整（有依据）
- 更新后的 WBS（任务清单 + 优先级 + 依赖关系）
- 横切底座施工顺序（建议：T4 TaskGraph DB → T1 NodeLoop DB → T3 Acceptance → T2 NodeLoop 执行器 → T5 TaskGraph 执行引擎 → 业务节点 → 前端）
- 若文档缺失（契约 §4/§5 等），记录"缺失，按摸排 §2.6 + 当前代码状态推导"
- 已确认的设计决策（不扩范围、不改 LangGraph 图骨架、不碰 P0/P1）

---

### 步骤 3：R10-3 首轮施工

**目标**：按 §3 步骤 2 确定的 WBS 逐任务施工，每任务完整收口再进下一个。  
**施工顺序建议**（参照 10-R10 规划 §3，结合依赖关系）：

```
T4 → T1 → T3 → T2 → T5 → T6(P2业务) → T7/T8/T9(P3业务) → T10(P2→P3 Gate) → T11(P3→P4 Gate) → T12/T13(前端) → T14(G4/G5/G7贯穿验收)
```

**每任务施工规则**：
1. 先独立输出自有方案 → 吸收规划材料 → 完善后施工（三步法，AGENTS.md §7.1）
2. 施工后立即运行 `uv run pytest -p no:cacheprovider -q`，保证不降低通过数
3. 亲手 HTTP 驱动核查真实落点（不只信 pytest）
4. 单任务收口证据（DB 行 / 文件 / API 响应，脱敏）

**关键红线（绝对禁止）**：
- 绕过 LangGraph 图调用 P2/P3 业务（D-037：P2/P3 节点必须是 `nodes.py` 里的真实图节点）
- TaskGraph 执行引擎替代 LangGraph 主编排（D-037/D-065：TaskGraph 是节点内部任务级依赖图，不是新编排层）
- P4-P6 业务内容（留 stub，不实现编码/验证/交付）
- 前端 mock 数据回退（D-097：失败必须显示真实失败态，不用 mock 掩盖）
- 静默 except（公理3：失败必须发声）

**横切底座施工要点**：

| 底座 | 关键约束 |
|------|---------|
| TaskGraph DB（T4） | ORM 替换 schemas/taskgraph.py 纯 Pydantic；含 supersedes/superseded_by 版本链；source_status 去 "mock" |
| NodeLoop（T1+T2） | 9 步状态机真实代码路径；调 context_assembler / model_gateway / execution_provider；三模式行为差异 |
| Acceptance 执行器（T3） | 接 seed AgentType.acceptance + self_check_acceptance；返回 accepted/rework_required/gate_required 等枚举 |
| TaskGraph 执行引擎（T5） | 边策略 7 维（串/并/重试/失败边/合并/返工/Gate 边）本期齐全（R11 不补策略） |

---

### 步骤 4：R10-4 首轮验收

**目标**：独立验收 Agent（**未参与 R10-3 施工**，D-082）亲手驱动平台复核。  
**验收方法（V1-V6）**：
- V1：函数级落点（TaskGraph DB 行 / NodeLoop 状态机节点 / Evidence/Artifact 真实存在）
- V2：API 契约（P2 评估 API / P3 计划 API / Gate 晋级 API schema + 状态码）
- V3：持久化恢复（P2 评估结果 / P3 StagePlan + TaskGraph 重启后恢复）
- V4：Playwright 端到端（P2/P3 页面前后端联调；0 console error）
- V5：Evidence 闭合（P2/P3 每步 Artifact/Evidence 可索引，内容真实非 stub）
- V6：负路径（P2 评估失败返工 / P3 计划生成失败 / 重复触发幂等 / 无 Key 诚实降级）

**G10 金标准更新**（G10 在 R10 增加 S5/S6 场景）：
```
S5 P2 完整路径：P1 Gate 批准 → P2 评估执行 → 评估报告落盘 → P2→P3 Gate 批准 → P3 阶段就绪
S6 P3 完整路径：P3 规划执行 → StagePlan/TaskGraph 生成 → P3→P4 Gate 批准（P4 仍为 stub）
```

**验收判定标准**：
- P0（阻断）：P2/P3 节点仍是 stub，不真实执行；G10 S5/S6 失败；主要 Evidence 为空
- P1（重要，可条件通过）：Acceptance 执行器未完全接线；TaskGraph 边策略缺失；前端 P2/P3 页未联调
- P2（次要）：文档未同步；追踪文件未更新

---

### 步骤 5：R10-5 后续修复

**目标**：修复 R10-4 发现的 P0/P1 问题，直到独立验收 accepted。  
**规则**：每次修复后重新独立验收（D-082）；不得自行标 accepted；修复报告记录改动范围和验证证据。

---

## 4. 关键事实源路径

```
硬规则/工作流：
  /home/king/rebuild/AGENTS.md
  /home/king/rebuild/skills/R-建设执行/rebuild-work-guidelines/SKILL.md

R10 规划（必读）：
  产物/已完成/R9/R9-5-施工总规划/10-R10-P2评估与P3规划链路.md
  产物/已完成/R9/R9-5-施工总规划/00-总规划.md（§1.2 边界 + §3 R10 行 + §4 模板 + §5 G1-G10）

R9 基线确认：
  交接/当前/R9-5+R9-6-联合验收报告.md
  交接/当前/R9-6-施工自验报告.md（代码改动清单）

阶段契约（若存在）：
  文档/01-平台功能设计/01-P0-P6阶段契约.md §4(P2) §5(P3)
  文档/01-平台功能设计/02-StagePlan-TaskPlan-TaskGraph规范.md
  文档/01-平台功能设计/03-NodeLoop与执行模式.md

决策记录（含 D-037/D-065/D-082/D-091/D-092/D-066/D-023）：
  文档/00-项目治理/01-决策记录.md

进度追踪（五件）：
  证据/进度追踪/{01-功能表,02-阻塞项,03-待确认项,04-施工记录,05-验收索引}.md

核心代码（R10 主要改动区）：
  backend/app/graph/nodes.py（p2_node/p3_node stub → 真实体）
  backend/app/schemas/taskgraph.py（纯 Pydantic → 建 ORM 于 app/models/task_graph.py）
  backend/app/services/stage_service.py（submit_stage_plan mock → 真实化）
  backend/app/graph/stage_loop.py（P2/P3 小循环复用入口）
  backend/app/graph/stage_reports.py（P2/P3 三报告复用入口）
  backend/app/services/context_assembler.py（P2/P3 上下文 stage 过滤）
  backend/app/services/model_gateway.py（P2 评估调模型）
  frontend/src/pages/workspace/（新建 StagePageP2.tsx / StagePageP3.tsx）
```

---

## 5. 安全红线（绝对遵守）

```
绝对禁止输出或写入：Key、Token、Secret、Password、.env 明文、API Key 明文、认证凭据
禁止写入目标：报告、日志、Trace、Audit、前端页面、截图说明、交接材料、正式文档、Git 提交
安全记录写法：发现某类配置文件，内容已脱敏，需用户或本地环境自行确认
```

---

## 6. 收口要求

R10 完成后，收口六步（Skill §2.3）必须全执行：
1. 登记文档状态（追踪五件更新）
2. 归档交接材料（交接/当前/ → 交接/已完成/R10/）
3. 归档产物草稿（产物/草稿/R10-* → 产物/已完成/R10/）
4. Git 提交（代码 + 追踪文件 + 交接材料，force-add）
5. 清空当前区（交接/当前/ 仅剩 R11 启动材料）
6. 追踪一致性检查（06§3 13 项）

收口验收须由**独立验收 Agent**（未参与 R10 施工，D-082）亲手驱动 G10 S1~S6 全场景。

---

> **交接材料结束。** 新窗口接手后：按启动序列读材料 → 按步骤 1（R10-1）开始实际状态核查 → 出具核查报告 → 立报告确认范围 → 再往后推进。
> **绝不跳过核查直接施工。**
