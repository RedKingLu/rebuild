# R10-1 实际状态核查报告 + 自立报告

> 文件路径：`交接/当前/R10-1-实际状态核查报告.md`
> 文件身份：**交接材料 / 状态核查 + 自立报告**（R10 五步流程步骤 1 产出物；非事实源、非实现契约）
> 出具人：Claude (opus-4-8[1M])，R10 施工 Agent
> 出具日期：2026-07-01
> 平台版本：rebuild V26.1.1 | 当前阶段：**R10**（R9 accepted 后首期业务灌注）
> 核查方法：亲手启动/探测运行中服务 + 真实 HTTP 调用 + 代码 grep + 独立重跑 pytest。**不引用旧报告数字。**
> 铁律遵循：先状态核查 → 立报告 → 再吸收历史 → 再施工 → 再验收。本报告仅完成"核查 + 立报告"。

---

## 0. 核查身份与方法声明

- 本 Agent 按 R10 交接提示词 §3 步骤 1 执行，亲手探测平台真实状态，**不仅凭报告推断**。
- 服务探活：`curl` 真实 HTTP 调用运行中的 :8000 后端与 :5173 前端。
- 代码核查：`grep`/`ls`/`wc` 在 `/home/king/rebuild/backend`、`/home/king/rebuild/frontend` 实测。
- 测试基线：**独立重跑** `uv run pytest -p no:cacheprovider -q`，记录本次实测数字。
- 安全红线：全程未输出/写入任何 Key/Token/Secret/密码明文。

---

## 1. 服务在线与测试基线（实测）

### 1.1 服务探活

| 项 | 实测结果 | 判定 |
|----|---------|------|
| 后端 `GET /api/health` | `{"status":"ok","platform":"rebuild","version":"V26.1.1","r_stage":"R4"}` | ✅ 在线；⚠️ `r_stage` 仍为 `R4`（陈旧字段） |
| 后端 `GET /api/projects` meta | `source_status=real, capability_status=available, not_connected_reason="", persistence=durable` | ✅ 无 R4 污染（F-4 修复保持） |
| 前端 `GET /` (:5173) | HTTP 200 | ✅ 在线 |

### 1.2 pytest 独立重跑（本次实测，非引用旧报告）

```
407 passed, 2 skipped, 0 failed, 1 warning   (118.61s / 0:01:58)
```

- 2 skipped = 依赖有效 LLM API Key 的真实调用测试（非代码缺陷）。
- 1 warning = starlette TestClient httpx 弃用告警（非缺陷）。
- 与 R9 accepted 基线 **407/2/0 完全一致**。**R10 施工必须维持此基线不降低。**

---

## 2. P2/P3 stub 清单（文件 + 行号，实测）

> **重要实探修正**（交接提示词 §0-点6 授权"以实探为准"）：交接 §2.2 描述"`p2_node`/`p3_node` 当前为 stub，返回 `{"stage":"p2","stub":True}`" 与实际代码结构**有出入**。实际代码**不存在** `p2_node`/`p3_node` 独立函数——`grep "def p2_node\|def p3_node"` 零命中。

### 2.1 实际的 P2/P3 stub 机制（工厂 + 处理器注册）

| 落点 | 文件:行 | 实测内容 |
|------|---------|---------|
| 节点工厂（非逐阶段函数） | `app/graph/nodes.py:101` `make_work_node(stage)` / `:196` `make_gate_node(stage)` | 图节点由工厂按 `stage` 生成，P0-P6 共用同一套工厂 |
| P2-P6 stub 分支 | `app/graph/nodes.py:107-114` | `if handler is None:` 返回 `{"current_stage":stage, "stage_status":{stage:"future_r10"}, "events":[future_r10 stub 事件]}` |
| stub 触发条件 | `app/graph/nodes.py:105` `get_handler(stage)` | P0/P1 已 `register_handler`（真实业务）；**P2-P6 未注册 handler → 落 stub 分支** |
| 顶部注释 | `app/graph/nodes.py:11-14` | 明确 "P2-P6 are `future_r10` stubs...Tests inject fakes" |

**R10 施工含义（据实探修正）**：P2/P3 真实化的正确做法 = **实现并 `register_handler("p2", ...)` / `register_handler("p3", ...)` 注册真实 StageHandler**（含 `goal`/`acceptance_criteria`/`planned_actions`/`execute()`/`review()`），而**非**新增/改写 `p2_node`/`p3_node` 函数。此扩展点比交接措辞更干净：**不动图装配骨架、不动工厂**，只补 handler 注册。交接 §2.2 的"把 p2_node/p3_node 从 stub 改真实体"应理解为"注册真实 P2/P3 handler"。

### 2.2 `future_r10` 全量命中

```
app/graph/nodes.py:13   （注释）
app/graph/nodes.py:108  （stub 分支注释）
app/graph/nodes.py:111  stage_status = future_r10
app/graph/nodes.py:112-113  future_r10 stub 事件
```
> `future_r10` 仅集中于 `nodes.py` 单一 stub 分支，无散落。R10 完成后此分支对 P2/P3 不再命中（P4-P6 仍保留 stub）。

---

## 3. 横切底座现状（实测）

### 3.1 待建（R10 新建，当前确认不存在）

| 底座 | 目标文件 | 实测 |
|------|---------|------|
| TaskGraph ORM | `app/models/task_graph.py` | ❌ 不存在（待建 T4） |
| StagePlan/TaskPlan/PlanDelta ORM | `app/models/stage_plan.py` | ❌ 不存在（待建 T4/T8） |
| NodeLoop 运行实例 ORM | `app/models/task_node_run.py` | ❌ 不存在（待建 T1） |
| NodeLoop 9 步执行器 | `app/services/node_loop.py` | ❌ 不存在（待建 T2） |
| TaskGraph 调度引擎 | `app/services/task_graph_service.py` | ❌ 不存在（待建 T5/T6/T8） |
| Acceptance 执行器 | `app/services/acceptance_service.py` | ❌ 不存在（待建 T3） |
| P2 评估服务 | `app/services/assessment_service.py` | ❌ 不存在（待建 T9/T10） |
| P3 规划生成器 | `app/services/planning_service.py` | ❌ 不存在（待建 T13-T16） |
| NODE_STATUSES 枚举 | `app/core/status.py` | ❌ 不存在（仅有 `P_STAGES`，:18；待补 T1） |
| 前端 StagePageP2/P3 | `frontend/src/pages/workspace/StagePageP2.tsx` / `StagePageP3.tsx` | ❌ 不存在（待建 T18/T19） |

### 3.2 TaskGraph schema 现状（纯 Pydantic，待升 ORM）

`app/schemas/taskgraph.py` 实测：
- 文件顶部注释 `:3` = `"R4: Mock-only — no real graph execution."`
- 全部为 `pydantic.BaseModel`（无 `Mapped`/`__tablename__`/ORM）。
- `source_status = "mock"` 遍布：`StagePlanResponse:20`、`TaskPlanResponse:29`、`TaskGraphNode:38`、`TaskGraphEdge:48`、`TaskGraphResponse:60`。
- **T4/T5 施工**：升格为 ORM（落 `app/models/`）+ 补齐边策略 8 维 + 去 `source_status="mock"`。

### 3.3 stage_service mock 现状（实测，含交接未提及项）

`app/services/stage_service.py`：
- `:1-4` 顶部注释 = `"Stage service — read-only stage state + mock promotion. R4: ... transition_mode=mock"`
- `get_stages()` `:41-42` = `source_status="mock", transition_mode="mock"` —— **⚠️ 交接 §2.2 未提及：阶段列表读取本身也是 mock**（不止 `submit_stage_plan`）。
- `submit_stage_plan()` `:53-64` = mock 返回（`source_status="mock", transition_mode="mock"`）✅ 交接已标，T13 真实化。
- `promote()` `:66-106` = **已真实**（`transition_mode="real"`，路由 `GateService.decide`，W9 drive_promotion 已接）✅ 与规划 §2.3 一致，不动。

### 3.4 已确认可直接复用的真实底座（实测存在且非空）

| 真实件 | 文件 | 实测 |
|--------|------|------|
| StageLoop 小循环（D-091） | `app/graph/stage_loop.py`（143 行） | ✅ `class StageLoop` + `async def run(...)` 真实 |
| StageReports 三报告（D-092） | `app/graph/stage_reports.py`（83 行） | ✅ `class StageReports`，KINDS=(start_plan/construction/acceptance)，`_write_json` 落盘真实 |
| 节点工厂 + Gate interrupt | `app/graph/nodes.py`（252 行） | ✅ `make_work_node`/`make_gate_node`/`make_router`；Gate 经 `interrupt()`（:203）；GateBackend 注入（:66-79） |
| GraphState reducer | `app/graph/state.py`（交接 §2.1 记载） | ✅ artifacts/evidence/events/audit/gates/errors 累加（R9 已建） |
| 晋级 promote（真实） | `app/services/stage_service.py:66-106` | ✅ transition_mode=real |

---

## 4. 残留阻塞点扫描（实测 + 对账）

### 4.1 静默 except 实测（grep）

按文件分布（`except ...:` 紧跟 `pass` 为唯一体，排除 test）：

```
full_stack_profiler.py(7) aet_service.py(4) routes_stages.py(4) mcp_service.py(3)
workspace_service.py(2) tool_registry.py(2) opencode_server.py(2) execution_provider.py(2)
trace_writer.py(2) audit_writer.py(2) routes_workspace.py(2) routes_gates.py(2)
model_gateway.py(1) external_context_builder.py(1) context_assembler.py(1)
gate_backend.py(1) checkpoint.py(1) routes_projects.py(1) routes_agent_chat.py(1)
```

- 按文件累计 **41 处**（另一 grep 口径含跨行 48 处，取决于计数方式）。
- 与 R9 联合验收报告 §3-F6 登记的 **~47 处历史债务量级一致**，属 R9-6 WP-5 范围外历史债务。
- 交接 §2.3 归属：**R10 P2 阶段专项清理（建议 R10-2 横扫）**。

### 4.2 已知残留（交接 §2.3 逐项对账 — 实探确认）

| 项 | 交接归属 | 实探确认 |
|----|---------|---------|
| WP-5 外历史静默 except（~47） | R10 P2 专项清理 | ✅ 实测 41-48 处，量级吻合 |
| opencode serve 路径（ACP） | R11 | 未在本次核查范围（P2/P3 不涉及） |
| P4-P6 节点业务 | R11/R12 | ✅ nodes.py stub 分支保留 P4-P6 |
| git/github 真实 clone（需凭据） | R10+ | precheck 端点已建（R9 记载），前端接入待补 |
| `r_stage:"R4"` 字段 | 本期顺手更正为 R10 | ✅ 实测 `routes_health.py:26,36` 两处 = `"R4"`（陈旧）；本期改 R10 |

### 4.3 阻塞项登记体系一致性（发现 P2 级问题）

- `证据/进度追踪/02-阻塞项.md` §194-215 汇总表当前仅列 **1 项 open**（B-FE-01，非 R5 范围归 R6），其余全 resolved。
- **但 R9 时代残留**（静默 except ~47、W8 委托 FlowRuntime 延 R10、T5/T7 astream 域事件延 R10）**仅登记于 R9 联合验收报告 §8，未回迁到 `02-阻塞项.md`**。
- **判定**：追踪登记体系存在滞后（P2 级，非阻断）。建议 R10 收口时把 R9 残留 + R10 新增阻塞统一登记到 `02-阻塞项.md`，消除"报告有、登记表无"的漂移（AGENTS §13.1）。

---

## 5. 核查结论小结

| 维度 | 结论 |
|------|------|
| 服务在线 | ✅ 后端 :8000 / 前端 :5173 均在线 |
| 测试基线 | ✅ 独立重跑 407/2/0（118.61s），与 R9 基线一致 |
| P2/P3 stub | ✅ 确认为 stub（工厂 + 未注册 handler 机制，非 p2_node/p3_node 函数）；实探修正交接 §2.2 措辞 |
| TaskGraph ORM | ✅ 确认缺失（纯 Pydantic Mock-only，source_status=mock） |
| NodeLoop/Acceptance | ✅ 确认未实现（目标文件全不存在；StageLoop/StageReports 框架真实可复用） |
| 前端 P2/P3 页 | ✅ 确认缺失 |
| 静默 except | ◐ 41-48 处历史债务（P2，R10 建议横扫） |
| 陈旧字段 | ⚠️ `/api/health` r_stage=R4（本期更正） |
| 追踪登记漂移 | ◐ 02-阻塞项.md 未回迁 R9 残留（P2，收口时统一） |

**总判定**：交接 §2 描述与实际状态**基本一致**，仅两处需以实探为准修正（见 §2.1「P2/P3 stub 机制」、§3.3「get_stages 亦 mock」）。R10 范围（P2 评估 + P3 规划 + TaskGraph 引擎 + NodeLoop + Acceptance + 前端 P2/P3 页 + 两道 Gate）**边界清晰、地基就绪、可启动施工**。

---

# 第二部分：R10-1 自立报告

## 6. R10 范围确认（据实探 + 规划 §1.2）

R10 唯一使命：**把 P2 评估与 P3 规划两个 stub 替换为真实阶段业务**，并补齐三件横切底座。范围内：

```
A. P2 风险/可行性评估服务（评估报告/风险清单/阻塞项/不确定项/验证缺口/资源需求 六类产出）
B. P3 方案 + StagePlan + TaskPlan(Batch) + TaskGraph 生成（显式边策略）
C. TaskGraph 执行引擎（ORM + 节点调度 + 边策略 7 维）
D. NodeLoop 9 步真实实现
E. Acceptance 执行器（承 D-082 独立验收）
F. StagePageP2 / StagePageP3 前端页
G. P2→P3、P3→P4 Gate（含三报告 D-092）
```

**明确不做**（规划 §1.3）：P0/P1 重做、P4 编码业务（R11）、P5/P6 业务（R12）、Fusion（R13）、远程资源调用编排（R14）、社区在线来源（R15）。

## 7. 估计施工量（据规划 §3 WBS，22 任务）

| 组 | 任务 | 量档 |
|----|------|------|
| ① 横切底座 | T1(M) NodeLoop DB · T2(L) NodeLoop 执行器 · T3(L) Acceptance · T4(L) TaskGraph ORM · T5(M) 边策略校验 · T6(L) 调度引擎 · T7(M) 从属接线 · T8(M) PlanDelta | ~7-9 人日 |
| ② P2 业务 | T9(L) 评估服务 · T10(M) Evidence · T11(M) p2 handler · T12(M) API/schema | ~3-4 人日 |
| ③ P3 业务 | T13(L) StagePlan · T14(L) TaskPlan · T15(L) TaskGraph 生成 · T16(M) Evidence · T17(M) p3 handler | ~4-5 人日 |
| ④ 前端+Gate+验收 | T18(M) StagePageP2 · T19(L) StagePageP3 · T20(M) 两道 Gate · T21(L) 测试 · T22(S) 追踪 | ~4-5 人日 |

**合计估计 ~18-23 人日**（规划口径 S≤0.5 / M 0.5-1.5 / L 1.5-3 人日）。施工顺序建议（交接 §3 步骤 3）：`T4→T1→T3→T2→T5→T6→T7/T8 → T9-T12(P2) → T13-T17(P3) → T18-T20(前端/Gate) → T21/T22(验收/追踪)`。

## 8. 待确认项（Q-R10-*，供用户裁决）

> 规划 §7.2 已列 Q-1~Q-6 技术路线待确认项；本 Agent 复核后认为**其中 4 项需在 R10-3 施工前定调**，其余可按倾向默认推进（施工中若触发分歧再上报）。以下按待确认项模板列出需用户裁决者：

### 待确认项 Q-R10-1（承规划 Q-1）
- **问题**：TaskGraph 持久化粒度——TaskNode 定义（task_node）与运行实例（task_node_run）分表，还是合表带 node_status？
- **背景**：影响 T1/T4 表设计与版本替代/多次运行能力。
- **建议方案**：**分表**（定义态 vs 运行态分离），便于 supersedes 版本链 + 一个 TaskGraph 多次运行。
- **风险**：合表更简单但版本链/多次运行会污染定义。
- **需要用户裁决**：是（影响地基表结构，改造成本高）。

### 待确认项 Q-R10-2（承规划 Q-2）
- **问题**：P2 评估在无可用模型 Key 时，降级为"纯规则评估"（标记"无模型辅助"）还是直接阻塞？
- **背景**：影响 T9 评估服务的失败态设计；当前 backend/.env Key 状态需本地确认（脱敏，不读值）。
- **建议方案**：**降级 + 诚实标记**（符合 G8 诚实失败 + 契约 §4.7-5 模型输出标 analysis_only）。
- **需要用户裁决**：是（涉及 P2 失败态语义）。

### 待确认项 Q-R10-3（承规划 Q-3 + Q-5）
- **问题**：(a) P3 是否每次都生成 TaskGraph，还是允许"无 TaskGraph + 理由"两路（契约 §5.4-4 允许线性简单迁移）？(b) P3→P4 Gate 批准后 P4 本期是纯 `future_r11` stub，还是允许引擎"空跑"P4 前置节点？
- **建议方案**：(a) **两路都支持**（简单项目可线性 TaskPlan）；(b) **纯 stub**（执行调度整体留 R11，避免半实现，符合 STOP-1）。
- **需要用户裁决**：是（影响 T15/T17 生成策略与 P4 边界）。

### 待确认项 Q-R10-4（承规划 Q-4）
- **问题**：Acceptance 执行器独立性（D-082）是否延伸到模型层（必须与执行 Agent 用不同 ModelProfile），还是仅 Agent 实例独立？
- **建议方案**：**仅 Agent 实例独立**（ModelProfile 独立性非 D-082 硬要求，避免过度约束 R9-5-2 网关协同）。
- **需要用户裁决**：是（影响 T3 与网关协同）。

> 其余 Q-6（边策略默认值 null vs 显式 default）倾向"显式 default 不静默"（承 RK-4），施工中按此默认，无需前置裁决。

## 9. 实探对交接 §2 的修正登记（以实探为准）

| # | 交接 §2 描述 | 实探修正 |
|---|-------------|---------|
| 1 | `p2_node`/`p3_node` 为 stub，返回 `{"stage":"p2","stub":True}` | 无独立 `p2_node`/`p3_node` 函数；stub 由 `make_work_node` 工厂在 `handler is None` 时产出 `{current_stage, stage_status:{stage:"future_r10"}, events}`。真实化 = 注册 P2/P3 StageHandler |
| 2 | `stage_service.submit_stage_plan :53-64` 为 mock | 属实；**补充**：`get_stages :41-42` 阶段列表读取本身也是 mock（source_status/transition_mode="mock"），T12 真实化时一并处理 |
| 3 | `r_stag:"R4"`（health 返回旧值） | 属实，实测 `routes_health.py:26,36` 两处；本期改 R10 |

---

## 10. 下一步

- **步骤 2（R10-2 历史吸收）**：深读 R10 规划 §3-§7 + 相邻期契约（`01-P0-P6阶段契约.md §4/§5`、`02-StagePlan-TaskPlan-TaskGraph规范.md`、`03-NodeLoop与执行模式.md`，若存在），建立完整 WBS + 横切底座施工顺序，出具 `R10-2-历史吸收报告.md`。
- **待用户确认**：§8 的 Q-R10-1~Q-R10-4 建议在 R10-3 施工前定调（尤其 Q-R10-1 表结构、Q-R10-2 P2 无 Key 降级）。

> **本报告结束。** 核查亲手驱动取证（HTTP + grep + pytest），未直接采信旧报告数字。R10-1 完成，等待推进步骤 2 或用户对 §8 待确认项的裁决。
