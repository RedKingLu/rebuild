# VISION —— rebuild 为什么存在

> **语言 / Language**：中文（当前页，**权威版本**） · [English](./VISION.md)
> 本仓库对外文档以**中文为权威版本**：英文版是中文版的翻译，两版表述冲突时**以中文版为准**。
> 语言变体清单见 [`.i18n.yaml`](./.i18n.yaml)。本页内的文档链接指向中文版。

> 平台：rebuild · 版本：V26.2（在建） · 许可：[MIT](./LICENSE)
> 配套阅读：[README.md](./README.zh.md)（如何跑起来）、[ARCHITECTURE.md](./ARCHITECTURE.zh.md)（怎么实现的）、[ROADMAP.md](./ROADMAP.zh.md)（下一步做什么）
> 本文档面向外部读者，只回答四个问题：为什么存在、解决谁的什么问题、当前能做到什么、**明确做不到什么**。

---

## 1. 一句话

**rebuild 把"把一套已有软件改造成另一套形态"这件事，做成有阶段、有证据、有人裁决的工程流程，而不是一次赌运气的批量生成。**

---

## 2. 为什么存在

大量还在生产上跑的软件都面临被迫改造：数据库要换、运行时版本停止支持、操作系统或 CPU 架构要替换、框架老到没人维护、需要跨平台移植。这类工作有两个共同特征：

- **风险不对称**。改错一处，损失远大于改对一处的收益。
- **不能中断业务**。没有"先推倒重来"的选项。

现实中处理它的两种常见做法都有明显缺陷。

**做法一：人工逐文件改。** 结论可信，但成本高、周期长，且过程中产生的判断（为什么选这个替代方案、哪些地方明知有风险仍然放过）通常只存在于某几个人的记忆里，交接即丢失。

**做法二：把整个代码库丢给大模型批量改。** 速度快，但会产生一种特别危险的产物 —— **看起来完整、实际立不住的代码**。模型会生成不存在的依赖包、给出没有验证过的等价性判断、在无法完成时输出一份措辞得体的完成报告。改造方无法从产物本身判断哪部分可信。

rebuild 的存在前提是：**第二种做法的失败模式不是模型能力问题，而是流程缺少约束**。如果一个系统在能力不足时会诚实说"做不到"，在每个结论上都挂得住可复查的证据，并在关键节点把决定权交回给人，那么用 AI 做改造才是可以接受的。

因此 rebuild 优先解决的不是"改得多快"，而是 **"改完之后，你能不能验证它、能不能追溯它、能不能知道哪里还不能信"**。

---

## 3. 解决谁的什么问题

### 3.1 面向的使用者

rebuild 面向**手里有源代码、需要对它做形态改造**的技术团队与个人：

| 使用者 | 他的实际问题 |
|---|---|
| 负责一次替换/升级项目的技术负责人 | 需要先说清工作量、风险点和不确定项，才能立项和排期，而不是边做边发现 |
| 实际动手改造的工程师 | 需要一份能落地的任务分解，以及每次改动的可复核依据，而不是一个黑箱产物 |
| 需要对结果签字的人 | 需要知道"通过"是基于什么证据判定的，以及哪些部分并未被验证 |

### 3.2 不解决谁的问题

- 没有源代码的评估与咨询需求 —— 平台无从下手，会明确拒绝而不是勉强产出。
- 从零新建一个系统 —— 没有"原状"就没有可判定的改造目标态。
- 日常编码辅助 —— 那是编辑器内 AI 助手的场景，rebuild 以项目为单位推进阶段，粒度完全不同。

---

## 4. 平台相信什么（设计信条）

这五条决定了 rebuild 与"能跑就行"的自动化脚本的根本差别。它们是设计选择，会牺牲一部分便利性。

**1. capability-first：能力不够就诚实报告，绝不伪造成功。**

缺有效模型凭据、缺可绑定的源码、环境不具备、证据不足时，平台返回 `blocked` 或 `evidence_gap`，而不退化成占位内容、mock 数据或规则模板冒充完成。一份写着"这里我没做到，原因是 X"的结论，价值高于一份无法追溯的漂亮产物。

**2. No Evidence No Completed。**

任何"已完成"都必须挂得上证据。产物、验证记录、执行轨迹可回查。判定不通过时，不允许用"看起来对"放行。

**3. 推理归 LLM，采集与验证归确定性代码。**

识别、理解、规划、风险研判、验收裁量交给 Agent 推理；克隆代码、列文件、跑命令、计数、编译、跑测试、做 diff 交给确定性工具。两者不互相冒充 —— 确定性代码不假装懂业务，模型不假装编译通过。

**4. 关键节点由人裁决。**

阶段晋级和高风险动作设有 Gate，需要人看过证据后授权。这是设计选择，不是尚未做完的自动化 —— 代码改造的责任不应由模型独自承担。

**5. BYOK（自带模型凭据）。**

模型 API Key 由使用者自己提供，只从环境变量读取、加密存储，接口只返回凭据状态而不回显明文。平台不内置模型，也不代管你的额度。

---

## 5. 什么样的工作适合交给 rebuild

### 5.1 典型场景（平台重点支持，配套资源相对更丰富）

| 场景 | 标识 | 说明 |
|---|---|---|
| 信创切换 | `xinchuang_switch` | 迁移到国产化软硬件与数据库栈 |
| 软件现代化 | `modernization` | 老旧运行时、框架、前端组件的版本现代化改造 |
| 软件移植 | `porting` | 跨平台、跨运行时的移植 |

这三类是平台**重点投入**的方向：各自配套了目标态词表、验收锚点、风险清单与选型候选，资源厚度明显高于通用兜底。

### 5.2 开放扩展：包括但不限于上述三类

场景体系是**开放的**。上述典型场景**不是全集**，平台支持"**包括但不限于**"这三类的场景：

- 你可以**新增自定义场景包**，也可以**复制典型场景包后按自己的情况改写** —— 场景包不是锁死的只读内置资产。
- 新增一个场景的目标形态是"**新增一个场景包目录即可生效**"，而不是去改代码里的枚举或下拉常量。
- 没有专用场景包时，平台走通用重构方法论兜底，并**明确标注"无专用场景知识"**，不会静默套用某个既有场景（例如信创）的口径。

云原生化改造、单体拆分为微服务、架构解耦、依赖去风险化、跨云迁移等类型，都通过同一机制加入，不需要平台先"支持"它。

### 5.3 准入三判据：场景开放，但准入不开放

任何场景（典型的或自定义的）要进入流程，须**同时**满足三条：

1. **有源代码** —— 存在可获取、可读取的真实代码。
2. **有可判定的目标态** —— 说得清"改成什么样才算成功"。
3. **产出物可机器验证** —— 结果能被编译、测试、diff 或等价性检查所检验。

缺任一条，平台会明确告知"这不适合用 rebuild 做"。这三条判据同时挡住了两件事：平台滑向"什么都能做"的泛自动化工具；验证链路被无法验证的任务架空。

---

## 6. 当前能做到什么

平台按 P0–P6 七个可裁剪阶段推进项目：**P0 接入 → P1 建档 → P2 评估 → P3 规划 → P4 执行 → P5 验证 → P6 交付**。流程可裁剪，例如只做评估：`P0 → P1 → P2 → P6`。

状态分三档标注，**不把规划写成已实现**。

### 6.1 已实现且有真实运行证据

| 能力 | 说明 |
|---|---|
| 源码接入 | 本地目录 / Git 仓库 / ZIP 包 / GitHub 仓库导入，工作区隔离，源码目录只读保护 |
| 项目事实源建档 | 结构、技术栈、依赖、构建与运行方式、配置、风险线索 |
| 迁移评估与方案规划 | 多维度评估报告、风险清单、待决策项，形成方案与任务图 |
| 受控改造执行 | 按任务图逐节点执行，产出代码与补丁；写入受策略与 Gate 约束 |
| **容器内真实构建** | 宿主没有对应 SDK 但 Docker 可用时，按需拉取官方 SDK 镜像在隔离环境真实执行 restore/build，产出真实编译结论与诊断明细 |
| **臆造依赖识别** | 对 NuGet / npm / Maven 做存在性与版本可解析性校验，把模型编造出来的依赖包尽早暴露 |
| 抗伪造判定 | 产物立不住时，验证阶段不会因为"构建成功"就放行；已在真实运行中验证过这条链路会诚实拦下 |
| 阶段 Gate 与中断恢复 | 阶段晋级 Gate 落库、可裁决；编排状态持久化，进程重启后可恢复续跑 |
| 证据 / 轨迹 / 审计 | 三条独立记录贯穿主链路，可回查 |
| 场景包运行期动态发现 | 新增或删除场景包目录，**无需重启后端**即刻生效，无需改任何代码 |
| 模型统一接入 | 所有模型调用经统一网关，不直连 Provider、不硬编码模型名与 endpoint |

### 6.2 部分实现（能力在场，但边界明确）

| 能力 | 现状与边界 |
|---|---|
| 依赖真实性校验的覆盖面 | 真实 registry 可达时，正反两面都有实测证据：一个模型编造的 NuGet 包被判 `package_not_found`（HTTP 404），同批另 7 个坐标判 `resolvable`。**边界**：网络受限或被重定向到非白名单主机时，平台标 `indeterminate` 而**不给结论**（守住"不把网络失败误判为包不存在"这条红线）；npm / Maven 的语义化版本范围表达式当前**不做求解**，诚实标"版本无法判定" |
| 验证维度覆盖 | 构建、测试、静态检查、行为等价、浏览器走查、数据库核对、性能基准等维度的能力已接线，但**环境不具备时诚实降级**，实际执行取决于你的环境 |
| 长时任务观测 | 有心跳注册表可**发现**任务挂起，但**不自动接管、不自动重试** |
| 生命周期挂点 | 定义了 10 个挂点，当前**只有工具调用前后 2 个真正接线**，其余为已定义未接线 |
| 场景知识的决策权重 | 场景包内容确实进入模型上下文并被引用，但实测其**决策权重低于显式填写的迁移目标** —— 场景包不能替代把目标态写清楚 |

### 6.3 规划中（尚未实现，不要按已有功能预期）

见 [ROADMAP.md](./ROADMAP.zh.md)。主要包括：千文件级真实规模项目的端到端验证、多用户与权限体系、预构建发行物、独立社区门户。

---

## 7. 明确做不到什么

**这一节比第 6 节重要。** 如果你只读一节，读这节。

### 7.1 它不是"提交仓库地址后回来收货"

- **阶段之间有用户 Gate。** 阶段晋级需要人查看证据后授权（例如从建档进入评估、从执行进入验证）。这不是待补完的自动化，是刻意保留的裁决点。未获授权的阶段不会自己往前走。
- **改造产物需人工复核后再采纳。** 平台的职责是把改造做出来并暴露它立不住的地方，不是替你签字。
- 因此**不存在无人值守流水线**这种用法。如果你需要的是那个，rebuild 现在不合适。

### 7.2 没有有效模型凭据，平台会停下，不会降级

无有效 Key 时，涉及识别与推理的能力**诚实返回 `blocked`**。平台**不会**退回规则引擎、模板或占位内容冒充完成。这是硬约束，不是配置项。平台也不内置模型。

### 7.3 真实构建结论依赖你的环境

P5 的真实构建结论有三种情况，只有前两种能给出真实编译结论：

1. 宿主机有对应 SDK；
2. 宿主机无 SDK，但 **Docker 可用**且能拉到对应 SDK 镜像；
3. 其余情况（Docker 不可达、镜像不在本地、无框架映射、未发现构建对象）—— **诚实标 `evidence_gap` 并写明具体原因，绝不伪造"构建通过"**。

也就是说：**没有可用的 Docker 或本机 SDK，你拿不到真实构建结论**，只会拿到一份说明为什么拿不到的记录。

### 7.4 看到大量"降级"标记是设计如此，不是坏了

能力探测是确定性的：没装浏览器自动化就没有浏览器走查证据；没有可达的远程主机就没有远程执行证据；没有测试工程就没有测试通过证据。这些情况一律标为 `evidence_gap` 且**非阻断**，同时记录"能力已接线、待环境真验"。

**请不要把满屏的 `evidence_gap` 读作平台没做完** —— 它表示平台拒绝在缺条件时编一个结论出来。反过来，如果你在一个什么都没装的环境里看到满屏"通过"，那才是应该报 bug 的时候。

### 7.5 机器验证 ≠ 业务等价保证

编译通过、测试通过、diff 可读，**不等于**业务语义完全等价。已实证的边界：容器构建成功只证明"可编译"，样本无测试工程时测试槽位会诚实标"需用户输入"，运行验证会标"不适用"。**语义等价仍需你的领域知识把关。**

### 7.6 验证规模的边界

这一条请仔细读，它决定了你对平台成熟度的预期是否正确。

**当前代码基线上已完成的完整端到端真跑（P0 一路到 P5/P6），源码规模只有 5 个文件** —— 那是一个为端到端验收专门准备的微型示例桩（一个页面 + 后台代码 + 配置 + 依赖清单）。它证明的是"整条链路能走通、抗伪造门禁会真的拦下立不住的产物"，**不证明规模承载能力**。

**更早的代码基线上，确实在千文件级真实系统上产出过阶段执行轨迹与执行阶段的 PoC 产物** —— 对象是一套约 1050 个文件、84 个 C# 源文件、约 50 个包依赖的真实 ASP.NET WebForms 系统，产出过数十个改造后代码文件与补丁。这段历史是真实的，**但它属于更早的基线，且是阶段性 PoC，不是当前基线上的完整 P0→P5 端到端验证**。

⇒ **准确结论：当前基线尚未在千文件级真实项目上做过完整端到端验证。** 其耗时、上下文压力、以及长链路中途中断后的恢复行为，属于已知未验证区域。这也是当前的第一优先级工作（见 [ROADMAP.md](./ROADMAP.zh.md)）。

> 附一条我们自己踩过的坑，供你判断类似说法时参考：上述两个样本**同名**，规模相差两百倍。跨轮次引用样本时必须核对实际内容，不能只认名字 —— 这类"同名不同物"是成熟度描述失真的常见来源。


### 7.7 运行形态的限制

| 限制 | 说明 |
|---|---|
| 单机单用户 | **没有登录、账号、租户隔离与权限体系。不要把服务暴露到公网。** |
| 容器内执行默认关闭 | 需显式 opt-in 并挂载 Docker socket（等同交出宿主 Docker 控制权），仅建议在可信单机环境启用 |
| 无预构建发行物 | 尚未发布 PyPI 包、npm 包或公共镜像，需从源码本地构建 |
| 界面语言 | 平台内用户可见文案以中文为主，技术标识符保留原文 |

### 7.8 风险分级的实现边界

平台定义了 L0–L5 完整风险词表，工具注册时的静态风险位覆盖全部六级并据此决定是否需要 Gate。但**运行期命令内容的自动分类目前只产出其中四级**（L0 与 L2 已定义、当前分类实现未使用）。这是如实标注的实现现状，不影响现有 Gate 的触发；调整它属授权阈值变更，不会被"顺手"改掉。

---

## 8. 不属于本项目的目标

以下方向明确**不在**范围内，不会因为"顺路"而扩展进来：

1. 通用 AI 办公平台；
2. 通用 Agent 市场；
3. 泛项目管理平台；
4. 无源代码情况下的纯咨询平台；
5. 面向所有软件工程任务的通用自动化平台；
6. 只展示 Agent 能力而不完成改造闭环的平台。

**"泛软件重构"不等于"泛软件工程"。** 平台解除的是"改造类型"的限定（信创 / 现代化 / 移植 / 云原生化 / …），并没有把新建开发、需求管理、运维、测试外包等软件工程活动纳入范围 —— 第 5.3 节的三判据就是这条分界线：新建开发没有"源码前提 + 可判定目标态"，运维没有"可机器验证的改造产出物"，都不满足准入。

---

## 9. 许可与参与

- 许可：[MIT](./LICENSE)，版权署名 `withwind`。
- 参与方式见 [CONTRIBUTING.md](./CONTRIBUTING.md)；安全问题请按 [SECURITY.md](./SECURITY.md) 的私密渠道上报，不要开公开 issue。
- 交流渠道的当前状态见 [ROADMAP.md](./ROADMAP.zh.md) 的「社区」一节。

---

> 本文档的写法约定：能力分"已实现 / 部分实现 / 规划中"三档；每写一条能力，同时写它的边界。如果你发现文中某条描述与实际运行行为不符，那是需要修正的缺陷 —— 请提 issue，这类反馈的优先级高于新功能请求。
